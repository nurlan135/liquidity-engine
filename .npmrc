# vitest@5 wants @types/node ^22||>=24 as a peerOptional, but the project
# pins @types/node ^20 (installed locally via --legacy-peer-deps since 01-01).
# Mirror that resolution on CI/hosted installs (Vercel runs plain npm install).
legacy-peer-deps=true
