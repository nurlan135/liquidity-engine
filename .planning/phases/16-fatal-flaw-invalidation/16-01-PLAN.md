---
phase: 16-fatal-flaw-invalidation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/ict/invalidation.ts
- src/lib/ict/invalidation.test.ts
- src/lib/store.ts
autonomous: true
requirements:
- FLAW-01
- FLAW-02
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User sees an invalidated verdict from checkFatalFlaw on the same snapshot as the trigger, where flaw deterministically supersedes fire and never flickers across polls with no new market information
  - User sees HARD flaws (rollover-week corruption, stale leg) killing the setup from any state versus SOFT flaws (SMT suppressed, confirmed opposite sweep) downgrading FIRING to ARMED only, each with a stated unblock condition
  - Every flaw verdict prints its specific Azerbaijani reason plus HARD/SOFT class and unblock; the banned generic single-word label never appears as a reason
  artifacts:
  - src/lib/ict/invalidation.ts
  - src/lib/ict/invalidation.test.ts
  - src/lib/store.ts
  key_links:
  - checkFatalFlaw consumes the exact TriggerOutput plus smt/judas/rollover/stale envelopes plus injected asOf, never raw candles or clocks
  - selectFatalFlaw derives the trigger first on one sharedEpoch call then applies the flaw by function order, never a second clock read
  - SOFT branches consult trigger verdict and carry trigger reasonKey forward as carriedArmedReason per D-15
---

<objective>
Tracer slice: one production-quality path from the real trigger snapshot through checkFatalFlaw to selectFatalFlaw, verified end to end before expansion.
Implements D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-11, D-12, D-13, D-14, D-15 (see task actions; full sentence table plus challenge bank plus verbatim prose lock land in 16-02 per D-08, D-09, D-10).

Purpose: Prove the flaw-after-trigger supersession architecture on first commit instead of after layered expansion.
Output: New pure invalidation module, co-located disjunction tests, minimal store selector.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fatal-flaw-invalidation/16-CONTEXT.md
@.planning/phases/16-fatal-flaw-invalidation/16-RESEARCH.md
@.planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: pure checkFatalFlaw core with HARD/SOFT disjunction per D-01 D-02 D-05 D-06 D-07 D-12</name>
  <files>src/lib/ict/invalidation.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/smt.ts, src/lib/ict/judas.ts, src/lib/ict/rollover.ts, src/lib/ict/types.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Create new pure module src/lib/ict/invalidation.ts following the trigger.ts structural shell per D-07: file-top comment naming locked decisions D-01 through D-07 plus D-12 and D-15, then an explicit Pure allow-list stating trigger plus smt plus judas plus rollover plus stale envelopes plus injected asOf epoch only, with no clock reads, no store imports, no candle arrays. Value-import nothing except types; type-import TriggerOutput and TriggerReasonKey from @/src/lib/ict/trigger, SmtOutput from @/src/lib/ict/smt, JudasOutput from @/src/lib/ict/judas, RolloverFlag from @/src/lib/ict/types, using @/-aliased type-only imports per the trigger.ts lines 1-7 precedent. Export type FlawClass with HARD plus SOFT, export type FlawReasonKey with ROLLOVER_WEEK plus STALE_LEG plus SMT_SUPPRESSED plus OPPOSITE_SWEEP plus NONE, export interface FatalFlawInput with trigger plus smt (SmtOutput or null) plus judas (JudasOutput or null) plus rollover (RolloverFlag or null) plus stale (object with nq, es, nq1h, nq15m booleans) plus asOf number, each field carrying a doc comment stating its vote or no-vote role. Export interface FatalFlawOutput serializable per D-11 with invalidated boolean plus downgraded boolean (mutually exclusive) plus flawClass (FlawClass or null, null when clean) plus reasonKey plus reason verbatim Azerbaijani plus unblock per-flaw re-arm condition per D-13 plus sentence plus challenge plus carriedArmedReason (TriggerReasonKey or null) per D-15 plus asOf carried for Phase 18 replay. Export function checkFatalFlaw taking FatalFlawInput per D-05 and implementing the first-match-wins disjunction body per D-01 and D-02: HARD rollover when rollover non-null with rolloverSuspect strictly true, then HARD stale when any of the four stale booleans is true, then SOFT SMT when smt non-null with suppressed strictly true (either reason CORR_DECOUPLED or rollover-week, corr optional and handled absent), then SOFT opposite-sweep when judas non-null with confirmed strictly true and sweepSide non-null and the sweepSide-implied direction (HIGH implies SHORT, LOW implies LONG, copying the directionOf precedent) disagrees with trigger.direction with trigger.direction non-null, then clean returning the trigger verdict standing. HARD branches return invalidated true from ANY trigger verdict including WAIT_FOR_MANIPULATION and ARMED per D-12; every SOFT branch is gated on trigger verdict FIRE_LONG or FIRE_SHORT and carries trigger.reasonKey as carriedArmedReason, while SOFT on non-FIRING returns clean with the trigger verdict standing and flaw noted but not applied. Closed-candle confirmation per D-03 is inherited from the confirmed flag and suppressed state, single closed candle suffices, no N-close depth check. Mirror the trigger.ts boundary-throw discipline: input null or non-object throws with got-string naming String(input), asOf non-finite or non-positive throws with got-string naming String(asOf), malformed trigger envelope throws with got-string, while null smt/judas/rollover envelopes degrade honestly to clean-or-specific (never throw on null). Ship minimal prose now (full table in 16-02): one base Azerbaijani reason sentence per non-NONE key, one fixed unblock template per key naming the fixed 0.70 threshold string for SMT (reusing CORR_MIN 0.7, never interpolating live corr), one neutral fallback sentence, one standing-caution challenge entry; every non-clean verdict carries reason plus flawClass plus unblock so the banned generic single-word label never appears per D-04. Reuse zero new thresholds, therefore no FLAW_ constants in this tracer. Include flawClass on the output so Phase 17 can style HARD versus SOFT distinctly per D-14, with no styling code here.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/invalidation.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/ict/invalidation.ts exports checkFatalFlaw plus FlawClass plus FlawReasonKey plus FatalFlawInput plus FatalFlawOutput with the ten output fields. Behavior assertion: rollover-suspect kills an ARMED trigger, any stale leg kills a WAIT trigger, suppressed SMT downgrades FIRE_LONG to ARMED with carried reason, opposite confirmed sweep downgrades FIRE_SHORT to ARMED, clean input leaves FIRE standing. Test command: npx vitest run src/lib/ict/invalidation.test.ts passes.</acceptance_criteria>
  <done>Single flaw-after-trigger path works end to end through pure checkFatalFlaw and is committed with the disjunction order as code.</done>
  <reversibility rating="reversible">Pure additive module; predicate order is a visible diff if the taxonomy ever changes.</reversibility>
</task>

<task type="auto">
  <name>Wave 0 flaw tests: disjunction table plus determinism plus boundary throws per FLAW-01 FLAW-02</name>
  <files>src/lib/ict/invalidation.test.ts</files>
  <read_first>src/lib/ict/invalidation.ts, src/lib/ict/trigger.test.ts, src/lib/ict/smt.ts, src/lib/ict/judas.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Create co-located test file src/lib/ict/invalidation.test.ts copying the trigger.test.ts header idiom (vitest plus fromZonedTime plus NY_TZ plus module import plus envelope type imports) with Partial-override fixture builders: fixtureFlawTrigger defaulting to a FIRE_LONG TriggerOutput with direction LONG and reasonKey FIRE_LONG, fixtureSmt defaulting to a non-suppressed signal, suppressedSmt with reason CORR_DECOUPLED and corr 0.4, fixtureJudas defaulting to candidate false confirmed false preRun false sweepSide null sweepTime null displacementMult 0, fixtureRollover defaulting to rolloverSuspect false with contractHint empty and proximityWarning null, fixtureStale defaulting to all four legs false, plus the nyMinuteEpoch DST-aware epoch builder with SESSION constant. No fake timers since instants are injected. Cover the taxonomy table per D-01 D-02 D-12: rolloverSuspect true with an ARMED trigger yields invalidated true with flawClass HARD and reasonKey ROLLOVER_WEEK; any single stale leg true with a WAIT_FOR_MANIPULATION trigger yields invalidated true with flawClass HARD and reasonKey STALE_LEG; suppressed SMT with a FIRE trigger yields downgraded true with flawClass SOFT and reasonKey SMT_SUPPRESSED and non-empty unblock and carriedArmedReason equal to the trigger reasonKey; confirmed HIGH sweep against a LONG FIRE trigger yields downgraded true with reasonKey OPPOSITE_SWEEP; suppressed SMT with an ARMED trigger leaves the trigger verdict standing with downgraded false and invalidated false; clean envelopes leave FIRE standing with reasonKey NONE and flawClass null. Cover determinism per FLAW-01: same inputs plus same asOf run 100 times produce byte-identical output including reason and sentence and challenge strings. Cover the race fixture: FIRE trigger plus flaw on the same snapshot yields the flaw verdict carrying both the trigger reason (via carriedArmedReason) and the flaw reason. Cover the ban: every non-clean verdict asserts a specific reasonKey plus non-empty unblock, and no reason equals the banned generic single-word label. Cover boundary throws: null input throws with got-string, non-finite asOf throws with got-string, malformed trigger envelope throws with got-string, null smt/judas/rollover degrade without throwing. (Verbatim toBe prose pins for the final D-10 wording land in 16-02; this task pins reasonKey plus class plus flags plus unblock non-emptiness.)</action>
  <verify>
    <automated>npx vitest run src/lib/ict/invalidation.test.ts -t "taxonomy"</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: rollover kills from ARMED, stale kills from WAIT, SMT downgrades FIRING with carried reason, SOFT on ARMED leaves verdict standing, clean leaves FIRE standing. Test command: npx vitest run src/lib/ict/invalidation.test.ts passes including the determinism and boundary cases. Source assertion: taxonomy plus determinism plus boundary describe blocks exist.</acceptance_criteria>
  <done>Disjunction table plus determinism plus boundary discipline are green.</done>
</task>

<task type="auto">
  <name>Minimal selectFatalFlaw plus shared epoch reuse per D-05 D-06</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts, src/lib/ict/invalidation.ts, src/lib/ict/trigger.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Extend tracked store module src/lib/store.ts beside selectTrigger following the selectTrigger block lines 830-869 structural precedent. Add interface entry selectFatalFlaw returning FatalFlawOutput or null beside the selectTrigger declaration. Add value import of checkFatalFlaw plus type imports of FatalFlawInput, FatalFlawOutput, FlawReasonKey from @/src/lib/ict/invalidation following the trigger import-block style, keeping the canonical output type in invalidation.ts with a store-side type re-export only. Implement selectFatalFlaw with refuse plus try-catch-never-throw envelope copying the selectSMT and selectTrigger precedent: call sharedEpoch(get) exactly once for the single time truth, derive the trigger FIRST via get().selectTrigger() on that epoch, return null when trigger is null (SOFT never evaluated on null per D-12; HARD-on-null left to planner-confirmed null in this tracer, never a SOFT downgrade), then inside try read the identical envelopes the trigger saw via get().selectSMT() plus get().selectJudas() plus the existing selectRollover-style rollover derivation at the boundary with inner try-catch degrading to rollover null meaning no HARD rollover signal, plus the per-leg stale booleans object from nq, es, nq1h, nq15m state, then return checkFatalFlaw with trigger, smt, judas, rollover, stale, asOf set to the single epoch. Never re-slice candles, never derive a second epoch, never pass a candles parameter. Catch path returns null so the selector never throws into render.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/store.ts exports selectFatalFlaw with the FatalFlawOutput-or-null interface and checkFatalFlaw import. Behavior assertion: live London confirmed FIRE sweep yields a flaw output through the selector with asOf equal to the trigger epoch, stale intraday legs yield null without throw. CLI output: npx vitest run src/lib/store.test.ts passes.</acceptance_criteria>
  <done>One poll-driven path from detectors through selectTrigger to selectFatalFlaw works and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detectors→checkFatalFlaw | Malformed trigger/smt/judas/rollover envelopes cross here; flaw inherits detector-clean outputs only and throws with got-strings on malformed shapes |
| store-selector→render | Degraded null versus flaw verdict crosses here; selector never throws into render, null means degraded |
| flaw-output→ticket/§6 | Verdict plus class plus reason plus unblock cross here; no order-shaped fields ever emitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | checkFatalFlaw stale-leg input plus selectFatalFlaw epoch sharing | high | mitigate | HARD-stale kill on any stale leg per D-01; selector derives trigger first on one sharedEpoch call and refuse-nulls via selectTrigger null, so a stale-data verdict can never present at full strength |
| T-16-02 | Information disclosure | Flaw reason/sentence/challenge strings | low | accept | Neutral informative copy with class plus unblock always attached per D-04 and D-14; no red-error styling in this phase, Phase 17 styles HARD vs SOFT distinctly |
| T-16-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; only installed vitest plus date-fns-tz plus zustand are used, no package-legitimacy gate needed |
</threat_model>

<verification>
Per-task vitest runs plus npx tsc --noEmit at plan merge plus full npx vitest run gate before verify-work. Purity guard npx vitest run src/lib/ict/purity.test.ts must stay green with new invalidation.ts auto-scanned via the ./*.ts glob.
</verification>

<success_criteria>
Tracer HARD-kill and SOFT-downgrade paths green, determinism 100-run identical, boundary throws with got-strings, selectFatalFlaw shares the trigger epoch, purity guard green.
</success_criteria>

<output>
Create `.planning/phases/16-fatal-flaw-invalidation/16-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ict/invalidation.ts: FlawClass, FlawReasonKey (ROLLOVER_WEEK, STALE_LEG, SMT_SUPPRESSED, OPPOSITE_SWEEP, NONE), FatalFlawInput, FatalFlawOutput (invalidated, downgraded, flawClass, reasonKey, reason, unblock, sentence, challenge, carriedArmedReason, asOf), checkFatalFlaw, boundary assert helpers, minimal reason/unblock/sentence/challenge strings
- src/lib/ict/invalidation.test.ts: fixtureFlawTrigger, suppressedSmt, fixtureJudas, fixtureRollover, fixtureStale, nyMinuteEpoch, taxonomy plus determinism plus boundary tests
- src/lib/store.ts: selectFatalFlaw selector with single sharedEpoch call, checkFatalFlaw plus FatalFlawOutput imports
