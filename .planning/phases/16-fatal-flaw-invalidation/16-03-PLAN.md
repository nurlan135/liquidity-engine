---
phase: 16-fatal-flaw-invalidation
plan: '03'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/ict/invalidation.ts
- src/lib/ict/invalidation.test.ts
- src/lib/store.test.ts
autonomous: true
gap_closure: true
requirements:
- FLAW-02
estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: med
must_haves:
  truths:
  - User sees SMT-suppressed SOFT downgrading FIRING to ARMED live through selectFatalFlaw, each with a stated unblock condition and carried ARMED reason
  - The confirmed opposite-sweep SOFT branch is documented as unreachable through the production selector path so Phase 17 never depends on it, while the pure-layer branch stays pinned for synthetic inputs
  - The SOFT taxonomy as shipped behaves live exactly as tested, with no dead logic presented as live capability
  artifacts:
  - src/lib/ict/invalidation.ts
  - src/lib/ict/invalidation.test.ts
  - src/lib/store.test.ts
  key_links:
  - selectFatalFlaw SMT-suppressed FIRE fixture yields downgraded SOFT with carriedArmedReason through the real selector path
  - Selector-level impossibility proof pins that live trigger direction always equals the implied sweep direction on the same snapshot
---

<objective>
Close the single Phase 16 verification gap (CR-01 CONFIRMED): make the SOFT taxonomy honest as shipped by proving SMT SOFT live through the selector and documenting opposite-sweep as pure-layer synthetic-only. Picks review option (c) — document the impossibility with selector-level tests — because option (a) would widen the D-05 input shape and violate the D-02 no-AMD-contradiction ban, and option (b) would delete the D-02-locked OPPOSITE_SWEEP member and expand blast radius across every taxonomy consumer. Implements D-02, D-04, D-12, D-15 as already locked; changes no production verdict logic.

Purpose: Stop the shipped taxonomy from over-promising an opposite-sweep downgrade Phase 17 section 6 can never render, while proving the SMT half of SOFT downgrades live.
Output: SMT-live selector downgrade test, selector impossibility test, corrected test title, source header note — no verdict-logic change.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fatal-flaw-invalidation/16-CONTEXT.md
@.planning/phases/16-fatal-flaw-invalidation/16-RESEARCH.md
@.planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: SMT SOFT downgrade proven live through selectFatalFlaw end to end</name>
  <files>src/lib/store.test.ts</files>
  <read_first>src/lib/store.test.ts, src/lib/store.ts, src/lib/ict/invalidation.ts, src/lib/ict/smt.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Add one selector test named flaw-soft-live-smt in src/lib/store.test.ts beside the flaw-stale and flaw-coherence cases, copying the seedTriggerFire plus resetDualState plus refresh idiom. The test seeds with seedTriggerFire then calls selectFatalFlaw exactly once as its first derivation (never pre-call selectTrigger, because the first selectTrigger consumes the session FIRE into ARMED_ALREADY_FIRED and the SOFT FIRING gate would then force clean). Assert on that first flaw output: downgraded is true, invalidated is false, flawClass is SOFT, reasonKey is SMT_SUPPRESSED, carriedArmedReason is FIRE_LONG, unblock is non-empty and contains the fixed 0.70 threshold substring, reason equals REASON_BY_KEY SMT_SUPPRESSED entry. Add a one-line comment above the test stating the seed decouples correlation with four daily rows so evaluateSMT returns suppressed CORR_DECOUPLED while the trigger still fires because SMT is a read-only agree-tag that never blocks FIRE. Keep the existing flaw-stale and flaw-coherence tests untouched. Per D-12 the assertion targets FIRING only; per D-15 it pins the carried FIRE_LONG key; per D-13 it pins the fixed-threshold unblock; per D-07 no clock or store change is made.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL appears in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: seeded London LOW-sweep state yields selectFatalFlaw with downgraded true plus reasonKey SMT_SUPPRESSED plus carriedArmedReason FIRE_LONG on the first derivation without pre-consuming the fire. Source assertion: src/lib/store.test.ts contains a test named flaw-soft-live-smt asserting downgraded plus SOFT plus SMT_SUPPRESSED plus FIRE_LONG carry. Test command: npx vitest run src/lib/store.test.ts passes with the new case green.</acceptance_criteria>
  <done>SMT SOFT is proven live through the production selector path with carried reason and unblock, committed.</done>
  <reversibility rating="reversible">Additive store test only; deleting the test removes the pin with no production effect.</reversibility>
</task>

<task type="auto">
  <name>Opposite-sweep impossibility documented plus test title corrected plus header note</name>
  <files>src/lib/store.test.ts, src/lib/ict/invalidation.test.ts, src/lib/ict/invalidation.ts</files>
  <read_first>src/lib/ict/invalidation.ts, src/lib/ict/invalidation.test.ts, src/lib/store.test.ts, src/lib/ict/trigger.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>First, in src/lib/store.test.ts add one selector test named flaw-opposite-impossible-live beside flaw-soft-live-smt: seed with seedTriggerFire, read selectTrigger and selectJudas from identical state, assert trigger direction equals the sweep-implied direction (LOW implies LONG, HIGH implies SHORT) and assert selectFatalFlaw reasonKey is never OPPOSITE_SWEEP on live state (it is SMT_SUPPRESSED on the first derivation or NONE once ARMED), with a comment stating same-snapshot trigger direction is a total function of judas sweepSide so the pure isOppositeSweep predicate is always false in production and Phase 17 must treat live SOFT as SMT-only. Second, in src/lib/ict/invalidation.test.ts rename the test at line 136 from the HIGH-sweep wording to the LOW-sweep wording so the title matches its sweepSide LOW plus direction SHORT fixture, verify the companion LONG plus HIGH test title stays correct, and add a two-line comment above both synthetic opposite-sweep tests stating they use synthetically mismatched trigger-plus-judas envelopes unreachable via selectFatalFlaw and exist to pin pure-layer precedence only. Third, in src/lib/ict/invalidation.ts extend the file-top decision comment with one sentence stating the OPPOSITE_SWEEP SOFT branch fires only on synthetically mismatched envelopes and never through selectFatalFlaw on the same snapshot, keeping all verdict logic, reason strings, unblock strings, sentence table, and challenge bank byte-identical. Change no production branching, no reason text, no thresholds, no input shape per D-05.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/invalidation.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL appears in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: live seeded state never yields reasonKey OPPOSITE_SWEEP through selectFatalFlaw while synthetic pure-layer mismatched envelopes still yield OPPOSITE_SWEEP downgrade. Source assertion: src/lib/store.test.ts contains flaw-opposite-impossible-live; src/lib/ict/invalidation.test.ts line-136 title names LOW sweep and the companion names HIGH sweep; src/lib/ict/invalidation.ts header comment names the synthetic-only status of the opposite-sweep branch. Test commands: npx vitest run src/lib/ict/invalidation.test.ts passes and npx vitest run src/lib/store.test.ts passes and npx vitest run src/lib/ict/purity.test.ts passes.</acceptance_criteria>
  <done>Opposite-sweep impossibility is pinned at the selector level, the wrong-side title is corrected, and the source header states the synthetic-only status with zero verdict-logic change.</done>
  <reversibility rating="reversible">Test plus comment-only source change; reverting restores prior titles and header with no logic effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flaw-output→§6-render | Sentence plus challenge strings cross here into the Phase 17 section 6 block; this plan changes no strings, only documents which SOFT member is live |
| store-selector→render | Degraded null versus flaw verdict crosses here; new tests pin live SOFT versus documented-impossible paths without changing selector logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information disclosure | Flaw reason plus sentence plus challenge strings | low | mitigate | No string changed in this plan; verbatim toBe pins re-run green so replay byte-equality holds |
| T-16-04 | Tampering | SOFT taxonomy presentation | medium | mitigate | Selector tests pin SMT SOFT live and opposite-sweep never-live, so Phase 17 cannot render a downgrade the selector can never produce |
| T-16-SC | Tampering | npm installs | high | mitigate | Zero new packages; only installed vitest plus date-fns-tz plus zustand are used |
</threat_model>

<verification>
Per-task vitest runs plus npx vitest run src/lib/ict/purity.test.ts plus npx tsc --noEmit at plan merge. Full npx vitest run gate before verify-work re-run of the phase.
</verification>

<success_criteria>
SMT SOFT downgrade green live through selectFatalFlaw with carried FIRE_LONG reason and 0.70 unblock; selector impossibility test green proving OPPOSITE_SWEEP never surfaces live; wrong-side test title corrected; header note committed; purity guard and typecheck clean; no production verdict-logic diff.
</success_criteria>

<output>
Create `.planning/phases/16-fatal-flaw-invalidation/16-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/store.test.ts: flaw-soft-live-smt test (SMT-suppressed FIRE downgrade live with carriedArmedReason FIRE_LONG plus 0.70 unblock), flaw-opposite-impossible-live test (live direction-equals-implied assertion plus never-OPPOSITE_SWEEP pin with Phase 17 SMT-only note)
- src/lib/ict/invalidation.test.ts: corrected line-136 title naming LOW sweep for the FIRE_SHORT fixture, synthetic-envelope comments above both opposite-sweep tests pinning pure-layer-only status
- src/lib/ict/invalidation.ts: extended file-top decision comment documenting the OPPOSITE_SWEEP branch as synthetic-only unreachable via selectFatalFlaw; zero logic, string, threshold, or shape changes; FlawReasonKey union unchanged with OPPOSITE_SWEEP retained per D-02
