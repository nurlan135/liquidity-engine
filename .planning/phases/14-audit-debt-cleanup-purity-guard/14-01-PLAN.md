---
phase: 14-audit-debt-cleanup-purity-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/zone-bands.ts, components/charts/nq-chart.tsx, src/lib/zone-bands.test.ts]
autonomous: true
requirements: [DEBT-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - zoneBands accepts a minimal high/low/eq input and computes identical premium/discount halves
    - nq-chart zone getter passes exactly high/low/eq and dimming still flows via opacityScale
    - full test suite stays green with zero behavior change
  artifacts:
    - src/lib/zone-bands.ts (narrowed signature)
    - components/charts/nq-chart.tsx (trimmed call)
    - src/lib/zone-bands.test.ts (minimal literals)
  key_links:
    - zoneBands minimal input -> nq-chart getter -> ZoneFillPrimitive opacityScale dimming
---

<objective>
Narrow the zoneBands signature from full DealingRange to a minimal high/low/eq input per D-01, trim the dead keys at the nq-chart call site, and shrink the zone-bands test literals — with zero behavior change.

Purpose: Remove the dead thinHistory arg plus filler literals that confuse future readers (a landmine if zoneBands ever branches on the flag). Dimming keeps flowing via opacityScale, untouched.
Output: Three modified files, type-clean, suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-CONTEXT.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-PATTERNS.md
@src/lib/zone-bands.ts
@components/charts/nq-chart.tsx
</context>

<tasks>

<!-- planner-discipline-allow: thinHistory -->
<!-- planner-discipline-allow: window: 0 -->

<task type="auto">
  <name>Task 1: Narrow zoneBands signature to minimal high/low/eq input</name>
  <files>src/lib/zone-bands.ts</files>
  <read_first>src/lib/zone-bands.ts (full file, 33 lines — signature line 16, body lines 17-31); src/lib/ict/types.ts (DealingRange shape, to confirm the high/low/eq key names for the Pick); .planning/phases/14-audit-debt-cleanup-purity-guard/14-PATTERNS.md (zone-bands copy pattern)</read_first>
  <action>On line 1 replace the DealingRange type import with a minimal structural input spelling: the parameter type becomes a Pick of DealingRange over exactly the three keys high, low and eq (a local interface with three numeric fields high, low, eq is an acceptable equivalent, Pick preferred per PATTERNS). On line 16 change the signature to take the narrowed range parameter. Leave the isFiniteNumber helper, all three validation branches with their verbatim error strings, and the four-field return mapping (premiumTop from high, premiumBottom from eq, discountTop from eq, discountBottom from low) untouched. Do not open src/lib/ict/range.ts or src/lib/ict/types.ts for edits this phase — DealingRange dot thinHistory stays per D-02. Implements D-01, honors D-02.</action>
  <acceptance_criteria>Parameter type names exactly the keys high, low and eq and no other DealingRange key; function body references only the high, low and eq fields; the three validation error strings are byte-identical to the current text; scoped zone-bands tests pass at runtime (stale extra keys in old literals are ignored at runtime until Task 3 shrinks them).</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/zone-bands.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <done>Signature narrowed to the minimal input; runtime geometry identical.</done>
  <reversibility rating="reversible">Pure type narrowing with identical runtime body; git revert restores the old signature.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Trim the nq-chart zone getter call to three keys</name>
  <files>components/charts/nq-chart.tsx</files>
  <read_first>components/charts/nq-chart.tsx lines 280-310 (attachZoneFill getter plus opacityScale line); src/lib/zone-bands.ts (narrowed signature from Task 1)</read_first>
  <action>Inside the attachZoneFill getter (lines 289-304) delete the three dead object keys currently on lines 296-298 — the numeric window filler, the empty-string asOf stamp, and the unread history flag derived from comparing latest dot thinTier to the range-thin string — leaving an object literal with exactly high from latest dot rangeHigh, low from latest dot rangeLow, eq from latest dot eq. Keep the surrounding try/catch returning null and the line 305 opacityScale assignment (0.5 when status is stale or dimmed, else 1) byte-identical. Do not touch the second opacityScale assignment near line 486 in the separate effect block. Implements D-01 call-site half.</action>
  <acceptance_criteria>The getter object literal carries exactly the high, low and eq keys; the line 305 dimming assignment is byte-identical; the opacity-focused tests in zone-bands.test.ts pass, proving the dimming path is intact.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/zone-bands.test.ts -t opacity</automated>
    <fails_when>non-zero exit, or the run reports zero tests passed (the filter must hit the three opacity tests)</fails_when>
  </verify>
  <done>Call site passes only the consumed keys; dimming path untouched.</done>
  <reversibility rating="reversible">Deletion of unread keys only; git revert restores the literal.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Shrink zone-bands.test.ts literals to minimal objects</name>
  <files>src/lib/zone-bands.test.ts</files>
  <read_first>src/lib/zone-bands.test.ts lines 1-34 (all six zoneBands call literals); src/lib/zone-bands.ts (narrowed signature from Task 1)</read_first>
  <action>Replace all six zoneBands call literals (lines 7, 16, 19, 22, 28 and 31) with three-key objects keeping the same high/low/eq values, including the NaN, Infinity and out-of-span eq variants. Keep every expect assertion and the entire ZoneFillPrimitive opacity describe block (lines 36-104) verbatim. Implements D-01 test half.</action>
  <acceptance_criteria>Every zoneBands call in the file carries exactly the high, low and eq keys; all assertions are unchanged; the scoped file is green; project-wide tsc is clean.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/zone-bands.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in the output</fails_when>
  </verify>
  <done>Tests match the narrowed signature; whole project type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart getter → zone geometry | narrowed input type crosses from component into pure lib; shape mismatch would break zone render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | nq-chart zone getter edit | low | mitigate | geometry body untouched; opacity tests plus full suite prove identical render |
| T-14-02 | Tampering | zoneBands signature narrowing | low | mitigate | tsc excess-property checks on all call sites plus runtime suite green |
</threat_model>

<verification>
Plan-level: npm test (full suite) green; npx tsc --noEmit clean; git diff on the three files shows only the signature line, the three deleted call-site keys, and shrunk test literals — no logic lines changed.
</verification>

<success_criteria>
Terminal renders identically after the dead arg removal; dimming still flows via opacityScale (opacity tests green); DEBT-01 satisfied with zero behavior change.
</success_criteria>

## Artifacts this phase produces

Phase-wide artifact record (this plan contributes the DEBT-01 rows):
- Modified: src/lib/zone-bands.ts — zoneBands parameter narrowed to the minimal high/low/eq input (no new symbol; validation error strings unchanged)
- Modified: components/charts/nq-chart.tsx — zone getter literal reduced to exactly high/low/eq (no new symbol; opacityScale dimming untouched)
- Modified: src/lib/zone-bands.test.ts — call literals shrunk to three keys (assertions unchanged)
- Created (in 14-03): src/lib/ict/purity.test.ts — co-located Vitest grep guard with two specs (clock-read spec, store-import spec)
- Removed from public surface (in 14-02): the export keyword on thinTier(closedCount) and on the ThinBannerEntry type — both stay as module-internal helpers
- Doc-only (in 14-03): Asia fallback comment extension in src/lib/store.ts selectAsia (no new symbol, no logic change)

<output>
Create `.planning/phases/14-audit-debt-cleanup-purity-guard/14-01-SUMMARY.md` when done
</output>
