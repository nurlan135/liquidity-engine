---
phase: 07-smt-4h-1h-sequencing-math
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/smt.ts
  - src/lib/ict/smt.test.ts
  - src/lib/__fixtures__/smt-rollweek.json
autonomous: true
requirements:
  - ICT-08
  - ICT-09
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SMT comparator returns BULLISH / BEARISH / NO-SIGNAL with swing references (nqWindow, esWindow) and names the sweeping leg (sweeperLeg NQ | ES), never a bare detected flag"
    - "Rolling 20-day correlation below 0.70 suppresses SMT with reason CORR_DECOUPLED before any swing matching runs"
    - "Either leg rollover-suspect inside the comparison window suppresses SMT with reason rollover-week instead of a divergent signal"
    - "[Flagged assumption ICT-08-unclassified] No hidden edge contract beyond D-01..D-07 governs the SMT happy path; the bull/bear/choppy fixtures define truth for divergence vs NO-SIGNAL"
    - "[Flagged assumption ICT-09-unclassified] No hidden edge contract beyond D-08 governs joint suppression; the roll-week fixture (NQ gaps, ES clean) defines truth for suppressed-vs-divergent"
  artifacts:
    - path: "src/lib/ict/smt.ts"
      provides: "SMT comparator: SWING_K match, bps compare, correlation gate, joint rollover suppression"
      exports: ["SWING_K", "SMT_TOL_BPS", "CORR_WINDOW", "CORR_MIN", "SWING_LOOKBACK", "matchSwings", "pearsonCorr", "detectSMT", "evaluateSMT"]
      contains: "sweeperLeg"
    - path: "src/lib/ict/smt.test.ts"
      provides: "Bull/bear/choppy/lookback/decoupled/zero-variance/roll-week/edge/closedOnly/constant-pin tests"
      min_lines: 120
    - path: "src/lib/__fixtures__/smt-rollweek.json"
      provides: "Joint roll-week D1 pair (NQ gaps, ES clean) with nq/es arrays"
      contains: "\"es\""
  key_links:
    - from: "src/lib/ict/smt.ts"
      to: "src/lib/ict/rollover.ts"
      via: "evaluateSMT calls detectRollover per leg with shared ROLLOVER_ATR_MULT"
      pattern: "detectRollover"
    - from: "src/lib/ict/smt.ts"
      to: "src/lib/ict/regime.ts"
      via: "per-leg tripwire ATR from computeATR(legD1).at(-1)"
      pattern: "computeATR"
    - from: "src/lib/ict/smt.test.ts"
      to: "src/lib/__fixtures__/smt-rollweek.json"
      via: "joint suppression test imports the roll-week pair via @ alias"
      pattern: "smt-rollweek"
  prohibitions:
    - "Never emit a directional SMT signal on decoupled, thin-history, or rollover-suspect input; suppress with the stated reason instead of guessing"
    - "Never pair swings by array index across legs; pair by overlapping calendar window only"
    - "Never normalize across legs with adjusted-close data; raw OHLC plus basis-point compare only"
---

<objective>
Tracer plus hardening for the time-anchored SMT comparator (ICT-08) with joint rollover-week suppression (ICT-09) — per D-01 through D-08.

Purpose: This is the phase tracer (TRACER_MODE true): the first production-quality end-to-end slice proving joined D1 input → correlation gate → rollover check → swing match → directional signal works before FVG and aggregation expand from it.
Output: New pure module src/lib/ict/smt.ts, its vitest suite, and one roll-week JSON fixture. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: SMT core slice bull/bear happy path end to end</name>
  <files>src/lib/ict/smt.ts, src/lib/ict/smt.test.ts</files>
  <read_first>src/lib/ict/regime.ts, src/lib/ict/rollover.ts, src/lib/ict/types.ts, src/lib/ict/join.ts, src/lib/ict/rollover.test.ts</read_first>
  <action>Create src/lib/ict/smt.ts following the regime.ts house template (@-aliased type imports, caps constants at module top, closedOnly first line, fail-fast throw with echoed value). Export constants SWING_K = 2 per D-02, SMT_TOL_BPS = 25 per D-03, CORR_WINDOW = 20 and CORR_MIN = 0.70 per D-05, SWING_LOOKBACK = 60 per D-02. Export types MatchedSwing with fields windowStart, windowEnd, kind high|low, nqExtreme, esExtreme; SmtSignal with suppressed false plus direction BULLISH|BEARISH|NO-SIGNAL, sweeperLeg NQ|ES|null, nqWindow, esWindow, bpsGap per D-04; SmtSuppressed with suppressed true plus reason CORR_DECOUPLED|rollover-week and optional corr per D-06 and D-08. Implement isSwingHigh and isSwingLow as strict fractal checks over k neighbors each side with equality meaning no swing. Implement matchSwings taking NQ and ES D1 Candle arrays plus optional k defaulting to SWING_K, applying closedOnly to both legs, detecting swings with the same k on both legs, and pairing by overlapping calendar date window inside the trailing SWING_LOOKBACK bars, returning pairs ascending by windowStart. Implement detectSMT taking matched pairs plus optional toleranceBps defaulting to SMT_TOL_BPS, throwing on non-finite or non-positive tolerance while echoing the received value, returning NO-SIGNAL with null legs on empty input, evaluating the most recent pair first, labeling bearish when NQ takes its prior high while ES holds below its own prior high by more than tolerance with sweeperLeg NQ and the bullish mirror with sweeperLeg ES, computing bpsGap as absolute swept-minus-held over the reference extreme times 10000, treating a gap exactly equal to tolerance as NO-SIGNAL under strict-greater-than semantics, and defaulting to NO-SIGNAL. Feed D1 daily candles only per D-01; keep the D1 NQ anchor path untouched by working on the passed arrays without mutating caller data. Keep the module free of current-time reads with asOf passed as a plain string parameter, zero imports from any store module, and raw OHLC fields only with no adjusted-close data. Create src/lib/ict/smt.test.ts in the rollover.test.ts builder style with candle and trend helpers using Partial Candle overrides, covering a bullish case asserting direction BULLISH with sweeperLeg ES, a bearish mirror asserting sweeperLeg NQ, and a default NO-SIGNAL case, plus constant pins asserting SWING_K 2, SMT_TOL_BPS 25, CORR_WINDOW 20, CORR_MIN 0.70.</action>
  <verify>
    <automated>npm test -- src/lib/ict/smt.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
  </verify>
  <done>Tracer slice green: bull/bear/NO-SIGNAL happy paths pass through matchSwings plus detectSMT with the locked constants pinned.</done>
  <acceptance_criteria>npm test on the new suite passes; suite asserts direction BULLISH with sweeperLeg ES on the bull fixture, direction BEARISH with sweeperLeg NQ on the bear fixture, and all four locked constants equal 2, 25, 20, and 0.70 exactly.</acceptance_criteria>
</task>

<task type="auto">
  <name>SMT hardening: correlation gate, joint rollover suppression, negative fixtures</name>
  <files>src/lib/ict/smt.ts, src/lib/ict/smt.test.ts, src/lib/__fixtures__/smt-rollweek.json</files>
  <read_first>src/lib/ict/smt.ts, src/lib/ict/regime.ts, src/lib/ict/rollover.ts, src/lib/ict/types.ts</read_first>
  <action>Extend src/lib/ict/smt.ts with export pearsonCorr over two paired close arrays using the textbook numerator over the product of root-sum-squares, returning NaN on a zero-variance denominator and converting every non-finite result to decoupled at the caller, never propagating it. Add export evaluateSMT taking NQ D1 candles, ES D1 candles, and an asOf string, running in fixed gate order correlation first per D-07, then joint rollover, then matching: filter both legs through closedOnly and drop rows with any non-finite OHLC field at the boundary, pair the surviving closes by shared calendar date, return suppressed reason CORR_DECOUPLED with no corr field when fewer than CORR_WINDOW paired closes exist, compute pearsonCorr over the trailing CORR_WINDOW paired closes and return suppressed reason CORR_DECOUPLED carrying the corr value when it is non-finite or below CORR_MIN per D-05 and D-06 while skipping matchSwings entirely, then compute per-leg ATR as computeATR of that leg with the final series element and treat a leg with an empty ATR series as non-suspect, call detectRollover per leg with the shared multiplier and hints NQ D1 continuous and ES D1 continuous, return suppressed reason rollover-week when either flag is suspect per D-08, otherwise run matchSwings and detectSMT and return the signal. Create src/lib/__fixtures__/smt-rollweek.json as a plain object with nq and es D1 arrays of at least 25 bars where the NQ leg carries one close-to-close gap larger than three times its local ATR while the ES leg stays smooth, mirroring the join-misaligned.json plain-array style loaded by a single import. Extend src/lib/ict/smt.test.ts with a choppy plus-or-minus-1-bar offset pair asserting NO-SIGNAL, a lookback-shift case shifting one leg by 2 bars asserting the signal does not flip direction, a decoupled low-correlation pair asserting suppressed CORR_DECOUPLED with matching skipped at output level, a flat-ES 20-close window asserting suppressed output with no throw, a joint roll-week test importing the new JSON fixture asserting suppressed rollover-week rather than divergence, an exact-tolerance edge asserting NO-SIGNAL when the gap equals SMT_TOL_BPS, and a forming-exclusion test passing forming-flagged rows asserting they never enter pairs.</action>
  <verify>
    <automated>npm test -- src/lib/ict/smt.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn 'Date\.now' src/lib/ict/smt.ts; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module</fails_when>
    <automated>grep -rni 'adjclose' src/lib/ict/smt.ts src/lib/ict/smt.test.ts; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning adjusted-close data entered the SMT lane</fails_when>
  </verify>
  <done>Full SMT suite green including all negative fixtures; purity gates clean; ICT-08 and ICT-09 behaviors pinned.</done>
  <acceptance_criteria>Suite passes with cases asserting CORR_DECOUPLED suppression carrying a corr value below 0.70, rollover-week suppression on the JSON fixture, NO-SIGNAL on choppy offsets and on the exact-25bp edge, unchanged direction under plus-or-minus-2-bar shift, and both grep gates returning exit 1 with no output.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/smt.ts — exports SWING_K, SMT_TOL_BPS, CORR_WINDOW, CORR_MIN, SWING_LOOKBACK, types MatchedSwing / SmtSignal / SmtSuppressed / SmtOutput, functions isSwingHigh, isSwingLow, matchSwings, pearsonCorr, detectSMT, evaluateSMT
- src/lib/ict/smt.test.ts — builder helpers candle / trend plus bull, bear, choppy, lookback-shift, decoupled, zero-variance, roll-week, exact-edge, forming-exclusion, constant-pin cases
- src/lib/__fixtures__/smt-rollweek.json — { nq: Candle[], es: Candle[] } joint roll-week pair

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo D1 rows → evaluateSMT | Untrusted numeric OHLC crosses here; nulls and spikes possible |
| evaluateSMT → Phase 8/9 selectors | Suppressed envelopes carry reason strings rendered verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | evaluateSMT / pearsonCorr (smt.ts) | medium | mitigate | Drop non-finite OHLC rows at the module boundary before pairing; throw with echoed value on bad toleranceBps; convert non-finite correlation to CORR_DECOUPLED suppression, never divide or propagate NaN |
| T-07-02 | Tampering | evaluateSMT cross-leg compare (smt.ts) | medium | mitigate | Raw OHLC plus basis-point compare only; grep gate bans adjusted-close data in the SMT lane so roll gaps stay visible |
</threat_model>

<verification>
Full suite stays green so the D1 NQ anchor path (range, bias, regime, rollover, join) is byte-identical: `npm test` passes after both tasks.
</verification>

<success_criteria>
- SMT returns BULLISH / BEARISH / NO-SIGNAL with sweeperLeg plus nqWindow, esWindow, bpsGap on matched time-anchored pairs
- Correlation below 0.70 suppresses with CORR_DECOUPLED before matching; flat-leg windows suppress without throwing
- Either-leg rollover suspect suppresses with rollover-week on the JSON-backed joint fixture
</success_criteria>

<output>
Create `.planning/phases/07-smt-4h-1h-sequencing-math/07-01-SUMMARY.md` when done
</output>
