---
phase: 15-why-now-trigger-engine
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/ict/trigger.ts
- src/lib/ict/trigger.test.ts
- src/lib/store.ts
autonomous: true
requirements:
- TRIG-01
- TRIG-04
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User sees FIRE_LONG when LOW sweep confirms with killzone timing and displacement plus entry FVG, FIRE_SHORT on HIGH sweep mirror, else WAIT_FOR_MANIPULATION with verbatim Azerbaijani reason
  - All thresholds ship as exported TRIGGER_KZ_START_MIN 120, TRIGGER_KZ_END_MIN 300, TRIGGER_DISP_MULT 0.5, TRIGGER_LOG_CAP 50 with CALIBRATION-PROVISIONAL comments
  - selectTrigger derives one end-to-end verdict from live detectors via a shared epoch with no torn reads and appends ARMED-or-better to a capped firing log
  artifacts:
  - src/lib/ict/trigger.ts
  - src/lib/ict/trigger.test.ts
  - src/lib/store.ts
  key_links:
  - evaluateTrigger reads JudasOutput confirmed plus sweepSide plus displacementMult and FvgGap inventory, never raw candles
  - selectTrigger shares one epoch with selectAMD and injects alreadyFired boolean so ict stays pure
  - Firing log append carries asOf plus verdict plus per-gate booleans plus direction plus reasonKey plus sessionDate for Phase 18 replay
---

<objective>
Tracer slice: one production-quality path from detectors through evaluateTrigger to selectTrigger plus firing log, verified end to end before expansion.
Implements D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-10 (see task actions; D-08 serializer and D-09 prose lock land in 15-03).

Purpose: Prove the three-gate fusion architecture on first commit instead of after layered expansion.
Output: New pure trigger module, co-located gate tests, minimal store selector plus log slice.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-why-now-trigger-engine/15-CONTEXT.md
@.planning/phases/15-why-now-trigger-engine/15-RESEARCH.md
@.planning/phases/15-why-now-trigger-engine/15-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: pure evaluateTrigger core with TRIGGER constants per D-01 to D-05 plus D-10</name>
  <files>src/lib/ict/trigger.ts</files>
  <read_first>src/lib/ict/amd.ts, src/lib/ict/judas.ts, src/lib/ict/fvg.ts, src/lib/ict/smt.ts, src/lib/ict/aggregate.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Create new pure module src/lib/ict/trigger.ts following the amd.ts fusion precedent and judas.ts constant precedent. Export TRIGGER_KZ_START_MIN set to 120, TRIGGER_KZ_END_MIN set to 300, TRIGGER_DISP_MULT set to 0.5, TRIGGER_LOG_CAP set to 50, each with a CALIBRATION-PROVISIONAL comment seeded 2026-09-10 and acceptance band 1-4 fires per week. Export types TriggerVerdict with FIRE_LONG plus FIRE_SHORT plus ARMED plus WAIT_FOR_MANIPULATION, TriggerDirection LONG plus SHORT plus null, TriggerReasonKey with FIRE_LONG plus FIRE_SHORT plus ARMED_MISSING_TIMING plus ARMED_MISSING_PURGE plus ARMED_MISSING_DISPLACEMENT plus ARMED_ALREADY_FIRED plus WAIT_FOR_MANIPULATION, TriggerInput with judas plus amd plus smt plus fvg plus asOf plus alreadyFired, TriggerOutput with verdict plus direction plus reasonKey plus reason plus gates timing-purge-displacement plus entryFvg plus inputs echo of judas-amd-smt. Export function evaluateTrigger taking TriggerInput with detector-output types only and injected asOf epoch seconds plus injected alreadyFired boolean, never raw candle arrays. Implement timing gate as strict exclusive NY-minutes membership greater than TRIGGER_KZ_START_MIN and less than TRIGGER_KZ_END_MIN using formatInTimeZone with NY_TZ H and m idiom copied from judas.ts nyMinutesOf, plus nyDateOf helper using formatInTimeZone yyyy-MM-dd for the cooldown key. Implement purge gate per D-01 and D-02 as judas non-null and judas dot confirmed strictly true and sweepSide non-null, trusting confirmed alone for London-only since out-of-killzone sweeps are preRun by construction, with no NY session import or branch. Implement displacement gate per D-03 and D-04 as judas displacementMult greater-or-equal TRIGGER_DISP_MULT with TOL_EPS 1e-9 float tolerance plus resolvable entry FVG of required polarity selected as most recent originDate, where LONG needs BULLISH and SHORT needs BEARISH, returning pass false and entryFvg null when inventory null-empty-mitigated or polarity missing. Implement direction per D-10 as total map of sweepSide alone with HIGH mapping to SHORT and LOW mapping to LONG, magnitude-only displacement never consulted for direction, null sweepSide forcing purge false. Implement counting per D-05 as 3 passes to FIRE with direction, exactly 2 to ARMED with missing-gate reasonKey, 0 to 1 to WAIT_FOR_MANIPULATION, with alreadyFired true plus 3 of 3 downgrading to ARMED with reasonKey ARMED_ALREADY_FIRED and entryFvg null. Carry amd input for parity and downstream without voting, carry smt as read-only agree suffix only appending space plus SMT razılaşır period on concordance HIGH-BEARISH or LOW-BULLISH and empty string when suppressed or null, never blocking FIRE. Set entryFvg non-null only on FIRE verdicts, null on every ARMED and WAIT. Echo inputs judas-amd-smt in output for Phase 16 snapshot and Phase 18 replay. Add fail-at-boundary throws with got-string interpolation for non-object input, non-finite or non-positive asOf, malformed judas envelope with non-boolean candidate-confirmed or illegal sweepSide or non-finite sweepTime, non-boolean alreadyFired, with null detector inputs degrading to honest WAIT never throwing. Keep module pure with header comment stating detector outputs plus injected asOf plus alreadyFired only, no clock reads, no store imports, type-only detector imports plus NY_TZ plus formatInTimeZone. Keep output serializable with plain FvgGap data only.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/ict/trigger.ts exports TRIGGER_KZ_START_MIN 120, TRIGGER_KZ_END_MIN 300, TRIGGER_DISP_MULT 0.5, TRIGGER_LOG_CAP 50 and evaluateTrigger. Behavior assertion: LOW sweep 3-of-3 yields FIRE_LONG, HIGH sweep 3-of-3 yields FIRE_SHORT, 0-gate yields WAIT_FOR_MANIPULATION. Test command: npx vitest run src/lib/ict/trigger.test.ts passes.</acceptance_criteria>
  <done>Single happy path works end to end through pure evaluateTrigger and is committed with constants pinned.</done>
  <reversibility rating="reversible">Pure additive module plus aliases; tuning constants later is a deliberate test-pinned change.</reversibility>
</task>

<task type="auto">
  <name>Wave 0 trigger tests: happy paths plus constants plus choppy QUIET plus NY-never-fires per D-01 D-02</name>
  <files>src/lib/ict/trigger.test.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/amd.test.ts, src/lib/ict/judas.test.ts, src/lib/ict/fvg.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Create co-located test file src/lib/ict/trigger.test.ts copying amd.test.ts fixture idiom plus judas.test.ts epoch idiom without inventing new time builders. Include fixtureTriggerJudas with Partial override defaulting to candidate false confirmed false preRun false sweepSide null sweepTime null displacementMult 0, signalSmt default BEARISH with sweeperLeg NQ and bpsGap 30, suppressedSmt with reason CORR_DECOUPLED, fixtureFvg list builder in fvg.test.ts candle plus dated style, nyMinuteEpoch plus nyHourEpoch via fromZonedTime with NY_TZ, SESSION constant 2026-06-15, row builder with time open high low close plus overrides, fixtureRange anchor high 20100 low 20000 height 100. Cover TRIG-01 happy paths: LOW sweep with confirmed true plus displacementMult 0.6 plus BULLISH FVG plus asOf inside killzone 03:00 NY plus alreadyFired false yields FIRE_LONG, HIGH sweep mirror yields FIRE_SHORT. Cover constant pins with toBe for TRIGGER_DISP_MULT 0.5 and kz window 120 and 300 and TRIGGER_LOG_CAP 50. Cover choppy-sideways spam guard with price 20050 rows strictly inside 20100 and 20000 and judas null or unconfirmed yielding WAIT_FOR_MANIPULATION with 0 to 1 gates. Cover D-01 and D-02 pin: NY-hours asOf 10:00 with preRun-style judas confirmed false yields purge false and never FIRE. Use verbatim toBe pins for reasons once D-09 wording locked in Plan 03 prose task, reusing the same base sentences. Assert inputs echo preserved and entryFvg polarity matches direction on FIRE.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: FIRE_LONG on LOW 3-of-3, FIRE_SHORT on HIGH 3-of-3, WAIT_FOR_MANIPULATION on choppy fixture. Test command: npx vitest run src/lib/ict/trigger.test.ts passes with constant pins green. Source assertion: NY-sweep test exists and asserts purge false.</acceptance_criteria>
  <done>Gate happy paths plus constant pins plus choppy QUIET plus NY-never-fires are green.</done>
</task>

<task type="auto">
  <name>Minimal selectTrigger plus sharedEpoch plus firing log append per D-06 D-07</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts, src/lib/ict/trigger.ts, src/lib/ict/aggregate.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Extend tracked store module src/lib/store.ts beside selectJudas and selectAMD and selectLiquidityPath. Add interface entries selectTrigger returning TriggerOutput or null plus firingLog array plus firingLogOverflow number plus appendFiringLog taking TriggerOutput and asOf number. Import evaluateTrigger plus TriggerOutput plus TriggerVerdict plus TriggerDirection plus TriggerReasonKey plus TRIGGER_LOG_CAP plus FiringLogEntry type from the trigger module following lines 13 to 17 import-block style. Extract module-scope sharedEpoch helper from the selectAMD IIFE that reads nq1h lastUpdatedISO with Date fallback at caller boundary only, then reuse it in both selectAMD and new selectTrigger for single time truth with no torn reads. Implement selectTrigger with refuse plus try-catch-never-throw envelope copying selectSMT and selectJudas precedent: stale or empty nq15m or nq1h legs return null, then inside try read selectJudas plus selectSMT plus selectAMD with sharedEpoch plus inline FVG derivation via detectFVGs on nq candles followed by applyMitigation with empty or stale mapping to fvg null, compute sessionDate via formatInTimeZone on asOf with NY_TZ yyyy-MM-dd, derive alreadyFired by scanning firingLog for FIRE_LONG or FIRE_SHORT with equal sessionDate, call evaluateTrigger with judas amd smt fvg asOf alreadyFired, append ARMED-or-better results via appendFiringLog, return output, catch path returns null. Implement appendFiringLog to append only when verdict is not WAIT_FOR_MANIPULATION, skip FIRE entries when same sessionDate FIRE already exists per D-06, drop oldest beyond TRIGGER_LOG_CAP with overflow counter increment, minimal entry shape with asOf plus verdict plus gates timing-purge-displacement plus direction plus reasonKey plus sessionDate per D-07. Keep Blob and anchor click out of this phase, leaving serializer click to Phase 17 seam.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/store.ts exports selectTrigger and firingLog and appendFiringLog and sharedEpoch reuse in selectAMD. Behavior assertion: live London confirmed sweep with FVG yields FIRE verdict through selector, stale legs yield null without throw. CLI output: npx vitest run src/lib/store.test.ts passes.</acceptance_criteria>
  <done>One poll-driven path from detectors through selectTrigger to firing log works and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yahoo-rows→detectors→trigger | Malformed OHLC and epoch rows cross here; trigger inherits detector-clean outputs only |
| store-selector→render | Degraded null versus honest WAIT crosses here; selector never throws into render |
| firing-log→calibration-export | Verdict plus gate booleans cross here; no account or risk numbers ever logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | evaluateTrigger TriggerInput envelope (asOf, judas, FvgGap) | high | mitigate | Fail-at-boundary throw on non-finite or non-positive asOf, malformed judas envelope with got-string errors, FvgGap finiteness via assertFiniteGap precedent; null detector inputs degrade to WAIT, never coerce |
| T-15-02 | Denial of service | firingLog slice in store.ts | medium | mitigate | TRIGGER_LOG_CAP 50 with oldest-drop plus overflow counter; FIRE session-dedup prevents per-poll spam growth |
| T-15-03 | Information disclosure | firingLogToJson calibration export shape | low | mitigate | Entries carry asOf plus verdict plus gates plus direction plus reasonKey plus sessionDate only; no prices beyond entry FVG bounds, no account or risk numbers |
| T-15-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; package-legitimacy gate skipped with reason since only installed date-fns-tz plus vitest plus zustand are used |
</threat_model>

<verification>
Per-task vitest runs plus npx tsc --noEmit at plan merge plus npm test full-suite gate before verify-work. Purity guard npx vitest run src/lib/ict/purity.test.ts must stay green with new trigger.ts auto-scanned.
</verification>

<success_criteria>
Tracer FIRE_LONG and FIRE_SHORT happy paths green, constants pinned, choppy fixture QUIET, NY sweep never FIREs, selectTrigger end-to-end append works, purity guard green.
</success_criteria>

<output>
Create `.planning/phases/15-why-now-trigger-engine/15-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ict/trigger.ts: TRIGGER_KZ_START_MIN, TRIGGER_KZ_END_MIN, TRIGGER_DISP_MULT, TRIGGER_LOG_CAP, TriggerVerdict, TriggerDirection, TriggerReasonKey, TriggerInput, TriggerOutput, evaluateTrigger, nyMinutesOf, nyDateOf, REASON base sentences plus SMT agree suffix
- src/lib/ict/trigger.test.ts: fixtureTriggerJudas, signalSmt, suppressedSmt, fixtureFvg, nyMinuteEpoch, nyHourEpoch, SESSION fixture, gate happy-path plus constant-pin plus choppy-QUIET plus NY-never-fires tests
- src/lib/store.ts: sharedEpoch helper, selectTrigger selector, firingLog plus firingLogOverflow state, appendFiringLog action, FiringLogEntry interface
