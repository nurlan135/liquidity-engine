---
phase: 15-why-now-trigger-engine
plan: '03'
type: execute
wave: 3
depends_on:
- 15-02
files_modified:
- src/lib/ict/trigger.ts
- src/lib/ict/trigger.test.ts
- src/lib/store.ts
- src/lib/store.test.ts
autonomous: true
requirements:
- TRIG-02
- TRIG-03
- TRIG-04
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User reads verbatim Azerbaijani reasons for FIRE_LONG, FIRE_SHORT, each ARMED variant, and WAIT_FOR_MANIPULATION following amd.ts sentence precedent with at most one fixed SMT suffix
  - User can inspect a firing log capped at 50 entries with oldest-drop plus overflow counter, FIRE deduped per NY-date session, exporting calibration JSON with threshold versions
  - Every TRIGGER constant carries a CALIBRATION-PROVISIONAL comment and a boundary test; trigger.ts passes the co-located purity guard with no clock reads or store imports
  artifacts:
  - src/lib/ict/trigger.ts
  - src/lib/ict/trigger.test.ts
  - src/lib/store.ts
  - src/lib/store.test.ts
  key_links:
  - Reason strings are deterministic constants pinned with toBe, never interpolated with numbers or prices
  - firingLogToJson pure serializer is the Phase 17 Blob and anchor click seam
  - selectTrigger derives from sharedEpoch so trigger can never desync from selectAMD on time
---

<objective>
Polish: verbatim prose, firing-log cap plus dedup plus serializer, store selector tests, calibration pins, purity green.
Implements D-06, D-07, D-08, D-09 (see task actions; builds on D-01 through D-05 plus D-10 from 15-01 and 15-02).

Purpose: Lock the user-visible honesty surface and the calibration export seam Phase 17 and Phase 18 build on.
Output: Final prose constants, hardened store slice plus serializer, store tests, full-suite green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-why-now-trigger-engine/15-CONTEXT.md
@.planning/phases/15-why-now-trigger-engine/15-RESEARCH.md
@.planning/phases/15-why-now-trigger-engine/15-PATTERNS.md
@.planning/phases/15-why-now-trigger-engine/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Verbatim Azerbaijani prose plus reasonKey mapping per D-09</name>
  <files>src/lib/ict/trigger.ts, src/lib/ict/trigger.test.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/amd.ts, src/lib/ict/trigger.test.ts, .planning/phases/15-why-now-trigger-engine/15-RESEARCH.md</read_first>
  <action>Lock D-09 verbatim Azerbaijani reason constants in src/lib/ict/trigger.ts following amd.ts sentence precedent with deterministic selection and at most one fixed SMT suffix, no number or price interpolation, no banned vocabulary like məncə or ehtimal. Set REASON_FIRE_LONG to WHY NOW LONG colon Asia Range Təmizlənib period London Judas Swing Baş verib em-dash displacement təsdiqlidir comma giriş FVG hazırdır period. Set REASON_FIRE_SHORT to the same shape with WHY NOW SHORT prefix. Set REASON_ARMED_MISSING_TIMING to WHY NOW ARMED colon Quruluş hazırdır em-dash killzone pəncərəsi gözlənilir period. Set REASON_ARMED_MISSING_PURGE to WHY NOW ARMED colon Quruluş hazırdır em-dash London Judas təsdiqi gözlənilir period. Set REASON_ARMED_MISSING_DISPLACEMENT to WHY NOW ARMED colon Quruluş hazırdır em-dash displacement təsdiqi və giriş FVG gözlənilir period. Set REASON_ARMED_ALREADY_FIRED to WHY NOW ARMED colon Bu sessiyada siqnal artıq verilib em-dash təkrar giriş yoxdur period. Set REASON_WAIT to WAIT FOR MANIPULATION colon Zaman comma süpürmə və displacement razılaşmır em-dash manipulyasiya gözlənilir period. Map each TriggerReasonKey to its base sentence, appending space plus SMT razılaşır period only on unsuppressed concordant SMT per HIGH-BEARISH and LOW-BULLISH rule, empty string otherwise including suppressed and null and discordant. Update src/lib/ict/trigger.test.ts to pin every reason with toBe exact match, never toContain, for FIRE_LONG plus FIRE_SHORT plus all four ARMED variants plus WAIT, plus suffix present and absent cases. Update any Plan 01 and Plan 02 expected strings to these locked sentences.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/ict/trigger.ts contains the seven locked Azerbaijani sentences verbatim. Behavior assertion: each verdict reason matches with toBe including suffix cases. Test command: npx vitest run src/lib/ict/trigger.test.ts passes.</acceptance_criteria>
  <done>All verdict prose pinned verbatim with toBe and SMT suffix rule green.</done>
  <reversibility rating="reversible">Wording change is a constant edit with test pins; no structural coupling.</reversibility>
</task>

<task type="auto">
  <name>Firing-log slice hardening plus firingLogToJson serializer per D-07 D-08</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts, src/lib/ict/trigger.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Harden the firing-log slice in tracked src/lib/store.ts per D-07 minimal entry and D-08 cap plus JSON export. Define exported interface FiringLogEntry with asOf number plus verdict TriggerVerdict plus gates timing purge displacement booleans plus direction TriggerDirection plus reasonKey TriggerReasonKey plus sessionDate string NY-date key. Implement appendFiringLog to append only when verdict is not WAIT_FOR_MANIPULATION, skip FIRE_LONG and FIRE_SHORT when a same-sessionDate FIRE already exists per D-06, drop oldest entries beyond TRIGGER_LOG_CAP 50 with firingLogOverflow counter increment, slice idiom copying fvg.ts active dot slice of negative FVG_MAP_BOUND. Implement exported pure function firingLogToJson taking entries array and returning JSON string of object with entries plus thresholds object holding TRIGGER_DISP_MULT plus TRIGGER_KZ_START_MIN plus TRIGGER_KZ_END_MIN plus exportedAt caller ISO string, no Blob or createObjectURL or anchor code since download click is the Phase 17 seam. Keep serializer pure and unit-testable without rendering, entries carrying verdicts plus gate booleans only with no account or risk numbers and no prices beyond entry FVG bounds. Wire selectTrigger to call appendFiringLog on ARMED-or-better results and to skip append on already-fired downgrade repeats that would spam the log.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/store.ts exports FiringLogEntry and firingLogToJson with thresholds and exportedAt shape. Behavior assertion: cap 50 drops oldest with overflow count, same-session FIRE dedups, WAIT never appends. Test command: npx vitest run src/lib/store.test.ts passes.</acceptance_criteria>
  <done>Firing log capped plus deduped with pure calibration serializer ready for Phase 17 click seam.</done>
</task>

<task type="auto">
  <name>Store selector plus serializer tests plus TRIG-04 calibration pins plus purity green</name>
  <files>src/lib/store.test.ts, src/lib/ict/trigger.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/store.test.ts, src/lib/ict/trigger.ts, src/lib/ict/purity.test.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Extend tracked src/lib/store.test.ts copying stale-refusal plus empty plus all-degraded blocks and resetDualState plus stubFourLegs plus mockIntradayEnvelope plus stopDualPoll lifecycle from the same file. Cover selectTrigger stale nq15m yielding null, empty legs yielding null without throwing via not dot toThrow, shared-epoch coherence with selectAMD on the same poll, FIRE path appending one log entry with sessionDate plus gates plus direction plus reasonKey, same-session repeat FIRE deduped with no second append, 51 ARMED appends capping at 50 with oldest dropped and overflow counter incremented, firingLogToJson shape holding entries plus thresholds TRIGGER_DISP_MULT 0.5 plus kz 120 and 300 plus exportedAt string. Extend src/lib/ict/trigger.test.ts with TRIG-04 calibration pins asserting each TRIGGER constant has a CALIBRATION-PROVISIONAL comment via source read plus value toBe pins. Run purity guard npx vitest run src/lib/ict/purity.test.ts asserting trigger.ts auto-scanned with zero Date dot now and zero zustand or store imports, then full npm test and npx tsc --noEmit before commit.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: stale and empty yield null without throw; cap plus dedup plus serializer shape green. Test command: npx vitest run src/lib/store.test.ts passes. CLI output: npx vitest run src/lib/ict/purity.test.ts passes and npm test full suite green.</acceptance_criteria>
  <done>Store selector plus log plus serializer plus calibration pins green with purity guard and full suite passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reason-string→report | Fixed Azerbaijani sentences cross here; interpolation would break parity pins and leak prices |
| firing-log→calibration-JSON | Bounded entries cross here; threshold versions included so reviews map fires to constant sets |
| ict-module→purity-guard | New trigger.ts crosses the glob guard here; any clock or store import fails the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering | Reason strings with interpolation | medium | mitigate | Deterministic sentence selection only with toBe pins; no number or price interpolation; SMT suffix is the single fixed append |
| T-15-07 | Information disclosure | firingLogToJson payload | low | mitigate | Payload holds entries plus threshold versions plus exportedAt only; no account, risk, position, or P&L fields; Phase 17 Blob click consumes the string without reshaping |
| T-15-08 | Tampering | trigger.ts purity (clock and store imports) | high | mitigate | Co-located purity.test.ts glob guard runs in this plan; injected asOf plus alreadyFired are the only time and state paths; block on high per ASVS L1 gate |
| T-15-SC | Tampering | npm installs | high | mitigate | Zero new packages; legitimacy gate skipped with reason since only installed date-fns-tz plus vitest plus zustand are used |
</threat_model>

<verification>
npx vitest run src/lib/ict/trigger.test.ts plus npx vitest run src/lib/store.test.ts plus npx vitest run src/lib/ict/purity.test.ts after tasks, then npx tsc --noEmit plus npm test full-suite gate before verify-work.
</verification>

<success_criteria>
Verbatim prose pinned, log cap 50 plus dedup plus serializer proven, selector stale and coherence proven, TRIG-04 provisional pins green, purity guard green, full suite 300-plus-new green.
</success_criteria>

<output>
Create `.planning/phases/15-why-now-trigger-engine/15-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ict/trigger.ts: REASON_FIRE_LONG, REASON_FIRE_SHORT, REASON_ARMED_MISSING_TIMING, REASON_ARMED_MISSING_PURGE, REASON_ARMED_MISSING_DISPLACEMENT, REASON_ARMED_ALREADY_FIRED, REASON_WAIT, smt agree-suffix helper
- src/lib/store.ts: FiringLogEntry interface, firingLog plus firingLogOverflow state, appendFiringLog action with cap plus dedup, firingLogToJson pure serializer with thresholds plus exportedAt
- src/lib/store.test.ts: selectTrigger stale plus empty plus coherence tests, log cap plus oldest-drop plus dedup tests, firingLogToJson shape test with threshold versions
- src/lib/ict/trigger.test.ts: verbatim toBe prose pins, CALIBRATION-PROVISIONAL comment pins
