---
phase: 02-terminal-composition
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/freshness.ts
  - src/lib/status-strip.test.ts
  - components/dashboard/status-strip.tsx
  - src/lib/zone-bands.ts
  - src/lib/zone-bands.test.ts
  - components/charts/zone-primitive.ts
  - components/charts/nq-chart.tsx
  - app/globals.css
autonomous: true
requirements: [DATA-03, UI-02]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Strip shows LIVE with seconds-ago age on fresh envelopes and STALE with minute age on stale envelopes, never silent
    - Baku Saturday and Sunday render the CLOSED strip state with last closed candles intact and no synthesized bars
    - Chart shades the premium half magenta and the discount half green with a dashed cyan EQ line and a solid cyan DOL marker
    - Stale chart desaturates zone fills and shows a STALE badge while closed chart shows a MARKET CLOSED ribbon
    - Forming candles render only with the Formalaşan şam chip and thin history renders without stretching
  artifacts:
    - src/lib/freshness.ts
    - src/lib/status-strip.test.ts
    - components/dashboard/status-strip.tsx
    - src/lib/zone-bands.ts
    - src/lib/zone-bands.test.ts
    - components/charts/zone-primitive.ts
    - components/charts/nq-chart.tsx
    - app/globals.css
  key_links:
    - StatusStrip reads stale plus lastUpdatedISO from useDashboard and derives state through the freshness helper
    - zone-primitive attaches to the candlestick series and recomputes price-to-pixel geometry on every draw
    - NqChart consumes mapCandlesToSeries plus zoneBands plus priceLineInputs and removes price-lines before re-creating
  prohibitions:
    - must NOT synthesize OHLC candles for weekends, holidays, or gaps; render last closed candles with honest STALE or CLOSED states
    - must NOT present fixture sentiment, calendar, or interpretation prose as live market data; the scenario label stays visible
    - must NOT render bias, DOL, or confidence from insufficient history; degrade to COMPRESSION with a stated reason
---

<objective>
Freshness truth plus chart overlays: a tested Baku-aware status derivation with its global strip component per D-02 and D-03, and the D-11 zone-fill primitive with stale and closed overlays on the tracer chart. Per D-12 every date decision flows through the Phase 1 Baku util with injected clock.

Purpose: Make staleness unmissable and zones visible, the two visual proofs DATA-03 and UI-02 gate on.
Output: Freshness lib plus StatusStrip plus zone bands helper plus primitive plus an NqChart that desaturates on stale and ribbons on closed.

Flagged assumptions (spec-less probe fallback, surfaced not dropped):
- A-ui2-1 (UI-02 boundary): a last close exactly on EQ renders the position line as Ekvilibrium with no premium or discount edge claimed.
- A-ui2-2 (UI-02 precision): EQ and DOL price-line values are passed through at full float precision with no rounding; axis labels show the library default formatting.
- A-d3-1 (DATA-03): CLOSED derives from Baku Saturday or Sunday only per RESEARCH A6; a weekday CME holiday shows LIVE-with-aging-data guarded by the server stale flag.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-composition/02-CONTEXT.md
@.planning/phases/02-terminal-composition/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Freshness derivation plus StatusStrip</name>
  <files>src/lib/freshness.ts, src/lib/status-strip.test.ts, components/dashboard/status-strip.tsx</files>
  <read_first>src/lib/time.ts, src/lib/store.ts, components/dashboard/terminal-shell.tsx, components/ui/card.tsx</read_first>
  <behavior>
    - Test 1: deriveStatus on a fresh envelope with lastUpdatedISO 12 seconds before now returns state LIVE with ageSec near 12.
    - Test 2: deriveStatus on an envelope with stale true returns STALE regardless of age.
    - Test 3: deriveStatus on a Baku Saturday instant returns CLOSED even when the envelope is fresh.
    - Test 4: formatStripAge renders under 60 seconds as seconds-ago copy and 60 seconds or more as minute copy.
    - Test 5: deriveStatus throws on an unparseable lastUpdatedISO string.
  </behavior>
  <action>Create the pure freshness module with named exports StatusState union of LIVE STALE CLOSED strings, deriveStatus accepting an object with stale boolean and lastUpdatedISO string plus a now Date defaulting to current time only at the boundary, deciding CLOSED first when the Baku weekday of now via the Phase 1 Baku formatter is Saturday or Sunday per D-03, then STALE when the envelope stale flag is true or age reaches 120 seconds, otherwise LIVE, computing age seconds from now minus parsed lastUpdatedISO with a finite guard. Export formatStripAge turning age seconds into the UI-SPEC Azerbaijani strip copy shapes for live, stale with the son keş göstərilir tail, and the closed BAZAR BAĞLIDIR line. Cover with the co-located suite using fixed Date instants including a known Baku Saturday noon and a weekday noon, never calling real clock inside assertions. Build StatusStrip as a named export starting with the client pragma, reading stale and lastUpdatedISO through the dashboard store with a shallow-compared selector, ticking a local now state on a 10 second interval isolated to this component, rendering a section element carrying the status-strip slot with the LIVE accent dot only in LIVE state, mono numerals for age, muted styling otherwise, English identifiers with Azerbaijani rendered copy per D-04, and styling through theme tokens plus the cn merger with no literal color values. Per D-02 this strip spans all panels once the shell imports it in the integration plan.</action>
  <verify>
    <automated>npx vitest run src/lib/status-strip.test.ts</automated>
    <fails_when>non-zero exit, or No test files found in output, or any Failed Tests section in output</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Status derivation is tested on fixed instants and the strip renders the three states from envelope truth.</done>
  <acceptance_criteria>src/lib/status-strip.test.ts exits 0 via npx vitest run src/lib/status-strip.test.ts; src/lib/freshness.ts exports deriveStatus and formatStripAge; components/dashboard/status-strip.tsx carries the status-strip slot and reads stale plus lastUpdatedISO from the dashboard store; npx tsc --noEmit exits 0</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Zone bands helper plus fill primitive</name>
  <files>src/lib/zone-bands.ts, src/lib/zone-bands.test.ts, components/charts/zone-primitive.ts</files>
  <read_first>src/lib/ict/types.ts, src/lib/ict/range.ts, components/charts/nq-chart.tsx</read_first>
  <behavior>
    - Test 1: zoneBands on a range with high 100 low 0 eq 50 returns premium bottom 50 top 100 and discount bottom 0 top 50.
    - Test 2: zoneBands throws on non-finite high low or eq.
    - Test 3: zoneBands throws when eq lies outside the high low span.
  </behavior>
  <action>Create the pure bands module with a named ZoneBands interface holding premiumTop premiumBottom discountTop discountBottom numbers and a named zoneBands function accepting a DealingRange, guarding finite inputs and eq inside span with thrown Errors, returning the premium half from eq to high and the discount half from low to eq per D-11. Cover with the co-located suite including the 100/0/50 halves case and both throw paths. Implement the zone fill primitive as a named ZoneFillPrimitive class in the charts directory following the official session-highlighting ISeriesPrimitive pattern: constructor takes a getter returning current ZoneBands, the pane renderer recomputes y coordinates from the series priceToCoordinate on every draw so pan and zoom never desync, premium fill uses the terminal premium token at 8 percent and discount fill the terminal discount token at 8 percent, exposes updateBands triggering updateAllViews, and provides attachZoneFill plus detachZoneFill helpers wrapping series attachPrimitive and detachPrimitive. Include the sanctioned fallback in the same module as a named overlay fallback pair builder tracing range high and range low across the visible window with autoscale disabled, documented as the executor fallback when the primitive spike overruns its time-box per D-11. Keep English identifiers and comments throughout.</action>
  <verify>
    <automated>npx vitest run src/lib/zone-bands.test.ts</automated>
    <fails_when>non-zero exit, or No test files found in output, or any Failed Tests section in output</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Band geometry is tested pure logic and the primitive paints both halves from live scale coordinates with a documented fallback.</done>
  <acceptance_criteria>npx vitest run src/lib/zone-bands.test.ts exits 0; src/lib/zone-bands.ts exports zoneBands; components/charts/zone-primitive.ts contains attachPrimitive and priceToCoordinate and updateAllViews; the primitive module contains no addCandlestickSeries text</acceptance_criteria>
</task>

<task type="auto">
  <name>NqChart zone plus stale and closed overlays</name>
  <files>components/charts/nq-chart.tsx, app/globals.css</files>
  <read_first>components/charts/nq-chart.tsx, src/lib/chart-mapper.ts, src/lib/zone-bands.ts, components/charts/zone-primitive.ts, app/globals.css</read_first>
  <behavior>
    - Chart with a fresh envelope paints both zone fills with EQ dashed and DOL solid lines and no overlay badge.
    - Chart with a stale envelope keeps candles visible, desaturates fills, and shows the STALE badge top-right.
    - Chart with a weekend envelope keeps last closed candles with the MARKET CLOSED ribbon and no animation.
  </behavior>
  <action>Extend the chart island props with rangeHigh rangeLow eq dolPrice dolName status string and forming boolean alongside existing candles, wiring zoneBands output into the ZoneFillPrimitive attached once with updateBands on range change and detached on unmount, keeping EQ dashed 1px and DOL solid 1px price-lines managed through refs with removal before re-creation. Render overlay divs inside the chart block: a STALE badge overlay pinned top-right when status is stale with zone fills switched to 4 percent opacity, a MARKET CLOSED ribbon overlay with no animation when status is closed keeping last closed candles rendered and never synthesizing bars per D-03, a Formalaşan şam superscript chip on the latest row when forming is true, and the documented Məlumat yoxdur copy with Yenilə CTA when candles are empty. Extend the .dark block in globals.css with stale desaturation tokens at 4 percent and closed ribbon tokens, preserving the existing import header and terminal tokens from the tracer plan, with components referencing tokens only. Verify visually in dev plus typecheck; canvas shading itself stays manual per the validation strategy.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
    <automated>npm run test -- chart-mapper</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Chart shows zones plus EQ and DOL with honest stale and closed overlays and typecheck plus mapper suite green.</done>
  <acceptance_criteria>npx tsc --noEmit exits 0; components/charts/nq-chart.tsx imports zoneBands and the zone fill helpers; components/charts/nq-chart.tsx contains STALE badge markup and MARKET CLOSED ribbon markup and the Formalaşan şam chip; app/globals.css still contains the terminal accent token under .dark</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store envelope → StatusStrip derivation | Stale truth crosses into user-visible freshness copy here |
| Zone bands → canvas primitive | Derived prices cross into pixel geometry here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | freshness derivation (src/lib/freshness.ts) | medium | mitigate | Derive from server-authoritative stale flag plus lastUpdatedISO age per D-02; throw on unparseable timestamps; strip never invents LIVE from local clock alone |
| T-02-04 | Tampering | zone geometry (src/lib/zone-bands.ts) | low | mitigate | Finite-input plus eq-in-span guards throw before the primitive paints; malformed ranges render error copy instead of misplaced shading |
| T-02-SC | Tampering | npm installs for chart deps | high | mitigate | Zero new installs; lightweight-charts stays lockfile-pinned with the RESEARCH legitimacy audit passed; any future install needs a blocking human checkpoint |
</threat_model>

<verification>
Freshness plus zone suites green, typecheck clean, and a dev render shows LIVE/STALE/CLOSED strip states with shaded zones plus EQ and DOL lines on the chart.
</verification>

<success_criteria>
deriveStatus and zoneBands suites pass, StatusStrip renders all three states from envelope truth, NqChart paints zones with stale and closed overlays, and typecheck is clean.
</success_criteria>

<output>
Create .planning/phases/02-terminal-composition/02-02-SUMMARY.md when done
</output>

## Artifacts this phase produces

Plan 02 creates: freshness helpers deriveStatus plus formatStripAge with StatusState in src/lib/freshness.ts; StatusStrip named component in components/dashboard/status-strip.tsx; zone geometry helper zoneBands with ZoneBands in src/lib/zone-bands.ts; ZoneFillPrimitive class with attachZoneFill plus detachZoneFill plus overlay fallback builder in components/charts/zone-primitive.ts; extended NqChart props for range plus status plus forming with STALE badge plus MARKET CLOSED ribbon plus Formalaşan şam chip; stale and closed tokens under .dark in app/globals.css. Test files: src/lib/status-strip.test.ts, src/lib/zone-bands.test.ts.
