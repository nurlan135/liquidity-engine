---
phase: 02-terminal-composition
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/lib/blocked-side.ts
  - src/lib/blocked-side.test.ts
  - src/lib/report.ts
  - src/lib/report.test.ts
  - components/dashboard/report.tsx
  - components/dashboard/terminal-shell.tsx
autonomous: true
requirements: [UI-01, UI-03, UI-04, DATA-03]
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Report renders exactly 6 sections in fixed order with section 2 live and sections 1 plus 3 through 6 dimmed with UNAVAILABLE markers
    - Blocked side derives from bias with struck-through magenta row and terse BLOCKED label, both sides standing down in COMPRESSION
    - Terminal shell composes header plus global strip plus three panels plus dimmed modules with independent scroll and single-column stack below 1024px
    - Poll loop refreshes every 60s with visibility gate plus guarded manual Yenilə and every failure raises a coalesced toast
    - Full test suite is green with Azerbaijani copy throughout and English identifiers
  artifacts:
    - src/lib/blocked-side.ts
    - src/lib/blocked-side.test.ts
    - src/lib/report.ts
    - src/lib/report.test.ts
    - components/dashboard/report.tsx
    - components/dashboard/terminal-shell.tsx
  key_links:
    - Report reads bias plus DOL plus regime through dashboard store selectors that call src/lib/ict pure functions
    - TerminalShell imports StatusStrip plus SentimentPanel plus CalendarPanel plus Report and owns the single poll effect
    - Toast surface raises on poll failure while the strip keeps last-known state visible, never silent
  prohibitions:
    - must NOT hide the blocked side; it renders struck-through, never removed
    - must NOT drop UNAVAILABLE markers to fake completeness; dimmed sections stay visible
    - must NOT render fake bias values during load; skeletons only, never placeholder numbers
---

<objective>
Report plus shell integration per D-08 and D-09: section 2 live from bias, DOL, and regime selectors with the struck-through blocked side, all other sections and modules dimmed with UNAVAILABLE markers, and the shell owning the 60s poll loop with toasts so DATA-03 holds across failure paths. Closes the phase with the full suite green.

Purpose: Turn the proven tracer, overlays, and fixture panels into the complete dark-terminal composition the roadmap gates on.
Output: Blocked-side plus report contract libs with tests, Report component, and a fully wired TerminalShell with poll loop and failure toasts.

Flagged assumptions (spec-less probe fallback, surfaced not dropped):
- A-u1 (UI-01): below 1024px the grid collapses to a single left to center to right stack with 12px gaps via lg classes; no other breakpoints exist.
- A-u3 (UI-03): the report always renders exactly 6 sections in fixed order; long rationale prose wraps at 2 lines with ellipsis inside panel width.
- A-u4 (UI-04): BULLISH strikes the SHORT row with BLOCKED premium and BEARISH strikes the LONG row with BLOCKED discount; COMPRESSION renders both rows dimmed with no strike since the rationale string already states both sides stand down.
- A-d3 (DATA-03): failure with cached candles keeps numbers visible with STALE strip plus one coalesced toast; failure with zero candles renders the documented error copy plus toast; concurrent poll failures never stack toasts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-composition/02-CONTEXT.md
@.planning/phases/02-terminal-composition/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Blocked-side helper plus report contract</name>
  <files>src/lib/blocked-side.ts, src/lib/blocked-side.test.ts, src/lib/report.ts, src/lib/report.test.ts</files>
  <read_first>src/lib/ict/bias.ts, src/lib/ict/dol.ts, reference/institutional_rules.md</read_first>
  <behavior>
    - Test 1: deriveBlockedSide on BULLISH returns SHORT blocked with the premium label.
    - Test 2: deriveBlockedSide on BEARISH returns LONG blocked with the discount label.
    - Test 3: deriveBlockedSide on COMPRESSION returns both sides standing down with no strike target.
    - Test 4: REPORT_SECTIONS holds exactly 6 entries in fixed order with section 2 live and the rest unavailable.
    - Test 5: section titles match the institutional rules ÇIXIŞ FORMATI literals.
  </behavior>
  <action>Create src/lib/blocked-side.ts with named exports BlockedSide union of LONG SHORT BOTH NONE strings, BLOCKED_PREMIUM_LABEL holding BLOCKED premium, BLOCKED_DISCOUNT_LABEL holding BLOCKED discount, and deriveBlockedSide accepting a BiasDirection and returning blocked side plus strike boolean plus label: BULLISH blocks SHORT with strike and the premium label, BEARISH blocks LONG with strike and the discount label, COMPRESSION returns BOTH with strike false and an empty label since the rationale string carries the stand-down message per assumption A-u4. Create src/lib/report.ts with named exports ReportSection interface with index title state fields, REPORT_SECTIONS constant with the six UI-SPEC literal titles in order where only index 2 is live and the rest are unavailable, and REGIME_BADGE constant mapping expansion to Genişlənmə, compression to Sıxılma, plus pre-news override text Yüksək təsirli xəbər gözlənilir. Cover both modules with src/lib/blocked-side.test.ts and src/lib/report.test.ts, naming the suites so npm run test -- blocked-side and npm run test -- report match. English identifiers and comments throughout with locked English chips UNAVAILABLE and BLOCKED untouched.</action>
  <verify>
    <automated>npx vitest run src/lib/blocked-side.test.ts src/lib/report.test.ts</automated>
    <fails_when>non-zero exit, or No test files found in output, or any Failed Tests section in output</fails_when>
  </verify>
  <done>Blocked-side derivation and the six-section contract are pinned by tests with both suites green.</done>
  <acceptance_criteria>npx vitest run src/lib/blocked-side.test.ts src/lib/report.test.ts exits 0; src/lib/blocked-side.ts exports deriveBlockedSide and both label constants; src/lib/report.ts exports REPORT_SECTIONS with length 6 and exactly one live entry at index 2</acceptance_criteria>
</task>

<task type="auto">
  <name>Report component with live section 2</name>
  <files>components/dashboard/report.tsx</files>
  <read_first>src/lib/store.ts, src/lib/report.ts, src/lib/blocked-side.ts, components/ui/card.tsx</read_first>
  <behavior>
    - Section 2 shows regime badge plus position line plus delivery-cycle line plus DOL line plus bias readout with rationale.
    - Blocked side row renders struck-through magenta with the terse BLOCKED label while the allowed side renders plain.
    - Sections 1 and 3 through 6 render dimmed with UNAVAILABLE marker chips and the header shows the NQ=F bias line.
  </behavior>
  <action>Build Report as a named export starting with the client pragma, reading range, position, bias output, DOL target, and regime output through shallow-compared dashboard store selectors that call the ict modules, never duplicating math. Render the Heading 16px header line with the shape # NQ=F | HTF BIAS: plus the English bias enum, then all six sections from REPORT_SECTIONS in order: section 2 live with Display 20px bias value in bullish green or bearish magenta, regime badge from REGIME_BADGE with pre-news override when a calendar event sits inside the window, position line Premium or Discount or Ekvilibrium from position versus the 0.48 and 0.52 buffer, delivery-cycle line, DOL line Alqoritmin əsas hədəfi with name plus mono price, mandatory rationale string verbatim, and the side rows where deriveBlockedSide drives the struck line-through magenta row with its label while COMPRESSION dims both rows without strike per A-u4. Render sections 1 and 3 through 6 dimmed at opacity 0.45 with no pointer events and an UNAVAILABLE literal chip pinned top-right per D-08. Show pulse skeleton rows at 12px rhythm while lastUpdatedISO is null and the Məlumat yoxdur copy when selectors have no bias output. English identifiers with Azerbaijani rendered copy per D-04, theme tokens plus the cn merger, data-slot report plus data-slot report-section attributes, no literal color values.</action>
  <verify>
    <automated>npm run test -- report</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Report shows live section 2 with struck blocked side and five dimmed UNAVAILABLE sections with suites green.</done>
  <acceptance_criteria>components/dashboard/report.tsx contains data-slot report and UNAVAILABLE and line-through; the report imports deriveBlockedSide from @/src/lib/blocked-side and REPORT_SECTIONS from @/src/lib/report; npm run test -- report exits 0; npx tsc --noEmit exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Shell integration with poll loop and failure toasts</name>
  <files>components/dashboard/terminal-shell.tsx</files>
  <read_first>components/dashboard/terminal-shell.tsx, components/dashboard/status-strip.tsx, components/dashboard/sentiment-panel.tsx, components/dashboard/calendar-panel.tsx, components/dashboard/report.tsx, components/charts/nq-chart.tsx, src/lib/store.ts, components/ui/toast.tsx, components/ui/button.tsx</read_first>
  <behavior>
    - D-01: first paint fires one refresh, then a 60s interval (matching the proxy 60s TTL) fires only while the tab is visible with in-flight polls skipped, plus manual Yenilə on the same guarded path.
    - Failed poll with cached candles keeps numbers plus STALE strip plus exactly one toast with the error copy.
    - Full suite stays green and the terminal matches the 280px 1fr 320px grid with dimmed modules and report in place.
  </behavior>
  <action>Finish TerminalShell as the single composition owner: replace the inline strip section with the imported StatusStrip from plan 02, place SentimentPanel in the left panel beside dimmed liquidity-map rows for modules 1, 3, and 4 plus the SMT row each at opacity 0.45 with UNAVAILABLE chips per D-08, place NqChart wired to store candles plus range eq plus DOL plus status plus forming with the dynamic ssr false loader intact plus Report below the chart beside dimmed execution-protocol rows, and place CalendarPanel in the right panel beside dimmed ticket rows plus the dimmed fatal-flaw line. Add the single poll effect calling refresh on mount then every 60_000 milliseconds only when document.visibilityState is visible, clearing on unmount, with the Yenilə Button disabled plus spinner glyph while inFlight per UI-SPEC E6. Watch lastError in a separate effect and raise one coalesced toast per error value through the exported manager from components/ui/toast with the UI-SPEC Azerbaijani error copy, skipping when lastError is null and never stacking concurrent toasts. Keep the grid grid-cols-1 lg:grid-cols-[280px_1fr_320px] with gap-3, independent panel scroll with no horizontal overflow, header with wordmark plus Bakı session line plus mono confidence plus Yenilə, English identifiers with Azerbaijani copy per D-04, tokens plus the cn merger, data-slot attributes, no literal color values, no math in components. Close by running the full suite green.</action>
  <verify>
    <automated>npm run test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Complete terminal composes all panels with the poll loop and toasts, full suite green, typecheck clean.</done>
  <acceptance_criteria>npm run test exits 0; npx tsc --noEmit exits 0; components/dashboard/terminal-shell.tsx contains StatusStrip and SentimentPanel and CalendarPanel and Report imports plus setInterval with 60_000 plus visibilityState; the shell contains UNAVAILABLE markers and a Yenilə control disabled on inFlight</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Poll loop → Hobby quota | Client timer crosses into metered server work here |
| Upstream error text → toast copy | Untrusted failure detail crosses into user-visible copy here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial of Service | 60s poll effect (components/dashboard/terminal-shell.tsx) | low | mitigate | 60s cadence matching proxy TTL plus visibility gate plus client singleflight per D-01; manual Yenilə reuses the same guarded path so quota burn stays flat |
| T-02-08 | Information Disclosure | poll failure toast (terminal-shell effect) | low | mitigate | Toast renders the locked Azerbaijani error copy only, never raw upstream status text or payload fragments |
| T-02-SC | Tampering | npm installs for shell deps | high | mitigate | Zero new installs; Button plus Card plus Toast wrappers stay lockfile-pinned with the RESEARCH legitimacy audit passed; any future install needs a blocking human checkpoint |
</threat_model>

<verification>
Full suite npm run test green, npx tsc --noEmit clean, and the manual visual checklist passes: 3-panel grid with dimmed modules, live chart with zones plus EQ plus DOL, section 2 live with struck blocked side, sentiment plus calendar responding to scenario switches, STALE and CLOSED states honest, Azerbaijani copy throughout.
</verification>

<success_criteria>
Blocked-side plus report suites green, Report live with five dimmed sections, shell integrated with poll loop and coalesced toasts, full suite green, phase ready for verification.
</success_criteria>

<output>
Create .planning/phases/02-terminal-composition/02-04-SUMMARY.md when done
</output>

## Artifacts this phase produces

Plan 04 creates: blocked-side helper deriveBlockedSide with BLOCKED label constants in src/lib/blocked-side.ts; report contract REPORT_SECTIONS plus REGIME_BADGE in src/lib/report.ts; Report named component with live section 2 plus struck blocked side plus five dimmed UNAVAILABLE sections in components/dashboard/report.tsx; finished TerminalShell owning StatusStrip plus panel composition plus 60s visibility-gated poll effect plus coalesced failure toasts in components/dashboard/terminal-shell.tsx. Test files: src/lib/blocked-side.test.ts, src/lib/report.test.ts.
