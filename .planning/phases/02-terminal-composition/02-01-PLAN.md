---
phase: 02-terminal-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/store.ts
  - src/lib/store.test.ts
  - src/lib/chart-mapper.ts
  - src/lib/chart-mapper.test.ts
  - components/dashboard/terminal-shell.tsx
  - components/charts/nq-chart.tsx
  - app/page.tsx
  - app/globals.css
  - src/lib/sentiment.ts
  - src/lib/sentiment.test.ts
  - src/lib/countdown.ts
  - src/lib/calendar.test.ts
autonomous: true
requirements: [STATE-01, DATA-03, UI-01, UI-02]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Single Zustand store holds the Yahoo envelope and refresh writes candles plus freshness meta in one set
    - Global strip derives LIVE versus STALE from envelope truth plus lastUpdatedISO age, never from local clock alone
    - Terminal shell renders the 280px slash 1fr slash 320px 3-panel grid with a Yenilə manual refresh button
    - Chart renders live candles as YYYY-MM-DD business-day times with an EQ dashed price-line and a DOL solid marker
    - Forming candles never enter math and empty envelopes render Məlumat yoxdur copy instead of crashing
    - Candle order from the proxy is preserved untouched and double refresh of the same envelope yields identical state
  artifacts:
    - src/lib/store.ts
    - src/lib/store.test.ts
    - src/lib/chart-mapper.ts
    - src/lib/chart-mapper.test.ts
    - components/dashboard/terminal-shell.tsx
    - components/charts/nq-chart.tsx
    - app/page.tsx
    - app/globals.css
    - src/lib/sentiment.ts
    - src/lib/sentiment.test.ts
    - src/lib/countdown.ts
    - src/lib/calendar.test.ts
  key_links:
    - store.refresh fetches same-origin GET /api/yahoo and validates the envelope before any set call
    - selectors call computeRange plus computePosition plus computeBias plus computePrimaryDOL plus computeRegime from src/lib/ict
    - terminal-shell dynamically imports NqChart with ssr false from a use client shell, never from app/page.tsx
    - chart setData consumes mapCandlesToSeries output with time passed as business-day strings
  prohibitions:
    - must NOT synthesize OHLC candles for weekends, holidays, or gaps; render last closed candles with honest STALE or CLOSED states
    - must NOT present fixture sentiment, calendar, or interpretation prose as live market data; the scenario label stays visible
    - must NOT render bias, DOL, or confidence from insufficient history; degrade to COMPRESSION with a stated reason
---

<objective>
Tracer slice for the terminal: proxy to store to chart rendering live Module 2 candles with the global status strip, plus the pure sentiment and countdown libs that later panels build on. Per D-10 all derived values come from selectors calling src/lib/ict pure functions, and per D-11 the chart loads via dynamic ssr false inside a use client shell.

Purpose: Prove the composition architecture end to end on fresh context before fixtures, report, and zone shading pile on.
Output: Working poll-to-pixels path with green store, mapper, sentiment, and countdown suites plus terminal tokens in globals.css.

Flagged assumptions (spec-less probe fallback, surfaced not dropped):
- A-tr-1 (DATA-03): the tracer strip implements LIVE and STALE only; Baku-weekend CLOSED derivation arrives in 02-02, so a Saturday run shows last closed candles with a STALE age instead of the CLOSED ribbon.
- A-st-1 (STATE-01 adjacency): a forming candle adjacent to the last closed candle is excluded from every selector via closedOnly yet still rendered with the Formalaşan şam chip; a flat range with high equal to low yields position 0.5.
- A-st-2 (STATE-01 empty): an empty candle array renders the Məlumat yoxdur copy plus Yenilə CTA and never throws inside selectors.
- A-st-3 (STATE-01 ordering): candle order is preserved exactly as the proxy delivered it; no selector re-sorts, and a disordered envelope is rejected at refresh validation.
- A-st-4 (STATE-01 idempotency and concurrency): refreshing twice with the same envelope produces identical state, and parallel refresh calls coalesce on the inFlight guard with no torn writes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-composition/02-CONTEXT.md
@.planning/phases/02-terminal-composition/02-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: proxy to store to chart with status strip, one path only</name>
  <files>src/lib/store.ts, src/lib/store.test.ts, components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx, app/page.tsx</files>
  <read_first>src/lib/yahoo.ts, src/lib/ict/types.ts, src/lib/ict/range.ts, src/lib/ict/bias.ts, src/lib/ict/dol.ts, src/lib/ict/regime.ts, app/page.tsx, app/layout.tsx, components/ui/card.tsx, node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md</read_first>
  <behavior>
    - Test 1: refresh with a mocked /api/yahoo envelope writes candles, lastUpdatedISO, stale false, and source into the store in one update.
    - Test 2: refresh while inFlight is true performs zero additional fetch calls and leaves state untouched.
    - Test 3: refresh on a failed fetch keeps last-known candles, records lastError, and resets inFlight to false.
    - Test 4: selector derivation on 20 closed candles returns a range whose eq equals (high plus low) over 2 with bias and DOL drawn from the ict modules.
    - Test 5: a forming candle in the array is excluded from the derived range but retained in stored candles.
  </behavior>
  <action>Build the single Zustand store module src/lib/store.ts exporting a named useDashboard created with zustand create. State holds candles as Candle array, contractHint string, lastUpdatedISO string or null, stale boolean, source string, inFlight boolean, scenario fixed to crowded-long, lastError string or null, and asOfBaku string. Implement refresh as an async action with a client singleflight guard on get().inFlight that returns early without fetching, fetching /api/yahoo with cache no-store, validating the JSON envelope before set (candles is an array, every open high low close is finite, dates strictly ascending, minimum one row; reject anything else and treat as failure), setting the envelope fields plus asOfBaku from getAsOfBakuDate with the fetch instant, and on failure preserving last-known candles while setting lastError and resetting inFlight in all paths. Implement setScenario for the later switcher. Implement selectors in the same module that derive range via computeRange on stored candles and asOfBaku, lastClose from the last stored candle close, position via computePosition, bias via computeBias with closedOnly count, DOL via computePrimaryDOL, regime via computeRegime, and levels via computeLevels, never duplicating ict logic and never importing ict into components directly. Build TerminalShell as a named export in components/dashboard/terminal-shell.tsx starting with use client, composing Card shells into a grid with Tailwind classes grid grid-cols-1 gap-3 lg:grid-cols-[280px_1fr_320px], a header strip with the LIQUIDITY ENGINE // NQ=F wordmark plus a Yenilə Button using RefreshCw from lucide-react wired to refresh and disabled while inFlight, an inline global strip section with data-slot status-strip showing LIVE versus STALE derived from store stale plus age from lastUpdatedISO, left center and right Card panels with data-slot attributes panel-left panel-center panel-right, and a chart skeleton div with data-slot chart-skeleton and min-h-[400px] animate-pulse shown while lastUpdatedISO is null. Load the chart with next/dynamic inside this shell only, mapping the named NqChart export with ssr false and the skeleton as loading. Build NqChart as a named export in components/charts/nq-chart.tsx starting with use client, accepting props candles array, eq number, dolPrice number, dolName string, and overlay string, importing lightweight-charts lazily with await import inside the creation effect so the module top stays DOM free, creating the chart once per container with chart.addSeries(CandlestickSeries) in v5 form, pushing data through an exported pure mapCandlesToSeries helper that maps each candle date straight to time with no conversion and throws on empty input, managing EQ dashed and DOL solid price-lines through refs with removePriceLine before every re-create, and destroying with chart.remove on unmount. Replace app/page.tsx wholesale as a Server Component with no use client pragma and no dynamic ssr false call, rendering TerminalShell inside a dark full-viewport wrapper plus the Toaster from components/ui/toast, deleting all create-next-app placeholder content. Per D-04 keep identifiers and comments English while rendered copy uses the UI-SPEC Azerbaijani strings. Never call addCandlestickSeries, never synthesize candles, never put math in components.</action>
  <verify>
    <automated>npm run test -- store</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>The single happy path works end to end: mocked poll writes the envelope, the strip shows freshness, the shell grid renders, the chart shows candles with EQ and DOL lines, and both verify commands pass.</done>
  <acceptance_criteria>src/lib/store.test.ts exits 0 via npm run test -- store; app/page.tsx contains TerminalShell and contains no next/image reference; components/charts/nq-chart.tsx contains addSeries(CandlestickSeries and contains no addCandlestickSeries text; components/dashboard/terminal-shell.tsx contains data-slot status-strip and a Yenilə Button disabled on inFlight; store selectors import computeRange, computeBias, computePrimaryDOL, and computeRegime from @/src/lib/ict paths</acceptance_criteria>
  <reversibility rating="reversible">Single store module at src/lib/store.ts reverts cleanly via git with no migration and no published contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Chart mapper extraction plus terminal tokens</name>
  <files>src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts, components/charts/nq-chart.tsx, app/globals.css</files>
  <read_first>components/charts/nq-chart.tsx, src/lib/ict/types.ts, app/globals.css</read_first>
  <behavior>
    - Test 1: mapCandlesToSeries maps each candle to time open high low close with the date string untouched.
    - Test 2: mapCandlesToSeries throws on an empty array and on any non-finite OHLC value.
    - Test 3: priceLineInputs returns the range eq and the DOL price unchanged for line creation.
  </behavior>
  <action>Extract the pure mapping helpers from NqChart into src/lib/chart-mapper.ts as named exports mapCandlesToSeries and priceLineInputs with no lightweight-charts import, keeping finite-input guards that throw Error on empty or non-finite input and preserving business-day date strings verbatim. Update components/charts/nq-chart.tsx to import both helpers from @/src/lib/chart-mapper and delete the local copies so exactly one implementation exists. Move the mapper coverage into src/lib/chart-mapper.test.ts placed under src so the vitest include pattern picks it up. Append terminal tokens under the existing .dark block in app/globals.css using the UI-SPEC color contract: terminal background, panel surface, cyan accent, up-candle green, down-candle magenta, premium zone fill at 8 percent magenta, discount zone fill at 8 percent green, and chart canvas background, each as a --terminal prefixed CSS variable. Update nq-chart series and price-line colors to reference those variables through getComputedStyle or style variables rather than literal color values, and keep all other component styling on existing theme tokens with cn from cn and data-slot attributes intact. Do not convert app/layout.tsx and do not add barrel files.</action>
  <verify>
    <automated>npm run test -- chart-mapper</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Mapper logic lives in exactly one tested module, the chart consumes it, terminal tokens exist under .dark, and both verify commands pass.</done>
  <acceptance_criteria>app/globals.css contains --terminal-accent under the .dark block; components/charts/nq-chart.tsx contains @/src/lib/chart-mapper in an import; components/charts/nq-chart.tsx contains var(--terminal-up) and var(--terminal-down); npm run test -- chart-mapper exits 0</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Sentiment True AVG plus countdown pure libs</name>
  <files>src/lib/sentiment.ts, src/lib/sentiment.test.ts, src/lib/countdown.ts, src/lib/calendar.test.ts</files>
  <read_first>src/lib/ict/bias.ts, src/lib/ict/dol.ts, src/lib/time.ts, reference/institutional_rules.md</read_first>
  <behavior>
    - Test 1: trueAvg excludes brokers whose names contain insta or fibogroup case-insensitively and averages the kept longPct values.
    - Test 2: trueAvg returns crowded LONG when buy average reaches 60, SHORT when sell reaches 60, and null otherwise.
    - Test 3: trueAvg throws when no included broker remains or any percentage is non-finite.
    - Test 4: formatCountdown renders a future target as Ng Ns sonra day and hour parts from an injected now with no Date.now inside.
    - Test 5: isPreNews returns true inside the 24 hour window and false outside it with an injected now.
  </behavior>
  <action>Create src/lib/sentiment.ts with named exports BrokerQuote interface with broker longPct shortPct numbers, EXCLUDED_SUBSTRINGS constant holding insta and fibogroup, CROWDED_THRESHOLD constant 60, and trueAvg function returning buy, sell as 100 minus buy, crowded LONG SHORT or null, excludedCount, and an Azerbaijani rationale string, guarding every percentage as finite within 0 to 100 and throwing when the kept set is empty, following the threshold plus rationale shape of computeBias. Create src/lib/countdown.ts beside the Baku util with named exports formatCountdown accepting targetISO string and now Date defaulting to new Date only at the boundary, computing whole days plus whole hours with date-fns difference helpers and returning 0g 0s sonra for past or current targets, and isPreNews accepting targetISO, now, and windowHours defaulting to 24. Keep both modules pure with no I/O and no clock access except the injected now parameter, English identifiers and comments, Azerbaijani rationale and flag prose. Cover both modules with src/lib/sentiment.test.ts and src/lib/calendar.test.ts using fixed Date instants in the style of the Phase 1 time tests, naming the countdown suite calendar.test.ts so the validation command npm run test -- calendar matches.</action>
  <verify>
    <automated>npx vitest run src/lib/sentiment.test.ts src/lib/calendar.test.ts</automated>
    <fails_when>non-zero exit, or No test files found in output, or any Failed Tests section in output</fails_when>
  </verify>
  <done>True AVG with the 60 percent crowded flag and Baku-clock countdown with pre-news window are implemented as tested pure functions.</done>
  <acceptance_criteria>npx vitest run src/lib/sentiment.test.ts src/lib/calendar.test.ts exits 0; src/lib/sentiment.ts exports trueAvg and CROWDED_THRESHOLD; src/lib/countdown.ts exports formatCountdown and isPreNews; neither new module contains Date.now outside the default parameter</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo upstream → /api/yahoo → store.refresh | Untrusted market JSON crosses into client state here |
| Store selectors → chart setData | Derived numbers cross into canvas rendering here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | store.refresh envelope validation (src/lib/store.ts) | medium | mitigate | Validate candles array with finite OHLC plus ascending dates plus minimum one row before set; mapper throws on non-finite; chart renders error copy plus toast on failure, never raw payload |
| T-02-02 | Denial of Service | 60s poll loop against Hobby quota (terminal-shell effect in 02-02, guard here) | low | mitigate | Client inFlight singleflight guard in this plan; 60s cadence plus visibility gate land in 02-02; manual Yenilə reuses the same guarded path |
| T-02-SC | Tampering | npm installs for chart and store deps | high | mitigate | Zero new installs in this phase; zustand plus lightweight-charts already lockfile-pinned with the RESEARCH legitimacy audit passed; any future install needs a blocking human checkpoint |
</threat_model>

<verification>
Full suite npm run test stays green, npx tsc --noEmit is clean, and a dev render shows the shell grid with a LIVE strip and candles plus EQ and DOL lines from a mocked or live envelope.
</verification>

<success_criteria>
Store suite green, mapper suite green, sentiment plus calendar suites green, typecheck clean, and the tracer path from poll to pixels is committed.
</success_criteria>

<output>
Create .planning/phases/02-terminal-composition/02-01-SUMMARY.md when done
</output>

## Artifacts this phase produces

Plan 01 creates: Zustand store useDashboard with refresh plus setScenario actions and ict-calling selectors in src/lib/store.ts; pure chart mapping helpers mapCandlesToSeries plus priceLineInputs in src/lib/chart-mapper.ts; TerminalShell named component with inline global strip and dynamic NqChart loading in components/dashboard/terminal-shell.tsx; NqChart named component with v5 addSeries lifecycle in components/charts/nq-chart.tsx; True AVG helper trueAvg with CROWDED_THRESHOLD in src/lib/sentiment.ts; countdown helpers formatCountdown plus isPreNews in src/lib/countdown.ts; terminal --terminal CSS variables under .dark in app/globals.css; replaced Server Component app/page.tsx composing TerminalShell plus Toaster. Test files: src/lib/store.test.ts, src/lib/chart-mapper.test.ts, src/lib/sentiment.test.ts, src/lib/calendar.test.ts.
