---
phase: 10-type-safety-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/layout.tsx]
autonomous: true
requirements: [TYPE-01]
estimate:
  tokens: 14000
  raw_tokens: 7000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "tsc --noEmit exits zero with .next/ removed (clean-checkout simulation) per D-01 D-03"
    - "app/layout.tsx uses the plain children ReactNode props form with no generated-global helper per D-01"
    - "Imports, JSX, metadata, and lang az stay byte-identical — annotation line only per D-02"
    - "next build succeeds and regenerates .next/ per D-04"
    - "Root layout page renders identically before and after the fix (visual glance confirms parity) per D-04"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with clean-checkout-safe props annotation"
      contains: "children: React.ReactNode"
  key_links:
    - from: "app/layout.tsx"
      to: "react types via React.ReactNode"
      via: "inline props object type with no generated .next/types dependency"
      pattern: "children: React\\.ReactNode"
    - from: "tsconfig.json"
      to: "app/layout.tsx"
      via: "tsc --noEmit typecheck with .next/ removed passes without generated globals"
      pattern: "\\.next/types"
---

<objective>
Fix the pre-existing clean-checkout tsc error in app/layout.tsx via the locked annotation swap and prove exit-zero plus identical rendering, per D-01 D-02 D-03 D-04 D-05 and TYPE-01.

Purpose: TypeScript compiles cleanly with zero errors on any clean checkout with no runtime change — the phase goal.
Output: app/layout.tsx with a clean-checkout-safe props annotation, tsc exit-zero proof with .next/ removed, next build success, and a visual-glance parity check.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-type-safety-fix/10-CONTEXT.md
@.planning/phases/10-type-safety-fix/10-PATTERNS.md
@app/layout.tsx
@tsconfig.json
@next-env.d.ts
@node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md
</context>

<tasks>

<task type="tracer">
  <name>Reproduce clean-checkout tsc failure and swap props annotation</name>
  <read_first>app/layout.tsx plus node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md lines 57-62 plus .planning/phases/10-type-safety-fix/10-CONTEXT.md plus .planning/phases/10-type-safety-fix/10-PATTERNS.md plus tsconfig.json plus next-env.d.ts per D-01 D-02</read_first>
  <files>app/layout.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. First simulate a clean checkout by deleting the generated .next directory, then run npx tsc --noEmit and capture the single route-helper error reported on app/layout.tsx line 20 to confirm reproduction, never trusting the warm tree per the scout finding. Then edit app/layout.tsx line 20 only to the exact replacement line export default function RootLayout({ children }: { children: React.ReactNode }) { preserving the RootLayout name per D-01. Keep the import block, metadata object, JSX shell, and html lang az attribute byte-identical with zero JSX or behavior edits per D-02. Do not add any import for the previous helper per the convention note, leave the manifest and script set untouched, do not modify CI or lint configuration per D-05. Leave .next absent after the edit so the follow-up typecheck proves clean-checkout safety.</action>
  <acceptance_criteria>app/layout.tsx contains the RootLayout signature with children colon React.ReactNode per D-01; app/layout.tsx does NOT contain LayoutProps; git diff for app/layout.tsx shows exactly one changed line with imports, JSX, metadata, and lang unchanged per D-02; npx tsc --noEmit exits 0 with the .next directory absent</acceptance_criteria>
  <verify>
    <automated>rm -rf .next && npx tsc --noEmit</automated>
    <fails_when>exit code is non-zero or output contains the string error TS</fails_when>
  </verify>
  <done>Clean-checkout failure reproduced, annotation swapped, tsc exits zero with .next still removed</done>
</task>

<task type="auto">
  <name>Sweep remaining type errors and prove parity via build plus visual glance</name>
  <read_first>app/layout.tsx plus tsconfig.json plus package.json scripts section plus .planning/phases/10-type-safety-fix/10-CONTEXT.md per D-03 D-04 D-05</read_first>
  <files>app/layout.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine with .next still absent. Run npx tsc --noEmit and read the full error list per D-03. If errors beyond the known line-20 helper remain, fix each one with annotation or type-position edits only, touching no JSX, no runtime logic, and no behavior per D-03. Do not add new scripts to the package manifest, do not modify CI, workflow, or lint configuration per D-05. Then run npm run build from the repo root to prove production compile and regenerate the .next directory per D-04, leaving the tree warm for later phases. Then start the dev server with npm run dev in the background, wait for ready output, and prepare the root page for the embedded human visual check per D-04.</action>
  <acceptance_criteria>npx tsc --noEmit with .next removed reports zero errors per D-03 and TYPE-01; npm run build exits 0 and the .next directory exists again per D-04; git diff --stat lists only app/layout.tsx per D-05; root page visual glance confirms identical rendering per D-04</acceptance_criteria>
  <verify>
    <automated>rm -rf .next && npx tsc --noEmit && npm run build</automated>
    <fails_when>exit code is non-zero or output contains any of the strings error TS, Failed to compile, Type error</fails_when>
    <human-check>Start the dev server with npm run dev, open the root page, glance at the layout shell, fonts, and chrome, and confirm identical rendering to before the fix per D-04 following the v2.0 human-confirmed re-verify pattern. No HTML diff is required. tsc-clean plus lint alone is not sufficient.</human-check>
  </verify>
  <done>Zero clean-checkout tsc errors, build succeeds with .next restored, visual parity confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→app/layout.tsx | Trusted developer edit; annotation-only change to one props line, no user input crosses here |
| tsc/build toolchain→repo | Local typecheck and build read source only; no network, no secrets, no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | app/layout.tsx props annotation | low | accept | Single-line annotation change only; imports, JSX, metadata, and lang az verified byte-identical via git diff; tsc plus next build gate the change |
| T-10-SC | Tampering | package installs | low | accept | No package installs in this phase; no new dependencies; no legitimacy checkpoint required; package.json untouched per D-05 |

No high-or-above severity items exist for this annotation-only type fix: no new inputs, no auth, no data flow change; the only file touched is the root layout props annotation.
</threat_model>

<verification>
Full clean-checkout proof in order: remove .next, run npx tsc --noEmit for exit zero, run npm run build for exit zero and .next regeneration, then perform the visual glance of the live root page for parity. Confirm git diff on app/layout.tsx is the single annotation line with no import, JSX, metadata, or lang changes, and confirm no package.json, CI, or lint changes exist.
</verification>

<success_criteria>
tsc --noEmit exits zero on a clean checkout simulation with .next removed; layout page renders identically before and after the fix with zero runtime behavior change.
</success_criteria>

## Artifacts this phase produces

No new symbols are created by this phase: no new exports, no new functions, no new flags, no new files. The single deliverable is one changed annotation line in the existing file app/layout.tsx, swapping the props type to the plain children ReactNode object form.

<output>
Create `.planning/phases/10-type-safety-fix/10-01-SUMMARY.md` when done
</output>
