---
phase: 08-amd-sessions-asia-range-judas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - src/lib/ict/asia.ts
  - src/lib/ict/asia.test.ts
autonomous: true
requirements:
  - ICT-12
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Asia Range covers the 20:00-00:00 NY window per D-01, after conforming edits land first in REQUIREMENTS.md ICT-12 and ROADMAP.md criterion 1"
    - "Range high/low is wick-to-wick per D-02, post-midnight 00:00-02:00 rows are excluded per D-03, and missing candles are skipped without interpolation per D-04"
    - "March spring-forward, November fall-back, and maintenance-break fixtures resolve through per-candle IANA America/New_York wall-clock and stay green"
    - "Empty windows return null instead of throwing, and consecutive polls on identical input return deep-equal output"
  artifacts:
    - path: "src/lib/ict/asia.ts"
      provides: "Asia Range from 1H NQ closed candles, 20:00-00:00 NY wick-to-wick"
      exports: ["ASIA_START_NY_HOUR", "ASIA_END_NY_HOUR", "AsiaRange", "asiaRange"]
      contains: "closedOnlyIntraday"
    - path: "src/lib/ict/asia.test.ts"
      provides: "Clean range, DST triple, post-midnight exclusion, gap-skip, forming exclusion, null, determinism tests"
      min_lines: 120
  key_links:
    - from: "src/lib/ict/asia.ts"
      to: "src/lib/ict/aggregate.ts"
      via: "NY_TZ imported from aggregate, never redefined; per-candle wall-clock shape reused"
      pattern: "NY_TZ"
    - from: "src/lib/ict/asia.ts"
      to: "src/lib/ict/types.ts"
      via: "closedOnlyIntraday at the boundary plus IntradayCandle epoch-second input shape"
      pattern: "closedOnlyIntraday"
    - from: "src/lib/ict/asia.test.ts"
      to: "src/lib/ict/aggregate.test.ts"
      via: "row plus nyHourEpoch plus fromZonedTime plus block builders copied verbatim"
      pattern: "nyHourEpoch"
  prohibitions:
    - "Never encode the pre-D-01 start hour anywhere after the conforming edits; the named constant pins 20"
    - "Never resolve session membership with a fixed UTC offset or the Chicago display clock"
    - "Never interpolate missing candles or include post-midnight rows in the range"
---

<objective>
Tracer plus conforming edits for the Asia Range core (ICT-12) — per D-01 through D-06.

Purpose: This is the phase tracer (TRACER_MODE true): the first production-quality end-to-end slice proving 1H pipe rows resolve through NY wall-clock into a wick-to-wick range before Judas and AMD expand from it. It establishes the 1H-range side of the D-05 mixed-interval split (costly), and it repairs the 19:00 text contradiction in both canonical files so no later plan references the wrong window.
Output: Conforming edits in .planning/REQUIREMENTS.md and .planning/ROADMAP.md, plus new pure module src/lib/ict/asia.ts and its vitest suite. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-amd-sessions-asia-range-judas/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Conforming 19:00 to 20:00 edits per locked D-01</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/08-amd-sessions-asia-range-judas/08-CONTEXT.md</read_first>
  <action>In .planning/REQUIREMENTS.md, on the ICT-12 requirement line only, change the Asia start hour stated there to the D-01 locked value while preserving the en-dash separator and the 00:00 NY end of the window. In .planning/ROADMAP.md, on the Phase 8 success criterion 1 line only, apply the identical start-hour change per D-01. Touch no other lines in either file, add no new sections, and keep all requirement IDs and criterion numbering byte-identical.</action>
  <verify>
    <automated>grep -n "20:00–00:00 NY" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>grep exits non-zero or either file prints no matching line</fails_when>
    <automated>grep -rn "19:00" .planning/REQUIREMENTS.md .planning/ROADMAP.md; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning the superseded start hour still stands in a canonical file</fails_when>
  </verify>
  <done>Both canonical files state the D-01 window; the superseded start hour appears nowhere in either file.</done>
  <acceptance_criteria>REQUIREMENTS.md ICT-12 line contains 20:00–00:00 NY; ROADMAP.md Phase 8 criterion 1 contains 20:00–00:00 NY; scoped grep for the old start hour across both files returns exit 1 with no output.</acceptance_criteria>
</task>

<task type="tracer">
  <name>Tracer: Asia Range core slice with DST triple-test green</name>
  <reversibility rating="costly">The 1H-range side of the 1H-range/15M-sweep split flows into detector signatures, fixtures, and Phase 9 selector inputs per D-05.</reversibility>
  <files>src/lib/ict/asia.ts, src/lib/ict/asia.test.ts</files>
  <read_first>src/lib/ict/aggregate.ts, src/lib/ict/types.ts, src/lib/ict/aggregate.test.ts, src/lib/time.ts</read_first>
  <action>Create src/lib/ict/asia.ts following the aggregate.ts house template (@-aliased type imports, caps constants at module top, forming-exclusion first line, fail-fast throw with echoed value). Import NY_TZ from the aggregate module per D-01 and D-07 (reuse it, do not redefine it), formatInTimeZone from date-fns-tz, and the IntradayCandle type plus closedOnlyIntraday from the types module. Export constants ASIA_START_NY_HOUR = 20 per D-01 and ASIA_END_NY_HOUR = 24 with a D-01 comment, and export interface AsiaRange with numeric high, low, height plus sessionDate string dated by the 20:00 NY open date. Implement export asiaRange taking IntradayCandle rows plus a sessionDate string: throw with the echoed received value on non-array rows input; drop forming rows via closedOnlyIntraday, then drop rows with any non-finite OHLC field and rows with non-finite or non-positive time at the boundary; sort ascending by time with first-row-wins timestamp dedupe on copies so caller arrays are never mutated; resolve each surviving candle through formatInTimeZone at call time with minute precision (minutes since midnight = hour times 60 plus minute, never a fixed offset, never the Chicago display clock); keep rows whose NY calendar date equals sessionDate with minutes at or after 1200 and strictly before 1440, which enforces the 20:00 inclusive and 00:00 exclusive edges per D-01 and D-03; return null on an empty window per the honest-degrade rule; otherwise return wick-to-wick high as the max of row highs and low as the min of row lows per D-02 with height equal to high minus low, skipping gaps with no interpolation per D-04. Accept NQ 1H rows per the D-05 mixed-interval split and take no ES input and no comparison parameters per D-06. Keep the module free of current-time reads with sessionDate passed as a plain string parameter and zero imports from any store module. Create src/lib/ict/asia.test.ts copying the row builder, the fromZonedTime-based nyHourEpoch helper, and the consecutive-block builder verbatim from aggregate.test.ts lines 13 through 45 with STEP 3600, and cover: a clean five-candle 20:00 to 00:00 window asserting exact high, low, and height; constant pins asserting ASIA_START_NY_HOUR equals 20, ASIA_END_NY_HOUR equals 24, and imported NY_TZ equals America/New_York; post-midnight exclusion where 00:00 to 02:00 rows leave high and low unchanged; gap-skip where a removed 22:00 candle still yields the extremes of the four present rows; forming exclusion where forming-flagged rows never enter the range; empty-window null on input with no in-window rows; determinism where consecutive calls return deep-equal output; sessionDate attribution where the range over the evening window carries the 20:00 open date; a Baku-edge case asserting toBakuYMD of the 20:00 open instant equals the expected Baku calendar date; and three tests whose names carry the DST prefix — March spring-forward pair around the 2026-03-08 07:00Z instant, November fall-back pair around the 2026-11-01 06:00Z instant, and a maintenance-break case with 16:00 to 21:00 NY rows asserting only the 20:00-plus rows contribute.</action>
  <verify>
    <automated>npm test -- src/lib/ict/asia.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>npm test -- src/lib/ict/asia.test.ts -t DST</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/asia.ts src/lib/ict/asia.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module or its test</fails_when>
  </verify>
  <done>Tracer slice green: conforming edits landed, Asia Range resolves end to end on 1H rows, and the DST triple-test passes.</done>
  <acceptance_criteria>Suite passes asserting exact high, low, and height on the clean window, ASIA_START_NY_HOUR exactly 20 with NY_TZ America/New_York, unchanged extremes under post-midnight rows, correct extremes with the 22:00 candle removed, null on the empty window, deep-equal consecutive outputs, the DST-filtered run reporting all three DST tests passed, and the purity grep gate returning exit 1 with no output.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/asia.ts — exports ASIA_START_NY_HOUR, ASIA_END_NY_HOUR, type AsiaRange, function asiaRange (new in this plan; judas.ts and amd.ts consume AsiaRange in later plans)
- src/lib/ict/asia.test.ts — row plus nyHourEpoch plus block builders plus clean-range, constant-pin, post-midnight, gap-skip, forming-exclusion, null, determinism, attribution, Baku-edge, DST triple cases
- Conforming edits — REQUIREMENTS.md ICT-12 line and ROADMAP.md Phase 8 criterion 1 line now state the D-01 20:00 start

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure functions on caller-supplied 1H candle arrays; no I/O, no user input, no store — same posture as Phase 7 plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | asiaRange boundary (asia.ts) | medium | mitigate | closedOnlyIntraday first, then finite-OHLC plus finite-positive-time drop, ascending sort, timestamp dedupe keeping the first row; non-finite input never reaches min/max; non-array input throws with the echoed value |
| T-08-02 | Tampering | Session-date attribution (asia.ts) | low | mitigate | Per-candle IANA America/New_York resolution at call time with minute precision; 20:00 inclusive and 00:00 exclusive edges pinned by named constants and the DST triple-test |
</threat_model>

<verification>
Full suite stays green so existing range, bias, regime, rollover, join, SMT, FVG, and aggregate tests hold: `npm test` passes after both tasks.
</verification>

<success_criteria>
- REQUIREMENTS.md ICT-12 and ROADMAP.md criterion 1 state the D-01 20:00 start with no residual old-hour text
- Asia Range resolves 20:00-00:00 NY wick-to-wick from 1H closed rows with gaps skipped and post-midnight rows excluded
- March, November, and maintenance-break DST fixtures pass through per-candle IANA resolution
</success_criteria>

<output>
Create `.planning/phases/08-amd-sessions-asia-range-judas/08-01-SUMMARY.md` when done
</output>
