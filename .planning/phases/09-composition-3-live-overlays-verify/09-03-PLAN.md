---
phase: 09-composition-3-live-overlays-verify
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - scripts/verify-phase9-drill.sh
  - .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md
autonomous: false
requirements:
  - DEPLOY-02
  - UI-05
  - UI-06
  - ICT-15
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ES cold-start drill records cold versus warm totals per leg and passes only when both legs answer 200 with valid non-stale envelopes"
    - "Intraday 1h and 15m payloads answer 200 within maxDuration 15 on the live URL with bounded ranges unchanged, and any redesign happens only on drill failure per D-16"
    - "Live §3 render check probes data-slots s3-liquidity-path, s3-smt-status, s3-amd-timing, and s3-conviction on the deployed URL and passes only when all four are present"
    - "Live chart overlay check probes asia-lines, judas-markers, and smt-marker presence paths without asserting live signal visibility"
    - "Residual environment sensitivity (cold-start variance, upstream latency, weekend thin data) is recorded as a flagged assumption in the drill summary, never silently absorbed"
  artifacts:
    - path: "scripts/verify-phase9-drill.sh"
      provides: "Per-leg cold-start plus intraday payload plus §3 and overlay slot drill against the live URL"
      contains: "s3-conviction"
    - path: ".planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md"
      provides: "API coverage decision for the reuse-only Yahoo proxy surface plus the drill outcome record"
      contains: "No new external API integration"
  key_links:
    - from: "scripts/verify-phase9-drill.sh"
      to: "app/api/yahoo/route.ts"
      via: "Drill exercises the unchanged route shape with maxDuration 15 and allowlist-before-URL-build intact"
      pattern: "maxDuration"
    - from: "scripts/verify-phase9-drill.sh"
      to: "components/dashboard/report.tsx"
      via: "Render check probes the four §3 data-slots the live block owns"
      pattern: "s3-conviction"
    - from: "scripts/verify-phase9-drill.sh"
      to: "components/charts/nq-chart.tsx"
      via: "Overlay check probes the asia-lines, judas-markers, and smt-marker paths the chart owns"
      pattern: "asia-lines"
  prohibitions:
    - "Never redesign the route shape on a green drill; redesign only on drill failure with the failure pasted in the summary"
    - "Never present a passing local check as live verification; the drill runs against the deployed Vercel URL"
    - "Never treat an unverified checklist claim as checked; every drill step records its observed output"
user_setup:
  - service: vercel
    why: "DEPLOY-02 drill runs against the deployed v2.0 URL; only a human can confirm the production deployment exists and supply its base URL"
    env_vars:
      - name: LIVE_URL
        source: "Vercel Dashboard project page for the v2.0 deployment, production domain URL"
---

<objective>
Live-URL verification for Phase 9: per-leg ES cold-start drill, intraday payload under maxDuration, and §3 plus overlay render check on the deployed Vercel URL, per D-16.

Purpose: DEPLOY-02 closes only against production — cold-start timing, payload fit, and slot presence are meaningless locally. Verify-don't-redesign: the drill proves the current route shape fits and redesigns only on drill failure.
Output: New drill script scripts/verify-phase9-drill.sh, coverage decision 09-COVERAGE.md, and the drill outcome in the plan summary. No source changes, no route changes, no new packages.
SCHEMA_PUSH_REQUIRED=false (no ORM schema files in scope; filesystem fixtures plus Yahoo proxy plus Zustand only).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composition-3-live-overlays-verify/09-CONTEXT.md
@.planning/phases/09-composition-3-live-overlays-verify/09-RESEARCH.md
@.planning/phases/09-composition-3-live-overlays-verify/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Phase 9 drill script plus coverage decision</name>
  <files>scripts/verify-phase9-drill.sh, .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md</files>
  <read_first>scripts/verify-deploy.sh, app/api/yahoo/route.ts, components/dashboard/report.tsx, components/charts/nq-chart.tsx, node_modules/next/dist/docs/01-app/02-guides/deploying-to-platforms.md</read_first>
  <reversibility rating="reversible">Standalone drill script plus planning memo; no source or route changes.</reversibility>
  <action>Create scripts/verify-phase9-drill.sh as a zero-dependency curl-plus-node bash script taking the live base URL as its single argument, modeled on the scripts/verify-deploy.sh warmup plus envelope plus slot-contract shape: fail closed with exit 1 on first failure and exit 2 on missing URL argument. Implement four drill steps with PASS and FAIL lines — step 1 ES daily cold-start records %{time_total} and %{http_code} for GET live-url slash api slash yahoo with symbol ES=F and interval 1d then repeats for the warm total, passing only on 200 with a node-parsed envelope holding a non-empty candles array, parseable lastUpdatedISO, and stale strictly false; step 2 intraday payload records totals for symbol NQ=F with interval 1h and interval 15m, passing only on 200 inside 15 seconds with non-empty candles on each leg, proving the bounded 3mo and 1mo ranges fit maxDuration 15 per D-16; step 3 §3 render check fetches the live page body and passes only when all four strings data-slot equals s3-liquidity-path, s3-smt-status, s3-amd-timing, and s3-conviction are present, with the rollover-banner slot-contract fallback (served HTML, then JS chunk scan, then repo source contract) reused for any slot missing from served HTML since client rendering may defer slots; step 4 overlay presence check reuses the same slot-contract fallback for data-slot asia-lines, judas-markers, and smt-marker without asserting live signal visibility, since overlays depend on live session state. Keep app/api/yahoo/route.ts byte-identical: assert statically inside the script that the route source carries maxDuration equals 15, the allowlist guards, status 502, no-store plus Retry-After 60, and retryAfter 60 shapes, and state the verify-dont-redesign rule in the script header — redesign only on drill failure with the failure pasted. Create .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md with the reasoned declaration No new external API integration: Phase 9 adds no new upstream host, no new route, and no new query surface — the nq1h and nq15m legs reuse the Phase 6 parameterized proxy with already-allowlisted symbol NQ=F and intervals 1h and 15m over the pinned bounded ranges — plus sections mapping each leg to its allowlist entry, each drill step to its requirement, and the drill outcome table filled at run time with the observed totals.</action>
  <verify>
    <automated>bash -n scripts/verify-phase9-drill.sh</automated>
    <fails_when>bash syntax check exits non-zero or prints any syntax error</fails_when>
    <automated>grep -q 's3-conviction' scripts/verify-phase9-drill.sh && grep -q 'asia-lines' scripts/verify-phase9-drill.sh && grep -q 'maxDuration = 15' scripts/verify-phase9-drill.sh</automated>
    <fails_when>any probe exits non-zero, meaning the drill script misses its §3 slot, overlay path, or route-shape assertion</fails_when>
    <automated>git diff --stat -- app/api/yahoo/route.ts | grep -v "^$"; test $? -eq 1</automated>
    <fails_when>git diff prints any stat line for the route, meaning the verify-only contract was broken by a route edit</fails_when>
  </verify>
  <done>Drill script syntax-clean with all four drill steps plus the route-shape guard, coverage decision recorded, route byte-identical.</done>
  <acceptance_criteria>bash -n passes with no output; script source carries all four §3 slots, all three overlay paths, and the maxDuration 15 assertion; coverage file carries the No new external API integration declaration with leg-to-allowlist and step-to-requirement maps; git diff over the route is empty.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Live-URL drill with human-supplied deployment URL</name>
  <files>scripts/verify-phase9-drill.sh, .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md</files>
  <read_first>scripts/verify-phase9-drill.sh, .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md</read_first>
  <action>Run the DEPLOY-02 live-URL drill per D-16: ask the human for the deployed v2.0 base URL (LIVE_URL env or direct answer), run bash scripts/verify-phase9-drill.sh against that URL, paste the observed cold versus warm totals and slot outcomes into 09-COVERAGE.md, and complete the human visual glance — open the live URL in a browser, confirm the §3 three sub-blocks plus conviction line render, confirm Asia lines and any Judas or SMT pins are legible on the D1 chart with ES off-chart, and confirm stale legs dim rather than hide. Flag residual environment risk (cold-start variance, upstream latency, weekend thin data) as an assumption in the summary per the DEPLOY-02 edge predicate. Redesign the route only on drill failure with the failure pasted; a green drill changes no source.</action>
  <verify>
    <human-check>Confirm the drill script passes against the live Vercel URL, the four §3 slots are present, overlays are legible with ES off-chart, and cold versus warm totals are recorded in 09-COVERAGE.md; answer PASS with the URL and totals, or FAIL with the failing step and its output.</human-check>
  </verify>
  <done>DEPLOY-02 verified on the live URL with observed totals recorded, or a pasted drill failure scoping the redesign.</done>
  <acceptance_criteria>Human answers PASS naming the live URL with cold and warm totals plus slot presence, or FAIL naming the failing drill step with its output; 09-COVERAGE.md holds the observed outcome and the flagged environment assumption.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- scripts/verify-phase9-drill.sh — ES cold-start, intraday payload, §3 slot, and overlay presence drill with route-shape guard (new in this plan; human runs it against LIVE_URL)
- .planning/phases/09-composition-3-live-overlays-verify/09-COVERAGE.md — No-new-integration declaration with leg-to-allowlist and step-to-requirement maps plus the observed drill outcome (new in this plan)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Vercel URL → drill script | Untrusted latency plus upstream Yahoo behavior cross here; drill records observations, never secrets |
| Route source → static assertions | Static shape checks guard the verify-only contract without touching production state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | Yahoo query surface via drill URLs (route.ts) | medium | mitigate | Drill URLs use only allowlisted symbol and interval values; the route allowlist-before-URL-build stays intact and the script asserts it statically so unknown values still 400 and never forward upstream |
| T-09-02 | Information disclosure (integrity) | Stale-as-live on the live URL (drill envelope checks) | medium | mitigate | Every leg envelope asserts stale strictly false with a parseable lastUpdatedISO on the fresh path; the script fails closed on malformed JSON and the coverage record keeps cold versus warm totals so stale masquerading as live is visible |
</threat_model>

<verification>
Plan gate: script syntax plus static guards green, route untouched, human drill PASS recorded — `bash -n scripts/verify-phase9-drill.sh` passes, slot and route-shape greps pass, `git diff` over the route is empty, then the blocking human checkpoint records PASS with URL and totals in 09-COVERAGE.md.
</verification>

<success_criteria>
- Drill script proves ES cold-start, intraday payload under maxDuration, and §3 plus overlay slot presence on the live URL
- Route shape unchanged per verify-dont-redesign unless the drill fails with pasted evidence
- Coverage decision and observed totals recorded with residual environment risk flagged
</success_criteria>

<output>
Create `.planning/phases/09-composition-3-live-overlays-verify/09-03-SUMMARY.md` when done
</output>
