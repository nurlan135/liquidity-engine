---
phase: 13-thin-history-honesty
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: [components/charts/zone-primitive.ts, components/charts/nq-chart.tsx]
autonomous: true
requirements: [HIST-01]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Zones mute to half strength on both thin tiers through one identical muted treatment, and stale plus thin never compound per D-05 D-10 (HIST-01)"
    - "EQ, DOL, Q1, Q3, OTE-B, OTE-S lines mute to gray on both thin tiers while widths, styles, and titles stay unchanged per D-05 D-06 D-10 (HIST-01)"
    - "Asia lines, Judas pins, SMT markers, candles, and wicks hold full strength on every tier per D-06 (HIST-01)"
    - "Full tier renders byte-identical to today per the UI-SPEC populated consideration (HIST-01)"
    - "Thin logic adds no viewport calls: candles render at natural width even if sparse per D-07 (HIST-01)"
    - statement: "Dimmed zones read as provisional while Judas and SMT markers hold full strength holds on held-out visual verification per D-08 and D-06"
      verification: backstop
  artifacts:
    - path: "components/charts/zone-primitive.ts"
      provides: "Live opacity read from the primitive field on every draw"
      contains: "opacityScale"
    - path: "components/charts/nq-chart.tsx"
      provides: "Single-branch thin dimming of zones plus level lines with marker and Asia paths untouched"
      contains: "dimmed"
  key_links:
    - from: "components/charts/nq-chart.tsx"
      to: "components/charts/zone-primitive.ts"
      via: "zone opacity assignment flows through the live-read primitive so mutating the field changes draw alpha"
      pattern: "zoneRef\\.current\\.opacityScale"
    - from: "components/charts/nq-chart.tsx"
      to: "components/charts/nq-chart.tsx"
      via: "one dimmed boolean drives both zone opacity and level-line colors, computed from the thinTier prop"
      pattern: "dimmed"
---

<objective>
Make thin tiers read as provisional on canvas: fix the zone-primitive opacity snapshot defect, then dim zones plus level lines through one uniform branch while markers, Asia, and candles hold full strength, per D-05 D-06 D-07 D-08 D-10 and HIST-01.

Purpose: Available candles stay fully legible while Premium/Discount geometry reads as provisional, never authoritative — the phase goal's dimming half.
Output: zone-primitive.ts reading opacityScale live on every draw, and nq-chart.tsx dimming zones plus EQ/DOL/Q1/Q3/OTE lines behind one dimmed boolean with Asia, markers, and viewport logic untouched. Banner decisions D-01 D-02 D-03 D-04 D-09 D-11 D-12 D-13 D-14 D-15 are implemented in Plan 01; this plan owns the canvas half.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-thin-history-honesty/13-CONTEXT.md
@.planning/phases/13-thin-history-honesty/13-RESEARCH.md
@.planning/phases/13-thin-history-honesty/13-PATTERNS.md
@.planning/phases/13-thin-history-honesty/13-UI-SPEC.md
@.planning/phases/13-thin-history-honesty/13-VALIDATION.md
@components/charts/zone-primitive.ts
@components/charts/nq-chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix the zone-primitive opacity snapshot so the field reads live</name>
  <read_first>components/charts/zone-primitive.ts renderer lines 35-72 plus view lines 74-84 plus primitive lines 90-129 including attached lines 102-108 plus .planning/phases/13-thin-history-honesty/13-RESEARCH.md Pitfall 1 plus Assumption A2 plus .planning/phases/13-thin-history-honesty/13-PATTERNS.md zone-primitive.ts fix pattern per D-05 D-10</read_first>
  <files>components/charts/zone-primitive.ts</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. Change the opacity plumbing so every draw reads the live primitive field: replace the numeric snapshot threading with a scale-getter callback of type () => number (or equivalently pass the primitive reference), threading from the ZoneFillPrimitive constructor argument through ZoneFillView into ZoneFillRenderer, and inside ZoneFillRenderer draw read the current scale from the getter at draw time instead of a stored constructor value. In attached construct the view with a getter closure that reads this.opacityScale of the primitive. Keep the constructor-facing default of 1, keep updateBands firing requestUpdate as today, keep AUTOSCALE and fallback-builder behavior byte-identical, and leave buildZoneOverlayFallback untouched. Add a unit or render-level assertion proving that mutating the opacityScale field after attach changes the alpha applied at draw: drive ZoneFillRenderer draw with a stub bitmap scope capturing the globalAlpha assigned, set the primitive field to 0.5, and assert the captured alpha equals 0.5, plus assert the default draw applies alpha 1 with no globalAlpha override. Place the test beside the existing zone coverage in src/lib/zone-bands.test.ts-adjacent node-safe scope or as a new zone-primitive unit file only if the former cannot host it without DOM, and keep all chart-library access out of the module top per the lazy pattern. Then run the adjacent stale-path behavior through the same suite so the stale 0.5 precedent is proven preserved per Assumption A2.</action>
  <acceptance_criteria>components/charts/zone-primitive.ts contains no constructor-stored numeric opacity snapshot read at draw: the renderer draw reads the scale from a getter or primitive reference each call; ZoneFillView and ZoneFillRenderer no longer accept a plain number snapshot as the opacity source; attached constructs the view with a live closure over the primitive field; default remains 1; new or extended test asserts post-attach mutation to 0.5 changes draw alpha to 0.5 and default draw leaves full strength; full npm test exits 0 proving the stale path is preserved per D-05 D-10; npx tsc --noEmit exits 0</acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the output summary shows any failed test file or zero passed tests</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or output contains the string error TS</fails_when>
  </verify>
  <done>Opacity field reads live on every draw with alpha-mutation coverage and the stale precedent preserved, suite and typecheck green per D-05 D-10</done>
</task>

<task type="auto">
  <name>Dim zones plus level lines on thin tiers with one muted branch</name>
  <read_first>components/charts/nq-chart.tsx asia tone line 196 plus stale tone line 354 plus mount EQ/DOL lines 175-188 plus mount Asia lines 193-215 plus mount markers 219-230 plus mount Q1/Q3/OTE lines 236-263 plus zone attach lines 271-294 plus update-effect line cycle lines 339-352 plus update EQ/DOL lines 353-369 plus update Q1/Q3/OTE lines 370-406 plus update Asia lines 408-431 plus update markers lines 432-454 plus zone refresh lines 456-460 plus dep array line 461 plus src/lib/thin-tier.ts ThinTier type from Plan 01 plus .planning/phases/13-thin-history-honesty/13-UI-SPEC.md Thin-dimming color rules plus .planning/phases/13-thin-history-honesty/13-CONTEXT.md D-05 D-06 D-07 D-10 per D-05 D-06 D-07 D-08 D-10</read_first>
  <files>components/charts/nq-chart.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine with Plan 01 landed so the thinTier prop defaulting to full, the propsRef sync, and the data-thin-tier container attribute already exist. In both the mount effect and the update effect compute a single const dimmed from thin-tier not equal to full, then extend the existing zone opacity assignments to status stale or dimmed yielding 0.5 else 1 so stale plus thin never compound per D-10. For EQ, DOL, Q1, Q3, OTE-B, and OTE-S line creation in both effects compute a level color as dimmed yielding MUTED_GRAY else the existing accent tone, changing only the color argument and leaving line widths, styles, and titles byte-identical per D-05 D-06 D-10. Touch no Asia line branch, no marker builder or refresh branch, no candle or wick option, and add no setVisibleLogicalRange, fitContent, time-scale, or scroll calls anywhere per D-06 D-07. Keep the existing remove-then-create cycle for recolor per the house precedent and keep everything inside effects or render with no top-level window access. Wrap the tier derivation in the file's established try/catch plus fallback convention so a thin failure renders the full-strength chart per interaction rule 5. Then run typecheck, the full test suite, and the production build, then start npm run dev and seed thin histories at 5, 25, and 40 closed candles to confirm dimmed zones plus muted level lines on the thin tiers, full-strength Asia plus Judas plus SMT plus candles, and a clean console per D-08.</action>
  <acceptance_criteria>components/charts/nq-chart.tsx contains a single dimmed boolean derived from the thin tier; zone opacity reads status stale or dimmed yielding 0.5 per D-05 D-10; EQ, DOL, Q1, Q3, OTE-B, OTE-S creation sites in both effects select MUTED_GRAY #71717A when dimmed with unchanged widths styles titles per D-05 D-06 D-10; Asia branches still select only overlayStale tone and marker builder still takes only overlayStale with no dimmed reference per D-06; no occurrences of fitContent, setVisibleLogicalRange, setVisibleRange, or scrollToPosition anywhere in nq-chart.tsx per D-07; git diff --name-only for canvas files lists zone-primitive.ts and nq-chart.tsx only; npx tsc --noEmit exits 0; npm test exits 0; npm run build exits 0; dev glance confirms provisional zones plus muted levels on thin tiers with full-strength markers and Asia plus a clean console per D-08</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or output contains the string error TS</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the output summary shows any failed test file or zero passed tests</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit or output contains any of the strings Failed, error TS, Type error</fails_when>
    <human-check>With npm run dev seeded to 5, 25, and 40 closed candles: at 5 the Tier-1 banner shows above a chart whose zone fills read visibly muted and whose EQ, DOL, Q1, Q3, OTE-B, OTE-S titles render in muted gray while candles, Asia-H, Asia-L, J, J?, and S marks hold full strength; at 25 the identical dimming holds under the Tier-2 banner; at 40 no banner renders and everything reads full strength byte-identical to today; the MARKET CLOSED ribbon coexists with the banner without overlap; DevTools console shows zero errors or warnings across all three seeds per D-05 D-06 D-08 D-10</human-check>
  </verify>
  <done>Uniform provisional rendering on both thin tiers with full-strength marks, no viewport logic, suite plus build green and human glance signed off per D-05 D-06 D-07 D-08 D-10</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nq-chart.tsx→lightweight-charts canvas | Opacity and color values cross into the canvas renderer via the primitive and price-line options; numeric and token-derived values only, never parsed input |
| editor→components/charts/zone-primitive.ts | Trusted developer edit to the primitive draw path; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Tampering | zone-primitive.ts draw path | low | mitigate | Read the scale from the primitive's own numeric field and apply it only to globalAlpha around the two zone fills; introduce no string-to-style interpolation and keep buildZoneOverlayFallback untouched |
| T-13-04 | Denial of service | nq-chart.tsx thin dim branches | low | accept | Dim branches change only color and opacity values inside the existing createPriceLine and zone-refresh calls with no new loops, listeners, or observers, so frame cost is unchanged |
| T-13-SC | Tampering | npm installs | low | accept | No package installs in this plan; lightweight-charts 5.2.1 already installed, so no legitimacy checkpoint required |
</threat_model>

<verification>
In order from the repo root: run npx tsc --noEmit for exit zero, run npm test for exit zero, run npm run build for exit zero, confirm git diff --name-only over components and src lists exactly components/charts/zone-primitive.ts and components/charts/nq-chart.tsx plus Plan 01 files, confirm no fitContent or setVisibleLogicalRange or setVisibleRange or scrollToPosition appears in nq-chart.tsx, and walk the human visual protocol at 5, 25, and 40 closed candles covering provisional zones, muted levels, full-strength marks, and a clean console.
</verification>

<success_criteria>
Zones mute uniformly on both thin tiers, level lines mute to gray with geometry and titles unchanged, Asia plus Judas plus SMT plus candles hold full strength on every tier, full tier renders byte-identical to today, no viewport logic is added, and the human glance plus clean console sign off.
</success_criteria>

## Artifacts this phase produces

Modified opacity plumbing inside components/charts/zone-primitive.ts: ZoneFillRenderer plus ZoneFillView threading a live scale getter with draw-time read and alpha-mutation coverage; modified dimming branches inside components/charts/nq-chart.tsx: the single dimmed boolean driving zone opacityScale plus EQ/DOL/Q1/Q3/OTE-B/OTE-S line colors with Asia, marker, candle, and viewport paths unchanged.

<output>
Create `.planning/phases/13-thin-history-honesty/13-02-SUMMARY.md` when done
</output>
