---
phase: 12-deployment-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified: [.planning/PROJECT.md]
autonomous: true
requirements: [DEPL-01, DEPL-02, DEPL-03]
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Kept live-URL scripts pass green against production unauthenticated after the protection change per D-01 D-08 (DEPL-02)"
    - "PROJECT.md Key Decisions carries exactly one new hygiene row with the confirmed date and readback strings per D-11 D-12 (DEPL-01, DEPL-02, DEPL-03)"
    - "Both verify scripts remain byte-identical to their pre-phase state per D-08"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "One appended Key Decisions row recording the intentional post-drill state"
      contains: "Deploy hygiene"
    - path: ".planning/phases/12-deployment-hygiene/12-03-SUMMARY.md"
      provides: "Script outputs plus the PROJECT.md diff"
  key_links:
    - from: ".planning/phases/12-deployment-hygiene/12-02-SUMMARY.md"
      to: ".planning/PROJECT.md"
      via: "verbatim transcription of the dated token confirmation and readback strings into the row"
      pattern: "Deploy hygiene"
    - from: "scripts/verify-deploy.sh"
      to: "https://liquidity-engine-nine.vercel.app/"
      via: "unchanged kept script run against the live production URL"
      pattern: "PASS"
  prohibitions:
    - statement: "MUST NOT document the protection or token state as applied before the user confirms it — no document-now-apply-later drift per D-04"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or modify scripts/verify-deploy.sh or scripts/verify-phase9-drill.sh — they are kept tooling per D-08, not remnants"
      status: resolved
      verification: judgment
    - statement: "MUST NOT create a branch or preview deployment just to probe preview protection — a 404 means nothing to probe, never a gate failure"
      status: resolved
      verification: judgment
---

<objective>
Re-prove public production with the kept scripts after the dashboard change and record the intentional end state as one PROJECT.md row — per D-01 D-03 D-04 D-08 D-11 D-12.

Purpose: Dashboard flips mean nothing until the live URL proves prod stayed public, and the phase goal requires the documented intentional state; this plan closes both using only user-confirmed evidence.
Output: Green script runs plus one appended PROJECT.md table row, recorded in 12-03-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deployment-hygiene/12-CONTEXT.md
@.planning/phases/12-deployment-hygiene/12-RESEARCH.md
@.planning/phases/12-deployment-hygiene/12-PATTERNS.md
@.planning/phases/12-deployment-hygiene/12-UI-SPEC.md
@.planning/phases/12-deployment-hygiene/12-01-SUMMARY.md
@.planning/phases/12-deployment-hygiene/12-02-SUMMARY.md
@scripts/verify-deploy.sh
@scripts/verify-phase9-drill.sh
</context>

<tasks>

<task type="auto">
  <name>Re-prove public production with the kept scripts, scripts untouched</name>
  <read_first>scripts/verify-deploy.sh lines 1-20 (usage plus BASE handling) plus scripts/verify-phase9-drill.sh lines 1-24 (usage plus BASE handling) plus .planning/phases/12-deployment-hygiene/12-02-SUMMARY.md (whether a protection switch was flipped or already matching, plus any supplied preview URL) plus .planning/phases/12-deployment-hygiene/12-UI-SPEC.md Dashboard Readback Contract per D-01 D-08</read_first>
  <files>scripts/verify-deploy.sh, scripts/verify-phase9-drill.sh</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine in Git Bash. Run bash scripts/verify-deploy.sh https://liquidity-engine-nine.vercel.app and bash scripts/verify-phase9-drill.sh https://liquidity-engine-nine.vercel.app exactly as-is with no flags added and no edits to either script per D-08, expecting every step to print PASS per D-01. Confirm both scripts remain byte-identical with git status showing no modification under scripts/ per D-08. If and only if the 12-02 summary supplies an existing preview URL, curl it once expecting the login-required fingerprint 302 redirecting to vercel.com/sso-api; a 404 on that URL is recorded as no preview deployment to probe, never a failure; if no preview URL was supplied, record the same disposition line without probing anything and never create a branch or preview to manufacture a target. Record every command plus full output for the SUMMARY.</action>
  <acceptance_criteria>Both scripts print PASS on every step including PAGE plus ENVELOPE and exit 0 per D-01 D-08; git status -- scripts/ shows no modifications per D-08; preview disposition recorded as 302 login fingerprint or the no-preview-to-probe line with nothing created</acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-deploy.sh https://liquidity-engine-nine.vercel.app</automated>
    <fails_when>non-zero exit, or output contains the string FAIL</fails_when>
    <automated>bash scripts/verify-phase9-drill.sh https://liquidity-engine-nine.vercel.app</automated>
    <fails_when>non-zero exit, or output contains the string FAIL</fails_when>
  </verify>
  <done>Public-prod liveness re-proven green with both kept scripts byte-identical and preview disposition recorded per D-01 D-08</done>
</task>

<task type="auto">
  <name>Append the one-row intentional-state record to PROJECT.md</name>
  <read_first>.planning/PROJECT.md Key Decisions table rows around lines 78-97 (pipe-table convention) plus .planning/phases/12-deployment-hygiene/12-02-SUMMARY.md (dated token confirmation string plus verbatim protection scope and method strings) plus .planning/phases/12-deployment-hygiene/12-UI-SPEC.md PROJECT.md Row Contract plus .planning/phases/12-deployment-hygiene/12-PATTERNS.md Exact row to append per D-03 D-04 D-11 D-12</read_first>
  <files>.planning/PROJECT.md</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. Append exactly one row at the end of the Key Decisions table in .planning/PROJECT.md, after the last existing row and before the Current Milestone section, in the exact form pipe [12] Deploy hygiene: public prod plus Vercel-login previews in parentheses Standard Protection, semicolon drill token deleted with the YYYY-MM-DD date taken verbatim from the 12-02 token confirmation, semicolon drill-preview branch gone pipe Intentional post-drill state, dashboard-verified pipe check Done DEPL-01/02/03 pipe, defaulting the protection clause to that exact wording only when the 12-02 readback matches Standard Protection plus Vercel Authentication, otherwise transcribing the actual pasted strings per D-03. Add no paragraph, no new section, no DEPLOYMENT.md, no retelling of the drill history per D-11 D-12. Never write the row before both 12-02 confirmations exist — no document-now-apply-later drift per D-04.</action>
  <acceptance_criteria>.planning/PROJECT.md contains exactly one line matching Deploy hygiene with the 12-02 calendar date and the readback strings per D-03 D-11 D-12; git diff -- .planning/PROJECT.md shows one added table row and no other hunks; no DEPLOYMENT.md file created; scripts/ untouched per D-08</acceptance_criteria>
  <verify>
    <automated>grep 'Deploy hygiene' .planning/PROJECT.md</automated>
    <fails_when>non-zero exit, or empty output</fails_when>
  </verify>
  <done>Intentional post-drill state recorded as one compact PROJECT.md row with confirmed date and readback per D-03 D-04 D-11 D-12</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→production URL | Unauthenticated live-URL proof via kept scripts; response evidence only, no credentials cross here |
| 12-02-SUMMARY→PROJECT.md | User-confirmed evidence transcribed verbatim; agent adds no claim beyond what the confirmations state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Information Disclosure | Deployment Protection change accidentally closed public prod | high | mitigate | Re-run both kept live-URL scripts unauthenticated in this plan; any non-200 on prod fails the gate per D-01 |
| T-12-05 | Repudiation | Undocumented protection or token state drifts back unnoticed | medium | mitigate | One dated PROJECT.md row naming the scope, method, deletion date, and branch state per D-11 D-12, transcribed from readback per D-03 |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this phase; curl, node, and bash only, so no legitimacy checkpoint required |
</threat_model>

## Probe fallback assumptions (spec-less fallback, EDGE_ABSENT=1 PROHIB_ABSENT=1)

- DEPL-02 unresolved probe item → flagged assumption: if the production URL changed since 03-04, the executor substitutes the current URL from PROJECT.md Context and records the substitution; the scripts take the base URL as an argument so no edit is needed.
- DEPL-02 unresolved probe item → flagged assumption: if a preview URL was supplied and returns 200 unauthenticated, that is a genuine protection failure — record it as a finding for the user, do not downgrade it to no-preview-to-probe.

<verification>
In order from the repo root: both kept scripts exit 0 with PASS on every step; git status shows scripts/ unmodified; PROJECT.md diff adds exactly one Key Decisions row with the confirmed date and readback; no new files created.
</verification>

<success_criteria>
Deployment Protection state is intentional and its setting is documented; no stale Vercel token remains per the confirmed deletion; no drill-preview remnant remains; public prod re-proven live.
</success_criteria>

## Artifacts this phase produces

No new source files, no new exports, no new functions, no new flags, no new scripts. The deliverables are one appended table row in the existing file `.planning/PROJECT.md` (Key Decisions table, exact row form given in the task action) and the phase evidence file `.planning/phases/12-deployment-hygiene/12-03-SUMMARY.md`. Both verify scripts are reused byte-identical, never modified.
