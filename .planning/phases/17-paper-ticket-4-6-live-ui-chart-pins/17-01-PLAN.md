---
phase: 17-paper-ticket-4-6-live-ui-chart-pins
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/ticket.ts
- src/lib/ticket.test.ts
- src/lib/store.ts
- src/lib/store.test.ts
autonomous: true
requirements:
- TICK-01
- TICK-02
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User sees a paper ticket derived in fixed order direction to entry to SL to TP ladder to R-R gate to EXECUTE or STAND ASIDE verdict
  - Ticket computes size as risk divided by stop-distance in NQ contracts and refuses with verbatim reason on degenerate inputs
  - selectTicket never returns null on gate fail; every gate fail yields STAND ASIDE plus verbatim failing reason
  - Flaw supersedes ticket and stale or thin inputs degrade with provenance instead of full-strength numbers
  artifacts:
  - src/lib/ticket.ts
  - src/lib/ticket.test.ts
  - src/lib/store.ts
  - src/lib/store.test.ts
  key_links:
  - computeTicket consumes TriggerOutput plus FatalFlawOutput without re-deriving gates
  - selectTicket shares one sharedEpoch with selectTrigger and selectFatalFlaw and carries asOf for Phase 18 replay
---

<objective>
Tracer slice for the paper ticket: pure fixed-order derivation plus store wiring, proven end-to-end before any UI renders it. Covers TICK-01 and TICK-02 per D-01 through D-05 and D-07, with OQ-1 through OQ-4 locked at plan time.

Purpose: Prove the honesty invariant (SL is an input to R-R, R-R gates EXECUTE) and the sizing math on the agent's freshest context before panels, report blocks, banner, and chart build on top.
Output: Working computeTicket plus ticketInputs plus selectTicket plus co-located and store tests, all green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-CONTEXT.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-RESEARCH.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-PATTERNS.md
@src/lib/ict/trigger.ts
@src/lib/ict/invalidation.ts
@src/lib/confluence.ts
@src/lib/store.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end EXECUTE_LONG paper ticket — one happy path only</name>
  <files>src/lib/ticket.ts, src/lib/ticket.test.ts</files>
  <read_first>src/lib/ict/trigger.ts lines 1-70 for TriggerOutput shape plus verdict direction reasonKey gates entryFvg; src/lib/ict/invalidation.ts lines 25-70 for FatalFlawOutput invalidated downgraded flawClass reason sentence challenge carriedArmedReason asOf; src/lib/confluence.ts lines 1-12 for outside-ict placement header; src/lib/ict/levels.ts lines 1-16 for LevelsOutput eq q1 q3 bullOTE bearOTE position</read_first>
  <action>Create src/lib/ticket.ts as pure brokerage math beside confluence.ts, never inside src/lib/ict, with header stating brokerage math not ICT methodology, pure with injected asOf and no store imports and no Date.now per D-01 and D-04. Export TICKET_RR_MIN equals 3 with CALIBRATION-PROVISIONAL comment, PAPER_EQUITY_USD equals 25000 with CALIBRATION-PROVISIONAL and ASSUMED note, NQ_POINT_VALUE equals 20 with ASSUMED note per OQ-1 lock. Export TicketVerdict EXECUTE_LONG plus EXECUTE_SHORT plus STAND_ASIDE and TicketOutput with verdict direction entry sl tp tp1 tp2 tp3 rMultiples rr reason sizeContracts degraded stale thin leg asOf. Implement computeTicket in fixed order per D-01: direction from FIRE verdict else STAND ASIDE, entry as OTE pocket intersect entry-FVG with LONG using bullOTE plus BULLISH gap and SHORT using bearOTE plus BEARISH gap, SL from locked rule table per OQ-2 lock LONG SL equals min of entryFVG.bottom and asiaLow and SHORT SL equals max of entryFVG.top and asiaHigh, TP ladder structure-first per D-01 and D-02 with LONG TP1 equals asiaHigh and TP2 equals opposing DOL high pool and TP3 equals range far edge and SHORT mirror and unresolvable legs left null with reason never fixed-R fillers, R-R gated on TP1 only per OQ-3 lock, verdict EXECUTE only when every step resolves and flaw is clean and rr greater than or equal to 3. Wire flaw precedence inside computeTicket per D-04: invalidated true yields STAND ASIDE plus flaw reason, downgraded true yields STAND ASIDE plus flaw reason plus carriedArmedReason context. Use assertValid helpers with got-String throws on malformed envelopes, return STAND ASIDE on null inputs never throw on null, finite-positive asOf guard, TOL_EPS float compare idiom. Add one tracer test in ticket.test.ts for FIRE_LONG plus clean flaw plus resolvable structure yielding EXECUTE_LONG with finite entry sl tp1 rr greater than or equal to 3 and sizeContracts finite.</action>
  <verify><automated>npx vitest run src/lib/ticket.test.ts</automated><fails_when>vitest reports any failing test or missing test file</fails_when></verify>
  <acceptance_criteria>computeTicket FIRE_LONG happy path returns EXECUTE_LONG with non-null entry sl tp1 rr and sizeContracts; malformed envelope throws with got message; null trigger yields STAND ASIDE never null; npx vitest run src/lib/ticket.test.ts passes</acceptance_criteria>
  <done>Single EXECUTE_LONG path works end-to-end from trigger plus flaw plus levels plus asia plus dol plus range through computeTicket and is committed</done>
</task>

<task type="auto" tdd="true">
  <name>R-R TP1 gate plus sizing plus refusal table</name>
  <files>src/lib/ticket.ts, src/lib/ticket.test.ts</files>
  <read_first>src/lib/ict/trigger.ts lines 159-190 for assertValid boundary-throw idiom; src/lib/ict/invalidation.test.ts lines 13-80 for fixture builder plus toBe verbatim reason pattern; reference/institutional_rules.md Modul 4 section for R-R Min 1 to 3 authority</read_first>
  <behavior>
    - Test: TP1 R exactly 3.0 passes to EXECUTE when all else clean
    - Test: TP1 R 2.99 yields STAND ASIDE with reason naming the computed ratio in R-R 1 to X form
    - Test: zero stop distance yields STAND ASIDE refusal with reason never NaN sizeContracts
    - Test: missing entry or SL null yields STAND ASIDE refusal with reason
    - Test: non-finite riskPct yields boundary throw with got message
    - Test: unresolvable TP2 or TP3 omitted as null while TP1 still gates, never synthesized filler
    - Test: QUIET trigger and ARMED trigger each yield STAND ASIDE with that gate verbatim reason per D-04
  </behavior>
  <action>Extend ticket.ts R-R logic to compute per-leg R-multiples for tp1 tp2 tp3 as output only per D-01 and gate EXECUTE solely on TP1 ratio per OQ-3 lock and D-03: rr equals TP1 distance divided by stop distance direction-aware, pass when rr plus TOL_EPS greater than or equal to TICKET_RR_MIN else STAND ASIDE with reason string R-R 1 to ratio with one decimal plus EXECUTE blocked naming the ratio. Implement sizeContracts as floor of equity times riskPct divided by 100 divided by absolute entry minus sl times NQ_POINT_VALUE using PAPER_EQUITY_USD default with explicit equity input override, refuse with reason setting sizeContracts null when entry or sl null or non-finite, when absolute stop distance less than or equal to 0 or below epsilon, when riskPct non-finite or less than or equal to 0 or above clamp band 0.1 to 5. Keep one const per gate-fail reason plus lookup table, Azerbaijani verbatim test-pinned with toBe, sole sanctioned interpolation being the R-R ratio. Pin TICKET_RR_MIN PAPER_EQUITY_USD NQ_POINT_VALUE with boundary tests.</action>
  <verify><automated>npx vitest run src/lib/ticket.test.ts</automated><fails_when>vitest reports any failing test in the R-R boundary or refusal table</fails_when></verify>
  <acceptance_criteria>R-R 3.0 passes and 2.99 STAND ASIDE with ratio named; zero stop distance and missing levels refuse with reason and null sizeContracts; per-leg multiples reported for all resolved legs; TICKET_RR_MIN equals 3 pinned by test</acceptance_criteria>
  <done>R-R TP1 gate plus risk-divided-by-distance sizing plus every refusal path test-pinned and green</done>
</task>

<task type="auto">
  <name>ticketInputs slice plus selectTicket plus degraded envelope</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts lines 435-443 for sharedEpoch single-time-truth, lines 841-906 for selectTrigger refuse-null plus try-catch envelope and selectFatalFlaw same-snapshot pattern, lines 914-948 for appendFiringLog cap dedup idiom, lines 256-260 for selector interface declarations; src/lib/store.test.ts lines 1110-1300 for resetDualState stubFourLegs seedTriggerFire plus stale-to-null plus coherence plus verbatim reason shapes</read_first>
  <action>In store.ts add ticketInputs slice with riskPct default 1 plus setRiskPct clamped to 0.1 to 5 refusing NaN negative oversize and paperLog session-ephemeral array capped at 50 with entries carrying asOf verdict direction entry sl tp sizeContracts per OQ-4 lock plus appendPaperLog following firing-log idiom, plain state never derived per D-07. Add selectTicket derived selector with exactly one sharedEpoch call, refuse-null returning null on stale or empty trigger-critical legs nq1h nq15m plus nq for FVG levels per Pitfall 4 with owning-leg lastError carrying reason, then get selectTrigger then flaw precedence invalidated true to STAND ASIDE plus flaw reason and downgraded true to STAND ASIDE plus flaw reason plus carriedArmedReason, then computeTicket with levels from selectLevels range from selectRange dol from selectDOL asia from selectAsia riskPct from ticketInputs, wrap derivation in try-catch returning null never throwing into render per D-04 and D-05. Return degraded stale thin leg envelope so panels render opacity-45 plus STALE or THIN tag naming leg with numbers visible but marked and sizing locked with reason on degraded inputs. Re-export TicketOutput type beside lines 31-37 with canonical definition stays in ticket.ts comment. Extend store.test.ts with ticketInputs clamp tests, selectTicket flaw-precedence INVALIDATED to STAND ASIDE plus flaw reason test, refuse-null on stale test, degraded envelope test, coherence back-to-back identical plus asOf equals shared epoch test, never pre-calling selectTrigger before first selectTicket on FIRE poll per D-12 ordering trap note.</action>
  <verify><automated>npx vitest run src/lib/store.test.ts</automated><fails_when>vitest reports any failing store selector test including ticketInputs or selectTicket cases</fails_when></verify>
  <acceptance_criteria>riskPct defaults 1 and clamps 0.1 to 5 refusing NaN; INVALIDATED yields STAND ASIDE plus flaw reason; stale legs yield null with lastError preserved; degraded envelope carries leg name; back-to-back selectTicket calls identical with pinned epoch</acceptance_criteria>
  <done>ticketInputs plus selectTicket plus paperLog wired with flaw precedence and degraded provenance, store tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger/flaw selectors to computeTicket | Untrusted-shape envelopes cross here; malformed shapes must throw with got message, nulls degrade to STAND ASIDE |
| store inputs to ticket math | Operator riskPct crosses here; NaN and oversize must be clamped or refused, never computed into fake size |
| stale or thin legs to ticket | Degraded truth crosses here; full-strength numbers on stale inputs would be false precision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | computeTicket derivation order | high | mitigate | Fixed-order early-stop skeleton implemented in this plan; boundary throws plus STAND ASIDE with verbatim reason on every gate fail; R-R gated on TP1 only |
| T-17-02 | Tampering | sizeContracts risk-divided-by-distance | high | mitigate | Refuse-with-reason on zero stop distance, missing levels, non-finite riskPct; floor to whole NQ contracts using exported PAPER_EQUITY_USD and NQ_POINT_VALUE shown on panel |
| T-17-03 | Spoofing | PAPER vocabulary in ticket types | medium | mitigate | PaperTicket and paperLog names only in this plan; banned identifiers Filled Submit Order placeOrder never introduced; Position scoped per Pitfall 1 allowlist in Plan 02 test |
| T-17-04 | Tampering | stale/thin false precision | high | mitigate | selectTicket refuse-null on stale trigger-critical legs plus degraded stale thin leg envelope for dimmed rendering in Plan 02 panels |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; no install step exists in any task of this plan |
</threat_model>

<verification>
Per-task vitest runs plus per-plan full ticket plus store pass. Sampling follows 17-VALIDATION.md: after every task commit run npx vitest run src/lib/ticket.test.ts src/lib/report.test.ts, after wave run npm test.
</verification>

<success_criteria>
computeTicket fixed order proven with EXECUTE_LONG tracer; R-R TP1 boundary 3.0 versus 2.99 pinned; sizing refusal table green; selectTicket flaw precedence plus refuse-null plus degraded envelope green; full suite shows no regressions in trigger or flaw tests.
</success_criteria>

<output>
Create `.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ticket.ts — pure computeTicket plus TICKET_RR_MIN plus PAPER_EQUITY_USD plus NQ_POINT_VALUE plus TicketOutput
- src/lib/ticket.test.ts — derivation table plus R-R boundary plus refusal plus constant pins
- src/lib/store.ts — ticketInputs slice plus selectTicket plus paperLog
- src/lib/store.test.ts — clamp plus flaw-precedence plus refuse-null plus degraded plus coherence tests
