---
phase: 17-paper-ticket-4-6-live-ui-chart-pins
plan: '03'
type: execute
wave: 2
depends_on:
- 17-01
- 17-02
files_modified:
- components/dashboard/execution-protocol.tsx
- components/dashboard/ticket-panel.tsx
- components/dashboard/fatal-flaw.tsx
- components/dashboard/report.tsx
- components/dashboard/terminal-shell.tsx
autonomous: true
requirements:
- TICK-04
- TICK-03
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User sees three live panels replacing the UNAVAILABLE execution-protocol ticket fatal-flaw cards with verbatim reasons
  - User sees live report sections 4 5 6 blocks with verbatim trigger ticket flaw prose
  - PAPER banner renders non-dismissible at terminal top on every frame even when ticket panel scrolled away
  - STAND ASIDE renders verdict plus verbatim reason never a null or blank ticket
  - Stale or thin ticket renders dimmed with STALE or THIN tag naming the failed leg
  artifacts:
  - components/dashboard/execution-protocol.tsx
  - components/dashboard/ticket-panel.tsx
  - components/dashboard/fatal-flaw.tsx
  - components/dashboard/report.tsx
  - components/dashboard/terminal-shell.tsx
  key_links:
  - Panels subscribe to stable selector functions and derive during render, never useShallow on nested ticket output
  - terminal-shell passes ticket chart props using the exact optional prop names Plan 04 implements
  - Chart pin and lines clear on STAND ASIDE via empty-marker-set path owned by Plan 04
---

<objective>
Live execution UI: three thin panels plus sections 4-6 report blocks plus persistent PAPER banner plus shell wiring that feeds the chart. Covers the render half of TICK-04 per D-04 D-05 D-06 D-08 and D-10, plus the panel half of TICK-03 PAPER vocabulary.

Purpose: Turn the proven ticket selector into the full execution picture the user reads, with paper identity visible on every frame.
Output: Three new thin panels, live sections 4-6 blocks, terminal-top PAPER banner, shell swap of all three UNAVAILABLE cards, ticket-to-chart prop wiring.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-CONTEXT.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-RESEARCH.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-PATTERNS.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-UI-SPEC.md
@components/dashboard/sentiment-panel.tsx
@components/dashboard/report.tsx
@components/dashboard/terminal-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Three thin live panels ExecutionProtocol TicketPanel FatalFlaw</name>
  <files>components/dashboard/execution-protocol.tsx, components/dashboard/ticket-panel.tsx, components/dashboard/fatal-flaw.tsx</files>
  <read_first>components/dashboard/sentiment-panel.tsx lines 31-58 plus 91-108 for use client plus Card data-slot plus CardTitle 11px semibold uppercase tracking plus Button onClick disabled data-slot idiom; components/dashboard/report.tsx lines 62-86 for stable selector-function subscription plus derive during render never useShallow on nested output, lines 34-38 plus 134-140 for Məlumat yoxdur null copy plus skeleton pulse while lastUpdatedISO null; 17-UI-SPEC.md Typography plus Color plus Copywriting Contract for text roles mono tabular-nums opacity-45 degraded KAĞIZ QEYD İMTİNA copy</read_first>
  <action>Create three thin math-free panels each with use client first line, Card holding the exact data-slot of the UNAVAILABLE card it replaces execution-protocol ticket fatal-flaw, CardHeader plus CardTitle with text-11px font-semibold uppercase tracking-0.1em class, CardContent with verbatim prose per D-04 D-05 D-08. ExecutionProtocol subscribes selectTrigger stable function and prints reason verbatim at text-base semibold with direction and gates. TicketPanel subscribes selectTicket stable function and derives during render, prints EXECUTE direction OTE-times-FVG entry invalidation SL TP1 TP2 TP3 ladder per-leg R-multiples R-R gate size in NQ contracts all numeric copy font-mono tabular-nums with Display 20px semibold mono for R-R entry SL TP prices and size, risk percent steppersrostering setRiskPct clamped setters with Risk label default 1 percent, EXECUTE-side CTA KAĞIZ QEYD appending paperLog versus STAND ASIDE-side İMTİNA dismissing without logging using shadcn button plus card only, null selector renders Məlumat yoxdur, skeleton pulse while lastUpdatedISO null, degraded stale or thin renders opacity-45 plus visible STALE or THIN tag naming failed leg with numbers dimmed and sizing locked with reason per D-05, R-R fail reason names computed ratio. FatalFlaw subscribes selectFatalFlaw stable function and prints flaw sentence plus challenge verbatim with INVALIDATED neutral styling no red green direction tone per Phase 16 lock. Never useShallow on selectTicket output, never placeOrder Filled Submit Order Position identifiers, never destructive token anywhere.</action>
  <verify><automated>npm test</automated><fails_when>vitest reports any failing suite after the panel files land</fails_when><human-check>Render terminal: EXECUTE shows direction entry SL TP ladder R-R size plus KAĞIZ QEYD, STAND ASIDE shows verdict plus verbatim reason plus İMTİNA, stale shows dimmed plus STALE tag naming leg</human-check></verify>
  <acceptance_criteria>Three panels render verdict plus verbatim reason with locked copy roles; risk stepper clamps via store setters; KAĞIZ QEYD logs paper note and İMTİNA does not; degraded shows opacity-45 plus named STALE or THIN tag; full suite stays green</acceptance_criteria>
  <done>ExecutionProtocol TicketPanel FatalFlaw live, math-free, verbatim, degraded treatment correct</done>
</task>

<task type="auto">
  <name>Report sections 4-6 live blocks</name>
  <files>components/dashboard/report.tsx</files>
  <read_first>components/dashboard/report.tsx lines 112-181 for section 3 live block branch shape with leg lastError verbatim chain plus lastUpdatedISO null skeleton plus data-slot report-section plus s3 sub-slots, lines 183-198 for generic unavailable branch placement; src/lib/report.ts lines 17-24 for flipped live state on 4 5 6 with section 5 PAPER prefix</read_first>
  <action>In report.tsx add three live branches if section.index equals 4 5 6 placed before the generic unavailable branch, copying the section 3 branch shape per TICK-04. Section 4 block subscribes selectTrigger stable function and prints trigger reason verbatim with title 4 WHY NOW EXECUTION PROTOCOL. Section 5 block subscribes selectTicket stable function and prints ticket levels entry SL TP1 TP2 TP3 plus R-R plus size verbatim with PAPER-prefixed title per D-06, STAND ASIDE prints verdict plus failing reason never blank. Section 6 block subscribes selectFatalFlaw stable function and prints flaw sentence plus challenge verbatim carried from Phase 16 lock. Each block keeps leg lastError verbatim chain idiom, lastUpdatedISO null skeleton versus null-selector Məlumat yoxdur copy, data-slot report-section plus per-block sub-slots mirroring s3-liquidity-path s3-smt-status s3-amd-timing, math-free printing selector prose only. Keep sections 1 unavailable branch and section 2 3 branches untouched.</action>
  <verify><automated>npx vitest run src/lib/report.test.ts</automated><fails_when>vitest reports live-count mismatch or title literal mismatch in report constants</fails_when><human-check>Render report: sections 4 5 6 show live verbatim prose with section 5 PAPER title, section 1 still UNAVAILABLE</human-check></verify>
  <acceptance_criteria>Sections 4 5 6 render live verbatim trigger ticket flaw prose; section 5 title carries PAPER prefix; section 1 remains unavailable; report constants test green</acceptance_criteria>
  <done>Sections 4 through 6 live in report render matching flipped constants</done>
</task>

<task type="auto">
  <name>Shell swap plus PAPER banner plus ticket-to-chart wiring</name>
  <files>components/dashboard/terminal-shell.tsx</files>
  <read_first>components/dashboard/terminal-shell.tsx lines 340-378 for three UNAVAILABLE cards with data-slot execution-protocol ticket fatal-flaw, lines 112-131 for thin rollover banner idiom with data-slot role status copy, lines 74-84 for judasBarDate containing-bar loop mapping sweepTime, lines 283-335 for chart-empty skeleton error precedent with Məlumat yoxdur plus Yenilə plus POLL_FAILURE_COPY; 17-UI-SPEC.md Copywriting Contract for KAĞIZ PAPER banner copy and degraded tag</read_first>
  <action>In terminal-shell.tsx replace the three UNAVAILABLE cards one-to-one with ExecutionProtocol TicketPanel FatalFlaw keeping the same data-slots, leaving the locked UNAVAILABLE chip untouched elsewhere per D-04. Add terminal-top non-dismissible strip with data-slot paper-banner role status KAĞIZ PAPER copy in Label 11px mono tracking-widest rendered on every frame independent of envelope truth placed above StatusStrip outside the scrolled grid so it stays visible when ticket panel scrolled away per D-06. Map fireBarDate from ticket asOf with the same containing-bar loop as judasBarDate, never reusing judasBarDate for the T pin per Pitfall 6. Pass ticket-to-chart optional props to NqChart with exact names Plan 04 implements: ticketVerdict ticketDirection ticketEntry ticketSL ticketTP1 ticketTP2 ticketTP3 fireBarDate, passing null or undefined entries on STAND ASIDE or INVALIDATED so the empty-marker-set path clears pins and removed refs clear lines per D-10. Keep poll loop StatusStrip chart-empty skeleton error paths untouched.</action>
  <verify><automated>npm test</automated><fails_when>vitest reports any failing suite after the shell swap lands</fails_when><human-check>Render terminal: PAPER banner pinned at top while scrolling, three live panels in place of UNAVAILABLE cards, EXECUTE shows chart pin plus lines and STAND ASIDE clears them with no ghost levels</human-check></verify>
  <acceptance_criteria>All three UNAVAILABLE cards replaced keeping data-slots; PAPER banner visible on every frame independent of envelope truth; NqChart receives ticket props with nulls on STAND ASIDE; full suite stays green</acceptance_criteria>
  <done>Shell shows live execution UI with persistent PAPER banner and chart wiring, no ghost levels path open</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selectors to thin panels | Verbatim prose crosses here; panels must print reason without interpolation except the sanctioned R-R ratio |
| ticket verdict to chart props | EXECUTE versus STAND ASIDE crosses here; ghost what-if levels on STAND ASIDE would mislead |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-07 | Spoofing | PAPER banner plus panel vocabulary | high | mitigate | Terminal-top non-dismissible KAĞIZ PAPER banner on every frame plus PAPER section 5 title plus KAĞIZ QEYD İMTİNA CTA copy in this plan per D-06 and D-08 |
| T-17-08 | Tampering | degraded false precision in panels | high | mitigate | opacity-45 plus STALE or THIN tag naming leg with numbers dimmed and sizing locked with reason in this plan per D-05 |
| T-17-09 | Tampering | ghost levels after verdict flips | medium | mitigate | Shell passes nulls on STAND ASIDE or INVALIDATED and Plan 04 clears via empty-marker-set plus unconditional line removal per D-10 |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; shadcn button plus card reused, no install step in any task of this plan |
</threat_model>

<verification>
npm test after panel and shell tasks, report constants test after report task, plus UAT visual glance per 17-VALIDATION.md manual-only row for TICK-04 panels banner and chart. Sampling: after every task commit run npx vitest run src/lib/ticket.test.ts src/lib/report.test.ts, after wave run npm test.
</verification>

<success_criteria>
Three live panels replace UNAVAILABLE cards with verbatim reasons; sections 4-6 blocks live with PAPER section 5 title; PAPER banner persistent at terminal top; STAND ASIDE explains with reason never blank; degraded dimmed with named leg tag; ticket props flow to chart with nulls on STAND ASIDE.
</success_criteria>

<output>
Create `.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- components/dashboard/execution-protocol.tsx — trigger status plus verbatim reason thin panel
- components/dashboard/ticket-panel.tsx — entry SL TP ladder plus risk inputs plus KAĞIZ QEYD İMTİNA thin panel
- components/dashboard/fatal-flaw.tsx — flaw verdict plus sentence plus challenge thin panel
- components/dashboard/report.tsx — sections 4 5 6 live verbatim blocks
- components/dashboard/terminal-shell.tsx — panel swap plus PAPER banner plus ticket-to-chart prop wiring
