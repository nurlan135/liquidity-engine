---
phase: 17-paper-ticket-4-6-live-ui-chart-pins
plan: '05'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/store.ts
- src/lib/store.test.ts
autonomous: true
gap_closure: true
requirements:
- TICK-01
- TICK-04
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Flaw is judged on the same FIRE trigger object the ticket prices, never on the ARMED_ALREADY_FIRED post-log echo
  - Back-to-back selectTrigger calls on one poll agree, and every render-path panel shows the same verdict on the same frame
  artifacts:
  - src/lib/store.ts
  - src/lib/store.test.ts
  key_links:
  - selectTriggerPure derives without logging and selectTrigger plus selectFatalFlaw plus selectTicket all share that one pure derivation
  - commitTriggerLog is the single logging commit point invoked from the poll tick, never from render
---

<objective>
Close the CR-03 selector-impurity gap: separate trigger derivation from firing-log writes so flaw-supersedes-ticket and the three live panels are deterministic on one shared snapshot. Covers failed truths 4 and 8 per D-04 and D-05; ticket.ts stays pure with no store imports.

Purpose: Restore the Phase 16 flaw-after-trigger same-snapshot contract and eliminate mount-order verdicts before Phase 18 replays the chain.
Output: Pure render-path selectors plus one explicit log commit point plus snapshot-equality regression tests, full suite green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-CONTEXT.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-VERIFICATION.md
@src/lib/store.ts
@src/lib/store.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Pure trigger derivation plus single-snapshot flaw and ticket</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts lines 459-467 for sharedEpoch single-time-truth, lines 868-901 for selectTrigger derivation plus appendFiringLog call, lines 911-933 for selectFatalFlaw re-derivation, lines 941-975 for appendFiringLog WAIT skip plus FIRE dedup plus ARMED_ALREADY_FIRED skip, lines 1014-1052 for selectTicket trigger-then-flaw order, lines 628-700 for startDualPoll tick where the commit point must live</read_first>
  <action>Add a selectTriggerPure selector that holds the current selectTrigger derivation verbatim minus the state write: same refuse-null on stale or empty nq1h and nq15m legs, one sharedEpoch call, selectJudas plus selectSMT plus selectAMD plus inline FVG derivation, alreadyFired read from firingLog, evaluateTrigger return, try-catch returning null, zero set calls. Rewrite selectTrigger as a thin pure delegate to selectTriggerPure with no appendFiringLog call so render never mutates log state. Add a commitTriggerLog action that invokes selectTriggerPure exactly once and then appendFiringLog with the derived output and its epoch exactly once, returning the derived output, and invoke it once per poll tick inside the startDualPoll interval handler where legs have just refreshed, keeping the WAIT skip plus FIRE session dedup plus already-fired skip ownership inside appendFiringLog unchanged. Rewrite selectFatalFlaw to derive from selectTriggerPure exactly once per call, reusing that same trigger object for checkFatalFlaw with the existing smt plus judas plus rollover plus stale inputs and the same epoch, never calling the logging path. Rewrite selectTicket to call selectTriggerPure exactly once, then evaluate checkFatalFlaw inline on that identical trigger object with one smt plus one judas plus one rollover plus one stale derivation on the same epoch, then computeTicket with levels plus range plus dol plus asia plus ticketInputs riskPct plus degraded envelope plus asOf epoch, preserving the refuse-null legs nq1h nq15m nq, the es-stale plus thin-tier degraded envelope, the flaw-precedence STAND ASIDE mapping with carriedArmedReason, and the try-catch null envelope per D-04 and D-05. Keep ticket.ts untouched with no store imports. Update the stale comments above selectTrigger, selectFatalFlaw, and selectTicket to state render-pure derivation plus explicit commit point.</action>
  <verify><automated>npx vitest run src/lib/store.test.ts</automated><fails_when>vitest exits non-zero or reports any failing store selector case</fails_when></verify>
  <acceptance_criteria>selectTrigger performs zero set calls during derivation and back-to-back calls on a FIRE poll return equal verdict FIRE_LONG; selectFatalFlaw and selectTicket consume one trigger object per call; commitTriggerLog is the only path that appends to firingLog; npx vitest run src/lib/store.test.ts passes</acceptance_criteria>
  <done>Render-path selectors are pure with one explicit poll-tick commit point and the single-snapshot chain holds end to end.</done>
</task>

<task type="auto">
  <name>Snapshot-equality regression plus masking-assertion update</name>
  <files>src/lib/store.test.ts</files>
  <read_first>src/lib/store.test.ts resetDualState plus seedTriggerFire helpers near the top of the file, lines 1187-1207 for the flaw-soft-live-smt no-pre-call note, lines 1267-1290 for the flaw-coherence pre-consume block, lines 1535-1562 for the ticket-coherence pre-consume block, src/lib/store.ts selectTriggerPure plus commitTriggerLog plus selectTicket from the prior task</read_first>
  <action>Rewrite the three masking blocks so they assert the fixed contract: delete the never pre-call selectTrigger comment near the flaw-soft test and add an explicit pre-call tolerance line that calls selectTriggerPure before selectFatalFlaw and still expects downgraded true with flawClass SOFT and reasonKey SMT_SUPPRESSED and carriedArmedReason FIRE_LONG; delete the consume-the-FIRE-first preambles in the flaw-coherence and ticket-coherence tests and assert the first derivation is FIRE_LONG with back-to-back JSON-stringified equality and asOf equal to the shared epoch pinned by the seed. Add two regression cases using resetDualState plus seedTriggerFire: one asserting two consecutive selectTriggerPure calls plus one selectTrigger call all return FIRE_LONG with identical reasonKey and direction on a single poll with firingLog length unchanged across the pure reads, and one asserting selectTicket on the FIRE seed carries a flaw evaluated on FIRE verdict material by checking the ticket STAND ASIDE reason contains the SOFT flaw marker rather than the ARMED_ALREADY_FIRED echo and that a direct selectTriggerPure verdict equals the ticket-priced direction. Keep every other store test untouched; do not alter ticket.ts, report, panel, or chart files.</action>
  <verify><automated>npx vitest run src/lib/store.test.ts</automated><fails_when>vitest exits non-zero or the new snapshot-equality cases fail</fails_when></verify>
  <acceptance_criteria>Masking pre-consume blocks removed and replaced with fixed-contract assertions; back-to-back trigger agreement case green; ticket flaw-snapshot equality case green; full npx vitest run src/lib/store.test.ts green</acceptance_criteria>
  <done>Regression net pins the fixed contract and the suite proves same-frame determinism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render selectors to firing log | Untrusted write direction crosses here; render must read log state but never append during derivation |
| poll tick to firing log | Single trusted commit direction crosses here; exactly one append per poll evaluation |
| trigger object to flaw and ticket | Shared snapshot crosses here; flaw must judge the identical object the ticket prices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering | selectTrigger render-path set | high | mitigate | Remove appendFiringLog from derivation into selectTriggerPure plus commitTriggerLog; this plan implements the split and the regression pins back-to-back agreement |
| T-17-06 | Tampering | selectTicket torn snapshot | high | mitigate | Derive trigger once via selectTriggerPure and evaluate checkFatalFlaw inline on that identical object; regression asserts flaw snapshot equals ticket trigger snapshot |
| T-17-07 | Information Disclosure | stale/thin false precision | medium | mitigate | Preserve refuse-null legs plus degraded stale thin leg envelope unchanged per D-05; no full-strength ticket on stale legs |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages in this plan; no install step exists in any task |
</threat_model>

<verification>
Per-task store suite plus final full-suite confirmation. Sampling: after each task run npx vitest run src/lib/store.test.ts, after the plan run npm test for the 389-test no-regression gate.
</verification>

<success_criteria>
Back-to-back trigger calls agree on FIRE_LONG with log length unchanged across pure reads; flaw carries SOFT on FIRE material with carriedArmedReason FIRE_LONG even after a prior pure read; ticket prices the same FIRE object the flaw judged; store suite plus full npm test green; ticket.ts still has no store imports.
</success_criteria>

<output>
Create `.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- selectTriggerPure in src/lib/store.ts — pure trigger derivation with no firing-log write, the single source for render-path reads
- commitTriggerLog in src/lib/store.ts — explicit once-per-poll logging commit point invoked from the poll tick
- Single-snapshot selectFatalFlaw plus selectTicket in src/lib/store.ts — flaw judged on the identical trigger object the ticket prices
- Snapshot-equality regression tests in src/lib/store.test.ts — back-to-back trigger agreement plus ticket flaw-snapshot equality, with masking pre-consume assertions updated to the fixed contract
