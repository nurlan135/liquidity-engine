---
phase: 18-verification-calibration-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/parity.test.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Trigger prose never contradicts AMD/SMT state on the same snapshot (test assertion only, no UI indicator)
    - FIRE_SHORT while SMT BULLISH confirmed fails and FIRE_LONG while SMT BEARISH confirmed fails
    - Suppressed SMT fires are allowed at trigger layer and assert SOFT downgrade at flaw layer, never a trigger contradiction
  artifacts:
    - src/lib/ict/parity.test.ts
  key_links:
    - parity feeds the identical judas/smt/amd object triple into evaluateTrigger and the contradiction predicates, never re-derived copies
    - NY-hours bars exempt from AMD-phase agreement but never from SMT-direction parity
---

<objective>
Same-snapshot AMD/SMT-vs-trigger reason parity harness (VERF-02) per D-04 and D-05.
Purpose: Proves trigger prose never contradicts detector state — parity visible as absence of failure with no UI indicator.
Output: Co-located `src/lib/ict/parity.test.ts` with exact contradiction predicates pinned as boolean asserts.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ict/trigger.ts
@src/lib/ict/invalidation.ts
@src/lib/ict/smt.ts
@src/lib/ict/amd.ts
@src/lib/ict/trigger.test.ts
@src/lib/ict/invalidation.test.ts
@.planning/phases/18-verification-calibration-harness/18-CONTEXT.md
@.planning/phases/18-verification-calibration-harness/18-RESEARCH.md
@.planning/phases/18-verification-calibration-harness/18-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: end-to-end FIRE_SHORT versus BULLISH contradiction on one snapshot</name>
  <files>src/lib/ict/parity.test.ts</files>
  <read_first>src/lib/ict/trigger.ts (evaluateTrigger call shape, directionOf sweep-side map, SMT read-only agree-tag never blocks FIRE), src/lib/ict/smt.ts (SmtOutput signal versus suppressed union), src/lib/ict/trigger.test.ts (fixtureTriggerJudas plus signalSmt plus suppressedSmt plus fixtureFvg plus nyMinuteEpoch builders, inputs echo contract)</read_first>
  <action>Build the thinnest production-quality parity slice per D-04 and D-05 in src/lib/ict/parity.test.ts: copy fixtureTriggerJudas plus signalSmt plus suppressedSmt plus fixtureFvg plus nyMinuteEpoch builders verbatim from trigger.test.ts lines 18-66 (signalSmt shape suppressed false with direction plus sweeperLeg NQ plus bpsGap 30, suppressedSmt shape suppressed true with reason CORR_DECOUPLED plus corr 0.4). Construct one shared triple with a confirmed LOW-sweep JudasOutput driving FIRE_SHORT via evaluateTrigger at a 03:00 NY epoch with alreadyFired false, then run the contradiction predicate that fails when verdict is FIRE_SHORT and smt is unsuppressed with direction BULLISH, asserting the predicate returns false for a BULLISH-paired snapshot and true for a BEARISH-paired snapshot with toBe pins. Assert the inputs echo contract out.inputs.judas toEqual judas and out.inputs.smt toEqual smt proving the same object triple feeds both sides. Keep the file pure with injected asOf and no store imports.</action>
  <acceptance_criteria>Single-snapshot FIRE_SHORT plus BULLISH contradiction predicate fails as designed while FIRE_SHORT plus BEARISH passes; inputs echo proves shared triple not re-derived copies</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/parity.test.ts</automated>
    <fails_when>non-zero exit, or contradiction predicate fails on any bar, or inputs echo shows re-derived copies</fails_when>
  </verify>
  <done>Single contradiction rule works end-to-end on one shared snapshot and is committed</done>
  <reversibility rating="reversible">Test-only file addition, deletable without migration</reversibility>
</task>

<task type="auto">
  <name>Expand to full contradiction roster with ARMED-key and AMD mutual exclusion</name>
  <files>src/lib/ict/parity.test.ts</files>
  <read_first>src/lib/ict/parity.test.ts (tracer slice), src/lib/ict/trigger.test.ts (ARMED matrix reasonKey total function of missing gate, SMT agree-tag section), src/lib/ict/amd.ts (accumulation means no judas candidate, NY-unavailable wholesale branch minutes 570-960)</read_first>
  <action>Expand the parity roster per D-05 strictest form in the same file: add the mirror rule FIRE_LONG plus unsuppressed BEARISH fails with toBe pins; add the reasonKey-versus-gates rule where ARMED_MISSING_PURGE with gates.purge true fails and each ARMED key matches exactly its missing gate per the total-function mapping; add the AMD accumulation rule where amd phase accumulation on the same snapshot as trigger gates.purge true fails because accumulation means no judas candidate while purge means confirmed true; add the NY-hours exemption where bars with nyMinutesOf in 570 to 960 skip AMD-phase agreement asserts but still run SMT-direction parity. Tune the rules not the strictness if any fixture disagrees per D-05: adjust fixture detector inputs to satisfy the locked semantics, never weaken a predicate to pass. Frame every SMT rule as contradiction (FIRE_SHORT plus BULLISH) never as requirement (FIRE requires agreement) so suppressed-SMT fires stay legal at trigger layer.</action>
  <acceptance_criteria>All five contradiction rules pinned with verbatim toBe asserts; NY-hours bars exempt from AMD agreement but covered by SMT parity; no rule framed as agreement requirement</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/parity.test.ts</automated>
    <fails_when>non-zero exit, or contradiction predicate fails on any bar, or NY-hours bar wrongly asserts AMD agreement</fails_when>
  </verify>
  <done>Full contradiction roster green with ARMED-key and AMD rules plus NY exemption</done>
  <reversibility rating="reversible">Predicate additions inside the test file only</reversibility>
</task>

<task type="auto">
  <name>Harden suppressed-SMT pair asserts plus SOFT-gating guard plus ARMED-or-better sweep</name>
  <files>src/lib/ict/parity.test.ts</files>
  <read_first>src/lib/ict/parity.test.ts (expanded roster), src/lib/ict/invalidation.test.ts (SMT-suppressed SOFT downgrade with carriedArmedReason, SOFT leaves ARMED standing with downgraded false and reasonKey NONE), src/lib/ict/invalidation.ts (SOFT gates on FIRING only)</read_first>
  <action>Harden the parity suite against the read-only-contract pitfall: for every suppressed-SMT FIRE bar assert the pair — trigger verdict stays FIRE and checkFatalFlaw on the same snapshot returns flawClass SOFT with reasonKey SMT_SUPPRESSED plus downgraded true plus carriedArmedReason non-null; for every suppressed-SMT ARMED bar assert the flaw stands clean with downgraded false and reasonKey NONE and never demand a downgrade on non-FIRING bars. Add an ARMED-or-better sweep that runs the full contradiction roster over every bar reaching ARMED or better using the identical shared triple per bar, asserting zero contradictions across the sweep. Keep test-assertion-only surface per D-04: no UI indicator, no new exports, no store imports.</action>
  <acceptance_criteria>Suppressed FIRE asserts flaw SOFT pair with carrier; suppressed ARMED asserts clean standing; ARMED-or-better sweep reports zero contradictions</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/parity.test.ts</automated>
    <fails_when>non-zero exit, or suppressed FIRE lacks SOFT pair, or suppressed ARMED wrongly downgraded, or sweep finds a contradiction</fails_when>
  </verify>
  <done>Suppressed-SMT pair semantics and SOFT-gating guard pinned with full sweep green</done>
  <reversibility rating="reversible">Assertion tightening inside the test file only</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector→parity | Judas/SMT/AMD objects cross into trigger and predicates; re-derived copies would void the proof |
| parity→operator | Absence of failure is the user-visible signal; a weakened predicate would fake trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | parity contradiction predicates (parity.test.ts) | high | mitigate | Frame SMT rules as contradiction never requirement; suppressed FIRE asserts flaw SOFT pair; tune fixtures not strictness per D-05 |
| T-18-05 | Information disclosure | false-precision on suppressed SMT | medium | mitigate | Suppressed SMT never blocks FIRE at trigger layer; flaw-layer SMT_SUPPRESSED downgrade asserted per bar |
| T-18-SC | Tampering | npm installs | high | mitigate | Zero new installs; vitest plus date-fns-tz already in tree |
</threat_model>

<verification>
Per-task quick runs: `npx vitest run src/lib/ict/parity.test.ts` after each commit; full `npm test` gate runs in Plan 03 before verify-work.
</verification>

<success_criteria>
Parity proves VERF-02: exact contradiction rules pinned with toBe asserts, same-snapshot triple shared never re-derived, NY exemption scoped to AMD only, suppressed-SMT pair semantics green, test-assertion-only surface with no UI changes.
</success_criteria>

## Artifacts this phase produces

- `src/lib/ict/parity.test.ts` — same-snapshot fixture builders (signalSmt/suppressedSmt/fixtureTriggerJudas/fixtureFvg/nyMinuteEpoch), contradiction predicates over `(trigger, amd, smt, judas)` (FIRE_SHORT/BULLISH, FIRE_LONG/BEARISH, ARMED-key-vs-gates, AMD-accumulation-vs-purge, NY-hours AMD exemption), suppressed-SMT flaw-pair asserts, SOFT-gating guard, ARMED-or-better sweep.

<output>
Create `.planning/phases/18-verification-calibration-harness/18-02-SUMMARY.md` when done
</output>
