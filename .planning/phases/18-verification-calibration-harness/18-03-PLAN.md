---
phase: 18-verification-calibration-harness
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/lib/ict/stale-drill.test.ts
autonomous: true
requirements:
  - VERF-03
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every stale leg individually plus all-stale plus thin-tier yields degraded-with-provenance, never a full-strength ticket
    - Each stale cell routes through checkFatalFlaw first (HARD STALE_LEG) then computeTicket (STAND_ASIDE with leg provenance)
    - Full suite stays green including banned-word quarantine and purity guard with zero new installs
  artifacts:
    - src/lib/ict/stale-drill.test.ts
  key_links:
    - stale-drill routes flaw-before-ticket mirroring selectTicket order because pure computeTicket never reads stale flags itself
    - thin-tier cell expects clean flaw plus degraded thin envelope via resolveThinTier, never a HARD kill
---

<objective>
Stale-serve leg-matrix drill proving graceful degradation with ticket open (VERF-03) per D-06 and D-07.
Purpose: Proves numbers degrade visibly with provenance — never a full-strength ticket on stale or thin inputs.
Output: Co-located `src/lib/ict/stale-drill.test.ts` with full leg matrix plus thin-tier cell plus full-suite gate.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ticket.ts
@src/lib/ict/invalidation.ts
@src/lib/ticket.test.ts
@src/lib/ict/invalidation.test.ts
@.planning/phases/18-verification-calibration-harness/18-CONTEXT.md
@.planning/phases/18-verification-calibration-harness/18-RESEARCH.md
@.planning/phases/18-verification-calibration-harness/18-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: end-to-end single stale leg flaw-first to STAND_ASIDE with provenance</name>
  <files>src/lib/ict/stale-drill.test.ts</files>
  <read_first>src/lib/ticket.test.ts (FIRE-capable frozen snapshot range 20260/20010 with bullOTE and BULLISH gap entry 20097.5 SL 20085 TP1 20210 RR 9 EXECUTE_LONG, fixtureTrigger/fixtureCleanFlaw/fixtureLevels/fixtureRange/fixtureDol/fixtureAsia/fixtureInput), src/lib/ict/invalidation.test.ts (fixtureStale plus cleanInput builders, first-match-wins order), src/lib/ticket.ts (flaw precedence invalidated to standAside, degraded envelope passthrough), src/lib/store.ts (selectTicket trigger to flaw to ticket order)</read_first>
  <action>Build the thinnest production-quality stale-drill slice per D-06 and D-07 in src/lib/ict/stale-drill.test.ts: copy the whole FIRE-capable tracer block from ticket.test.ts lines 12-104 (AS_OF epoch, range 20260 width 250, bullOTE 20062.5 to 20105, BULLISH gap 20090 to 20120, entry 20097.5, SL 20085, TP1 asiaHigh 20210, RR 9, EXECUTE_LONG) plus fixtureStale and cleanInput builders from invalidation.test.ts lines 64-84. Construct one cell with stale es true only: call checkFatalFlaw with fireTrigger plus smt plus judas plus rollover null plus stale nq false es true nq1h false nq15m false plus asOf, assert invalidated true plus flawClass HARD plus reasonKey STALE_LEG plus unblock length greater than 0 with toBe pins; then call computeTicket with the returned flaw plus degraded stale true thin false leg es, assert verdict STAND_ASIDE plus ticket reason equals REASON_BY_KEY STALE_LEG plus degraded toEqual stale true thin false leg es. Never pass stale flags directly into computeTicket because pure computeTicket carries staleness only inside flaw and degraded; flaw-first routing mirrors selectTicket order.</action>
  <acceptance_criteria>Single stale-es cell greens with HARD STALE_LEG flaw and STAND_ASIDE ticket carrying es provenance; direct-to-ticket stale bypass absent</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/stale-drill.test.ts</automated>
    <fails_when>non-zero exit, or flaw not HARD STALE_LEG, or ticket not STAND_ASIDE with es provenance, or full-strength EXECUTE on stale input</fails_when>
  </verify>
  <done>Single stale-leg flaw-first path works end-to-end with provenance and is committed</done>
  <reversibility rating="reversible">Test-only file addition, deletable without migration</reversibility>
</task>

<task type="auto">
  <name>Expand to full leg matrix plus all-stale plus V5 boundary coverage</name>
  <files>src/lib/ict/stale-drill.test.ts</files>
  <read_first>src/lib/ict/stale-drill.test.ts (tracer cell), src/lib/ict/invalidation.test.ts (per-leg stale loop over nq/es/nq1h/nq15m expecting invalidated plus HARD plus STALE_LEG), src/lib/ticket.test.ts (boundary got-string throws, null yields STAND_ASIDE never null)</read_first>
  <action>Expand to the full D-07 leg matrix in the same file: iterate legs nq, es, nq1h, nq15m injecting exactly one true leg per cell, each asserting invalidated true plus downgraded false plus flawClass HARD plus reasonKey STALE_LEG plus unblock non-empty, then routing the flaw into computeTicket and asserting STAND_ASIDE plus ticket reason STALE_LEG plus degraded toEqual stale true thin false leg naming the injected leg. Add the all-stale cell with all four legs true asserting HARD STALE_LEG first-match-wins with rollover absent and STAND_ASIDE never EXECUTE. Add V5 boundary coverage mirroring ticket.test.ts error-handling: one malformed-envelope case asserting a got-string throw on a string stale envelope, and one null-input case asserting STAND_ASIDE never null on null trigger. No cell may return EXECUTE on any stale input.</action>
  <acceptance_criteria>Six stale cells green (four single legs plus all-stale plus boundary pair) each with leg-correct provenance; zero EXECUTE verdicts on stale inputs</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/stale-drill.test.ts</automated>
    <fails_when>non-zero exit, or any stale cell returns EXECUTE, or provenance leg mismatches injected leg, or boundary throw/degrade missing</fails_when>
  </verify>
  <done>Full leg matrix green with provenance per cell and boundary coverage</done>
  <reversibility rating="reversible">Cell additions inside the test file only</reversibility>
</task>

<task type="auto">
  <name>Add thin-tier cell plus banned-word plus purity plus full-suite gate</name>
  <files>src/lib/ict/stale-drill.test.ts</files>
  <read_first>src/lib/ict/stale-drill.test.ts (leg matrix), src/lib/thin-tier.ts (thinTierCopy provenance strings lines 14-19), src/lib/ict/range.ts (ANCHOR_WINDOW 20), src/lib/ict/regime.ts (MIN_CANDLES_FULL 34), src/lib/store.test.ts (ticket-degraded selectTicket precedent)</read_first>
  <action>Close the D-07 matrix with the thin-tier cell plus phase gates: thin is not a flaw input so assert checkFatalFlaw returns clean (invalidated false, reasonKey NONE) on the FIRE snapshot with fresh stale, then assert computeTicket with degraded stale false thin true leg nq returns the dimmed-with-provenance path carrying degraded toEqual stale false thin true leg nq with thinTierCopy provenance strings, never a full-strength ticket. Keep the file pure with injected asOf and no store imports so the existing purity guard holds. Run the phase gates in order: banned-word quarantine suite, purity guard suite, then the full npm test suite proving all three new harness files plus existing trigger/invalidation/ticket/store suites stay green with zero new installs. No checked-in report file per D-08 and no new identifiers that could trip the banned-word test (no broker, placeOrder, Filled vocabulary).</action>
  <acceptance_criteria>Thin-tier cell green with clean flaw plus thin degraded envelope; banned-word suite green; purity suite green; full npm test suite green with zero new packages</acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or thin cell returns full-strength ticket, or banned-word grep matches, or purity grep matches new file, or any existing suite regresses</fails_when>
  </verify>
  <done>Thin-tier degradation proven with provenance and full suite green closing VERF-03</done>
  <reversibility rating="reversible">Test-only additions; full-suite run is read-only verification</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poll→flaw→ticket | Frozen Yahoo leg envelopes cross into flaw then ticket; bypassing flaw would fake degradation |
| thin-tier→ticket | Closed-D1 counts cross into degraded envelope; invented provenance would fake honesty |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | Information disclosure | false-precision ticket on stale/thin (stale-drill.test.ts) | high | mitigate | Every leg-matrix cell asserts STAND_ASIDE or dimmed-with-provenance plus leg-correct degraded envelope; flaw-first routing mandatory because pure computeTicket never reads stale flags |
| T-18-07 | Spoofing | paper-taken-for-real (banned words) | medium | mitigate | Phase 18 adds no identifiers; banned-word suite must stay green with no broker/placeOrder/Filled vocabulary |
| T-18-08 | Tampering | purity of new harness files | low | mitigate | Injected asOf per bar, no store imports, no Date.now; existing purity.test.ts glob guard asserts |
| T-18-SC | Tampering | npm installs | high | mitigate | Zero new installs; blocking human checkpoint only if an install appears, none planned |
</threat_model>

<verification>
Per-task quick runs of the drill file then the full gate: `npx vitest run src/lib/ict/stale-drill.test.ts` after each of the first two commits; `npm test` full suite after the third commit must stay green including banned-word and purity suites before verify-work.
</verification>

<success_criteria>
Drill proves VERF-03: full leg matrix (four single legs plus all-stale plus thin-tier) each asserting STALE/THIN tag with provenance and never a full-strength ticket, flaw-first routing throughout, V5 boundary coverage present, banned-word plus purity plus full suite green.
</success_criteria>

## Artifacts this phase produces

- `src/lib/ict/stale-drill.test.ts` — FIRE-capable frozen snapshot (ticket.test.ts tracer copy), fixtureStale/cleanInput builders, six-cell leg matrix (nq/es/nq1h/nq15m single plus all-stale) with HARD STALE_LEG plus STAND_ASIDE plus leg provenance asserts, thin-tier cell (clean flaw plus thin degraded envelope with thinTierCopy provenance), V5 malformed-throw plus null-degrade cases.

<output>
Create `.planning/phases/18-verification-calibration-harness/18-03-SUMMARY.md` when done
</output>
