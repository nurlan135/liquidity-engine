---
phase: 06-dual-symbol-proxy-data-contracts
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/ict/join.ts, src/lib/ict/join.test.ts, src/lib/__fixtures__/join-misaligned.json]
autonomous: true
requirements: [DATA-07]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - NQ and ES candles are inner-joined on timestamp only with misaligned rows never compared per DATA-07
    - Forming rows are dropped at the join boundary even if the parser regresses per D-15
    - Join returns intersected rows plus coverage nq es joined dropped and warns via coverage, never refuses per D-14
    - Join code is pure with no wall-clock reads and the D1 NQ anchor path is untouched per D-07
  artifacts:
    - src/lib/ict/join.ts
    - src/lib/ict/join.test.ts
    - src/lib/__fixtures__/join-misaligned.json
  key_links:
    - join.ts IntradayCandle: consumes the epoch shape locked in Plan 01, emits rows Plan 04 coverage display consumes
    - range.ts untouched: computeRange call sites keep reading raw NQ arrays with zero join parameters
    - yahoo.ts parser: independent layer, join-side drop holds even if parser flags regress
  prohibitions:
    - Never cross-substitute NQ data for a failed ES leg per D-06
    - Never merge per-leg stale into one boolean per D-03 goal
    - Never forward unknown symbols to Yahoo per D-08
    - Never let ES gaps shorten the NQ anchor window per D-07
---

<objective>
Pure join contract per D-07, D-13, D-14, D-15: build the timestamp inner-join as a wall-clock-free helper with coverage diagnostics, proven against a misaligned fixture, independent of the Plan 02 parser branch and the Plan 04 store work.

Purpose: Make cross-symbol comparison correctness structural so no future selector can compare misaligned rows.
Output: Pure join helper with misaligned-fixture tests green and D1 anchor suite untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-CONTEXT.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-RESEARCH.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-PATTERNS.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-VALIDATION.md
@src/lib/ict/types.ts
@src/lib/ict/range.ts
</context>

## Flagged assumptions (specless probe — this plan)

- DATA-07 unclassified unresolved: comparisons must operate on timestamp inner-joined rows only. Resolution: Map-intersect-sort join on numeric time per D-07 with exact dropped counts asserted by the misaligned fixture test in Task 2.
- DATA-04 ordering unresolved: equal timestamps keep deterministic ascending output. Resolution: joined rows sorted ascending by time with one row per shared timestamp, asserted in Task 2.

## Source audit (this plan)

| SOURCE | ID | Feature | Status |
|--------|----|---------|--------|
| REQ | DATA-07 | Timestamp inner-join before any cross-symbol comparison, no index zipping | COVERED |
| RESEARCH | — | Pure Map-intersect-sort helper in src/lib/ict with injected time only | COVERED |
| RESEARCH | — | Misaligned-fixture test pattern with exact dropped count | COVERED |
| RESEARCH | — | ANCHOR_WINDOW independence rule | COVERED |
| CONTEXT | D-07 | Join works on its own copy, D1 anchor selectors unchanged | COVERED |
| CONTEXT | D-13 | Coverage object nq es joined dropped always present | COVERED |
| CONTEXT | D-14 | Warn never refuse, rows plus counts always returned | COVERED |
| CONTEXT | D-15 | Join drops forming before intersecting | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Misaligned join fixture with ES middle gap and NQ null rows</name>
  <files>src/lib/__fixtures__/join-misaligned.json</files>
  <read_first>src/lib/__fixtures__/intraday-1h.json, src/lib/ict/types.ts</read_first>
  <action>Hand-build src/lib/__fixtures__/join-misaligned.json as a JSON object with nq and es arrays of IntradayCandle-shaped rows: 20 ascending hourly epoch times shared as the spine, the es array omitting the 3 middle spine times, the nq array including all 20 spine times but with 2 null-OHLC rows at non-last positions that a consumer must drop pre-join, one extra es-only leading time and one extra nq-only trailing time outside the spine to prove non-shared keys never join, and one shared spine time flagged forming true on both legs to prove join-side exclusion per D-15. Every value stays hand-written with whole-hour epochs; no network call.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/range.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>join-misaligned.json exists with the documented gap, null, extra, and forming structure, and the D1 anchor suite stays green.</done>
  <acceptance_criteria>Fixture parses with nq array length 21 and es array length 18, shared spine count 17, ES missing exactly 3 spine times, NQ holding exactly 2 null-OHLC rows, exactly 1 forming-shared time; range suite exits 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pure innerJoinOnTimestamp with coverage plus misaligned tests</name>
  <files>src/lib/ict/join.ts, src/lib/ict/join.test.ts</files>
  <read_first>src/lib/ict/types.ts, src/lib/ict/range.ts, src/lib/__fixtures__/join-misaligned.json</read_first>
  <action>Create src/lib/ict/join.ts per D-07, D-13, D-14, D-15 as a pure module with no wall-clock reads and no I/O: export a JoinCoverage interface with numeric nq es joined dropped, a JoinedRow interface with numeric time plus nq and es IntradayCandle fields, and innerJoinOnTimestamp taking nq and es IntradayCandle arrays and returning joined plus coverage. Implementation drops rows with non-finite OHLC pre-join, drops forming rows at the join boundary even if the parser regresses, builds a Map of es rows keyed by numeric time, walks nq rows collecting shared-time pairs, sorts joined ascending by time, counts dropped as non-shared plus pre-join-dropped rows, and always returns rows plus coverage without ever refusing. Create src/lib/ict/join.test.ts asserting the misaligned fixture joins exactly 14 rows with coverage nq 20 es 17 joined 14 dropped 7, forming rows excluded at the join even when passed forming true directly, empty inputs returning empty joined with zeroed coverage, clean fully-overlapping inputs returning every row with dropped 0, and output times strictly ascending with no index-zipped pairing. Leave src/lib/ict/range.ts and every computeRange caller untouched per D-07.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/join.test.ts src/lib/ict/range.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <verify>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/join.ts src/lib/ict/join.test.ts | grep -v "^.*://" ; test $? -eq 1</automated>
    <fails_when>exit code 0, which means a wall-clock read was found in the new join code</fails_when>
  </verify>
  <done>Pure join returns intersected rows plus exact coverage on the misaligned fixture, forming excluded at the boundary, range suite untouched and green.</done>
  <acceptance_criteria>join.test.ts asserts joined length 14 with coverage object equal to nq 20 es 17 joined 14 dropped 7, forming-direct test returns joined with zero forming rows, empty test returns joined length 0, clean test returns dropped 0, ascending-order assertion holds; purity grep exits 1 meaning no match; vitest exits 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser→join | Parser output crosses into the join; join re-validates rather than trusting flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | src/lib/ict/join.ts pre-join filtering | medium | mitigate | Drop non-finite OHLC rows pre-join and forming rows at the join boundary regardless of parser flags per D-15, so a regressed parser cannot leak a self-updating candle into comparisons |
| T-06-06 | Information Disclosure | src/lib/ict/join.ts coverage | low | accept | Coverage counts expose only row cardinalities already visible in envelope sizes; no prices or timestamps leak beyond what joined rows carry |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan; existing stack only, verified by unchanged package.json |
</threat_model>

<verification>
Full suite npm test stays green including the 133 baseline plus new join cases. Type gate npx tsc --noEmit shows no error naming any file outside the pre-existing app/layout.tsx LayoutProps residual. Purity grep over the new join files finds no wall-clock reads. No test performs a live network call.
</verification>

<success_criteria>
Cross-symbol comparisons have a timestamp inner-joined contract with exact coverage counts, forming excluded at both layers, purity holding, and the D1 NQ anchor path fully independent.
</success_criteria>

## Artifacts this phase produces

- innerJoinOnTimestamp in src/lib/ict/join.ts: pure Map-intersect-sort join on epoch time
- JoinCoverage, JoinedRow in src/lib/ict/join.ts: coverage and paired-row contracts
- src/lib/ict/join.test.ts: misaligned, forming, empty, clean, and ordering join tests
- src/lib/__fixtures__/join-misaligned.json: gapped NQ and ES intraday rows for join tests

<output>
Create `.planning/phases/06-dual-symbol-proxy-data-contracts/06-03-SUMMARY.md` when done
</output>
