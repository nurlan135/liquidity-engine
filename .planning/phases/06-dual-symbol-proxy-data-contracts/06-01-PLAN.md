---
phase: 06-dual-symbol-proxy-data-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/yahoo.ts, src/lib/ict/types.ts, app/api/yahoo/route.ts, src/lib/__fixtures__/es-daily.json, src/lib/__fixtures__/nq-daily-baseline.json, src/lib/yahoo.test.ts, app/api/yahoo/route.test.ts]
autonomous: true
requirements: [DATA-04]

estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Live Yahoo probe for ES=F at 1h and 15m has pasted row counts and pinned the server-derived range values per D-02
    - Bare GET /api/yahoo returns byte-identical NQ daily shape per D-05 while GET with symbol ES=F returns ES daily per D-04
    - Unknown symbol or interval gets 400 and is never forwarded to Yahoo per D-08
    - Each symbol-interval-range combo carries its own cache key and singleflight entry on the shared module Map per D-11 with uniform 60s TTL per D-09 and 30-min serve-stale ceiling per D-10
    - Daily server Envelope JSON shape is frozen with zero new top-level keys per D-05
  artifacts:
    - src/lib/yahoo.ts
    - src/lib/ict/types.ts
    - app/api/yahoo/route.ts
    - src/lib/__fixtures__/es-daily.json
    - src/lib/__fixtures__/nq-daily-baseline.json
    - src/lib/yahoo.test.ts
    - app/api/yahoo/route.test.ts
  key_links:
    - yahoo.ts fetchSymbol: allowlisted symbol and interval select the upstream path and composite cache key
    - route.ts GET: bare request delegates to the exact current NQ daily path with identical headers
    - types.ts IntradayCandle: epoch-seconds row shape locked here for Plans 02 and 03 to consume without depending on each other
  prohibitions:
    - Never cross-substitute NQ data for a failed ES leg per D-06
    - Never merge per-leg stale into one boolean per D-03 goal
    - Never forward unknown symbols to Yahoo per D-08
    - Never let ES gaps shorten the NQ anchor window per D-07
---

<objective>
Tracer slice per D-04, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-16: prove the parameterized ES=F daily path end to end through fetchSymbol plus route allowlist plus per-combo cache while the NQ pipe stays byte-identical, and lock the intraday epoch row shape plus the server-derived range matrix from the live probe so Plans 02 and 03 build on pinned values.

Purpose: Catch an architectural dead end after one commit by proving the parameterization pattern on the cheapest leg before intraday and client work starts.
Output: Live-probe log with pinned ranges, parameterized server proxy with green daily tests, frozen daily envelope shape.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-CONTEXT.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-RESEARCH.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-PATTERNS.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-VALIDATION.md
@src/lib/yahoo.ts
@app/api/yahoo/route.ts
@src/lib/ict/types.ts
@src/lib/yahoo.test.ts
</context>

## Flagged assumptions (specless probe — this plan)

- DATA-04 adjacency unresolved: when NQ and ES rows share the same timestamp the two legs keep separate cache entries and never merge. Resolution: composite key symbol-interval-range per D-11, asserted by the per-combo test in Task 2.
- DATA-04 empty unresolved: an empty ES upstream payload never yields a live envelope. Resolution: serve ES last-good as stale within the ceiling or throw to the 502 path per D-06, asserted by the empty-cache failure test in Task 2.
- DATA-04 concurrency unresolved: parallel refreshes of the same combo join one upstream call while different combos fetch independently. Resolution: per-key singleflight map per D-11, asserted by the concurrent-miss test in Task 2.

<tasks>

<task type="auto">
  <name>Task 1: Live Yahoo ES=F probe plus range pinning plus es-daily fixture</name>
  <files>src/lib/__fixtures__/es-daily.json, src/lib/__fixtures__/nq-daily-baseline.json</files>
  <read_first>src/lib/yahoo.ts, src/lib/__fixtures__/yahoo-null.json</read_first>
  <action>Hit the real Yahoo chart endpoint with curl for ES=F at 1h with range 3mo and at 15m with range 1mo, using the production browser User-Agent and Referer headers from the HEADERS record in src/lib/yahoo.ts, and record returned row counts plus any truncation flags. Pin the server-derived range map RANGE_FOR_INTERVAL as 1d to the existing 182-day period window, 1h to 3mo, 15m to 1mo, adjusting only if the probe shows truncation, and never adopt an unbounded full-history range on any looped fetch per D-02. Capture one bare GET /api/yahoo NQ daily response body and save it verbatim as src/lib/__fixtures__/nq-daily-baseline.json, the byte-identical baseline reference that Task 2 and Task 3 deep-equal against. Hand-build src/lib/__fixtures__/es-daily.json as a synthetic Yahoo chart payload for symbol ES=F with 10 ascending daily rows, one mid-array null run of 2 rows, finite OHLC throughout, meta exchangeName CME, following the shape of yahoo-null.json. No unit test may perform a network call; the probe is a dev curl only and fixtures stay hand-built per D-05 and D-06.</action>
  <verify>
    <automated>curl -s --max-time 20 "https://query1.finance.yahoo.com/v8/finance/chart/ES%3DF?range=3mo&interval=1h&events=history" -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0 Safari/537.36" -H "Referer: https://finance.yahoo.com/" -H "Accept: application/json" | head -c 300</automated>
    <fails_when>non-zero exit, empty output, or output lacking the chart result marker</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>Probe row counts for ES=F 1h/3mo and 15m/1mo are pasted in the plan SUMMARY, RANGE_FOR_INTERVAL is pinned, es-daily.json exists with 10 surviving rows after null filtering, nq-daily-baseline.json holds the verbatim NQ body, and the existing yahoo suite stays green.</done>
  <acceptance_criteria>SUMMARY lists ES=F 1h row count and 15m row count as integers plus the pinned range strings 3mo and 1mo; es-daily.json parses with meta.symbol equal to ES=F and timestamp array length equal to 12 with 2 null rows; nq-daily-baseline.json parses with contractHint containing NQ=F; npx vitest run src/lib/yahoo.test.ts exits 0 with all tests passed.</acceptance_criteria>
</task>

<task type="auto">
  <name>End-to-end ES=F daily through fetchSymbol with NQ default untouched</name>
  <files>src/lib/yahoo.ts, src/lib/ict/types.ts, src/lib/yahoo.test.ts</files>
  <read_first>src/lib/yahoo.ts, src/lib/ict/types.ts, src/lib/yahoo.test.ts, src/lib/__fixtures__/es-daily.json</read_first>
  <action>Parameterize src/lib/yahoo.ts per D-04 and D-11: add exported SYMBOL_ALLOWLIST with NQ=F and ES=F plus INTERVAL_ALLOWLIST with 1d, 1h, 15m, a Symbol and Interval union derived from those lists, an ENCODED_FOR record mapping each symbol to its percent-encoded chart path segment, a RANGE_FOR_INTERVAL record pinned by Task 1, and a cacheKey helper returning symbol colon interval colon range. Add fetchSymbol taking symbol, interval, now, plus optional fetch and sleep injections, implementing composite-key cache lookup with defensive candle copies, per-key singleflight via the existing inFlight map, the unchanged query1 to query2 alternation with 4s abort timeout and jittered backoff honoring Retry-After capped at 10s, per-leg validate-then-cache with symbol match against the allowlisted value and minimum 5 rows and finite OHLC and strictly ascending keys, serve-stale of that leg only within the 30-minute ceiling per D-10 and D-06, and never caching errors. Keep fetchNQDaily with its exact current signature delegating to fetchSymbol with NQ=F and 1d so the bare path is byte-identical per D-05, keep CACHE_TTL_MS at 60000 per D-09 and MAX_STALE_MS at 30 minutes per D-10, and keep the UpstreamError retryable discriminator branching per WR-11. Lock the D-16 epoch shape in src/lib/ict/types.ts by adding IntradayCandle with numeric time in UTC epoch seconds plus numeric OHLC and optional forming, plus closedOnlyIntraday filtering forming rows, leaving Candle and closedOnly untouched. Extend src/lib/yahoo.test.ts with mocked-fetch cases only: ES daily returns live envelope with contractHint containing ES=F, NQ default response body deep-equals the Task 1 baseline fixture, per-combo keys isolate NQ and ES entries, concurrent misses on one combo join a single upstream call while the other combo fetches independently, and empty-cache failure throws UpstreamError caching nothing.</action>
  <verify>
    <automated>npx vitest run src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>The single ES daily happy path works end to end through fetchSymbol with mocked fetch and is committed, NQ default output matches baseline, and per-combo isolation plus singleflight are pinned by tests.</done>
  <acceptance_criteria>fetchSymbol with ES=F and 1d returns source live with 10 candles and contractHint containing ES=F; fetchNQDaily output deep-equals the nq-daily-baseline.json fixture body; NQ warm cache followed by ES fetch triggers exactly one ES upstream call; three concurrent same-combo misses trigger exactly one upstream call; vitest exits 0.</acceptance_criteria>
  <reversibility rating="costly">Shared module Map with composite keys per D-11 touches every fetch, cache, and singleflight call site if later split per leg.</reversibility>
  <reversibility rating="costly">Epoch-seconds intraday row shape per D-16 flows into parser, join, chart mapper, and every intraday fixture once locked.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Route symbol and interval params with allowlist 400s and frozen NQ default</name>
  <files>app/api/yahoo/route.ts, app/api/yahoo/route.test.ts</files>
  <read_first>app/api/yahoo/route.ts, src/lib/yahoo.ts, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md</read_first>
  <action>Re-check the vendored Next.js route-handler guide for the request nextUrl searchParams convention plus force-dynamic and maxDuration usage before writing, per the AGENTS.md mandate. Change GET in app/api/yahoo/route.ts to accept the Next.js request object with a type-only import, reading symbol defaulting to NQ=F and interval defaulting to 1d from searchParams, rejecting any symbol outside NQ=F and ES=F and any interval outside 1d, 1h, 15m with a 400 JSON error that never reaches the upstream per D-08, delegating bare requests with no params to the exact current NQ daily path per D-05, building cache keys from allowlisted values only, preserving force-dynamic and maxDuration 15 and the stale-aware Cache-Control rule plus the 502 error with retryAfter 60 shape per D-12. Create app/api/yahoo/route.test.ts importing GET directly, stubbing the request as an object exposing nextUrl as a URL instance, stubbing global fetch to serve synthetic Yahoo payloads, and asserting bare GET NQ shape, ES daily envelope, 400 on unknown symbol without any fetch call, 400 on unknown interval, and identical Cache-Control semantics for fresh versus stale envelopes.</action>
  <verify>
    <automated>npx vitest run app/api/yahoo/route.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>Route serves ES daily via params, rejects unknown values with 400 before any upstream call, and bare GET output matches the NQ baseline with identical headers.</done>
  <acceptance_criteria>GET with symbol ES=F returns HTTP 200 with contractHint containing ES=F; GET with symbol AAPL=F returns HTTP 400 with error containing unsupported symbol and fetch mock records zero calls; GET with interval 5m returns HTTP 400; bare GET response body deep-equals the nq-daily-baseline.json fixture and carries public s-maxage 60 when fresh; both vitest files exit 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→route | Untrusted symbol and interval query values cross here |
| route→Yahoo | Server-side egress to query1 and query2 hosts with shared IP quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | app/api/yahoo/route.ts param parsing | high | mitigate | Strict allowlist check against SYMBOL_ALLOWLIST and INTERVAL_ALLOWLIST before any URL build, 400 JSON on mismatch, cache key built from the allowlisted value only per D-08 |
| T-06-02 | Tampering | src/lib/yahoo.ts validate-then-cache | high | mitigate | Per-leg isValidPayload with symbol match, min rows, finite OHLC, ascending keys before cache write, errors never cached, failure serves that leg last-good stale or throws to the 502 path |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan; existing stack only, verified by unchanged package.json |
</threat_model>

<verification>
Full suite npm test stays green including the 133 baseline plus new cases. Type gate npx tsc --noEmit shows no error naming any file outside the pre-existing app/layout.tsx LayoutProps residual. Bare GET body diff against the Task 1 baseline is empty. No unit test performs a live network call.
</verification>

<success_criteria>
User receives ES=F daily candles via parameterized symbol and interval proxy while the NQ pipe behaves exactly as before, unknown values get 400 without upstream contact, and per-combo cache plus singleflight are pinned by tests.
</success_criteria>

## Artifacts this phase produces

- fetchSymbol in src/lib/yahoo.ts: parameterized per-leg fetch with composite cache key and per-key singleflight
- SYMBOL_ALLOWLIST, INTERVAL_ALLOWLIST, RANGE_FOR_INTERVAL, cacheKey in src/lib/yahoo.ts: allowlists plus server-derived range map plus key builder
- IntradayCandle, closedOnlyIntraday in src/lib/ict/types.ts: epoch-seconds intraday row contract with forming filter
- GET with request params in app/api/yahoo/route.ts: allowlisted param parsing with frozen NQ default
- src/lib/__fixtures__/es-daily.json: synthetic ES=F daily Yahoo payload
- src/lib/__fixtures__/nq-daily-baseline.json: verbatim bare-GET NQ daily body for byte-identity tests
- app/api/yahoo/route.test.ts: route param, 400, and header contract tests

<output>
Create `.planning/phases/06-dual-symbol-proxy-data-contracts/06-01-SUMMARY.md` when done
</output>
