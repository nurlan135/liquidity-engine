---
phase: 06-dual-symbol-proxy-data-contracts
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/yahoo.ts, src/lib/__fixtures__/intraday-1h.json, src/lib/yahoo.test.ts]
autonomous: true
requirements: [DATA-05]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - NQ and ES 1H and 15M candles arrive through fetchSymbol with UTC epoch-seconds rows per D-16 while D1 output shape is unchanged
    - The last-row partial candle is flagged forming at the parser per D-15 and incomplete non-last rows are dropped before any join
    - Intraday upstream paths use bounded server-derived ranges only, never range=max on a loop per D-02
    - D1 NQ anchor selectors read the raw NQ array exactly as before with no join parameters per D-07
  artifacts:
    - src/lib/yahoo.ts
    - src/lib/__fixtures__/intraday-1h.json
    - src/lib/yahoo.test.ts
  key_links:
    - yahoo.ts intraday branch: interval selects the epoch parser and the bounded range path
    - types.ts IntradayCandle from Plan 01: parser output matches the locked shape Plan 03 joins
    - range.ts untouched: D1 computeRange call sites keep working with zero changes
  prohibitions:
    - Never cross-substitute NQ data for a failed ES leg per D-06
    - Never merge per-leg stale into one boolean per D-03 goal
    - Never forward unknown symbols to Yahoo per D-08
    - Never let ES gaps shorten the NQ anchor window per D-07
---

<objective>
Intraday expansion per D-05, D-15, D-16, D-02: extend the Plan 01 fetchSymbol spine with 1H and 15M legs that emit the locked epoch-seconds contract, flag the forming last row at the parser, and travel over bounded server-derived ranges only.

Purpose: Give the store and join real intraday rows without touching the proven D1 path.
Output: Intraday parser branch with fixture-driven tests green alongside the daily suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-CONTEXT.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-RESEARCH.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-PATTERNS.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-VALIDATION.md
@src/lib/yahoo.ts
@src/lib/ict/types.ts
@src/lib/yahoo.test.ts
</context>

## Flagged assumptions (specless probe — this plan)

- DATA-05 unclassified unresolved: the forming candle must never reach detectors even if one layer regresses. Resolution: parser flags forming plus join drops forming before intersecting per D-15; this plan owns the parser side, Plan 03 owns the join side.

## Source audit (this plan)

| SOURCE | ID | Feature | Status |
|--------|----|---------|--------|
| REQ | DATA-05 | NQ plus ES 1H and 15M with epoch contract, forming excluded, incomplete rows dropped | COVERED (parser side; join side in 06-03) |
| RESEARCH | — | Epoch-seconds contract, D1 strings untouched | COVERED |
| RESEARCH | — | Bounded range matrix, range=max forbidden on loops | COVERED |
| RESEARCH | — | Live-probe-pinned ranges inform sizes, fixtures stay hand-built | COVERED (probe in 06-01) |
| CONTEXT | D-02 | Uniform 60s cadence, bounded intraday windows | COVERED |
| CONTEXT | D-15 | Forming exclusion at both layers, parser flags here | COVERED |
| CONTEXT | D-16 | UTC epoch seconds intraday, D1 strings stay | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Synthetic intraday 1H Yahoo fixture with null run and forming tail</name>
  <files>src/lib/__fixtures__/intraday-1h.json</files>
  <read_first>src/lib/__fixtures__/yahoo-null.json, src/lib/__fixtures__/es-daily.json, src/lib/yahoo.test.ts</read_first>
  <action>Hand-build src/lib/__fixtures__/intraday-1h.json as a synthetic Yahoo chart payload for symbol ES=F at hourly spacing: 32 ascending epoch-second timestamps at exact 3600 multiples starting from a whole-hour base, meta symbol ES=F and exchangeName CME, quote arrays where rows 10 and 11 are full nulls across open high low close, every other non-last row carries finite OHLC drifting upward from base price 6020 in steps of 2, and the last row carries close only with open high low null so the parser must flag it forming per D-15. Sizes are informed by the Plan 01 live-probe row counts only; every value stays hand-written and no test performs a network call.</action>
  <verify>
    <automated>npx vitest run src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>intraday-1h.json exists with 32 timestamps, a 2-row null run, and a close-only tail, and the existing suite stays green.</done>
  <acceptance_criteria>Fixture parses as JSON with chart.result array length 1, timestamp array length 32 with each step equal to 3600, meta.symbol equal to ES=F, quote open indexes 10 and 11 null, last-row open high low null with last-row close finite; vitest exits 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Intraday parser branch plus bounded range paths plus epoch envelope tests</name>
  <files>src/lib/yahoo.ts, src/lib/yahoo.test.ts</files>
  <read_first>src/lib/yahoo.ts, src/lib/ict/types.ts, src/lib/yahoo.test.ts, src/lib/__fixtures__/intraday-1h.json</read_first>
  <action>Extend src/lib/yahoo.ts per D-05, D-15, D-16 without altering any D1 behavior: parameterize parseChartJson with the allowlisted symbol and interval, keeping the daily branch byte-identical with toBakuYMD date strings, and adding an intraday branch for 1h and 15m that emits IntradayCandle rows with numeric time equal to the raw epoch seconds, preserves the existing null-row drop plus last-row close-only forming rule, checks symbol match against the passed allowlisted symbol, and enforces strictly ascending time with an UpstreamError on duplicates or disorder. Add isValidIntradayPayload requiring minimum 5 rows, finite OHLC on every row, and ascending time. Route fetchSymbol intraday legs to a range-style upstream path built from the Plan 01 RANGE_FOR_INTERVAL map with the interval value allowlisted, keeping the daily period1 period2 182-day path untouched, keeping CACHE_TTL_MS 60000 and the 30-minute serve-stale ceiling and per-key singleflight and failover machinery identical, and never emitting a max range on any looped fetch. Leave src/lib/ict/range.ts and all computeRange call sites fully untouched per D-07. Extend src/lib/yahoo.test.ts with mocked-fetch cases only: intraday-1h fixture yields 30 rows with numeric epoch times and exactly one forming tail row, ES 1h envelope arrives live with symbol ES=F, symbol mismatch between payload meta and requested symbol throws UpstreamError, out-of-order intraday timestamps throw, and NQ daily output still deep-equals the Plan 01 baseline.</action>
  <verify>
    <automated>npx vitest run src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test file</fails_when>
  </verify>
  <done>1H and 15M legs return epoch rows with forming flagged at the parser, bounded ranges only, D1 output unchanged, and the full suite stays green.</done>
  <acceptance_criteria>Intraday fixture parse yields 30 rows with every time an integer epoch and rows 10 to 11 absent and last row forming true; fetchSymbol ES 1h returns source live with 30 candles; mismatch and disorder cases reject with name UpstreamError; NQ daily deep-equals baseline; npm test exits 0 with zero failures.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route→Yahoo | Intraday legs add 1h and 15m upstream paths on the shared egress quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | src/lib/yahoo.ts intraday validate-then-cache | high | mitigate | Per-leg isValidIntradayPayload with symbol match, min rows, finite OHLC, ascending time before cache write; errors never cached; failure serves that leg last-good stale or throws to the 502 path |
| T-06-04 | Denial of Service | src/lib/yahoo.ts intraday range paths | medium | mitigate | Server-derived RANGE_FOR_INTERVAL values only, bounded 3mo and 1mo windows, max range forbidden on any looped fetch, existing 4s timeout and Retry-After cap unchanged |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan; existing stack only, verified by unchanged package.json |
</threat_model>

<verification>
Full suite npm test stays green including the 133 baseline plus new intraday cases. Type gate npx tsc --noEmit shows no error naming any file outside the pre-existing app/layout.tsx LayoutProps residual. No unit test performs a live network call. D1 range tests untouched and green.
</verification>

<success_criteria>
User receives NQ and ES 1H and 15M candles with the epoch contract, forming flagged at the parser, incomplete rows dropped, bounded ranges only, and the D1 NQ anchor path untouched.
</success_criteria>

## Artifacts this phase produces

- parseChartJson intraday branch in src/lib/yahoo.ts: epoch-seconds rows with forming flag and ascending-time enforcement
- isValidIntradayPayload in src/lib/yahoo.ts: per-leg intraday shape guard
- Range-style intraday upstream path in src/lib/yahoo.ts: bounded server-derived ranges per interval
- src/lib/__fixtures__/intraday-1h.json: synthetic ES=F hourly Yahoo payload with null run and forming tail

<output>
Create `.planning/phases/06-dual-symbol-proxy-data-contracts/06-02-SUMMARY.md` when done
</output>
