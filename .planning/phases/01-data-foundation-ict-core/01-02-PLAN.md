---
phase: 01-data-foundation-ict-core
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/lib/ict/levels.ts, src/lib/ict/levels.test.ts, src/lib/ict/range.test.ts, src/lib/ict/bias.ts, src/lib/ict/bias.test.ts, src/lib/ict/dol.ts, src/lib/ict/dol.test.ts, src/lib/ict/regime.ts, src/lib/ict/regime.test.ts, src/lib/ict/rollover.ts, src/lib/ict/rollover.test.ts]
autonomous: true
requirements: [ICT-01, ICT-02, ICT-03, ICT-04, ICT-05, ICT-06, ICT-07]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - ICT-02 boundary explicit: price exactly at range high yields position 1.0 and at range low yields 0.0 with no clamping or re-anchor
    - ICT-02 precision explicit: quadrant and OTE absolute prices derive from range width in double precision and tests assert with closeTo tolerance
    - ICT-03 boundary explicit: close exactly equal to prior extreme does not re-anchor, only strict close beyond either extreme retires the range
    - ICT-03 precision explicit: re-anchor comparisons use raw price doubles with no rounding or epsilon band
    - ICT-03 idempotency explicit: running re-anchor check twice on same candles plus prior range returns identical retire flag and identical new anchors
    - ICT-03 concurrency explicit: concurrent re-anchor evaluations are stateless pure calls returning same output for same input with no shared mutation
    - ICT-04 empty explicit: zero closed candles yields COMPRESSION with insufficient-history rationale and never throws or returns empty bias
    - ICT-04 encoding explicit: every rationale string is English ASCII naming regime and position or history count with no Azerbaijani terms
    - ICT-04 idempotency explicit: same position plus regime plus count inputs always return identical bias and identical rationale text
    - ICT-04 concurrency explicit: parallel bias evaluations share no state and return identical outputs for identical inputs
    - ICT-05 boundary explicit: COMPRESSION equidistant from both extremes deterministically selects Range High with nearest-liquidity rationale
    - ICT-05 empty explicit: empty range input raises an explicit error instead of returning a fabricated DOL price
    - ICT-05 encoding explicit: DOL names are exactly Range High slash PDH and Range Low slash PDL ASCII with single spaces around slash
    - ICT-05 precision explicit: DOL price equals the selected extreme exactly with no rounding or offset
    - ICT-06 boundary explicit: current ATR exactly equal to its 20-bar average reads Compression with rationale naming both values
    - ICT-06 adjacency explicit: adjacent ATR windows are evaluated independently with no merging, each bar contributes once to Wilder smoothing in chronological order
    - ICT-06 empty explicit: fewer than 34 closed candles yields Compression with honest-degrade rationale and never Expansion
    - ICT-06 ordering explicit: regime inputs are consumed oldest-to-newest and identical candle order always yields identical regime output
    - ICT-06 precision explicit: Wilder ATR uses double-precision smoothing and tests assert ATR values with closeTo tolerance
    - ICT-07 boundary explicit: close-to-close gap exactly equal to 3 times ATR does not set rolloverSuspect, only strictly greater trips the flag
    - ICT-07 precision explicit: gap versus 3-times-ATR comparison uses raw doubles with no rounding and tripwire multiplier is exactly 3
    - ICT-07 concurrency explicit: parallel rollover evaluations are stateless and return identical flags for identical candle arrays
  artifacts:
    - src/lib/ict/levels.ts
    - src/lib/ict/bias.ts
    - src/lib/ict/dol.ts
    - src/lib/ict/regime.ts
    - src/lib/ict/rollover.ts
    - src/lib/ict/levels.test.ts
    - src/lib/ict/range.test.ts
    - src/lib/ict/bias.test.ts
    - src/lib/ict/dol.test.ts
    - src/lib/ict/regime.test.ts
    - src/lib/ict/rollover.test.ts
  key_links:
    - levels.ts consumes DealingRange anchors from range.ts plus last closed close to emit EQ quadrants OTE pockets and position
    - bias.ts consumes position plus regime plus closed count to emit bias with mandatory rationale per D-10
    - dol.ts consumes bias plus range plus last price to emit single named Primary DOL per D-11
    - regime.ts ATR output feeds bias.ts regime input as expansion or compression string
    - rollover.ts runs independently flag-and-continue and never blocks range bias or regime computation
  prohibitions:
    - Never perform wall-clock reads or locale formatting or store imports inside ict modules
    - Never include forming rows in any ict computation; every entry filters via closedOnly first
    - Never retire ranges on wick pierce; close-only semantics per D-06
    - Never absorb rollover gaps silently; flag-and-continue per D-07
---

<objective>
Expansion of the tracer math core per D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-16: levels with EQ quadrants OTE, bias with mandatory rationale, single named Primary DOL, ATR regime with honest degrade, rollover tripwire with hint, plus the full re-anchor rule table, each proven by co-located suites.

Purpose: Turn the proven tracer slice into the complete deterministic Module 2 math core that Phase 2 selectors consume.
Output: Five pure modules plus six test files, all green, with every ICT probe edge explicitly asserted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-ict-core/01-CONTEXT.md
@tsconfig.json
@package.json
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Levels math plus re-anchor rule table</name>
  <files>src/lib/ict/levels.ts, src/lib/ict/levels.test.ts, src/lib/ict/range.test.ts</files>
  <read_first>src/lib/ict/types.ts, src/lib/ict/range.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md, tsconfig.json</read_first>
  <acceptance_criteria>levels.ts exports computeLevels accepting range high low plus last closed close and returning eq plus q1 plus q3 plus bull and bear OTE pockets plus position; eq equals half sum of high and low; q1 equals low plus quarter width and q3 equals low plus three-quarter width; bull pocket lo equals high minus 0.79 width with hi equals high minus 0.62 width and bear mirror from low; position 1.0 at high and 0.0 at low exactly; range.test.ts asserts close-only retire on strict break plus wick pierce ignored plus equality no-retire plus identical repeat output plus forming exclusion; npx vitest run src slash lib slash ict slash levels dot test dot ts and range dot test dot ts reports all passed</acceptance_criteria>
  <action>Implement computeLevels in src slash lib slash ict slash levels.ts per D-13 accepting a DealingRange plus last closed close number and returning eq plus quadrant lines plus both absolute-price OTE pockets plus position number, using quarter and three-quarter fractions for quadrants and 0.62 to 0.79 fractions for pockets with the bull pocket measured down from the high and the bear pocket measured up from the low. Guard zero width by returning position 0.5. Keep strict purity with injected inputs only and no wall-clock or locale or store access, filtering forming rows via the shared helper before any math. Create levels.test.ts with synthetic candle builders covering EQ formula, quadrant fractions, both OTE pockets, boundary positions 1.0 and 0.0, precision assertions with closeTo tolerance, and forming-row exclusion. Create range.test.ts proving the D-06 table: strict close beyond either extreme retires and re-anchors over the trailing window, wick pierce with close inside does nothing, close exactly equal to the extreme does nothing, repeated evaluation returns identical flags and anchors, and forming rows never affect anchors.</action>
  <verify><automated>npx vitest run src/lib/ict/levels.test.ts src/lib/ict/range.test.ts</automated><fails_when>exit code is non-zero or stdout does not contain passed for both listed files</fails_when></verify>
  <done>EQ quadrants OTE plus position compute per D-13 and re-anchor obeys close-only semantics per D-06 with all probe edges green.</done>
</task>

<task type="auto">
  <name>Task 2: Bias with rationale plus single named Primary DOL</name>
  <files>src/lib/ict/bias.ts, src/lib/ict/bias.test.ts, src/lib/ict/dol.ts, src/lib/ict/dol.test.ts</files>
  <read_first>src/lib/ict/types.ts, src/lib/ict/levels.ts, src/lib/ict/range.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md</read_first>
  <acceptance_criteria>bias.ts exports ANCHOR WINDOW 20 and computeBias accepting position plus regime plus closed count with top-down rules thin-history then 0.48 to 0.52 buffer then discount bullish then premium bearish plus mandatory rationale on every path; zero candles yields COMPRESSION with insufficient-history rationale; dol.ts exports computePrimaryDOL accepting range plus bias plus last price returning single name plus price with bullish mapping to high and bearish to low and compression to nearer extreme with equidistant tie resolving to high; DOL names match expected PDH PDL spellings exactly; npx vitest run bias and dol suites reports all passed</acceptance_criteria>
  <action>Implement computeBias in src slash lib slash ict slash bias.ts per D-09 and D-10 with top-down evaluation: closed count below ANCHOR WINDOW 20 returns COMPRESSION with honest-degrade rationale naming the counts, position within 0.48 to 0.52 inclusive returns COMPRESSION with equilibrium-fair rationale naming position and regime, position below 0.48 returns BULLISH with discount rationale naming position and regime and longs-only discipline, position above 0.52 returns BEARISH with premium rationale naming position and regime and shorts-only discipline. Require rationale on every output in English with regime always named. Implement computePrimaryDOL in src slash lib slash ict slash dol.ts per D-11 returning a single name plus price object: bullish selects range high labeled Range High PDH side, bearish selects range low labeled Range Low PDL side, compression selects the nearer extreme by absolute distance with equidistant ties resolving deterministically to the high plus equilibrium nearest-liquidity rationale. Reject empty range input with an explicit error rather than fabricating a level. Cover with bias.test.ts asserting all four table rows plus zero-candle degrade plus repeat-call identical output, and dol.test.ts asserting all three mappings plus tie-break plus exact price equality plus name spellings plus empty-input error.</action>
  <verify><automated>npx vitest run src/lib/ict/bias.test.ts src/lib/ict/dol.test.ts</automated><fails_when>exit code is non-zero or stdout does not contain passed for both listed files</fails_when></verify>
  <done>Bias and Primary DOL derive from codified position plus regime rules with mandatory rationales and honest thin-history degrade.</done>
</task>

<task type="auto">
  <name>Task 3: ATR regime plus rollover tripwire</name>
  <files>src/lib/ict/regime.ts, src/lib/ict/regime.test.ts, src/lib/ict/rollover.ts, src/lib/ict/rollover.test.ts</files>
  <read_first>src/lib/ict/types.ts, src/lib/ict/range.ts, src/lib/ict/bias.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md</read_first>
  <acceptance_criteria>regime.ts exports ATR PERIOD 14 plus REGIME AVG WINDOW 20 plus MIN CANDLES FULL 34 with Wilder smoothing and binary Expansion versus Compression output; below 34 bars returns Compression with honest-degrade rationale; equality of current ATR to average returns Compression; rollover.ts exports ROLLOVER ATR MULT 3 and detectRollover accepting candles plus ATR value plus asOf plus contract hint returning suspect flag plus hint plus nullable proximity warning with strict greater-than tripwire and equality no-flag; 250-point synthetic gap fires flag while anchors continue; no test reads the futures adjusted-price array; npx vitest run regime and rollover suites reports all passed</acceptance_criteria>
  <action>Implement computeRegime in src slash lib slash ict slash regime.ts per D-12 using Wilder ATR with period 14 compared against its own 20-bar simple average requiring 34 closed candles for full confidence, returning Compression with honest-degrade rationale below that count, Expansion when current ATR strictly exceeds the average with rationale naming both values, otherwise Compression. Consume candles oldest-to-newest with each bar contributing once to smoothing in chronological order and keep binary output with no neutral band. Implement detectRollover in src slash lib slash ict slash rollover.ts per D-07 computing absolute close-to-close gaps against 3 times the supplied ATR value, setting suspect true only on strict greater-than with equality leaving the flag clear, attaching the supplied contract hint string plus a proximity warning when asOf falls within 10 calendar days of a quarterly third Friday in March June September or December, and continuing computation off current anchors without freezing. Cover with regime.test.ts asserting thin degrade, expansion, compression, equality-to-compression, chronological stability, and double-precision closeTo checks, plus rollover.test.ts asserting the synthetic large-gap tripwire, the equality no-flag boundary, flag-and-continue anchors unchanged, and proximity warning presence near roll week.</action>
  <verify><automated>npx vitest run src/lib/ict/regime.test.ts src/lib/ict/rollover.test.ts</automated><fails_when>exit code is non-zero or stdout does not contain passed for both listed files</fails_when></verify>
  <done>Regime and rollover follow pinned numeric parameters with honest degrade and flag-and-continue semantics, all edges green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy to ict-math | Only validated closed candles plus explicit asOf date enter these pure modules |
| ict-math to phase-2-selectors | Range bias DOL regime rollover outputs leave here as typed data plus rationale strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | src/lib/ict/range.ts plus levels.ts | medium | mitigate | Close-only re-anchor plus forming exclusion plus zero-width 0.5 guard, proven by range and levels suites with boundary and precision assertions |
| T-02-02 | Tampering | src/lib/ict/rollover.ts | high | mitigate | Strict greater-than 3-times-ATR tripwire with flag-and-continue, never silent absorb and never freeze, proven by synthetic-gap test; adjusted-price array never parsed |
| T-02-03 | Information Disclosure | src/lib/ict/bias.ts plus dol.ts plus regime.ts rationale | low | accept | Rationale strings are deterministic English rule traces with no upstream payload echo; accepted because output honesty outweighs the negligible fingerprinting surface |
| T-02-SC | Tampering | npm installs | low | accept | No package installs in this plan; vitest toolchain already approved in 01-01 |
</threat_model>

<verification>
Targeted suites per task plus full npx vitest run and npx tsc noEmit at plan end. Purity spot check: search ict sources excluding tests for wall-clock, locale, and store tokens and confirm zero hits before commit.
</verification>

<success_criteria>
All five math modules plus six test files exist, full ICT probe edge set for ICT-02 through ICT-07 plus ICT-03 re-anchor table green, typecheck clean, no forming leakage, no wick re-anchor, no silent rollover absorb.
</success_criteria>

## Artifacts this phase produces

This plan creates: src slash lib slash ict slash levels.ts exporting computeLevels; src slash lib slash ict slash bias.ts exporting ANCHOR WINDOW and computeBias; src slash lib slash ict slash dol.ts exporting computePrimaryDOL; src slash lib slash ict slash regime.ts exporting ATR PERIOD, REGIME AVG WINDOW, MIN CANDLES FULL, computeATR, computeRegime; src slash lib slash ict slash rollover.ts exporting ROLLOVER ATR MULT and detectRollover; plus co-located levels.test.ts, range.test.ts, bias.test.ts, dol.test.ts, regime.test.ts, rollover.test.ts proving every ICT probe edge.

<output>
Create `.planning/phases/01-data-foundation-ict-core/01-02-SUMMARY.md` when done
</output>
