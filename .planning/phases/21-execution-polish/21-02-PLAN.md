---
phase: 21-execution-polish
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/lib/ict/pools-parity.test.ts
  - src/lib/ict/calibration.test.ts
  - components/dashboard/firing-log-panel.tsx
  - components/dashboard/ticket-panel.tsx
autonomous: true
requirements: [POL-01, POL-02, POL-03]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Operator reads the 1 to 4 per week band status plus the pools on versus off verdict table inline in the extended log panel with no separate summary block
    - Full 20-session transition table replays with pools toggled and every trigger flaw ticket triple matches exactly including buffered price deltas
    - Buffered SL and TP placement is visible on moved chart lines and noted in ticket prose while the R/R gate stays on TP1
  artifacts:
    - src/lib/ict/pools-parity.test.ts replays the full 20-session table pools on and off
    - src/lib/ict/calibration.test.ts pins verdict counting overflow inclusion and empty-log guard
    - components/dashboard/firing-log-panel.tsx renders band verdict plus context-only proof table
    - components/dashboard/ticket-panel.tsx renders the verbatim buffer note
  key_links:
    - parity driver reuses the replay table bars with session fired-state owned by the driver
    - proof table reads the same firingLog array as the entries list never a second source
    - chart SL and TP lines derive from buffered ticket values with z-order unchanged
---

<objective>
Complete the calibration evidence surface and the exact-match parity proof that the tracer skeleton only sketches, per D-01, D-02, D-03, D-04, D-06, D-07, D-11, D-12.

Purpose: Give the operator reviewable fire-rate evidence with pools proven context-only, and lock the pools-never-vote guarantee with a live-shaped replay proof.
Output: Full parity suite green, calibration view live in the panel, buffered placement visible in prose and on chart.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-execution-polish/21-CONTEXT.md
@.planning/phases/21-execution-polish/21-RESEARCH.md
@.planning/phases/21-execution-polish/21-PATTERNS.md
@.planning/phases/21-execution-polish/21-UI-SPEC.md
@src/lib/ict/calibration.ts
@src/lib/ict/replay.test.ts
@src/lib/ict/parity.test.ts
@components/dashboard/firing-log-panel.tsx
@components/dashboard/ticket-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Full 20-session pools toggled parity plus calibration helper pins</name>
  <files>src/lib/ict/pools-parity.test.ts, src/lib/ict/calibration.test.ts</files>
  <action>Extend the tracer parity suite from plan 01 to replay the full 20-session transition table from replay.test.ts with pools toggled per D-11, driving each bar through evaluateTrigger then checkFatalFlaw on the same snapshot then computeTicket with the identical buffered build in both arms per D-10 and D-17, session fired-state owned by the driver per the runReplay precedent, asserting exact triple equality on every bar per D-12 with buffered SL and TP deltas inside the match and zero tolerance band, keeping parity.test.ts byte-identical per D-09. Add calibration helper pins beside the suite covering FIRE-only counting, weeks as unique NY-week Mondays minimum 1, overflow counted conservatively toward fires, empty log plus zero overflow yielding null verdict, and the IN-BAND edges at exactly 1 and exactly 4 fires per week.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools-parity.test.ts src/lib/ict/calibration.test.ts</automated>
    <fails_when>vitest exits non-zero or any replay bar triple diverges between pools arms</fails_when>
  </verify>
  <done>Full 20-session replay passes with zero triple divergence and helper edge pins green</done>
</task>

<task type="auto">
  <name>Calibration proof table plus buffered ticket visibility</name>
  <files>components/dashboard/firing-log-panel.tsx, components/dashboard/ticket-panel.tsx</files>
  <action>Complete the FiringLogPanel calibration view per D-01, D-02, and D-04 on top of the tracer inline verdict line: add the pools on versus off verdict comparison table reading the same firingLog array as the entries list, identical rows in muted copy and any divergent row in the destructive token, thin history rendering the documented thin note with the verdict dimmed at opacity-45 never hidden, stale legs printing the verbatim NQ-leg lastError inline never cached verdicts, and the overflow line retained above entries with the verdict counting the full log including overflow. Add the verbatim buffer note line from the UI-SPEC copy contract beside data-slot ticket-reason in ticket-panel.tsx per D-07 in muted Body or mono styling, and confirm the chart SL and TP lines derive from the buffered ticket values so lines move off the extreme with EXECUTE-only rendering and unchanged z-order, threading buffered values through the existing ticket-lines path only if the chart currently reads structure levels directly. Keep the R/R gate copy and position untouched and never render pool-gated FIRE, stop-price, heatmap, or narrative copy anywhere.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts src/lib/chart-mapper.test.ts</automated>
    <fails_when>vitest exits non-zero or shell and mapper suites report failures on panel or line wiring</fails_when>
  </verify>
  <done>Proof table renders from the same log beside entries, buffer note renders in ticket prose, chart lines track buffered values with gate unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay fixtures to parity comparison | toggled pools presence crosses here |
| firing-log store to proof table | retained entries cross here |
| buffered ticket to chart lines | moved SL and TP values cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | pools-parity full replay | high | mitigate | Single buffered build shared by both arms, toggle confined to driver fixtures, exact-match bar with zero tolerance, parity.test.ts untouched anchor |
| T-21-02 | Spoofing | proof table and band verdict | medium | mitigate | Table reads the same firingLog array as entries, FIRE-only conservative counting, null verdict over empty log, divergent rows fail loud in destructive |
| T-21-04 | Information disclosure | stale and thin calibration states | medium | mitigate | Verbatim leg errors inline never cached verdicts, thin dims at opacity-45 never hides, empty keeps heading plus calibration body |
| T-21-SC | Tampering | npm and shadcn installs | high | mitigate | No installs in this plan, provenance gate lives in plan 03 before the slider file install |
</threat_model>

<verification>
Full suite green with the 20-session parity proof included, tsc clean, purity guard green, no trigger gate flaw key or derivation-order change anywhere.
</verification>

<success_criteria>
Operator reviews band status plus context-only proof inline, full replay proves identical verdicts pools on versus off, buffered placement visible in prose and on chart.
</success_criteria>

<output>
Create `.planning/phases/21-execution-polish/21-02-SUMMARY.md` when done
</output>
