---
phase: 21-execution-polish
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - components/ui/slider.tsx
  - components/dashboard/calibration-sandbox.tsx
  - components/dashboard/terminal-shell.tsx
  - src/lib/store.ts
  - src/lib/store.test.ts
  - src/lib/ict/trigger.ts
  - src/lib/ict/pools.ts
  - src/lib/ict/trigger.test.ts
  - src/lib/ict/pools.test.ts
  - src/lib/ict/replay.test.ts
  - src/lib/ticket.ts
  - src/lib/ticket.test.ts
  - src/terminal-shell.test.ts
  - .planning/PROJECT.md
autonomous: false
requirements: [POL-04]
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Threshold and pool-tolerance knobs render only from the installed seventh primitive inside a sandbox block near the log panel
    - Slider movement previews what-if dimmed with a visible unapplied tag and never rewrites live derivation until Apply
    - Apply writes constants plus re-pins boundary tests while refresh resets to pinned values with no persistence
  artifacts:
    - components/ui/slider.tsx wraps the base-ui slider primitive
    - components/dashboard/calibration-sandbox.tsx renders both knob families plus Apply
    - src/lib/store.ts holds a session-scoped preview slice separate from pinned constants
    - .planning/PROJECT.md allowlist reads seven primitives with slider appended
  key_links:
    - sandbox knobs render only from the ui slider wrapper never raw base-ui inline
    - terminal-shell mounts the sandbox block beside the log panel with a single import plus render
    - selectors read pinned constants only until Apply commits
    - preview state clears on refresh with zero localStorage writes
---

<objective>
Ship the threshold and pool-tolerance review sandbox with the seventh shadcn primitive and session-scoped preview plus explicit Apply, per D-13, D-14, D-15, D-16, D-22.

Purpose: Let the operator explore what-if threshold and tolerance values safely without corrupting live derivation, and close the retune plus re-pin calibration loop.
Output: Installed slider primitive, sandbox block with preview and Apply, pinned-constant-only selectors, amended docs.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-execution-polish/21-CONTEXT.md
@.planning/phases/21-execution-polish/21-RESEARCH.md
@.planning/phases/21-execution-polish/21-PATTERNS.md
@.planning/phases/21-execution-polish/21-UI-SPEC.md
@src/lib/store.ts
@src/lib/ict/trigger.ts
@src/lib/ict/pools.ts
@src/lib/ict/trigger.test.ts
@src/lib/ict/pools.test.ts
@src/lib/ticket.ts
@src/lib/ticket.test.ts
@src/terminal-shell.test.ts
@components/ui/button.tsx
@components/dashboard/terminal-shell.tsx
@components/dashboard/firing-log-panel.tsx
@components/dashboard/ticket-panel.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package legitimacy gate for the slider install</name>
  <what-built>Slider install provenance case prepared: shadcn CLI 4.21.0 file-copy of components/ui/slider.tsx over the already-declared base-ui react peer, no new npm runtime dependency per D-16, reusing the six-primitive install pattern</what-built>
  <how-to-verify>
    Confirm on npmjs.com/package/@base-ui__react and the shadcn-ui ui source repo that the wrapper target is legitimate:
    1. @base-ui/react at the declared ^1.8.0 line is the genuine base-ui package with expected age and download history
    2. shadcn slider output wraps the base-ui slider sub-module the same way the six existing primitives wrap their peers
    3. No new runtime dependency enters package.json — the install only copies one component file
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single npx shadcn add slider file install, or describe the provenance concern</resume-signal>
</task>

<task type="auto">
  <name>Slider primitive install plus session sandbox slice plus docs amendment</name>
  <files>components/ui/slider.tsx, src/lib/store.ts, src/lib/store.test.ts, .planning/PROJECT.md</files>
  <action>Run the approved file-only slider install producing components/ui/slider.tsx as the seventh primitive per D-16, wrapping the base-ui slider sub-module path exactly as the CLI output declares, thin wrapper with data-slot cn merge and props spread following the button.tsx precedent, thumb visual 16px with 44px hit-area and accent only on active-track fill plus focused thumb ring, knobs rendering ONLY from this wrapper never raw base-ui inline. Add a session-scoped preview slice in store.ts beside ticketInputs per D-15 holding both knob families from D-13, trigger gate thresholds seeded from TRIGGER_KZ_START_MIN, TRIGGER_KZ_END_MIN, TRIGGER_DISP_MULT plus pool tolerance seeded from EQUAL_TOL_BPS, MERGE_ATR_MULT, DOL_BOOST, BEHIND_PENALTY with planner-locked UI ranges, killzone minutes 0 to 600 integer, displacement 0.1 to 2.0 step 0.05, EQUAL_TOL 5 to 100 bps integer, merge multiple 0.05 to 1.0 step 0.05, DOL_BOOST 1.0 to 4.0 step 0.1, BEHIND_PENALTY 0.0 to 1.0 step 0.05, clamped setters refusing NaN and non-finite following the setRiskPct pattern, no localStorage reads or writes so refresh resets to pinned constants. Pin the slice with store tests covering seed values, clamping, NaN refusal, and refresh-reset session scope. Amend both PROJECT.md shadcn lines per D-16 and D-22, appending slider to the six-name allowlist as the first amendment since v1.0.</action>
  <verify>
    <automated>npm test -- src/lib/store.test.ts</automated>
    <fails_when>vitest exits non-zero or slice pins fail on seed clamp NaN-refusal or reset behavior</fails_when>
  </verify>
  <done>Slider wrapper installed from the approved primitive, preview slice pinned, docs read seven primitives, refresh resets with no persistence</done>
</task>

<task type="auto">
  <name>Sandbox block with preview plus explicit Apply</name>
  <files>components/dashboard/calibration-sandbox.tsx, components/dashboard/terminal-shell.tsx, src/lib/ict/trigger.ts, src/lib/ict/pools.ts, src/lib/ict/trigger.test.ts, src/lib/ict/pools.test.ts, src/lib/ict/replay.test.ts, src/lib/ticket.ts, src/lib/ticket.test.ts, src/terminal-shell.test.ts</files>
  <action>Build the sandbox block near FiringLogPanel as a Card with data-slot calibration-sandbox per D-14 and the ticket-panel shell precedent, mounting it from terminal-shell.tsx beside the FiringLogPanel import so the shell composes exactly one sandbox block next to the log panel per D-01 with no second mount and no pools voting change per D-17. Render Label-role fixed section labels TETİK HƏDLƏRİ plus HOVUZ TOLERANSLIĞI, one knob row per threshold and tolerance value rendered only from the installed ui Slider with mono tabular values, unapplied preview dimmed at opacity-45 with the visible BAXIŞ tag, and an explicit Tətbiq et Button size sm that commits preview into the pinned constant owners in trigger.ts, pools.ts, and ticket.ts plus re-pins the affected trigger, pools, replay threshold, and ticket boundary tests with updated seeded values and CALIBRATION-PROVISIONAL markers intact per the D-03 discipline, keeping selectors reading pinned constants with zero live re-derivation on movement and no selector contract change. Wire sliders to the preview slice only, degraded states disabling interaction honestly, no modal confirmations and no toast surfaces, calibration-stale printing verbatim leg errors inline. Extend terminal-shell.test.ts with a shell assertion that mounts the sandbox block with preview and Apply states.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts src/lib/ict/trigger.test.ts src/lib/ict/pools.test.ts src/lib/ticket.test.ts && grep -q 'calibration-sandbox' components/dashboard/terminal-shell.tsx && grep -q 'calibration-sandbox' src/terminal-shell.test.ts</automated>
    <fails_when>vitest exits non-zero, the shell suite fails to mount the sandbox block with preview and Apply states, the shell suite shows no sandbox slot assertion, or any re-pinned boundary suite diverges from the applied constants</fails_when>
  </verify>
  <done>Sandbox mounts once in the shell beside the log panel, previews dimmed with tag, Apply commits into the constant owners plus re-pins trigger pools replay and ticket boundary tests, movement alone changes zero verdicts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator slider input to preview state | untrusted numeric knob values cross here |
| preview state to pinned constants | Apply commit crosses here |
| registry CLI to components tree | copied slider file crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | slider knob values | high | mitigate | Clamped setters refuse NaN and non-finite, ranges locked per knob, selectors read pinned constants only until Apply, preview dims with BAXIŞ tag, no live re-derivation |
| T-21-06 | Spoofing | preview versus pinned truth | medium | mitigate | Separate preview slice from pinned constants, Apply commits plus re-pins boundary tests, refresh resets with zero localStorage, constants stay CALIBRATION-PROVISIONAL |
| T-21-02 | Spoofing | calibration stale states | medium | mitigate | Verbatim leg errors inline never cached verdicts, no toast surface, degraded disables interaction |
| T-21-SC | Tampering | npm and shadcn installs | high | mitigate | Blocking human checkpoint confirms base-ui plus shadcn provenance before the file-only install, no new runtime dependency, wrapper read from CLI output never guessed |
</threat_model>

<verification>
Slider renders from the installed primitive, preview never leaks into derivation, Apply re-pins boundaries, docs amended, full suite green.
</verification>

<success_criteria>
Both knob families adjustable in a dimmed preview sandbox, explicit Apply writes plus re-pins, refresh resets, installed primitive is the only knob renderer.
</success_criteria>

<output>
Create `.planning/phases/21-execution-polish/21-03-SUMMARY.md` when done
</output>
