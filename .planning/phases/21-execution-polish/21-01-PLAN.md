---
phase: 21-execution-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/calibration.ts
  - src/lib/ticket.ts
  - src/lib/ticket.test.ts
  - src/lib/ict/pools-parity.test.ts
  - components/dashboard/firing-log-panel.tsx
autonomous: true
requirements: [POL-01, POL-02, POL-03]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Band verdict derives FIRE-only from the firing log with overflow counted conservative and renders inline next to entries; empty log renders no verdict line
    - Buffered SL sits beyond the pool extreme by 0.25x ATR and TP legs resolve before the extreme with the R/R gate unchanged on TP1
    - Trigger plus flaw plus ticket triple matches exactly pools-on vs pools-off on the tracer replay bar using one shared buffered build
  artifacts:
    - src/lib/ict/calibration.ts exports summarizeFiringLog
    - src/lib/ticket.ts exports SL buffer and TP pullback multiples as CALIBRATION-PROVISIONAL
    - src/lib/ict/pools-parity.test.ts exists while parity.test.ts is untouched
    - components/dashboard/firing-log-panel.tsx renders the inline verdict line
  key_links:
    - calibration.ts helper feeds firing-log-panel.tsx inline read with zero math in render
    - ticket.ts buffered build is shared by both parity arms with pools toggle driver-only
    - replay bar fixtures feed the single-snapshot trigger flaw ticket triple
---

<objective>
Tracer slice proving the Phase 21 architecture end to end on first commit: pure band-verdict math plus buffered ticket math plus a one-bar pools on/off parity skeleton plus the inline panel verdict read per D-01, D-02, D-05, D-06, D-09, D-10, D-12, D-17, D-19, D-21.

Purpose: Catch an architectural dead end (buffer threading, verdict counting, harness shape) before the full calibration view, full 20-session replay, and slider sandbox expand from it.
Output: Working helper plus buffered derivation plus skeleton harness plus inline verdict line, all green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-execution-polish/21-CONTEXT.md
@.planning/phases/21-execution-polish/21-RESEARCH.md
@.planning/phases/21-execution-polish/21-PATTERNS.md
@.planning/phases/21-execution-polish/21-UI-SPEC.md
@src/lib/ict/trigger.ts
@src/lib/ticket.ts
@src/lib/store.ts
@src/lib/ict/replay.test.ts
@src/lib/ict/parity.test.ts
@components/dashboard/firing-log-panel.tsx
</context>

<tasks>

<task type="tracer">
  <name>Tracer buffered ticket plus band verdict plus one-bar parity plus inline panel read</name>
  <files>src/lib/ict/calibration.ts, src/lib/ticket.ts, src/lib/ict/pools-parity.test.ts, components/dashboard/firing-log-panel.tsx</files>
  <action>Create pure helper summarizeFiringLog in a new ict module that takes retained entries plus overflow count plus session dates and returns fires, weeks, firesPerWeek, and verdict IN-BAND for 1 to 4 fires per week else OUT-OF-BAND, with FIRE-only counting per D-01 and the research open-question resolution, weeks as max of 1 and unique NY-week Mondays, overflow counted conservatively as additional fires so the verdict never false-HOLDs on trimmed history, and null verdict on zero entries plus zero overflow so the panel renders nothing over an empty log. Keep the module free of clock reads and store imports per D-21. Extend ticket derivation with two exported CALIBRATION-PROVISIONAL multiples seeded 2026-09-17 awaiting firing-log review: SL inset beyond the pool extreme at 0.25x ATR per D-05 mirroring the merge idiom, and TP inset before the extreme at 0.10x ATR as the planner-locked provisional default per D-06 resolving the research open question, threading ATR and pool extreme as injected optional inputs with null degrading to unbuffered structure levels, preserving fixed step order and the TICKET_RR_MIN gate on TP1 per D-07, TP legs staying null when unresolvable, extreme never a target, pool geometry read from the locked shape per D-18 without changing rank-1 or lifecycle semantics. Build the new pools-parity suite file per D-09 leaving parity.test.ts byte-identical, replaying exactly one deterministic replay-table bar through evaluateTrigger then checkFatalFlaw on the same snapshot then computeTicket, each arm running the identical buffered build with the pools toggle living only in the driver fixtures per D-17, asserting exact triple equality including buffered SL and TP deltas per D-10 and D-12 with no tolerance band, using the parity.test.ts shared-triple identity idiom per D-20. Extend FiringLogPanel per D-19 by reading the helper during render from firingLog plus firingLogOverflow and rendering one inline verdict line beside the entries per D-02 with data-slot calibration-verdict, HOLD copy in terminal-up and BREAK copy in destructive at Heading role, keeping the existing empty heading plus calibration body and the overflow line above entries.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools-parity.test.ts</automated>
    <fails_when>vitest exits non-zero or the suite reports any failed or skipped test</fails_when>
  </verify>
  <done>One-bar parity passes with exact triple match, helper returns IN-BAND on a 2-per-week fixture and null on empty, panel renders the inline verdict line beside entries</done>
</task>

<task type="auto">
  <name>Buffer boundary matrix pins in the ticket suite</name>
  <files>src/lib/ticket.test.ts</files>
  <action>Extend the ticket suite with boundary rows pinning the new multiples per D-08 using the existing tracer-fixture shape with hand-computed math in comments: LONG SL lands beyond the extreme by exactly 0.25x ATR, SHORT mirrors, TP1 and TP2 inset before the extreme by the provisional pullback multiple, null ATR or null extreme degrades to unbuffered structure levels, unresolvable legs stay null, wrong-side and STOP_EPS refuses hold, and the R/R gate still passes or refuses on TP1 only at the unchanged floor. Keep reason strings verbatim and toBe-pinned, interpolating only the sanctioned R/R ratio.</action>
  <verify>
    <automated>npm test -- src/lib/ticket.test.ts</automated>
    <fails_when>vitest exits non-zero or the buffer matrix rows report mismatch on SL TP or gate outcome</fails_when>
  </verify>
  <done>Buffer matrix rows pin SL beyond extreme, TPs before extreme, null-input degrade, and unchanged gate position</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector-envelope to ticket math | malformed ATR or extreme values cross here |
| firing-log store to calibration read | retained entries plus overflow count cross here |
| test driver to verdict comparison | pools toggled fixtures cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | src/lib/ticket.ts buffer insertion | high | mitigate | Clamp and finite-guard injected ATR and extreme with got-string throws on malformed non-null input, null degrades to unbuffered levels, STOP_EPS and wrong-side refuses hold, boundary matrix pins exact placement |
| T-21-02 | Spoofing | calibration verdict read | medium | mitigate | FIRE-only counting with conservative overflow inclusion, null verdict on empty log so no confident HOLD renders over zero entries, verbatim HOLD and BREAK copy test-pinned |
| T-21-03 | Tampering | pools-parity driver | high | mitigate | Both arms share one buffered computeTicket build, pools toggle lives only in driver fixtures never in trigger flaw ticket signatures, parity.test.ts stays untouched as regression anchor, exact-match bar with zero tolerance |
| T-21-SC | Tampering | npm and shadcn installs | high | mitigate | No new registry dependency this plan, shadcn CLI provenance checked at install time in plan 03 behind a blocking human checkpoint for any ASSUMED or SUS package |
</threat_model>

<verification>
Full suite stays green with pools live, tsc clean, purity guard green, parity.test.ts byte-identical to baseline.
</verification>

<success_criteria>
Tracer triple matches exactly on one replay bar, buffered SL and TP placement pinned, inline verdict renders beside entries and nothing over an empty log.
</success_criteria>

<output>
Create `.planning/phases/21-execution-polish/21-01-SUMMARY.md` when done
</output>
