---
phase: 21-execution-polish
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - components/dashboard/calibration-sandbox.tsx
  - components/ui/slider.tsx
  - src/lib/store.ts
  - src/terminal-shell.test.ts
autonomous: true
requirements: [POL-04]
gap_closure: true
gap_ids: [G-21-3]
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Drift dims preview with a conspicuous BAXIŞ badge rendered outside the dim container
    - Apply records the HOLD verdict copy with TUTULDU visible inside the sandbox and re-seeds knobs to pins
    - Each scalar knob renders exactly one thumb
  artifacts:
    - components/dashboard/calibration-sandbox.tsx hoisted badge plus verdict
    - src/lib/store.ts reactive applied flag in zustand state
    - components/ui/slider.tsx scalar single-thumb derivation
    - src/terminal-shell.test.ts isolated preview plus thumb tag verdict assertions
  key_links:
    - sandbox preview tag sits outside the opacity-45 dim container
    - sandbox applied block renders review.verdict TUTULDU copy
    - apply flips zustand applied flag plus re-seeds preview in one set
---

<objective>
Close gap G-21-3: make the sandbox drift tag conspicuous, show the TUTULDU HOLD verdict in-sandbox post-Apply, make the applied flag reactive, fix doubled slider thumbs, pin pristine to constants, and isolate the shell tests.

Purpose: Turn the verified-working Apply mechanics into what the operator actually sees — visible badge during drift, recognizable verdict after Apply, single-thumb knobs — without changing derivation or persistence semantics.
Output: Fixed sandbox, slider, store slice, and hardened shell-test coverage; parity.test.ts untouched.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-execution-polish/21-CONTEXT.md
@.planning/phases/21-execution-polish/21-UAT.md
@.planning/debug/sandbox-uat-issue.md
@components/dashboard/calibration-sandbox.tsx
@components/ui/slider.tsx
@src/lib/store.ts
@src/terminal-shell.test.ts
</context>

<tasks>

<task type="auto">
  <name>Fix sandbox badge plus verdict plus reactive flag plus thumbs plus pristine</name>
  <files>components/dashboard/calibration-sandbox.tsx, components/ui/slider.tsx, src/lib/store.ts</files>
  <action>Gap reason per G-21-3: preview tag renders inside the opacity-45 dim container as muted microcopy (near-invisible); review.verdict never renders in the sandbox; module-level applied flag is a latent reactivity hazard; slider wrapper falls back to [min,max] rendering two thumbs per scalar knob; pristine compares against literals. Reference existing code: calibration-sandbox.tsx lines 94-271, slider.tsx lines 1-51, store.ts lines 85-100 and 1214-1265 plus CALIBRATION_REVIEW_HOLD_COPY and calibrationAppliedCopy at 381-392. In store.ts move calibrationReviewApplied into zustand state as a reactive boolean field on DashboardState initialized false, delete the module-level let, make selectCalibrationReview read the flag from get() state, make applyCalibrationPreview set the flag true in the same set() call that re-seeds calibrationPreview to the pinned TRIGGER plus pools constants, keep resetCalibrationPreview and resetCalibrationPreviewToPinned re-seeding preview only with no flag change, no localStorage reads or writes, selectors keep reading pinned module constants with zero live re-derivation on movement, CALIBRATION-PROVISIONAL markers untouched. In calibration-sandbox.tsx hoist the data-slot sandbox-preview-tag element out of the opacity-45 div so it renders as a sibling before the dimmed block, restyle it as a conspicuous badge with semibold non-muted warning-chip treatment instead of muted microcopy while keeping PREVIEW_TAG_COPY text and data-slot unchanged, render review.verdict inside the data-slot sandbox-applied block so the TUTULDU HOLD copy appears in-sandbox post-Apply alongside appliedCopy, replace the seven pristine literals with the imported TRIGGER_KZ_START_MIN, TRIGGER_KZ_END_MIN, TRIGGER_DISP_MULT plus EQUAL_TOL_BPS, MERGE_ATR_MULT, DOL_BOOST, BEHIND_PENALTY constants. In slider.tsx derive thumb count from value first so a scalar value yields a single-element array and only the both-undefined case falls back to min-max, keeping all other classes, props spread, and thumbAlignment unchanged. Do not touch parity.test.ts. Do not add live re-derivation, localStorage, or constant rewrites.</action>
  <verify>
    <automated>npm test -- src/lib/store.test.ts src/terminal-shell.test.ts</automated>
    <fails_when>vitest exits non-zero or the sandbox still dims the tag, omits the verdict, leaks module state, or renders doubled thumbs</fails_when>
  </verify>
  <done>Tag renders outside the dim container as a conspicuous badge, applied block shows TUTULDU verdict plus applied copy, applied flag lives in zustand state, scalar knobs render one thumb, pristine compares against imported constants, suite green</done>
</task>

<task type="auto">
  <name>Harden shell-test isolation plus thumb tag verdict assertions</name>
  <files>src/terminal-shell.test.ts</files>
  <action>Gap reason per G-21-3 debug session: shell beforeEach calls vi.resetModules but keeps the already-imported useDashboard binding, so calibrationPreview and the applied flag leak across tests and only one test exercises preview and apply. Reference existing code: beforeEach at lines 169-195 and sandbox test at lines 1130-1177. Extend the existing beforeEach setState to also reset calibrationPreview to the pinned seeds and the moved zustand applied flag to false so every test starts pristine regardless of module reuse. Extend the sandbox test with three assertions: each of the seven sandbox-knob slots contains exactly one slider-thumb element, the sandbox-preview-tag element has no opacity-45 ancestor after drift, and the sandbox-applied block text contains the TUTULDU verdict alongside the existing KZ applied copy and boundary re-confirmation line. Keep the existing pristine, BAXIŞ, pinned-constant, and re-seed assertions intact. Do not touch src/lib/ict/parity.test.ts or any other suite file.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts</automated>
    <fails_when>vitest exits non-zero or thumb-count, tag-placement, or verdict assertions fail</fails_when>
  </verify>
  <done>Shell tests reset preview plus applied flag in beforeEach and assert one thumb per knob, hoisted tag, and in-sandbox TUTULDU verdict with suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator slider input to preview state | untrusted numeric knob values cross here |
| preview state to applied record | Apply commit crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | slider knob values | high | mitigate | Clamped setters refuse NaN and non-finite, scalar value renders one thumb, selectors read pinned constants only until Apply, preview dims with hoisted badge, no live re-derivation |
| T-21-06 | Spoofing | preview versus pinned truth | medium | mitigate | Separate preview slice from pinned constants, pristine compares against imported constants, Apply re-seeds plus records verdict in zustand state, refresh resets with zero localStorage |
</threat_model>

<verification>
Drift shows a conspicuous badge outside the dim block, Apply shows TUTULDU verdict plus applied copy with knobs re-seeded, one thumb per knob, shell tests isolated, full scoped suites green, parity.test.ts untouched.
</verification>

<success_criteria>
Gap G-21-3 truth holds: sandbox drift dims preview with a visible BAXIŞ tag, Tətbiq et records the HOLD applied copy with verdict and re-seeds preview to pinned seeds, scoped suites green.
</success_criteria>

<output>
Create `.planning/phases/21-execution-polish/21-04-SUMMARY.md` when done
</output>
