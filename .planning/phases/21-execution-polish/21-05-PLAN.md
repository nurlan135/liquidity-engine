---
phase: 21-execution-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/calibration-sandbox.tsx
  - src/terminal-shell.test.ts
autonomous: true
requirements: [POL-04]
gap_closure: true
gap_ids: [CR-01]
estimate:
  tokens: 12000
  raw_tokens: 6000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - Drift dims preview with a conspicuous BAXIŞ badge pre- AND post-Apply
    - Post-Apply knob drift re-shows the badge outside the dim container, never mistaken for the applied set
  artifacts:
    - components/dashboard/calibration-sandbox.tsx constant-pinned pristine comparison
    - src/terminal-shell.test.ts Apply-then-drift regression leg
  key_links:
    - pristine gate compares preview against imported pinned constants only
    - sandbox preview tag sits outside the opacity-45 dim container pre- and post-Apply
---

<objective>
Close blocking gap CR-01: post-Apply knob drift must re-show the BAXIŞ badge plus opacity-45 dimming instead of masquerading as the applied set.

Purpose: Restore the phase goal "execution stays calibrated and uncorrupted" — the operator must never read a drifted what-if as the reviewed HOLD set (per D-14 preview-vs-applied separation).
Output: Two-line pristine fix plus a shell-test regression leg; no derivation, persistence, or verdict semantics change.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-execution-polish/21-CONTEXT.md
@.planning/phases/21-execution-polish/21-VERIFICATION.md
@components/dashboard/calibration-sandbox.tsx
@src/terminal-shell.test.ts
</context>

<tasks>

<task type="auto">
  <name>Fix post-Apply pristine gate plus Apply-then-drift regression test</name>
  <files>components/dashboard/calibration-sandbox.tsx, src/terminal-shell.test.ts</files>
  <action>In components/dashboard/calibration-sandbox.tsx lines 129-137, delete the session-flag disjunct from the pristine computation so the value derives solely from strict equality of each preview knob against its imported pinned constant (TRIGGER_KZ_START_MIN, TRIGGER_KZ_END_MIN, TRIGGER_DISP_MULT, EQUAL_TOL_BPS, MERGE_ATR_MULT, DOL_BOOST, BEHIND_PENALTY) per D-14 preview-vs-applied separation; leave the badge element (data-slot sandbox-preview-tag), the dim container (opacity-45), the applied block (data-slot sandbox-applied), and all store Apply/re-seed logic untouched. In src/terminal-shell.test.ts extend the existing sandbox test with a post-Apply leg: drive Apply, assert the applied copy renders, then drift one knob off its pin and assert the preview tag is present with closest opacity-45 null while a dimmed container exists; follow the existing beforeEach preview-plus-flag reset idiom and the hoisted-tag assertion pattern from plan 21-04. Gap reason: review.applied stays true for the rest of the session, permanently forcing pristine true and sealing the badge/dim gate shut after Apply.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts src/terminal-shell.test.ts</automated>
    <fails_when>vitest reports any failing test file or a failed/skipped sandbox badge assertion</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>tsc exits non-zero or prints any type error</fails_when>
  </verify>
  <done>Pristine contains no applied-flag term; Apply-then-drift renders the preview tag outside dimming and the full shell plus store suites stay green with tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox preview → operator judgment | Drifted what-if must never present as the reviewed applied set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05-01 | Spoofing | calibration-sandbox.tsx pristine gate | high | mitigate | Pin pristine to imported constants only; badge plus dim render on any drift pre- and post-Apply (this plan) |
</threat_model>

<verification>
Post-Apply drift shows the amber BAXIŞ chip undimmed above a dimmed preview; Tətbiq et still records the TUTULDU applied copy; refresh resets to pins. Full shell plus store suites green, tsc clean, parity.test.ts untouched.
</verification>

<success_criteria>
CR-01 closed: truth "Drift dims preview with a conspicuous BAXIŞ badge (pre- AND post-Apply)" holds; VERIFICATION.md gap re-check passes with no new warnings introduced.
</success_criteria>

<output>
Create `.planning/phases/21-execution-polish/21-05-SUMMARY.md` when done
</output>
