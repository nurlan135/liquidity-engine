---
phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende
plan: 03
type: execute
wave: 2
depends_on: [03.2-01, 03.2-02]
files_modified:
  - components/dashboard/terminal-shell.tsx
  - src/terminal-shell.test.ts
  - scripts/verify-deploy.sh
autonomous: true
requirements: [DATA-03, DEPLOY-01, STATE-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Chart header shows freshness copy (LIVE age, STALE age, CLOSED) derived from the same envelope truth as the strip, in Azerbaijani
    - verify-deploy.sh asserts the stale and 502 contract, the Baku date contract, and the rollover-banner slot contract as path checks that pass on any date
    - Header copy disagrees with the strip never, and the script stays zero-dependency curl plus node with syntax clean
  artifacts:
    - components/dashboard/terminal-shell.tsx
    - src/terminal-shell.test.ts
    - scripts/verify-deploy.sh
  key_links:
    - Header span reuses the shell 10s now tick plus deriveStatus plus formatStripAge, never reimplementing staleness
    - Script steps copy the ENVELOPE curl plus node fail-closed idiom and assert contracts rather than live events
    - data-slot chart-freshness hook is queried by the integration tests so all three states are asserted
---

<objective>
Close the W2 chart-header freshness contract (per D-06) and extend verify-deploy.sh with the three D-07 steps (stale and 502 drill, Baku DST-date, rollover-banner), with header copy tests plus script syntax proof.

Purpose: DATA-03 says freshness on the chart header literally, and the header carries none; the deploy script asserts the fresh path only, leaving the stale contract, the date contract, and the banner path unchecked.
Output: header freshness span plus render cases, three appended script steps with syntax clean.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-RESEARCH.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-VALIDATION.md
@components/dashboard/terminal-shell.tsx
@components/dashboard/status-strip.tsx
@src/lib/freshness.ts
@scripts/verify-deploy.sh
@app/api/yahoo/route.ts
@src/terminal-shell.test.ts
</context>

<tasks>

<task type="auto">
  <name>W2 chart-header freshness copy plus three-state proof</name>
  <files>components/dashboard/terminal-shell.tsx, src/terminal-shell.test.ts</files>
  <read_first>components/dashboard/terminal-shell.tsx, components/dashboard/status-strip.tsx, src/lib/freshness.ts, src/terminal-shell.test.ts, .planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md, AGENTS.md</read_first>
  <reversibility rating="reversible">Adds one span plus test cases; removal is deleting the span and the cases with no store change.</reversibility>
  <action>Extend the panel-center CardHeader in components/dashboard/terminal-shell.tsx per D-06. Beside the CHART - NQ=F D1 CardTitle add a span with data-slot chart-freshness rendering formatStripAge applied to deriveStatus of stale plus lastUpdatedISO against the existing 10s now tick state, importing formatStripAge alongside the existing deriveStatus import; reuse the now state owned at the top of the component, create no second interval, compute no age outside deriveStatus, and mirror the strip variants including the live-dot treatment. When lastUpdatedISO is null or deriveStatus throws, render the same loading or stale fallback copy the strip uses rather than English text or raw timestamps; all copy stays Azerbaijani with English identifiers. The levels subscription and NqChart props from the parallel render plan land first, so rebase onto them without touching their lines; this task edits only the CardHeader block and the freshness import. Extend src/terminal-shell.test.ts with three render cases reusing the installDom plus renderShell harness: a live case seeding a fresh envelope with lastUpdatedISO now asserting the chart-freshness query contains LIVE; a stale case seeding stale true asserting the query contains STALE; a closed case pinning the clock to a Baku Saturday noon via fake timers asserting the query contains BAZAR BAGLIDIR. Use ASCII-safe substring BAZAR BAGLIDIR instead of the dotted capital I form so the assertion is encoding-stable. Each case also asserts the status-strip query shows the same state word, proving header and strip never disagree. Keep the rollover banner, toast, poll loop, and chart props untouched; keep D-07 script work out of scope.</action>
  <verify>
    <automated>npx vitest run src/terminal-shell.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Header shows freshness in all three states matching the strip, proven by render tests.</done>
  <acceptance_criteria>chart-freshness span renders beside CardTitle using deriveStatus plus formatStripAge against the 10s now tick; live case contains LIVE, stale case contains STALE, Baku Saturday case contains BAZAR BAGLIDIR; each case asserts the strip shows the same state word; npx vitest run src/terminal-shell.test.ts exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>D-07 verify-deploy.sh STALE-DATE-BANNER steps plus syntax proof</name>
  <files>scripts/verify-deploy.sh</files>
  <read_first>scripts/verify-deploy.sh, app/api/yahoo/route.ts, src/lib/freshness.ts</read_first>
  <reversibility rating="reversible">Appends three script steps only; removal is deleting the appended blocks with no app change.</reversibility>
  <action>Append three steps to scripts/verify-deploy.sh per D-07, after the existing PAGE block and before the final all-checks-passed echo, copying the ENVELOPE curl plus node fail-closed idiom with zero new dependencies and the pass plus fail helpers. Step STALE-502 asserts the 502 contract statically against the live API without killing any upstream: fetch the API response headers and assert the fresh path serves a cache-control value that is not no-store, then assert the route source at app/api/yahoo/route.ts lines 14 through 20 carries status 502 plus no-store plus Retry-After 60 plus the error with retryAfter 60 JSON shape by grepping the file for status 502, no-store, Retry-After with 60, and retryAfter, failing closed when any pattern is absent; never assert a live 502 body since prod serves fresh. Step DST-DATE reuses the ENVELOPE body file: parse every candle date with node asserting each matches YYYY-MM-DD digit pattern and the sequence is strictly ascending, then assert the page body contains a Baku rendering hook by grepping for data-slot session-line or BAZAR BAGLIDIR or LIVE or STALE; never assert a transition occurs today. Step ROLLOVER-BANNER fetches the page body and asserts the slot-hook contract by grepping for data-slot rollover-banner in the served HTML or, when the slot renders client-side only, grepping the built page JavaScript for the same slot string; assert path presence of the slot contract, never live banner visibility. Each step fails closed with fail naming the step, passes with pass naming the step, and keeps exit 1 on first failure plus exit 2 on bad usage. Run bash minus n syntax check as the proof gate. Record the live-URL run as a manual step in the task summary since no live URL is reachable from the executor.</action>
  <verify>
    <automated>bash -n scripts/verify-deploy.sh</automated>
    <fails_when>non-zero exit, or any syntax error line in output</fails_when>
  </verify>
  <done>Three contract steps land with syntax clean and date-independent assertions.</done>
  <acceptance_criteria>verify-deploy.sh contains STALE-502 plus DST-DATE plus ROLLOVER-BANNER blocks after PAGE using curl plus node plus pass plus fail with no new dependency; STALE-502 greps route.ts for 502 plus no-store plus Retry-After 60 plus retryAfter; DST-DATE asserts YYYY-MM-DD plus ascending plus Baku hook; ROLLOVER-BANNER greps for data-slot rollover-banner; no step asserts a live event occurs today; bash -n scripts/verify-deploy.sh exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Pre-verify gates green across the wave</name>
  <files>components/dashboard/terminal-shell.tsx, scripts/verify-deploy.sh</files>
  <read_first>package.json, vitest.config.ts, components/dashboard/terminal-shell.tsx</read_first>
  <reversibility rating="reversible">Runs gates only; source edits only for regressions this phase introduced.</reversibility>
  <action>Run the repo gate sequence with no source edits unless a gate names a regression this phase introduced. Run lint first, then the full test suite against the 120-green baseline plus the new levels, store-fix, header, and rewritten range cases, then the production build, then the typecheck, then the bash syntax check. On any gate failure, fix only files this phase owns (chart-mapper, nq-chart, terminal-shell, store, range plus tests, verify-deploy.sh) and re-run the failing gate plus npx vitest run src/terminal-shell.test.ts and npx vitest run src/lib/store.test.ts src/lib/ict/range.test.ts before proceeding. Install no packages. This task runs after the wave-1 render and store plans merge, so it covers the combined tree.</action>
  <verify>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any error or warning line in output</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any error or Failed to compile line in output</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Gates plus build plus typecheck are green on the combined wave tree.</done>
  <acceptance_criteria>npm run lint exits 0; npm test exits 0 with zero failed suites and passed count at or above the 120 baseline plus new cases; npm run build exits 0; npx tsc --noEmit exits 0; bash -n scripts/verify-deploy.sh exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated envelope to header copy | Store state crosses into header text here; only deriveStatus state plus formatStripAge copy render |
| Live URL to deploy script | Untrusted HTTP responses cross into pass and fail scoring here; malformed bodies fail closed |
| Route source to script grep | Repo source crosses into the static contract assertion here; the script never touches prod state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-07 | Spoofing | Upstream echo into header text (ASVS V5) | medium | mitigate | Header renders only formatStripAge deterministic Azerbaijani copy from deriveStatus state; no upstream string, timestamp, or hint interpolates into the span; three-state tests assert LIVE, STALE, and BAZAR BAGLIDIR copy |
| T-03.2-08 | Tampering | Header reimplementing staleness and disagreeing with the strip | low | mitigate | Header reuses the shell 10s now tick plus deriveStatus plus formatStripAge exactly like StatusStrip; each render case asserts header and strip show the same state word |
| T-03.2-09 | Denial of Service | Flaky deploy steps red on lucky dates or killing prod | low | mitigate | DST-DATE asserts date shape plus ordering plus render hook, never a transition today; ROLLOVER-BANNER asserts the slot path, never live visibility; STALE-502 asserts the static 502 contract and never kills an upstream |
| T-03.2-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: next plus react plus vitest plus jsdom are already lockfile-pinned; no new package is added so no blocking-human checkpoint is owed |
</threat_model>

<verification>
Shell suite green with three-state header copy matching the strip, script syntax clean with three contract steps landed, full gates plus build plus typecheck green on the combined tree.
</verification>

<success_criteria>
DATA-03 reads true on the header itself and the deploy script checks the stale, date, and banner contracts on every run regardless of calendar luck.
</success_criteria>

<output>
Create `.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- components/dashboard/terminal-shell.tsx: chart-freshness span beside the CHART CardTitle reusing the 10s now tick plus deriveStatus plus formatStripAge with strip-mirroring variants.
- src/terminal-shell.test.ts: new W2 render cases named header-freshness-live, header-freshness-stale, header-freshness-closed.
- scripts/verify-deploy.sh: appended STALE-502 plus DST-DATE plus ROLLOVER-BANNER steps in curl plus node fail-closed idiom.
- Symbols created: none. No new files. No new dependencies.

## Source coverage audit

- GOAL (W2 header copy, D-07 script edge): covered by the two build tasks plus the gates task.
- REQ DATA-03 (freshness on the chart header): W2 covered by task 1. REQ DEPLOY-01 (deploy verification edge): D-07 covered by task 2. REQ STATE-02 (Baku date honesty): DST-DATE contract covered by task 2.
- RESEARCH (deriveStatus plus formatStripAge reuse with 10s tick, script zero-dep idiom with fail-closed node asserts, pitfalls 4 hydration and 5 flaky-date): each maps to task action constraints; pitfall 4 addressed by now-tick reuse, pitfall 5 by path-contract form.
- CONTEXT (D-06 freshness copy with LIVE age, STALE, CLOSED in Azerbaijani display copy; D-07 stale and 502-drill plus Baku DST-date plus rollover-banner steps as path-contract assertions never live-event assertions): D-06 and D-07 implemented exactly; compact-versus-full format discretion locked as full formatStripAge reuse.
- Exclusions are not gaps: ICT-02 render, W1, D-04, D-08, and D-01 retro-gate are owned by other plans in this phase, named explicitly in the task scope guards.
