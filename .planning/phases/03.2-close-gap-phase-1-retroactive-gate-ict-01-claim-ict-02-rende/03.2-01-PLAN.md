---
phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chart-mapper.ts
  - src/lib/chart-mapper.test.ts
  - components/charts/nq-chart.tsx
  - components/dashboard/terminal-shell.tsx
  - src/terminal-shell.test.ts
autonomous: true
requirements: [ICT-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - User sees quadrant (0.25/0.75) plus OTE-pocket levels as thin dashed price-lines on the NQ chart alongside the existing EQ and DOL lines
    - Levels derive from the existing selectLevels selector output with no math duplicated in components and no new packages
    - Null levels render no new lines and never block the chart, and the full suite stays green
  artifacts:
    - src/lib/chart-mapper.ts
    - components/charts/nq-chart.tsx
    - components/dashboard/terminal-shell.tsx
    - src/terminal-shell.test.ts
  key_links:
    - selectLevels output flows through the shell subscription into NqChart props so computed levels cannot be silently absorbed
    - levelLineInputs finite-guard plus mid-derivation is the single derivation point the chart calls, mirroring priceLineInputs
    - Shell stub-prop tests assert the new props on the levels fixture and their absence on null, replacing canvas inspection
---

<objective>
Render the orphaned quadrant/OTE levels on the chart (per D-02, D-03), closing ICT-02's user-visible half: a levelLineInputs mapper helper, a selectLevels subscription with prop pass-through in the shell, and four EQ-style thin dashed price-lines in NqChart with happy-path plus edge proof.

Purpose: computeLevels is tested but selectLevels has zero consumers, so the requirement sentence user sees quadrant/OTE levels is unwired; this slice wires it end to end.
Output: mapper helper plus unit tests, four new price-lines with mount/update/remove lifecycle, shell subscription plus pass-through, shell render cases.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-RESEARCH.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-VALIDATION.md
@src/lib/store.ts
@src/lib/ict/levels.ts
@src/lib/chart-mapper.ts
@components/charts/nq-chart.tsx
@components/dashboard/terminal-shell.tsx
@src/terminal-shell.test.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end levels render slice — mapper helper plus subscription plus four price-lines plus happy-path proof</name>
  <files>src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts, components/charts/nq-chart.tsx, components/dashboard/terminal-shell.tsx, src/terminal-shell.test.ts</files>
  <read_first>src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts, src/lib/ict/levels.ts, src/lib/store.ts, components/charts/nq-chart.tsx, components/dashboard/terminal-shell.tsx, src/terminal-shell.test.ts, .planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md, AGENTS.md</read_first>
  <reversibility rating="reversible">Adds one pure helper plus four price-line refs plus one subscription line plus tests; removal is deleting the helper, the refs, the subscription, and the cases with no migration.</reversibility>
  <action>Extend src/lib/chart-mapper.ts per D-02 with an exported LevelLineInputs interface holding q1, q3, oteBull, oteBear numbers plus an exported levelLineInputs function taking a LevelsOutput imported as a type from src/lib/ict/levels, finite-guarding q1, q3, bullOTE.lo, bullOTE.hi, bearOTE.lo, bearOTE.hi with the existing isFiniteNumber guard and throwing an Error naming levelLineInputs on any non-finite input, then returning q1 and q3 unchanged plus oteBull as (bullOTE.lo plus bullOTE.hi) divided by 2 plus oteBear as (bearOTE.lo plus bearOTE.hi) divided by 2. This four-line set (Q1, Q3, both OTE pocket mids) is the planner discretion lock on line count per D-03, keeping DOL solid visually distinct from dashed OTE lines. Add unit cases in src/lib/chart-mapper.test.ts reusing the existing import plus toEqual plus toThrow style: one case asserting mids equal (lo plus hi) divided by 2 on a fixed LevelsOutput, one case asserting throw on a NaN bound. Extend the NqChartProps interface in components/charts/nq-chart.tsx with an optional levels field typed as LevelsOutput or null imported as a type from src/lib/ict/levels. Add four refs q1LineRef, q3LineRef, oteBullLineRef, oteBearLineRef shaped as useRef unknown null beside eqLineRef and dolLineRef, extend propsRef and its sync effect with the levels field, extend the mount effect to create four lines after the DOL creation using levelLineInputs applied to props.levels when non-null with color accent, lineWidth 1, async-imported LineStyle.Dashed, and titles Q1 0.25, Q3 0.75, OTE-B 0.62-0.79, OTE-S 0.62-0.79, extend the update effect with remove-before-create for the four refs keyed on the same inputs and null them in the unmount cleanup beside the existing refs, and extend the update effect dependency array with levels. Canvas price-lines cannot carry data-slot hooks under jsdom, so testability rests on the shell stub-prop capture below plus the mapper unit tests; add no DOM overlay elements for the lines. Wire components/dashboard/terminal-shell.tsx per D-02 by adding a levels subscription on the line after the selectDOL subscription using useDashboard with useShallow applied to s.selectLevels, and pass levels into the existing NqChart call site alongside candles, rangeHigh, rangeLow, eq, dolPrice, dolName, status, forming with no change to the range non-null and dol non-null render gate and no change to deriveStatus, chartStatus, toast, banner, or poll logic. Prove the slice with two render cases in src/terminal-shell.test.ts reusing the installDom plus renderShell plus beforeEach reset harness and the 20-candle forming-free fixtureCandles shape: one case seeding the live envelope asserting chartCapture.props.levels deep-equals useDashboard getState selectLevels output with finite q1, q3, bullOTE, bearOTE, one case asserting the same props flow on the gapped rollover envelope while the rollover-banner query stays non-null. Use forming-free fixtures only so the parallel W1 closed-basis fix cannot shift these expectations. Keep the use client pragma, keep the nextjs-agent-rules block intact, install no packages.</action>
  <verify>
    <automated>npx vitest run src/lib/chart-mapper.test.ts src/lib/ict/levels.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npx vitest run src/terminal-shell.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>The single happy path works end-to-end and is committed.</done>
  <acceptance_criteria>src/lib/chart-mapper.ts exports levelLineInputs plus LevelLineInputs; mid case asserts oteBull equals (lo plus hi) divided by 2 and NaN bound throws; NqChartProps carries optional levels typed LevelsOutput or null; nq-chart.tsx creates four Dashed lineWidth 1 lines titled Q1 0.25, Q3 0.75, OTE-B 0.62-0.79, OTE-S 0.62-0.79 with remove-before-create plus cleanup nulling; terminal-shell.tsx subscribes selectLevels via useShallow and passes levels to NqChart; shell render cases assert stub levels prop deep-equals selector output on both clean and gapped envelopes; both automated commands exit 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Levels edge expansion — null honesty plus lifecycle lock</name>
  <files>src/terminal-shell.test.ts, components/charts/nq-chart.tsx, src/lib/chart-mapper.test.ts</files>
  <read_first>src/terminal-shell.test.ts, components/charts/nq-chart.tsx, src/lib/chart-mapper.test.ts, src/lib/ict/levels.ts</read_first>
  <reversibility rating="reversible">Adds test cases plus null guards only; removal is deleting the cases and guards with no production migration.</reversibility>
  <action>Extend src/terminal-shell.test.ts with two honesty cases reusing the harness from the tracer task. Add a null-levels case seeding an empty-candle envelope asserting the chart stub is absent and no levels-bearing render occurs, proving null levels never create lines and never block on throw. Add a thin-history case with about 5 closed candles asserting the shell renders without throw and the stub levels prop is either null or a finite LevelsOutput matching the selector. In components/charts/nq-chart.tsx harden both the mount and update price-line blocks so the four level lines are created only when levels is non-null and levelLineInputs succeeds, with stale refs removed before any recreate and no change to the EQ dashed plus DOL solid precedent. Add one mapper case asserting levelLineInputs throws when bullOTE.hi is positive Infinity. Change no ICT math, change no selector, change no store refresh behavior, keep W1 plus D-04 plus D-08 plus W2 plus verify-deploy work out of scope.</action>
  <verify>
    <automated>npx vitest run src/lib/chart-mapper.test.ts src/lib/ict/levels.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npx vitest run src/terminal-shell.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Null plus thin-history plus non-finite edge cases are green with the EQ and DOL line behavior unchanged.</done>
  <acceptance_criteria>Empty-candle envelope renders no chart stub and throws nothing; thin-history envelope renders without throw with stub levels prop null or finite matching selector; Infinity OTE bound throws naming levelLineInputs; level lines are null-guarded in both mount and update blocks; both automated commands exit 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated envelope to selectLevels | Store state crosses into level derivation here; only validated candles plus closed-basis close enter |
| levelLineInputs to price-lines | Derived numbers cross into canvas primitives here; only finite-guarded mids plus fixed title strings render |
| Selector output to shell props | Levels object crosses into the client component here; the stub-prop tests pin the mapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-01 | Tampering | Upstream payload echo into line prices or titles (ASVS V5) | medium | mitigate | Lines render only finite numbers passing the levelLineInputs isFiniteNumber guard plus the four fixed title strings Q1 0.25, Q3 0.75, OTE-B 0.62-0.79, OTE-S 0.62-0.79; titles never interpolate upstream text; mapper throw cases in both tasks lock the guard |
| T-03.2-02 | Repudiation | Silent absorb of computed levels (honesty) | medium | mitigate | Shell subscription reads selectLevels directly and passes it to NqChart; tracer asserts stub levels prop deep-equals selector output on clean and gapped envelopes; edge task asserts null honesty without blocking |
| T-03.2-03 | Denial of Service | Chart throw on null or non-finite levels | low | mitigate | NqChart creates level lines only when levels is non-null and levelLineInputs succeeds; empty plus thin-history render cases prove no throw |
| T-03.2-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: zustand plus lightweight-charts plus vitest plus jsdom are already lockfile-pinned; no new package is added so no blocking-human checkpoint is owed |
</threat_model>

<verification>
Mapper plus levels suites green, shell suite green with levels prop asserted on clean and gapped envelopes plus null honesty on empty and thin histories, EQ dashed plus DOL solid behavior unchanged, no new dependencies.
</verification>

<success_criteria>
ICT-02 render half exists: a levels envelope produces four thin dashed quadrant/OTE price-lines through the proven selector path, and null levels hide the lines without errors.
</success_criteria>

<output>
Create `.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/chart-mapper.ts: LevelLineInputs interface (q1, q3, oteBull, oteBear) plus levelLineInputs(levels) finite-guard plus OTE-mid derivation.
- src/lib/chart-mapper.test.ts: new levelLineInputs cases named mids-derive, nan-bound-throws, infinite-bound-throws.
- components/charts/nq-chart.tsx: optional levels prop (LevelsOutput or null) plus q1LineRef, q3LineRef, oteBullLineRef, oteBearLineRef with mount plus update remove-before-create lifecycle and cleanup nulling.
- components/dashboard/terminal-shell.tsx: levels subscription via useShallow selectLevels plus levels pass-through to NqChart.
- src/terminal-shell.test.ts: new ICT-02 render cases named levels-prop-clean, levels-prop-gapped-co-render, levels-null-empty, levels-thin-honest.
- Symbols created: levelLineInputs, LevelLineInputs. No new files. No new dependencies.

## Source coverage audit

- GOAL (quadrant/OTE visible on chart, ICT-02 satisfied): covered by tracer slice plus edge expansion.
- REQ ICT-02 (user sees EQ plus quadrant/OTE levels): render consumer covered here; math half already proven by levels suite and cited by the retro-gate plan.
- RESEARCH (selectLevels consumer pattern, D-03 dashed price-line form with 4-line cap, STATE-01 no-math-in-components, data-slot precedent adapted to stub-prop capture under jsdom): each maps to tracer action constraints; pitfalls 2 line-proliferation and 5 flaky-date steps addressed by the 4-mid lock and forming-free fixtures.
- CONTEXT (D-02 render consumer per D-02, D-03 EQ-style dashed lines with shaded-zone variant rejected, line count and styling under Claude's Discretion): D-02 and D-03 implemented exactly; 4-mid set plus titles recorded as discretion locks above.
- Exclusions are not gaps: W1, D-04, D-08, W2, D-07, D-01 retro-gate are owned by later plans in this phase, named explicitly in the task scope guards.
