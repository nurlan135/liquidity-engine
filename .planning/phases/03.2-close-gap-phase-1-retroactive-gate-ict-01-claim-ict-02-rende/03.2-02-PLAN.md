---
phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/store.ts
  - src/lib/store.test.ts
  - src/lib/ict/range.ts
  - src/lib/ict/range.test.ts
autonomous: true
requirements: [DATA-02, ICT-01, ICT-03, ICT-04, ICT-05, STATE-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Forming candle no longer skews last close, so position, bias, DOL, and levels share the closed-only basis with range and regime
    - Refresh failure marks the envelope stale immediately while preserving last-known candles, and the next success restores envelope truth
    - checkReanchor is gone and the rule-table suite pins the rolling-recompute behavior that actually carries re-anchoring, with the full suite green
  artifacts:
    - src/lib/store.ts
    - src/lib/store.test.ts
    - src/lib/ict/range.ts
    - src/lib/ict/range.test.ts
  key_links:
    - selectLastClose closedOnly basis feeds selectPosition, selectBias, selectDOL, and selectLevels identically
    - Refresh catch writes stale true without touching candles or lastUpdatedISO, and the success path restores stale from the envelope
    - range.test.ts no longer imports checkReanchor, so deletion cannot regress silently
---

<objective>
Apply the two one-line store fixes (per D-05 closed-only last close with Phase 1 D-04 locked direction, per D-04 immediate STALE on refresh failure) plus the D-08 dead-code decision (delete checkReanchor, rewrite the rule-table against rolling recompute), with divergence, latch, and rewritten rule-table proof.

Purpose: the forming close skews bias and DOL intraday while range and regime exclude it; refresh failure leaves stale false so the badge lags the already-immediate toast; the explicit close-break detector has zero callers outside tests while rolling ANCHOR_WINDOW recompute carries the behavior.
Output: fixed selectors, deleted export, rewritten tests, all suites green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-RESEARCH.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md
@.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-VALIDATION.md
@src/lib/store.ts
@src/lib/ict/range.ts
@src/lib/ict/types.ts
@src/lib/freshness.ts
@src/lib/store.test.ts
@src/lib/ict/range.test.ts
</context>

<tasks>

<task type="auto">
  <name>W1 closed-only last close plus divergence proof</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/ict/types.ts, src/lib/store.test.ts, src/lib/ict/range.ts, .planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-PATTERNS.md, AGENTS.md</read_first>
  <reversibility rating="reversible">One selector basis change plus added test cases; reversal is restoring the direct last-row read and deleting the cases.</reversibility>
  <action>Fix selectLastClose in src/lib/store.ts per D-05 following the Phase 1 D-04 closed-only direction. Replace the body that reads candles at candles.length minus 1 without filtering with a body that applies the already-imported closedOnly predicate from src/lib/ict/types to candles, returns null when the filtered array is empty, and otherwise returns the close of the last filtered row. Add no new import since closedOnly is already imported at the top of store.ts. Touch no other selector; selectPosition, selectBias, selectDOL, and selectLevels inherit the corrected basis automatically through their existing selectLastClose calls. Extend src/lib/store.test.ts with one divergence case reusing the fixtureCandles plus mockEnvelope harness: seed 21 closed candles with last closed row 2026-01-21 close 20205 plus a forming last row 2026-01-22 with close overridden to the far value 21000, then assert selectLastClose returns exactly 20205, assert selectPosition is close to computePosition applied to the selected range and 20205, assert stored candles still retain the forming row, and assert selectRange high still excludes the forming high. Run the full suite after the change and re-pin only expectations that encode the old forming-close skew; the existing derives-range, excludes-forming, and keeps-last-known cases use closed-filtered or range-only assertions and must pass unchanged. Keep the Yahoo parser, the refresh paths, deriveStatus, and chartStatus untouched; keep D-04, D-08, W2, and verify-deploy work out of scope.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts src/lib/ict/levels.test.ts src/lib/ict/bias.test.ts src/lib/ict/dol.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Last close reads the closed basis and the divergence case pins it with ripple suites green.</done>
  <acceptance_criteria>selectLastClose on the far-forming fixture returns 20205 not 21000; selectPosition matches computePosition of range and 20205; forming row retained in stored candles; range high excludes forming high; npx vitest run src/lib/store.test.ts src/lib/ict/levels.test.ts src/lib/ict/bias.test.ts src/lib/ict/dol.test.ts exits 0 with no existing case re-pinned except skew-encoding ones</acceptance_criteria>
</task>

<task type="auto">
  <name>D-04 immediate STALE on refresh failure plus latch proof</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/freshness.ts, src/lib/store.test.ts, components/dashboard/status-strip.tsx</read_first>
  <reversibility rating="reversible">One flag added to the catch-path set plus added test cases; reversal is removing the flag and the cases.</reversibility>
  <action>Fix the refresh catch path in src/lib/store.ts per D-04. Add stale true to the catch set alongside lastError and inFlight false, leaving candles, contractHint, lastUpdatedISO, source, and asOfBaku untouched so last-known data age stays visible, and leave the success path writing stale from json.stale exactly as is so recovery restores envelope truth automatically. Do not null lastUpdatedISO in the catch. Do not touch STALE_AFTER_SEC, deriveStatus weekend-wins or age semantics, the toast path, or the poll loop; D-04 kills the lag only for the refresh-failure path. Extend src/lib/store.test.ts with two latch cases reusing the mockEnvelope harness: a failure-sets case that refreshes successfully then fails fetch with network down and asserts stale true plus lastError network down plus candles preserved plus inFlight false, and a success-clears case that fails once then refreshes successfully and asserts stale false restored with lastError null. The pre-existing keeps-last-known case must pass unchanged since it asserts lastError without pinning stale. Toast behavior is unchanged and needs no new case.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Refresh failure marks stale immediately with candles preserved, and success restores envelope truth.</done>
  <acceptance_criteria>Failed fetch asserts stale true plus lastError plus preserved candles plus inFlight false; next success asserts stale false plus lastError null; lastUpdatedISO untouched by the catch; STALE_AFTER_SEC and deriveStatus unchanged; npx vitest run src/lib/store.test.ts exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>D-08 delete checkReanchor plus rule-table rewrite on rolling recompute</name>
  <files>src/lib/ict/range.ts, src/lib/ict/range.test.ts</files>
  <read_first>src/lib/ict/range.ts, src/lib/ict/range.test.ts, src/lib/ict/types.ts</read_first>
  <reversibility rating="costly">Deletes a public export other code could import; restoration is re-adding the function plus the old import, but no caller outside tests exists so the cost is documentation churn only.</reversibility>
  <action>Resolve D-08 per the researcher recommendation adopted as planner decision: DELETE the checkReanchor export from src/lib/ict/range.ts, keeping ANCHOR_WINDOW, computeRange, and computePosition byte-identical, and rewrite src/lib/ict/range.test.ts in the same commit so the suite never sits red. Remove checkReanchor from the test import, leaving ANCHOR_WINDOW and computeRange. Rewrite the five rule-table cases against the rolling-recompute behavior that actually carries re-anchoring, since computeRange takes extremes over the trailing window rows including wicks: strict close beyond either extreme extends the recomputed range to include the breakout candle with asOf equal to the breakout date; wick pierce with close inside ALSO extends the recomputed high or low to the wick extreme, documented in a comment as the rolling-extremes semantic that supersedes the old close-only detector; close exactly equal to either extreme leaves extremes unchanged; repeated evaluation on identical input returns identical ranges; forming rows never affect the recomputed range. Each rewritten case must call computeRange on windowed candle arrays, never the deleted function, and every comment naming the old close-only expectation must be replaced with rolling-recompute language. Run the range suite plus lint plus typecheck; lint must show zero unused-import warnings and tsc must pass with no lingering checkReanchor reference anywhere outside planning docs. Keep W1, D-04, W2, and verify-deploy work out of scope.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/range.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>Dead export deleted with the rule-table rewritten on true behavior and the full suite green.</done>
  <acceptance_criteria>src/lib/ict/range.ts exports only ANCHOR_WINDOW plus computeRange plus computePosition; range.test.ts imports only ANCHOR_WINDOW plus computeRange; close-break extends, wick pierce extends with supersede comment, equality holds, forming excluded, idempotent holds; npm run lint shows zero unused warnings for range files; npx tsc --noEmit exits 0; npm test exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated envelope to selectLastClose | Stored candles cross into close derivation here; the closedOnly filter is the single forming-exclusion predicate |
| Refresh failure to stale flag | Error path crosses into freshness truth here; only the stale boolean plus lastError message are written |
| Rolling window to range extremes | Closed rows cross into high, low, and eq here; forming rows never enter and no adjusted-price read exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-04 | Tampering | Forming-close skew of bias and DOL intraday (ASVS V5) | medium | mitigate | selectLastClose filters through closedOnly shared with range and regime; divergence case pins 20205 over far forming close 21000 with position following the closed basis |
| T-03.2-05 | Tampering | STALE flag latched forever or last-known data wiped (availability) | medium | mitigate | Catch writes only stale true plus lastError plus inFlight false with candles and lastUpdatedISO preserved; success path restores stale from the envelope; failure-sets plus success-clears cases lock both directions |
| T-03.2-06 | Repudiation | Re-anchor behavior unpinned after detector deletion (honesty) | low | mitigate | Rule-table rewritten against rolling recompute in the same commit with wick-extends semantics documented, so the suite pins true behavior instead of a dead function |
| T-03.2-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: zustand plus vitest plus jsdom are already lockfile-pinned; no new package is added so no blocking-human checkpoint is owed |
</threat_model>

<verification>
Store suite green with divergence plus latch cases, range suite green on rewritten rolling-recompute cases, bias plus DOL plus levels ripple suites green, full npm test green, lint clean, typecheck clean.
</verification>

<success_criteria>
Last close, staleness, and re-anchor behavior are honest: closed basis everywhere, immediate STALE with preserved candles and clean recovery, and no dead detector with tests pinning the true recompute semantics.
</success_criteria>

<output>
Create `.planning/phases/03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende/03.2-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/store.ts: selectLastClose closedOnly basis (per D-05, Phase 1 D-04 direction) plus refresh catch writing stale true (per D-04); success path and toast path unchanged.
- src/lib/store.test.ts: new cases named lastclose-closed-basis-divergence, refresh-failure-sets-stale, refresh-success-clears-stale.
- src/lib/ict/range.ts: checkReanchor export deleted (per D-08 delete decision); ANCHOR_WINDOW, computeRange, computePosition unchanged.
- src/lib/ict/range.test.ts: rule-table rewritten against rolling recompute named close-break-extends, wick-pierce-extends-rolling-extremes, equality-holds, idempotent-recompute, forming-excluded.
- Symbols created: none. Symbols removed: checkReanchor. No new files. No new dependencies.

## Source coverage audit

- GOAL (W1 skew fixed, F2 badge immediate, dead code decided): covered by the three tasks in order.
- REQ DATA-02 (proxy survival honesty): D-04 latch covered by task 2; full resilience math already proven and cited by the retro-gate plan. REQ ICT-01 plus ICT-04 plus ICT-05 (closed-basis math): W1 covered by task 1. REQ ICT-03 (re-anchor): D-08 delete plus rewrite covered by task 3. REQ STATE-02 (stale honesty): D-04 covered by task 2.
- RESEARCH (closedOnly reuse, catch shape preserving last-known, delete-plus-rewrite same-commit rule, pitfalls 1 ripple and 3 latch and 6 import coupling): each maps to task action constraints; pitfall 4 clock rules untouched.
- CONTEXT (D-04 immediate STALE with toast unchanged and no 120s ramp, D-05 closed-only basis with Phase 1 D-04 locked direction, D-08 delete-or-keep under planner discretion with rolling recompute as carrier): D-04 and D-05 implemented exactly; D-08 delete locked and recorded above.
- Exclusions are not gaps: ICT-02 render, W2 header copy, D-07 script steps, and D-01 retro-gate are owned by other plans in this phase, named explicitly in the task scope guards.
