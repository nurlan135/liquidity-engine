---
phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende
plan: 03
subsystem: ui
tags: [freshness, deriveStatus, formatStripAge, Azerbaijani-copy, verify-deploy, vitest, jsdom]

# Dependency graph
requires:
  - phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende
    provides: selectLevels render half of ICT-02 plus immediate-STALE latch (03.2-01, 03.2-02 merged work)
provides:
  - chart-freshness span beside the CHART CardTitle reusing the 10s now tick plus deriveStatus plus formatStripAge with strip-mirroring variants
  - Header three-state render proof (header-freshness-live/stale/closed) asserting header-strip agreement
  - STALE-502 plus DST-DATE plus ROLLOVER-BANNER contract steps in verify-deploy.sh with bash -n clean
affects: [03.2-04 retro-gate, verify-work]

# Actuals (#2632) — pairs with the plan's `estimate` to calibrate future estimates.
actuals:
  tokens: 6250
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns: [header derives from the same envelope truth as the strip, static-contract deploy assertions that never touch prod state]

key-files:
  created: []
  modified:
    - components/dashboard/terminal-shell.tsx
    - src/terminal-shell.test.ts
    - scripts/verify-deploy.sh

key-decisions:
  - "Header freshness derives against the shared 10s now tick with a locally derived freshness value, keeping a single interval"
  - "Closed-copy assertion uses an NFD ASCII-fold helper so BAZAR BAGLIDIR stays encoding-stable against the locked BAGLIDIR copy"
  - "npm run build cannot run in this worktree (node_modules absent); typecheck plus lint plus full suite stand as the gate evidence"

patterns-established:
  - "Header copy reuses deriveStatus plus formatStripAge exactly like StatusStrip, with the strip's own loading and stale fallback copy"
  - "Deploy steps assert contracts (static 502 shape, date shape plus ordering plus render hook, slot-hook path) never live events"

requirements-completed: [DATA-03, DEPLOY-01, STATE-02]

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Chart header shows LIVE/STALE/BAZAR BAGLIDIR copy derived from the same envelope truth as the strip, in Azerbaijani"
    requirement: "DATA-03"
    verification:
      - kind: integration
        ref: "src/terminal-shell.test.ts#header-freshness-live plus header-freshness-stale plus header-freshness-closed"
        status: pass
    human_judgment: false
  - id: D2
    description: "verify-deploy.sh asserts the stale/502 contract, the Baku date contract, and the rollover-banner slot contract as date-independent path checks with syntax clean"
    requirement: "DEPLOY-01"
    verification:
      - kind: other
        ref: "bash -n scripts/verify-deploy.sh"
        status: pass
    human_judgment: false
  - id: D3
    description: "Header copy disagrees with the strip never, and the script stays zero-dependency curl plus node"
    requirement: "STATE-02"
    verification:
      - kind: integration
        ref: "src/terminal-shell.test.ts#header-freshness-live plus header-freshness-stale plus header-freshness-closed"
        status: pass
    human_judgment: false

# Metrics
duration: 35min
completed: 2026-09-06
status: complete
plan_head_before: 7b2c5968ee21c89c66ba8687cbe4dc78b08ac4f1
commits: 2
---

# Phase 03.2 Plan 03: Header Freshness plus Deploy Contract Steps Summary

**Chart header carries the strip's own freshness truth (LIVE age, STALE age, BAZAR BAGLIDIR) via deriveStatus plus formatStripAge, and verify-deploy.sh checks the stale/502, Baku-date, and banner-slot contracts on every run regardless of calendar luck.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-06T13:00:18Z
- **Completed:** 2026-09-06T13:35:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- `data-slot="chart-freshness"` span beside the CHART CardTitle renders `formatStripAge(deriveStatus({ stale, lastUpdatedISO }, now))` against the existing 10s now tick — no second interval, no bespoke staleness math, strip-mirroring live-dot treatment and loading/stale fallbacks, Azerbaijani copy only
- Three header render cases prove header-strip agreement in every state: fresh envelope shows LIVE, stale-flagged envelope shows STALE, Baku Saturday noon shows BAZAR BAGLIDIR (ASCII-folded for encoding stability)
- verify-deploy.sh gains three zero-dependency steps after PAGE: STALE-502 (static 502 shape grep against route source plus fresh-path non-no-store headers, never a live upstream kill), DST-DATE (YYYY-MM-DD shape plus strictly ascending order from the ENVELOPE body plus a Baku render hook, never a transition today), ROLLOVER-BANNER (slot-hook contract via served HTML, built chunks, or repo source fallback, never live visibility)
- Gates: lint 0, full suite 20 files 133/133 green, `npx tsc --noEmit` 0, `bash -n` 0, targeted suites 35/35 (shell, store, range)

## Task Commits

Each task was committed atomically:

1. **Task 1: W2 chart-header freshness copy plus three-state proof** - `ef1dbd6` (feat)
2. **Task 2: D-07 verify-deploy.sh STALE-DATE-BANNER steps plus syntax proof** - `c33dbd4` (feat)
3. **Task 3: Pre-verify gates green across the wave** - no code commit (gates-only task; evidence recorded here)

**Plan metadata:** pending (docs commit below)

## Files Created/Modified

- `components/dashboard/terminal-shell.tsx` - formatStripAge import, headerCopy/headerFreshness derivation from the shared now tick, chart-freshness span with live-dot beside CardTitle
- `src/terminal-shell.test.ts` - W2 describe block: header-freshness-live, header-freshness-stale, header-freshness-closed, each asserting header and strip show the same state word
- `scripts/verify-deploy.sh` - STALE-502, DST-DATE, ROLLOVER-BANNER steps in the curl plus node fail-closed idiom with pass/fail helpers

## Decisions Made

- Pinned the LIVE/STALE render cases to Baku Friday noon via fake timers: deriveStatus weekend-wins on Baku Sat/Sun, so unstamped "now" envelopes coincidentally render CLOSED on a real Saturday. The pin keeps the cases date-independent while the closed case keeps its own Saturday pin.
- Closed-copy assertion uses an NFD ASCII-fold helper (`normalize('NFD')` stripping U+0300-U+036F) instead of asserting the dotted capital I form directly, per the plan's encoding-stable requirement; the implementation renders the locked `BAZAR BAĞLIDIR — son bağlanış şamları` copy verbatim.
- Task 3 ran the full gate sequence with no source edits: no regression named this phase's files, so per the plan's reversibility guard nothing was changed. `npm run build` cannot execute in this worktree (node_modules absent, `next/package.json` unresolvable); lint plus full suite plus typecheck plus bash syntax stand as the gate evidence, and the build gate stays open for the orchestrator's merged-tree run.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fast-forwarded the worktree to the intended base before starting**
- **Found during:** Task 1 setup (before any edit)
- **Issue:** The worktree HEAD (3a3a1cb) predated the merged 03.2-01/02 work; local main (7b2c596) carried it. Starting on the stale base would have re-applied merged changes as conflicts.
- **Fix:** Verified a clean tree (`git status --short` empty) and ran `git merge --ff-only main`, landing exactly on the prompt's expected base 7b2c596 with the merged levels-render and store-fix work intact. Same recovery the 03.2-01 executor used.
- **Files modified:** none (history pointer only)
- **Verification:** `git rev-parse HEAD` equals 7b2c596; `src/terminal-shell.test.ts` with the levels harness present on disk
- **Committed in:** no commit (fast-forward, no content change)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Base reconciliation only — no scope change, no merged-work modification, no extra production code.

## Issues Encountered

- First run of the three new header cases failed 3/3: `new Date().toISOString()` envelopes on a real Saturday derive CLOSED via weekend-wins, and the closed assertion used the ASCII form against the locked Ğ copy. Fixed with the Baku-Friday pin plus the ASCII-fold helper (see Decisions). Re-run: 16/16 green; full suite 133/133 green.
- `npm run build` fails in this worktree with `Could not find the Next.js package (next/package.json)` — node_modules is absent here (main-checkout install does not propagate into linked worktrees). Not a code regression: lint, the full vitest suite, and `tsc --noEmit` are all green. No package was installed per the plan's zero-install constraint; the production build must run on the merged tree where dependencies exist.
- Live-URL run of verify-deploy.sh is a manual step per the plan (no live URL reachable from the executor); recorded here. Static exercising done instead: all four 502 grep patterns PASS against route source, DST-DATE shape/order logic PASS on pass/fail-order/fail-shape fixtures, banner slot present once in terminal-shell source, `bash -n` clean.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| None | — | No new network endpoints, auth paths, file access, or schema changes. Header renders only deterministic formatStripAge copy from deriveStatus state (T-03.2-07); header reuses the shell tick plus deriveStatus plus formatStripAge with per-case header-strip agreement (T-03.2-08); DST-DATE asserts shape plus ordering plus hook, ROLLOVER-BANNER asserts the slot path, STALE-502 never kills an upstream (T-03.2-09); zero installs (T-03.2-SC). |

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 03.2-04 retro-gate can cite the header three-state proof as the DATA-03 evidence and the three script steps as the DEPLOY-01/STATE-02 contract evidence.
- Open gate: `npm run build` plus the live-URL `bash scripts/verify-deploy.sh <URL>` run, both requiring an environment with installed dependencies and a reachable deployment — flagged for the orchestrator's merged-tree verification.

## Self-Check: PASSED

- `components/dashboard/terminal-shell.tsx`, `src/terminal-shell.test.ts`, `scripts/verify-deploy.sh` all present on disk.
- Commits `ef1dbd6`, `c33dbd4` verified in `git log`.
- No stub patterns introduced (no TODO/FIXME/placeholder; header fallbacks reuse the strip's locked copy, never empty values).
- Full suite: 20 files, 133 tests, all passing. Lint clean. Typecheck clean. Bash syntax clean.

---
*Phase: 03.2-close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende*
*Completed: 2026-09-06*
