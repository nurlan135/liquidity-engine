---
phase: "03.2"
slug: "close-gap-phase-1-retroactive-gate-ict-01-claim-ict-02-rende"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-06"
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^5.0.0 |
| **Config file** | `vitest.config.ts` (`environment: 'node'`, `include: ['src/**/*.test.ts', 'app/**/*.test.ts']`, `testTimeout: 15000`) |
| **Quick run command** | `npx vitest run src/lib/store.test.ts src/lib/ict/levels.test.ts src/lib/ict/range.test.ts` |
| **Full suite command** | `npm test` (= `vitest run`; baseline 120/120 across 20 files per 03.1-VERIFICATION.md) + `npm run lint` + `npx tsc --noEmit` + `npm run build` (phase gate, 03.1 precedent) |
| **Estimated runtime** | ~30 seconds (unit suites) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <touched-area>` (quick run command above)
- **After every plan wave:** Run full `npm test` (must stay 120/120 + new cases green)
- **Before `/gsd-verify-work`:** Full suite green + `npm run lint` + `npx tsc --noEmit` + `npm run build`
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.2-0X-0X | retro-gate | 1 | DATA-01/02, ICT-01…06, STATE-02 | — | 01-01/01-02/01-03 suites green on current code; truth rows cite code+tests | unit (existing suites, re-run) | `npm test` | ✅ | ⬜ pending |
| 03.2-0X-0X | levels-render | 1 | ICT-02 | T-render-honesty | `selectLevels` fixture → NqChart receives q1/q3/OTE props; price-line create/remove called with dashed style + titles; null-levels → no new lines | unit + integration (jsdom, `data-slot` hooks) | `npx vitest run src/terminal-shell.test.ts` (new cases) | ❌ W0 | ⬜ pending |
| 03.2-0X-0X | fixes | 1 | ICT-01 (bias/DOL basis) | — | Forming-last-close fixture → `selectLastClose` returns prior closed close; position/bias/DOL follow closed basis | unit | `npx vitest run src/lib/store.test.ts` (new case) | ❌ W0 | ⬜ pending |
| 03.2-0X-0X | fixes | 1 | STATE-02 (stale honesty) | T-stale-latch | Failed `fetch` → `stale:true` + `lastError` set, candles preserved; next success → `stale:false` restored | unit | `npx vitest run src/lib/store.test.ts` (new cases) | ❌ W0 | ⬜ pending |
| 03.2-0X-0X | fixes | 1 | DATA-03 | — | Fresh/stale/closed fixtures → header `data-slot="chart-freshness"` shows LIVE-age / STALE / CLOSED copy | integration (jsdom) | `npx vitest run src/terminal-shell.test.ts` (new cases) | ❌ W0 | ⬜ pending |
| 03.2-0X-0X | fixes | 1 | DEPLOY-01 | — | Script extension: bash syntax + node-assert paths for stale/502, DST-date, banner steps | manual + `bash -n scripts/verify-deploy.sh` + live run | `bash scripts/verify-deploy.sh <URL>` | ❌ W0 | ⬜ pending |
| 03.2-0X-0X | fixes | 1 | ICT-04 (re-anchor) | — | `checkReanchor` removed; rewritten rule-table cases pin rolling-recompute behavior | unit | `npx vitest run src/lib/ict/range.test.ts` (rewritten) | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Source: 03.2-RESEARCH.md ## Validation Architecture (Phase Requirements → Test Map). Task IDs / plan / wave columns are planner-resolved at plan time.*

---

## Wave 0 Requirements

- [ ] `src/lib/store.test.ts` — W1 divergence case + D-04 stale-latch cases (failure sets, success clears, candles preserved)
- [ ] `src/terminal-shell.test.ts` — ICT-02 prop pass-through + `data-slot` line hooks + header-freshness copy cases (live/stale/closed)
- [ ] `src/lib/ict/range.test.ts` — D-08 rewrite (only if delete wins; otherwise keep-case documentation)
- [ ] `scripts/verify-deploy.sh` — D-07 steps land with `bash -n` clean; live-URL run is the manual gate
- [ ] Phase 1 `01-VERIFICATION.md` — docs artifact, no test file; depends on `npm test` green confirmation
- [ ] No framework/config gaps — vitest + jsdom + existing harnesses cover everything

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-07 live-URL run (stale/502, DST-date, banner steps against live/preview URL) | DEPLOY-01 | Script asserts path contracts, never live events; live run needs a real URL | `bash scripts/verify-deploy.sh <URL>` with preview/prod URL; record evidence |
| Quadrant/OTE line styling sanity (color/weight legibility) | ICT-02 | Visual judgment, not assertable | Open terminal, confirm lines legible against EQ/DOL language |

*If none beyond the above: "All other phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
