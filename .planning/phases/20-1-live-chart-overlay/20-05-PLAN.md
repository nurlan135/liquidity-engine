---
phase: 20-1-live-chart-overlay
plan: '05'
type: execute
wave: 2
depends_on:
- 20-04
files_modified:
- components/charts/nq-chart.tsx
- src/lib/chart-mapper.ts
- src/lib/chart-mapper.test.ts
- src/terminal-shell.test.ts
autonomous: true
gap_closure: true
requirements:
- CHRT-03
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Pool lines dim under stale and thin tiers with the uniform discipline, and null selector plus STAND_ASIDE leave zero pool lines on canvas
  artifacts:
  - components/charts/nq-chart.tsx
  key_links:
  - mount and update effects share one verdict predicate so ghosts cannot be re-created after removal
  - shell stub props prove the STAND_ASIDE verdict reaches the chart gate
  prohibitions:
  - No new src/lib/ict math (D-20)
  - Pools must never gate trigger, flaw, or ticket derivations (D-17)
  - Shell pool derivation stays verdict-free; the gate lives chart-side per D-15
---

<objective>
Close VERIFICATION gap 2 (truth #12, blocks CHRT-03/SC4): STAND_ASIDE must clear ghost lines instead of re-creating them after removal, per D-15.

Purpose: Success criterion 4 is blocked because ghost creation runs unconditionally in both chart effects, so the update cycle removes ghosts then immediately re-creates them. This plan gates creation on the verdict while keeping removal unconditional.
Output: Verdict-gated pool and ghost creation in mount plus update effects with stub-prop proof of zero lines under STAND_ASIDE.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-1-live-chart-overlay/20-CONTEXT.md
@.planning/phases/20-1-live-chart-overlay/20-VERIFICATION.md
@src/lib/store.ts
@src/lib/chart-mapper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict gate on pool and ghost creation in both chart effects</name>
  <files>components/charts/nq-chart.tsx, src/lib/chart-mapper.ts</files>
  <read_first>Files under edit plus the mount creation region, the update refresh region, the unconditional-removal paths, the EXECUTE-only ticket-leg block, and the pool-lines test slot.</read_first>
  <action>Add a pure verdict predicate in the chart mapper beside the existing finite guards that returns false only for the STAND_ASIDE verdict and true for every other verdict including null and undefined, keeping the parameter type local so the dependency direction stays component-ward per D-20. Consume that single predicate in both chart effects per D-15: wrap the whole pool creation region (scalar-or-array branch plus both ghost calls) in the mount effect and the whole refresh region (both pool refresh calls plus both ghost refresh calls) in the update effect, reusing the same ticketVerdict binding the EXECUTE-only ticket-leg block already reads. Leave every unconditional-removal path and the unmount null-every-ref cleanup untouched so null selector behavior per D-14 is unchanged, keep creation order after Asia and before ticket per D-11, keep rank brightness, ghost tone, and stale/thin dimming per D-10/D-12/D-13 exactly as they are, and extend the pool-lines slot condition with the same predicate so the sentinel stays honest when creation is gated. Touch no trigger, flaw, or ticket derivation per D-17 and add no ict math per D-20.</action>
  <verify>
    <automated>npm test -- src/lib/chart-mapper.test.ts</automated>
    <fails_when>Non-zero exit code, or the suite reports a red test row in output</fails_when>
  </verify>
  <done>Mount and update effects create zero pool or ghost lines under STAND_ASIDE while all other verdicts render exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Predicate pins plus STAND_ASIDE stub-prop proof and suite proof</name>
  <files>src/lib/chart-mapper.test.ts, src/terminal-shell.test.ts</files>
  <read_first>Files under edit plus the existing guard assertions, the multi-swing seed helpers, the renderShell harness, and the chart stub capture shape.</read_first>
  <action>Pin the gate from both sides per D-15/CHRT-03. First, add exact-match unit pins for the verdict predicate covering STAND_ASIDE false plus EXECUTE_LONG, EXECUTE_SHORT, and null true beside the existing guard assertions. Second, extend the shell suite with a STAND_ASIDE harness test reusing the deterministic multi-swing seed: hold the NQ leg healthy so the selector resolves pools while the ES leg goes stale so the ticket degrades to STAND_ASIDE without nulling pools, then mount and force the settled re-render through the subscribed-state flip the null-clears test already documents. Capture the chart stub props and assert the verdict prop reads STAND_ASIDE with pool arrays still present (shell derivation stays verdict-free) while the chart predicate applied to the captured verdict returns false, which by the Task 1 gate means zero pool and ghost lines are created. Finish with the full suite plus types plus lint on the touched files.</action>
  <verify>
    <automated>npm test -- src/lib/chart-mapper.test.ts src/terminal-shell.test.ts</automated>
    <fails_when>Non-zero exit code, or either suite reports a red test row in output</fails_when>
  </verify>
  <verify>
    <automated>npm test</automated>
    <fails_when>Non-zero exit code, or the passing file/test counts differ from the header summary</fails_when>
  </verify>
  <done>Predicate pinned for all verdict inputs, stub props prove zero pool/ghost lines under STAND_ASIDE, full suite green.</done>
</task>

</tasks>

<assumptions>
- The update effect already removes every pool and ghost handle unconditionally before the refresh region, so gating creation alone yields zero lines with no removal change.
- The shell keeps deriving pool props verdict-free per the 20-03 contract; clearing under STAND_ASIDE is the chart gate's job, reached through the already-fanned verdict prop.
- A null or undefined verdict preserves current rendering behavior; only STAND_ASIDE suppresses creation.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell derivation to chart props | Derived pool arrays plus ticket verdict cross into canvas price-line creation |
| verdict gate to canvas clearing | STAND_ASIDE verdict crosses into creation suppression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Information Disclosure | nq-chart stale ghost leftovers under STAND_ASIDE | low | mitigate | Single verdict predicate gates all pool and ghost creation in both effects while unconditional removal stays intact |
| T-20-12 | Tampering | nq-chart mount/update gate divergence | low | mitigate | Both effects consume the same mapper predicate; the pool-lines slot uses it too, and unit pins cover every verdict input |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan; gate not triggered |
</threat_model>

<verification>
- Mapper plus shell suites green, then full suite green
- Types clean and lint clean on all touched files
- Grep confirms no STAND_ASIDE-unconditional ghost creation call remains in either effect
</verification>

<success_criteria>
- Null selector plus STAND_ASIDE leave zero pool lines on canvas with dimming discipline otherwise unchanged
</success_criteria>

<output>
Create `.planning/phases/20-1-live-chart-overlay/20-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Verdict-gated pool and ghost creation | components/charts/nq-chart.tsx | Operator seeing ghost lines clear when the ticket stands aside (CHRT-03) |
| Verdict predicate with exact-match pins | src/lib/chart-mapper.ts | Chart mount/update gates and Phase 21 parity harness |
</output>
