---
phase: 20-1-live-chart-overlay
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
- src/lib/report.ts
- src/lib/report.test.ts
- components/dashboard/report.tsx
- components/dashboard/terminal-shell.tsx
- components/charts/nq-chart.tsx
autonomous: true
requirements:
- S1-01
- CHRT-01
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Report section index 1 renders as a live section with a rank-1 pain-threshold sentence instead of an UNAVAILABLE badge
  - Chart renders at least one BSL pool price-line pair from live selector output without blocking candles
  - Loading state shows skeleton rows and renders no pool prose or lines before the envelope resolves
  artifacts:
  - src/lib/report.ts
  - components/dashboard/report.tsx
  - components/dashboard/terminal-shell.tsx
  - components/charts/nq-chart.tsx
  key_links:
  - terminal-shell selectPools subscription flows into NqChart pool props
  - report section index 1 state live removes the unavailable branch for section 1 only
  prohibitions:
  - Pools must never gate FIRE verdicts or trigger, flaw, or ticket derivations
  - Section 1 must never render confident prose over a degraded or nulled selector envelope
  - Exact stop-price claims are forbidden; only projection wording with the methodology hedge is allowed
---

<objective>
Tracer slice: prove the full selectPools wiring path end to end with one live route before expansion. Contract flip plus minimal rank-1 report sentence plus minimal shell fan-out plus a single BSL pool line pair on the chart.

Purpose: Catch architectural dead-ends on the first commit while context is fresh; expansion plans build on this proven slice.
Output: Live section 1 contract, one rank-1 sentence path, one chart pair path, all committed.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-1-live-chart-overlay/20-CONTEXT.md
@.planning/phases/20-1-live-chart-overlay/20-PATTERNS.md
@.planning/phases/20-1-live-chart-overlay/20-UI-SPEC.md
@src/lib/store.ts
@src/lib/ict/pools.ts
</context>

<tasks>

<task type="tracer">
  <name>Tracer report contract flip plus minimal live rank-1 branch</name>
  <files>src/lib/report.ts, src/lib/report.test.ts, components/dashboard/report.tsx</files>
  <read_first>Files under edit (src/lib/report.ts, components/dashboard/report.tsx) plus the selector envelope contract (src/lib/store.ts selectPools range) and the pool shape (src/lib/ict/pools.ts LiquidityPool).</read_first>
  <action>Flip REPORT_SECTIONS index 1 from unavailable to live per D-22, updating the module comment that describes section 1 as unavailable, and update the section-state lock test to expect six live entries. Add a section.index === 1 live branch in report.tsx placed before the generic unavailable branch per D-03, following the section-3 branch shape: section title h3, lastUpdatedISO null skeleton rows, stable selector-function subscription to selectPools with derivation during render per D-21 (never a shallow hook on the nested selection object). Derive rank-1 as the first ACTIVE pool in the returned array order per D-18 (the array arrives rank-ordered; never recompute swept-ness, consume status verbatim). Render the sub-block with the s1-pain-threshold slot and fixed Aghri Heddi label, plus a rank-1 sentence naming side, zone bounds, ATR distance, and swept status with the inline projection hedge fragment per D-01 and D-04; numerals in mono tabular figures. Null selector renders the documented empty copy only per D-08. No trigger, flaw, or ticket derivation touched per D-17; no new ict math per D-20.</action>
  <verify>
    <automated>npm test -- src/lib/report.test.ts</automated>
  </verify>
  <acceptance_criteria>REPORT_SECTIONS lists index 1 as live with six live entries; section 1 branch exists before the unavailable branch with the required slots; null selector yields empty copy with no cached pools; no ict or store derivation files changed.</acceptance_criteria>
  <done>Contract flip plus minimal rank-1 report path committed with the section-state test green.</done>
</task>

<task type="tracer">
  <name>Tracer shell fan-out plus single chart pool pair</name>
  <files>components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx</files>
  <read_first>Files under edit (components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx) plus the Asia pair precedent and ticket-leg cycle in nq-chart and the PoolsSelection envelope in src/lib/store.ts.</read_first>
  <action>Subscribe to selectPools in terminal-shell beside the Asia/Judas/SMT selectors with the same stable-function derivation-during-render idiom per D-21, deriving during render the first ACTIVE BSL pool top and bottom plus degraded flags from the single selection return per D-19 (thin flagged not nulled; stale arrives as null). Fan the values out through new optional nullable pool props beside the Asia props. In nq-chart declare the optional pool props with null defaults, add one BSL pair through the remove-then-create ref cycle following the Asia mount/update precedent per D-09 with dashed width-1 lines titled for the rank-1 BSL top and bottom legs, colors resolved from the up-terminal token, finite-guarded inputs with per-leg try/catch that nulls only the failing leg, creation ordered after Asia and before ticket legs per D-11, unconditional removal so a null selector leaves zero lines per D-14, unmount cleanup nulling the new refs, and a pool-lines test slot beside the existing sentinels. Cap counting ranked ACTIVE only per D-16 holds trivially on this single-pair slice; full nearest-2-per-side plus ghosts land in the expansion plan.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts src/lib/chart-mapper.test.ts</automated>
  </verify>
  <acceptance_criteria>Shell passes pool props captured through the chart stub; chart declares the new props with null defaults and renders the single-pair path with unconditional removal and cleanup; null input creates no lines.</acceptance_criteria>
  <done>Single happy path from selector to report sentence to chart pair works end to end and is committed.</done>
</task>

</tasks>

<assumptions>
- Spec-less probe fallback: all 11 probe rows arrived unresolved, so they surface here as assumptions, never silent drops. Rows on empty/ordering/adjudication substance are resolved by D-05 (distinct copy per state), D-08 (null clears, never cached), D-14 (null clears all lines), D-16 (cap counts ranked ACTIVE only) cited in the task actions above.
- Genuinely open tie: equal-distance rank tie between two ACTIVE pools exactly at the nearest-2-per-side cut line has no locked rule; assumption is rank-1 is the first ACTIVE pool in selector return order (stable, deterministic). Flagged for Phase 21 calibration review, never tuned here.
- CHRT-01 boundary precision: price-line rounding follows the finite-guard pass-through (no rounding in wiring); assumption is pass-through prices are correct and any display rounding stays in the chart primitive.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store selector to report/chart render | Pre-verified Phase 19 selector output crosses into read-only UI prose and canvas lines |
| NQ leg error to report prose | Upstream poll error text crosses into verbatim inline copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | report.tsx leg-error verbatim chain | low | accept | Leg errors already surface verbatim in sections 3-6; section 1 follows the identical chain with no new error source |
| T-20-02 | Denial of Service | nq-chart createPriceLine loop | low | mitigate | Finite-guarded inputs with per-leg try/catch; a throw nulls only that leg and never blocks candles |
| T-20-03 | Tampering | selectPools return consumed in render | low | accept | Read-only consumer contract; no store or ict signature changes in this phase |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan; gate not triggered |
</threat_model>

<verification>
- Section-state lock test green plus shell and chart-mapper tests green
- Full suite stays green per the phase-exit proof pattern
</verification>

<success_criteria>
- Section 1 is live with a rank-1 sentence path and the chart shows the tracer pair path, both from the same selector return, with loading skeleton holding before resolution
</success_criteria>

<output>
Create `.planning/phases/20-1-live-chart-overlay/20-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| REPORT_SECTIONS index 1 live state | src/lib/report.ts | report.tsx section 1 branch, Phase 21 polish |
| Section 1 live branch with rank-1 sentence | components/dashboard/report.tsx | Operator reading the Pain Threshold map (expanded in 20-02) |
| Pool prop fan-out (rank-1 BSL top/bottom plus degraded flags) | components/dashboard/terminal-shell.tsx | NqChart pool props (expanded in 20-03) |
| BSL rank-1 price-line pair plus pool-lines slot | components/charts/nq-chart.tsx | Chart overlay rendering (expanded in 20-03) |
</output>
