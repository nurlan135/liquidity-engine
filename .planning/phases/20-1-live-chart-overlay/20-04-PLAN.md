---
phase: 20-1-live-chart-overlay
plan: '04'
type: execute
wave: 1
depends_on: []
files_modified:
- components/dashboard/report.tsx
- src/lib/report.ts
- src/lib/report.test.ts
- src/terminal-shell.test.ts
autonomous: true
gap_closure: true
requirements:
- S1-01
- S1-03
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Section 1 rank-1 live sentence names side, zone bounds, ATR distance, swept status, and the verbatim reason with the inline hedge
  - Report section index 1 renders as a live section with a rank-1 pain-threshold sentence instead of an UNAVAILABLE badge
  - Section 1 prose carries the proyeksiya hedge pinned by an exact-match test
  artifacts:
  - components/dashboard/report.tsx
  - src/lib/report.ts
  key_links:
  - selectPools rank-ordered return feeds rank-1 as the first pool of any status with tone by status
  - pool descriptor helper renders verbatim field values with zero detector changes
  prohibitions:
  - No new src/lib/ict math and no LiquidityPool shape change (D-20, D-18)
  - Pools must never gate trigger, flaw, or ticket derivations (D-17)
  - Exact stop-price claims are forbidden; only projection wording with the methodology hedge is allowed
---

<objective>
Close VERIFICATION gap 1 (truths #1/#7, blocks S1-01) plus the S1-03 hedge-pin warning: give the rank-1 sentence a real reason source and a reachable SWEPT path, per D-01/D-02/D-04.

Purpose: Success criterion 1 is blocked because the verbatim reason never renders and the SWEPT tone branch is unreachable. This plan makes both real without touching detector math.
Output: Rank-1 any-status sentence with deterministic reason clause, reachable SWEPT muted tone, and exact-match test pins for reason and hedge.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-1-live-chart-overlay/20-CONTEXT.md
@.planning/phases/20-1-live-chart-overlay/20-VERIFICATION.md
@src/lib/store.ts
@src/lib/ict/pools.ts
@src/lib/report.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rank-1 reason source plus any-status rank with reachable SWEPT tone</name>
  <files>components/dashboard/report.tsx, src/lib/report.ts, src/lib/report.test.ts</files>
  <read_first>Files under edit plus the selectPools envelope in src/lib/store.ts, the LiquidityPool shape and rank-order contract in src/lib/ict/pools.ts, and the section-1 branch in components/dashboard/report.tsx.</read_first>
  <action>Fix the two root causes from VERIFICATION gap 1 in the wiring layer only, per D-01/D-02/D-04/D-05. First, replace the ACTIVE-only rank-1 derivation with the first pool of the selector return in rank order regardless of status, so a fully-swept book names its top SWEPT pool instead of falling through to the no-pools line; the no-pools copy now renders only when the pools array is empty, and the existing status-driven muted-tone branch becomes reachable with tone by status. Second, add a pure deterministic pool-descriptor helper in the report lib that composes the reason clause display-only from verbatim pool field values (side token, touch count, formatted weight, status token), with a type-only pool import, no numeric derivation beyond display formatting, and no detector or shape change. Render the helper output in the rank-1 sentence where the dead reason read sits today, ahead of the existing inline hedge constant. Planner decision recorded: fix option A is chosen (reason source plus any-status rank-1) over narrowing D-02/SC1, because the selector precedent shows reasons originate in detectors while the D-20 purity guard forbids new ict math and D-18 locks the pool shape from Phase 19, so a deterministic wiring-layer descriptor from verbatim fields is the only change that keeps D-01/D-02 locked and closes SC1 without weakening them. Touch no trigger, flaw, or ticket derivation per D-17 and add no ict math per D-20. Add exact-match unit pins for the descriptor helper covering an ACTIVE pool and a SWEPT pool.</action>
  <verify>
    <automated>npm test -- src/lib/report.test.ts</automated>
    <fails_when>Non-zero exit code, or the passing summary line is absent from output</fails_when>
  </verify>
  <done>Rank-1 derives any-status with a rendered reason clause, SWEPT tone reachable by status, descriptor helper exact-match pinned, report-contract suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Sentence integration pins — live clause, SWEPT naming, hedge, suite proof</name>
  <files>src/terminal-shell.test.ts</files>
  <read_first>File under edit plus the existing multi-swing seed helpers, the renderShell harness, the chart stub capture, and the section-1 branch under test.</read_first>
  <action>Prove the closed truths end-to-end through the mounted shell per D-01/D-02/D-04/S1-03, reusing the deterministic multi-swing seed pattern already in the file. Assert with exact-match equality that a settled live selection renders the full rank-1 sentence carrying side, zone bounds, ATR distance, status token, reason clause, and the hedge constant together in one sentence node. Assert that an all-swept book (wick pierce with the close back inside, so lifecycle marks SWEPT never CONSUMED) names the top SWEPT pool with the status token visible in the muted tone, and that an empty pools array renders the no-pools copy with no confident prose. Keep the skeleton versus live split and the null-selector empty-copy path untouched. Finish with the full suite plus types plus lint on the touched files.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts</automated>
    <fails_when>Non-zero exit code, or the suite reports a red test row in output</fails_when>
  </verify>
  <verify>
    <automated>npm test</automated>
    <fails_when>Non-zero exit code, or the passing file/test counts differ from the header summary</fails_when>
  </verify>
  <done>Live sentence, SWEPT naming, and hedge pinned by exact-match tests; full suite, types, and lint green.</done>
</task>

</tasks>

<assumptions>
- The selector return arrives rank-ordered with ranked ACTIVE pools before SWEPT pools per the scorer contract, so the first element is the top-ranked pool of any status with no recomputed ranking in the component.
- CONSUMED pools never reach the component because the lifecycle drops them from the map, so only ACTIVE and SWEPT statuses need sentence paths.
- The hedge constant text is locked; the integration test pins it by reference to the rendered sentence, never by duplicating copy into prose.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store selector to section 1 prose | Pre-verified selector output crosses into read-only Azerbaijani prose |
| pool fields to reason clause | Verbatim numeric and status values cross into deterministic display copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-09 | Spoofing | report.tsx rank-1 sentence over empty or swept book | low | mitigate | Any-status rank-1 with tone by status; no-pools copy renders only on an empty array, never over a swept book |
| T-20-10 | Information Disclosure | report.tsx reason clause from pool fields | low | accept | Field values already reach the operator through zone bounds and canvas line prices; clause is deterministic display copy with no new source |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan; gate not triggered |
</threat_model>

<verification>
- Report-contract plus shell suites green, then full suite green
- Types clean and lint clean on all touched files
- Grep confirms banned out-of-scope phrases absent from the section 1 branch
</verification>

<success_criteria>
- Rank-1 sentence names side, bounds, ATR, status, reason, and hedge with SWEPT reachable and hedge exact-match pinned
</success_criteria>

<output>
Create `.planning/phases/20-1-live-chart-overlay/20-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Rank-1 any-status sentence with reason clause | components/dashboard/report.tsx | Operator reading the live Pain Threshold map (S1-01, S1-03) |
| Pool descriptor helper with exact-match pins | src/lib/report.ts | Rank-1 sentence and Phase 21 parity harness |
</output>
