---
phase: 20-1-live-chart-overlay
plan: '03'
type: execute
wave: 3
depends_on:
- 20-01
files_modified:
- components/dashboard/terminal-shell.tsx
- components/charts/nq-chart.tsx
- src/lib/chart-mapper.ts
- src/lib/chart-mapper.test.ts
autonomous: true
requirements:
- CHRT-01
- CHRT-02
- CHRT-03
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Chart renders BSL and SSL zone pairs capped to nearest two per side counting ranked ACTIVE pools only, with swept ghosts riding outside the cap
  - Rank-1 pair renders full brightness and rank-2 dimmer, with ticket entry, SL, and TP lines painted on top of pools and pools above zones
  - Swept pools render as dimmed ghosts that stay visible for retest context, never removed
  - Pool lines dim under stale and thin tiers with the uniform discipline, and null selector plus STAND_ASIDE leave zero pool lines on canvas
  artifacts:
  - components/charts/nq-chart.tsx
  - components/dashboard/terminal-shell.tsx
  key_links:
  - pool derivation in the shell splits ACTIVE versus SWEPT from the single selection return with zero recomputed status
  - chart creation order places pool lines after Asia and before ticket legs so ticket stays on top
  prohibitions:
  - Pools must never gate FIRE verdicts or trigger, flaw, or ticket derivations
  - Section 1 must never render confident prose over a degraded or nulled selector envelope
  - Exact stop-price claims are forbidden; only projection wording with the methodology hedge is allowed
---

<objective>
Expand the tracer single pair into the full chart overlay: nearest-2-per-side capped pairs with rank brightness, swept ghosts, z-order, and stale/thin dimming plus ghost clearing.

Purpose: Success criteria 3 and 4 live here — dashed pairs on canvas with rank-1 pop and honest dimming plus clearing.
Output: Complete pool overlay wiring from shell derivation through canvas lines.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-1-live-chart-overlay/20-CONTEXT.md
@.planning/phases/20-1-live-chart-overlay/20-PATTERNS.md
@.planning/phases/20-1-live-chart-overlay/20-UI-SPEC.md
@src/lib/store.ts
@src/lib/ict/pools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell pool derivation — nearest-2-per-side split plus degraded flags</name>
  <files>components/dashboard/terminal-shell.tsx</files>
  <read_first>File under edit (components/dashboard/terminal-shell.tsx) plus the ticket fan-out derivation template, the PoolsSelection envelope in src/lib/store.ts, and the rank-order contract in src/lib/ict/pools.ts.</read_first>
  <action>Derive the full pool prop set during render from the single selectPools return per D-19 and D-21, with no verdict gate and no bar-date loop since pools are D1-NQ geometry attaching by price per the pattern map: nearest-2-per-side counting ranked ACTIVE pools only per D-16 (first two ACTIVE BSL plus first two ACTIVE SSL in selector return order; equal-distance ties at the cut line resolve by return order per the flagged assumption), swept pools collected separately as ghosts riding outside the cap and never counted toward it, statuses consumed verbatim with zero recomputed swept-ness per D-18, degraded stale and thin flags reusing the already-flowing overlayStale and thinTierValue values, and null selection fanning out to all-null pool props so the chart path clears per D-14. Fan out through the optional nullable prop names established in the tracer beside the Asia props; the existing dynamic import stays client-only with no change. Touch no trigger, flaw, or ticket derivation per D-17 and add no ict math per D-20.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts src/lib/store.test.ts</automated>
  </verify>
  <acceptance_criteria>Chart stub captures nearest-2-per-side ACTIVE arrays plus separate swept ghost arrays with degraded flags; null selection captures all-null pool props; no last-known pools survive a null return.</acceptance_criteria>
  <done>Full shell derivation committed with shell and store suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Chart pool pairs — rank brightness, ghosts, z-order, dimming, clearing</name>
  <files>components/charts/nq-chart.tsx, src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts</files>
  <read_first>Files under edit (components/charts/nq-chart.tsx, src/lib/chart-mapper.ts) plus the Asia mount/update cycle, the ticket EXECUTE-only leg loop, the thin/stale dimming block, and the cleanup and test-slot markers.</read_first>
  <action>Render the full pool overlay following the Asia pair remove-then-create cycle per D-09: dashed width-1 pairs titled per the side-rank-leg convention from the copy contract, BSL legs in the up-terminal tone and SSL legs in the down-terminal tone, rank-1 pairs at full brightness and rank-2 pairs at the same hue halved per D-10, swept ghosts in muted gray staying visible never removed per D-12, stale and thin legs falling back to muted gray under the uniform halved discipline with no stronger special dim per D-13 reusing the existing overlay tone and dimmed computation. Create pool lines after Asia and before ticket entry/SL/TP so ticket stays on top and pools sit above zones per D-11; removal of every pool ref stays unconditional so null selector creates nothing and leaves zero lines per D-14, and the STAND_ASIDE path clears ghosts through the same unconditional removal shared with the ticket cycle per D-15. Guard each leg with the finite helper (new poolLineInputs helper mirroring the Asia guard or reuse of the Asia helper per planner call, with throw naming the helper and caller try/catch nulling only that leg) plus per-leg create try/catch; null every new ref on unmount; extend the effect dependency list with the new props; add the pool-lines test slot beside the Asia and ticket sentinels. Add helper guard tests beside the Asia guard assertions; chart behavior asserts through the shell stub props, never a real chart mount.</action>
  <verify>
    <automated>npm test -- src/lib/chart-mapper.test.ts src/terminal-shell.test.ts</automated>
  </verify>
  <acceptance_criteria>Max eight ACTIVE lines plus outside-cap ghosts with rank brightness split; swept legs visible dimmed; stale/thin legs muted; null and STAND_ASIDE inputs leave zero pool lines; helper guard tests green.</acceptance_criteria>
  <done>Full chart overlay committed with mapper and shell suites green.</done>
</task>

</tasks>

<assumptions>
- Spec-less probe fallback: CHRT-01 boundary row (nearest-2-per-side tie exactly at the cut line) is genuinely open; assumption is selector return order decides, deterministic and stable, flagged for Phase 21 review.
- CHRT-01 precision row: finite-guard pass-through with no wiring-side rounding; display rounding, if any, stays inside the chart primitive.
- S1-02/CHRT-03 adjacency substance (null versus stale versus thin precedence) resolved by D-08, D-13, D-14: null clears everything, thin dims without hiding, stale mutes without hiding.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell derivation to chart props | Derived pool arrays cross into canvas price-line creation |
| selector null to canvas clearing | Null envelope crosses into unconditional line removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Denial of Service | nq-chart pool line creation loop | low | mitigate | At most eight ACTIVE plus ghost legs, each finite-guarded with per-leg try/catch; throws null only that leg and never block candles |
| T-20-07 | Information Disclosure | nq-chart stale pool leftovers | low | mitigate | Unconditional removal before creation plus null-every-ref on unmount; null selector and STAND_ASIDE provably leave zero lines |
| T-20-08 | Tampering | terminal-shell pool derivation | low | accept | Pure derivation during render from the pre-verified selector return; statuses consumed verbatim with no recomputation and no derivation writes |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan; gate not triggered |
</threat_model>

<verification>
- Mapper plus shell plus store suites green
- Full suite proof per the phase-exit pattern before verify
</verification>

<success_criteria>
- Canvas shows capped pairs with rank pop, dimmed ghosts, ticket on top, uniform stale/thin dimming, and clean clearing on null and STAND_ASIDE
</success_criteria>

<output>
Create `.planning/phases/20-1-live-chart-overlay/20-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Nearest-2-per-side ACTIVE plus ghost derivation | components/dashboard/terminal-shell.tsx | NqChart pool props and Phase 21 parity harness |
| Full pool price-line pairs with rank, ghost, dim, and clear paths | components/charts/nq-chart.tsx | Operator seeing BSL/SSL zones on canvas (CHRT-01 through CHRT-03) |
| poolLineInputs finite guard (only if planner adds the helper) | src/lib/chart-mapper.ts | NqChart pool leg creation |
</output>
