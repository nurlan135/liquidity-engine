---
phase: 20-1-live-chart-overlay
plan: '02'
type: execute
wave: 2
depends_on:
- 20-01
files_modified:
- components/dashboard/report.tsx
- src/lib/report.test.ts
autonomous: true
requirements:
- S1-01
- S1-02
- S1-03
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low
must_haves:
  truths:
  - Section 1 with zero ACTIVE pools renders the distinct no-pools line, never confident prose over the gap
  - Section 1 with a null selector and no leg error renders only the empty copy, never cached pools
  - Section 1 on a stale leg prints the verbatim NQ-leg lastError
  - Section 1 rank-1 live sentence names side, zone bounds, ATR distance, swept status, and the verbatim reason with the inline hedge
  - Section 1 on thin history keeps pools visible dimmed at opacity-45 with the thin note, never hidden
  backstop:
    statement: Verbatim Azerbaijani pool reasons wrap inside panel flow without truncation or horizontal scroll
    verification: backstop
  artifacts:
  - components/dashboard/report.tsx
  key_links:
  - selectPools null return renders empty copy with zero cached pool prose
  - thin flag renders dimmed sub-block plus thin note instead of hiding pools
  prohibitions:
  - Pools must never gate FIRE verdicts or trigger, flaw, or ticket derivations
  - Section 1 must never render confident prose over a degraded or nulled selector envelope
  - Exact stop-price claims are forbidden; only projection wording with the methodology hedge is allowed
---

<objective>
Expand the tracer rank-1 branch into the full section 1 state matrix: distinct honest empty states plus swept tone plus inline hedge, all test-pinned.

Purpose: Success criteria 1 and 2 live here — a readable live Pain Threshold sentence and honest empties instead of confident prose over degraded truth.
Output: Complete section 1 branch covering live, swept, no-pools, stale, thin, and null states.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-1-live-chart-overlay/20-CONTEXT.md
@.planning/phases/20-1-live-chart-overlay/20-PATTERNS.md
@.planning/phases/20-1-live-chart-overlay/20-UI-SPEC.md
@src/lib/store.ts
@src/lib/ict/pools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 1 honest-empty matrix — no-pools, stale, thin, null</name>
  <files>components/dashboard/report.tsx</files>
  <read_first>File under edit (components/dashboard/report.tsx) plus the section-4 empty idiom and ticket-panel degraded tag precedent, plus the PoolsSelection envelope and the copy contract strings in 20-UI-SPEC.</read_first>
  <action>Extend the section.index === 1 branch from the tracer with the four honest states per D-05, each with its own line and never confident prose over a degraded banner: non-null selection with zero ACTIVE pools renders the documented no-pools body line at Heading role per D-05; null selector with an NQ-leg lastError prints that error verbatim through the getState leg-error chain per D-06, mirroring the section-3/4 chain with the NQ leg owning the error since pools are NQ-D1 geometry; null selector without a leg error renders the empty copy only per D-08 with zero cached pools; thin-flagged selection wraps the sub-block in opacity-45 and appends the documented thin note per D-07, pools staying visible dimmed never hidden to match the zone and level dimming discipline. Keep the skeleton versus live split per D-03 and the stable selector-function subscription per D-21; touch no trigger, flaw, or ticket derivation per D-17 and add no ict math per D-20.</action>
  <verify>
    <automated>npm test -- src/terminal-shell.test.ts src/lib/report.test.ts</automated>
  </verify>
  <acceptance_criteria>Each of the four states renders its documented copy: no-pools line with zero ACTIVE pools, verbatim NQ lastError on stale-null, empty copy alone on clean null with no pool names anywhere, dimmed sub-block plus thin note on thin with pools still visible.</acceptance_criteria>
  <done>Honest-empty matrix committed with existing shell and report-contract suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Rank-1 sentence polish — swept tone, ATR distance, inline hedge</name>
  <files>components/dashboard/report.tsx</files>
  <read_first>File under edit (components/dashboard/report.tsx) plus the pool shape and rank-order contract in src/lib/ict/pools.ts and the typography and copy rows in 20-UI-SPEC.</read_first>
  <action>Complete the live rank-1 sentence per D-01: side plus zone bounds plus ATR distance plus swept status with the fixed Aghri Heddi label and s1-pain-threshold slot, Heading role semibold with mono tabular figures for all numerals. A SWEPT rank-1 pool is named with its verbatim SWEPT status token in muted tone per D-02 so the retest-magnetism story is preserved and never silently dropped. Carry the projection hedge plus methodology caveat inline in the rank-1 sentence per D-04 with the documented hedge fragment, toBe-pinned, with no separate footer element. ATR distance derives display-only in the component from already-shipped read-only utilities against the NQ closed candles, never recomputed swept-ness and never a new ict module per D-20, and the derivation must throw-safe to empty copy rather than blanking the section. Verbatim Azerbaijani reasons render inside normal panel flow and must wrap without truncation or horizontal scroll; that wrap is a held-out visual backstop with no explicit evidence at verify time. Banned copy — pool-gated FIRE language, exact stop-price claims, heatmap language, LLM narrative — appears nowhere per the out-of-scope lock.</action>
  <verify>
    <automated>npm test -- src/lib/report.test.ts</automated>
  </verify>
  <acceptance_criteria>Live sentence names side, bounds, ATR distance, and status with the inline hedge and pinned copy; SWEPT rank-1 renders in muted tone with the status token visible; banned out-of-scope copy absent; report-contract suite green.</acceptance_criteria>
  <done>Full section 1 prose matrix committed, backstop wrap flagged as visual-only.</done>
</task>

</tasks>

<assumptions>
- Spec-less probe fallback: S1-02 ordering rows (null selector versus empty-pool versus stale versus thin precedence) are resolved by D-05 distinct copy per state plus D-08 null-clears plus the leg-error chain; where two flags coincide the plan assumes null-with-leg-error prints the verbatim error first, clean null prints empty copy, thin dims but stays visible.
- Equal-rank tie between two ACTIVE pools is genuinely open; rank-1 is the first ACTIVE pool in selector return order per the tracer assumption, carried forward here.
- ATR distance is display-only prose derived from shipped read-only utilities; it never feeds trigger, flaw, or ticket math per D-17.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store selector to section 1 prose | Pre-verified selector output crosses into read-only Azerbaijani prose |
| NQ leg error to section 1 stale line | Upstream poll error text crosses into verbatim inline copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Information Disclosure | report.tsx verbatim leg-error line | low | accept | Same NQ-leg error already prints verbatim in sections 3 and 5; no new error source and no paraphrase path |
| T-20-05 | Spoofing | report.tsx rank-1 sentence over degraded envelope | low | mitigate | Distinct copy per state with null rendering empty copy only; confident prose is unreachable over a nulled selector |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan; gate not triggered |
</threat_model>

<verification>
- Report-contract suite green plus shell suite green
- Grep confirms banned out-of-scope phrases absent from the section 1 branch
</verification>

<success_criteria>
- All six section 1 states render their documented copy with the hedge pinned and swept pools named in muted tone
</success_criteria>

<output>
Create `.planning/phases/20-1-live-chart-overlay/20-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Section 1 honest-empty matrix (no-pools, stale, thin, null) | components/dashboard/report.tsx | Operator reading degraded truth honestly (S1-02) |
| Rank-1 sentence with swept tone and inline hedge | components/dashboard/report.tsx | Operator reading the live Pain Threshold map (S1-01, S1-03) |
</output>
