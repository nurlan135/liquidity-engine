---
phase: 19-pools-math
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/swings.ts
  - src/lib/ict/smt.ts
  - src/lib/ict/pools.ts
  - src/lib/ict/pools.test.ts
  - src/lib/store.ts
autonomous: true
requirements:
  - POOL-01
  - POOL-03
  - POOL-06
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - D1 swing highs/lows flow through one shared k=2 strict-fractal contract into a BSL/SSL inventory (no forked swing loop)
    - Every pool carries side, top, bottom, touches, weight, originDate and ACTIVE/SWEPT/CONSUMED status with zone bounds as min-max span
    - selectPools returns a ranked array or null on stale/empty legs and never throws into render
  artifacts:
    - src/lib/ict/swings.ts
    - src/lib/ict/pools.ts
    - src/lib/ict/pools.test.ts
    - src/lib/store.ts (selectPools thin envelope)
  key_links:
    - swings.ts predicates -> pools.ts detector (single import, zero duplicated fractal logic per D-12)
    - pools.ts evaluatePools -> store.ts selectPools (same snapshot, one sharedEpoch call per D-21)
---

<objective>
Tracer slice: prove the full vertical path NQ D1 candles -> shared swings -> BSL/SSL zones -> lifecycle -> score/rank/cap -> guarded selectPools in one production-quality thin implementation per D-10/D-11/D-12/D-19/D-21.

Purpose: Catch architectural dead-ends (swing ownership, selector envelope, purity wall) on the first commit instead of after full matrix work.
Output: Working thin inventory + thin selector + thin boundary tests, all green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ict/smt.ts
@src/lib/ict/types.ts
@src/lib/ict/fvg.ts
@src/lib/store.ts
</context>

<tasks>

<task type="tracer">
  <name>Shared swings extraction + thin pools pipeline end-to-end</name>
  <files>src/lib/ict/swings.ts, src/lib/ict/smt.ts, src/lib/ict/pools.ts, src/lib/ict/pools.test.ts</files>
  <read_first>src/lib/ict/smt.ts lines 1-14 plus 47-131 (constants SWING_K=2, SWING_LOOKBACK=60, isSwingHigh/isSwingLow verbatim, hasFiniteOhlc, trailing slice); src/lib/ict/types.ts closedOnly plus Candle contract; src/lib/ict/fvg.ts lines 1-18 plus 37-43 plus 114-118 (module header, originIndexOrNeg1, WR-07 sort-before-slice); src/lib/ict/regime.ts computeATR short-history contract</read_first>
  <acceptance_criteria>swings.ts exports SWING_K=2 plus SWING_LOOKBACK=60 plus isSwingHigh plus isSwingLow moved verbatim; smt.ts imports them from ./swings and smt.test.ts stays green; pools.ts exports LiquidityPool plus POOL_MAP_BOUND=20 plus detectPools plus evaluatePools handling one BSL and one SSL path; pools.test.ts proves one swing-high becomes BSL and one swing-low becomes SSL on a 9-bar leg() fixture; purity.test.ts green with no exclusions added</acceptance_criteria>
  <action>Per D-12 move isSwingHigh/isSwingLow plus SWING_K=2 plus SWING_LOOKBACK=60 verbatim from src/lib/ict/smt.ts into new src/lib/ict/swings.ts (keep SMT_TOL_BPS=25 plus CORR_WINDOW=20 plus CORR_MIN=0.7 in smt.ts; smt.ts replaces bodies with re-export import from ./swings, zero behavior change per D-24). Create src/lib/ict/pools.ts with header comment Pure: injected candles only, zero store imports, no clock reads; define side BSL|SSL and status ACTIVE|SWEPT|CONSUMED literals verbatim plus type LiquidityPool { side, top, bottom, touches, weight, originDate, status }; export POOL_MAP_BOUND=20 with CALIBRATION-PROVISIONAL comment; export EQUAL_TOL_BPS=25 plus MERGE_ATR_MULT=0.25 plus DOL_BOOST=2.0 plus BEHIND_PENALTY=0.25 plus TOL_EPS_BPS=1e-9 all exported with CALIBRATION-PROVISIONAL comments per D-22; implement detectPools(candles: Candle[], asOf: string) as closedOnly then finite-OHLC drop then chronological-sort copy then slice(-60) then single scan where swing highs seed BSL candidates and swing lows seed SSL candidates (Candle[] only, never IntradayCandle per D-10); zone bounds equal min-max span of cluster per D-15; thin lifecycle single chronological pass strictly after originDate where high greater than top marks SWEPT and strictly-later close above top marks CONSUMED for BSL mirrored with low less than bottom and close below bottom for SSL per D-18 with origin absent kept and skipped per originIndexOrNeg1 precedent; thin scorer score = weight times dolBoost divided by (1 plus distAtr) with distAtr = abs(poolMid minus lastClose) divided by atr, dolBoost 2.0 on DOL side else 1.0 per D-07, behind-price penalty 0.25 multiplicative per D-08, ATR via computeATR with zero-or-empty branch refusing to rank (return unscored ACTIVE order, never divide by zero) per D-09; cap by originDate sort before slice(-20) per D-16; weight thin value touches>=2 ? 3.0 : 1.0 (full diminishing curve lands in Plan 02 per D-05); implement evaluatePools(candles: Candle[], asOf: string) composing detect plus lifecycle with asOf skip (c.date greater than asOf continue) plus score plus rank with Rank-1 equals nearest ACTIVE on DOL side per D-06. Must NOT touch trigger.ts/invalidation.ts/ticket.ts signatures and must NOT emit prose or chart code per D-17. Create src/lib/ict/pools.test.ts with leg() rail builder copied from smt.test.ts:18-29 plus constant pins (SWING_K 2, EQUAL_TOL_BPS 25, POOL_MAP_BOUND 20, MERGE_ATR_MULT 0.25, DOL_BOOST 2.0, BEHIND_PENALTY 0.25) plus one BSL-origin test plus one SSL-origin test plus shape-literal test.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools.test.ts src/lib/ict/smt.test.ts src/lib/ict/purity.test.ts</automated>
    <fails_when>swings extraction changed fractal edges (smt.test.ts red) or pools.ts trips purity self-scan (Date.now or store import) or thin BSL/SSL origin test fails</fails_when>
  </verify>
  <done>Thin detect-to-rank path works for one BSL and one SSL pool with exact shape literals; smt.test.ts and purity.test.ts green on the same commit</done>
  <reversibility rating="reversible">Move-only swing extraction plus additive pools.ts; revert by restoring smt.ts bodies from git</reversibility>
</task>

<task type="auto">
  <name>Thin selectPools guarded envelope in store</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts lines 465-469 (sharedEpoch verbatim) plus 806-819 (selectSMT guard shape) plus 886-914 (selectTriggerPure derive envelope); src/lib/ict/pools.ts evaluatePools signature from prior task in this plan</read_first>
  <acceptance_criteria>selectPools exists on the Zustand store; returns null on nq.stale true and on empty closed NQ leg; returns ranked PoolsOutput on healthy leg; never throws on garbage input; calls sharedEpoch exactly once per invocation</acceptance_criteria>
  <action>Per D-21 add selectPools to src/lib/store.ts copying the selectSMT guard verbatim but adapted to the NQ D1 leg only: read get().nq plus get().asOfBaku, if nq.stale return null, if closedOnly(nq.candles).length equals 0 return null, ES-stale must NOT null pools (do not read es.stale here); inside try call const epoch = sharedEpoch(get) exactly once then return evaluatePools(nq.candles, asOfBaku) plus epoch as { pools, asOf, epoch } thin shape (thin boolean flag lands in Plan 03); catch returns null with zero set calls; Null means degraded never a throw into render. Must NOT add trigger/flaw/ticket edges per D-17 and must NOT import IntradayCandle or killzone helpers into pools.ts per type wall.</action>
  <verify>
    <automated>npm test -- src/lib/store.test.ts src/lib/ict/pools.test.ts</automated>
    <fails_when>selectPools throws on null candles or returns non-null on stale NQ leg or ignores sharedEpoch single-call discipline</fails_when>
  </verify>
  <done>Healthy NQ leg yields ranked pools object; stale or empty NQ leg yields null; garbage input yields null without throwing</done>
  <reversibility rating="reversible">Additive selector only; removal restores prior store surface with no migration</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller array -> pools.ts pure entry | untrusted candle arrays cross here (wrong types, forming rows, non-finite OHLC, unsorted dates) |
| store leg -> selectPools | stale/empty/thin NQ leg state crosses here into render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | pools.ts scorer (distAtr divide) | high | mitigate | Explicit zero/empty-ATR branch refusing to rank; finite-guards on open/high/low/close; thin test pins NaN-free sort in this plan |
| T-19-02 | Information Disclosure (honesty) | selectPools empty/stale handling | high | mitigate | Refuse-null on stale NQ leg and empty closed leg; never return silent [] as no-pain; reason rides nq.lastError |
| T-19-SC | Tampering | npm installs | high | mitigate | No new packages in this phase (own-code plus installed zustand/vitest only); no install step, no legitimacy checkpoint needed |
</threat_model>

<verification>
Thin pools.test.ts plus smt.test.ts plus purity.test.ts green on every commit; store.test.ts thin selector checks green before wave merge.
</verification>

<success_criteria>
Shared swing truth proven with smt.test.ts green; one BSL plus one SSL pool flows detected-to-ranked through selectPools; purity guard green from first commit.
</success_criteria>

<output>
Create `.planning/phases/19-pools-math/19-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols from this plan: `src/lib/ict/swings.ts` (SWING_K, SWING_LOOKBACK, isSwingHigh, isSwingLow), `src/lib/ict/pools.ts` (LiquidityPool, EQUAL_TOL_BPS, POOL_MAP_BOUND, MERGE_ATR_MULT, DOL_BOOST, BEHIND_PENALTY, detectPools, evaluatePools), `src/lib/ict/pools.test.ts` (thin BSL/SSL pins), `selectPools` thin envelope on `src/lib/store.ts`.
