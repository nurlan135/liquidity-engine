---
phase: 19-pools-math
plan: '02'
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/lib/ict/pools.ts
  - src/lib/ict/pools.test.ts
autonomous: true
requirements:
  - POOL-02
  - POOL-03
  - POOL-04
  - POOL-05
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Equal highs/lows cluster into heavier pools with a strong but diminishing bonus, and swept pools keep their bonus
    - Same-side pools within a quarter ATR merge into the more extreme zone and the inventory never exceeds 20 trailing by originDate
    - Wick-pierce marks SWEPT, close-through marks CONSUMED, first sweep wins and retests never re-promote
    - Rank-1 is the nearest ACTIVE pool on the DOL side scored in ATR units with exported provisional constants
  artifacts:
    - src/lib/ict/pools.ts (equality curve, merge, lifecycle, scorer)
    - src/lib/ict/pools.test.ts (full POOL-02..05 matrix)
  key_links:
    - equality weight -> scorer score (bonus flows into rank, swept keeps bonus per D-04)
    - lifecycle status -> scorer eligibility (only ACTIVE pools can take Rank-1 per D-06)
---

<objective>
Expand the tracer thin pipeline into the full pinned math matrix: equality weighting (POOL-02 per D-01..D-05), ATR-merge plus trailing cap (POOL-03 remainder per D-13..D-16), full sweep lifecycle (POOL-04 per D-18), and full proximity-times-DOL scorer (POOL-05 per D-06..D-09/D-20).

Purpose: Lock every ranking number behind a boundary test so Phase 21 calibrates against pins instead of vibes, with zero tuning in this phase per D-22.
Output: Full pools.ts logic plus the complete pools.test.ts matrix, suite green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-pools-math/19-01-SUMMARY.md
@src/lib/ict/pools.ts
@src/lib/ict/smt.ts
@src/lib/ict/judas.ts
@src/lib/ict/fvg.ts
@src/lib/ict/regime.ts
@src/lib/ict/dol.ts
</context>

<tasks>

<task type="auto">
  <name>Equality clustering with strong diminishing bonus</name>
  <files>src/lib/ict/pools.ts, src/lib/ict/pools.test.ts</files>
  <read_first>src/lib/ict/smt.ts SMT_TOL_BPS=25 line plus TOL_EPS_BPS=1e-9 epsilon compare idiom; src/lib/ict/pools.ts detectPools plus EQUAL_TOL_BPS from Plan 01; src/lib/ict/pools.test.ts leg() builder from Plan 01</read_first>
  <acceptance_criteria>Two swing highs 3 bps apart cluster into one BSL pool with touches=2; two highs 200 bps apart stay separate; triple equal-highs outscore any nearby single-touch pool; 4th and 5th touches add strictly less than the 2nd-touch increment; swept pools retain their full equality bonus value</acceptance_criteria>
  <action>Per D-01..D-05 implement equality clustering in src/lib/ict/pools.ts: cluster same-side swings whose relative gap abs(a-b)/b times 10000 is less-or-equal to EQUAL_TOL_BPS=25 using the TOL_EPS_BPS=1e-9 epsilon idiom (compare gapBps minus toleranceBps greater than TOL_EPS_BPS means outside), minimum 2 touches earns the bonus, merged zone top/bottom equal min-max span of member extremes, originDate equals earliest member date. Define and export const EQUAL_BONUS_STEPS = [3.0, 2.0, 1.0, 0.5] with CALIBRATION-PROVISIONAL comment; weight equals touches plus cumulative bonus where bonus(n) sums steps for n>=2 (bonus(2)=3.0, bonus(3)=5.0, bonus(4)=6.0, bonus(5+)=6.5 plus 0.5 per touch beyond 5 capped at 2 extra steps), so increments strictly diminish 3.0 then 2.0 then 1.0 then 0.5 and triple equal-highs (weight 3+5=8) dominate nearby singles; lifecycle must not strip the bonus when status flips to SWEPT per D-04 (reduced presence is expressed via scorer eligibility only, never by editing weight). Add tests: 3-bps cluster yes, 200-bps split no, tolerance-boundary exact-25bps pinned, min-2-touches (lone swing weight equals 1.0, pair weight equals 2+3=5), triple-outranks-single ordering assert, diminishing increments assert (delta4 < delta2, delta5 < delta2), swept-keeps-bonus assert. No tuning beyond these locked numbers; no prose output.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools.test.ts src/lib/ict/smt.test.ts src/lib/ict/purity.test.ts</automated>
    <fails_when>200-bps pair still clusters or lone swing carries bonus weight or 4th-touch increment equals-or-exceeds the 2nd-touch increment</fails_when>
  </verify>
  <done>Equality matrix green: cluster/split, boundary pin, min-touches, triple dominance, diminishing curve, swept-keeps-bonus</done>
  <reversibility rating="reversible">Pure-math constant plus logic change inside pools.ts only; revert file from git with no outside callers affected</reversibility>
</task>

<task type="auto">
  <name>Full sweep lifecycle first-sweep-wins</name>
  <files>src/lib/ict/pools.ts, src/lib/ict/pools.test.ts</files>
  <read_first>src/lib/ict/judas.ts strict pierce lines (high greater than level, low less than level, both-extremes resolution); src/lib/ict/fvg.ts close-through loop plus sweep-then-reject plus originIndexOrNeg1 plus asOf skip; src/lib/ict/pools.ts lifecycle section from Plan 01</read_first>
  <acceptance_criteria>Wick pierce with close back inside flips ACTIVE to SWEPT; strictly-later close beyond the zone flips to CONSUMED; boundary touch with equality does not flip status; first sweep wins across multi-raid sequences; retest of a SWEPT pool leaves it SWEPT; origin date absent from candles keeps pool unscored-by-lifecycle; candles after asOf are ignored</acceptance_criteria>
  <action>Per D-18 harden the lifecycle pass in src/lib/ict/pools.ts: single chronological pass over closed candles strictly after each pool originDate; BSL swept when strictly-later high > zone.top, SSL swept when low < zone.bottom, touch with == is not a sweep and is pinned by test; BSL consumed when strictly-later close prints above zone.top, SSL consumed on close below zone.bottom; wick pierce with close back inside the zone yields SWEPT never CONSUMED (sweep-then-reject); first chronological sweep wins and later touches never re-promote a SWEPT pool back to ACTIVE; stale origin (findIndex -1) keeps the pool and skips evaluation; honor injected asOf string with c.date greater than asOf continue. Add tests: pierce-to-SWEPT, close-through-to-CONSUMED, touch-equals-no-flip, sweep-then-reject stays SWEPT, double-raid first-wins, retest-never-repromotes, stale-origin kept, asOf cutoff respected.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools.test.ts src/lib/ict/smt.test.ts src/lib/ict/purity.test.ts</automated>
    <fails_when>equality touch flips status or a retest re-promotes SWEPT to ACTIVE or CONSUMED pools remain in the active ranked map</fails_when>
  </verify>
  <done>Lifecycle matrix green with first-sweep-wins and no re-promotion pinned across BSL and SSL polarities</done>
  <reversibility rating="reversible">Lifecycle logic contained in pools.ts; revert restores thin behavior with selector untouched</reversibility>
</task>

<task type="auto">
  <name>ATR merge plus trailing cap plus full scorer ordering</name>
  <files>src/lib/ict/pools.ts, src/lib/ict/pools.test.ts</files>
  <read_first>src/lib/ict/regime.ts computeATR plus computeRegime atr-zero degrade; src/lib/ict/dol.ts computePrimaryDOL mapping (BULLISH to range high, BEARISH to range low, COMPRESSION to nearer extreme); src/lib/ict/fvg.ts WR-07 sort-before-slice; src/lib/ict/trigger.ts CALIBRATION-PROVISIONAL comment idiom; src/lib/ict/pools.ts scorer plus cap from Plan 01</read_first>
  <acceptance_criteria>Same-side pools within 0.25x ATR merge into the more extreme zone with min-max bounds; empty ATR history skips merging and keeps all pools; inventory over 20 keeps the newest 20 by originDate even for unsorted input; Rank-1 equals nearest ACTIVE pool on the DOL side; DOL-side score doubles versus identical off-side pool; behind-price pools score at quarter strength; zero ATR refuses to rank instead of producing non-finite scores</acceptance_criteria>
  <action>Per D-13..D-16 plus D-06..D-09 and D-20 implement in src/lib/ict/pools.ts: merge same-side pools whose distance to the more extreme edge is within MERGE_ATR_MULT=0.25 times ATR (ATR from computeATR on the same closed array; empty ATR array means skip merge entirely, keep all, pinned by test); merged pool keeps the more extreme edge, bounds equal min-max span, touches sum, weight recomputed from summed touches via the EQUAL_BONUS_STEPS curve, originDate earliest; cap with originDate sort before slice(-POOL_MAP_BOUND) with POOL_MAP_BOUND=20 per WR-07 precedent. Scorer: distAtr = abs(poolMid minus lastClose) / atr with poolMid = (top+bottom)/2 and lastClose the last closed candle close; score = weight * dolBoost / (1 + distAtr) where dolBoost = DOL_BOOST=2.0 when side matches DOL direction (BSL with range-high DOL, SSL with range-low DOL, COMPRESSION resolves to nearer extreme via computePrimaryDOL) else 1.0; behind-price (BSL below lastClose, SSL above) multiplies by BEHIND_PENALTY=0.25; zero or empty ATR takes the explicit refuse-to-rank branch returning pools in originDate order with scores of 0 and a rankSkipped flag, never NaN or Infinity in the comparator; every constant carries the CALIBRATION-PROVISIONAL comment and is exported, with zero tuning beyond locked values. Add tests: merge-inside-quarter-ATR, no-merge beyond it, empty-ATR skip-merge, over-cap newest-20 with unsorted input, constant pins, Rank-1 DOL-side nearest ACTIVE, 2.0 boost ratio, 0.25 behind ratio, ATR-units scale invariance (same fixture scaled 10x ranks identically), zero-ATR finite-scores assert.</action>
  <verify>
    <automated>npm test -- src/lib/ict/pools.test.ts src/lib/ict/smt.test.ts src/lib/ict/purity.test.ts</automated>
    <fails_when>merge drops the more extreme edge or over-cap keeps oldest instead of newest or any score is NaN/Infinity or scaled-fixture ranking diverges</fails_when>
  </verify>
  <done>Merge, cap, and scorer ordering fully pinned with provisional constants exported and boundary-tested</done>
  <reversibility rating="costly">Scorer constants are consumed by Phase 20 ranking display; changing values later needs Phase 21 calibration review, code shape itself reverts cleanly</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller array -> pools.ts pure entry | untrusted candle arrays cross here (wrong types, forming rows, non-finite OHLC, unsorted dates) |
| ATR/DOL donors -> scorer | short-history ATR edges and DOL direction cross here into ranking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | pools.ts scorer numeric boundaries | high | mitigate | Finite-OHLC drop at entry, explicit zero/empty-ATR refuse-to-rank branch, epsilon-guarded tolerance compares, NaN-free comparator pinned by tests in this plan |
| T-19-03 | Spoofing (methodology) | equality bonus mega-pool dominance | medium | mitigate | Diminishing EQUAL_BONUS_STEPS curve (3.0/2.0/1.0/0.5) pinned by test so 4th/5th touches add strictly less per D-05 |
| T-19-SC | Tampering | npm installs | high | mitigate | No new packages; no install step in this plan |
</threat_model>

<verification>
Full pools.test.ts matrix plus smt.test.ts plus purity.test.ts green after every task commit in this plan.
</verification>

<success_criteria>
POOL-02 equality bonus, POOL-03 merge plus cap, POOL-04 lifecycle, and POOL-05 scorer ordering all pinned by boundary tests with provisional constants exported and suite green.
</success_criteria>

<output>
Create `.planning/phases/19-pools-math/19-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Expanded symbols from this plan: `EQUAL_BONUS_STEPS` diminishing curve plus full equality/lifecycle/merge/scorer logic in `src/lib/ict/pools.ts`; full POOL-02..05 boundary matrix in `src/lib/ict/pools.test.ts`.
