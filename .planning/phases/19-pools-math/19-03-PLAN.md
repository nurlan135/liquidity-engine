---
phase: 19-pools-math
plan: '03'
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/lib/store.ts
  - src/lib/store.test.ts
  - src/lib/ict/swings.test.ts
autonomous: true
requirements:
  - POOL-01
  - POOL-06
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: med
must_haves:
  truths:
    - Swing predicate pins live with the shared swings owner and smt.test.ts stays green as the zero-behavior-change gate
    - selectPools degrades honestly on stale, empty, and thin NQ legs with a sharedEpoch time truth and never throws
    - ES staleness never nulls D1 NQ pool geometry
    - Full 420-test suite stays green at phase exit with purity guard clean
  artifacts:
    - src/lib/ict/swings.test.ts
    - src/lib/store.ts (selectPools hardened with thin flag)
    - src/lib/store.test.ts (POOL-06 matrix)
  key_links:
    - selectPools output -> Phase 20 report section-1 plus chart lines (same array, no independent swept-ness per D-21)
    - computeRange thinHistory -> selectPools degraded flag (dim signal, not a null)
---

<objective>
Harden the guarded delivery contract: full selectPools degrade matrix (POOL-06 per D-21) plus shared-swings regression pins (POOL-01 gate per D-12/D-24) plus phase-exit full-suite proof.

Purpose: Guarantee pools degrade honestly in production (stale/empty/thin) and the swing extraction changed nothing, so Phase 20 can render one trusted array.
Output: Hardened selectPools with thin flag, extended store.test.ts matrix, swings.test.ts pins, 420-test suite green.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-pools-math/19-01-SUMMARY.md
@src/lib/store.ts
@src/lib/store.test.ts
@src/lib/ict/range.ts
@src/lib/ict/swings.ts
</context>

<tasks>

<task type="auto">
  <name>selectPools hardened degrade envelope plus POOL-06 matrix</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts sharedEpoch plus selectSMT guard plus selectTriggerPure envelope plus selectTicket degraded-flag shape (stale/thin/leg); src/lib/ict/range.ts thinHistory (closed.length less than window); src/lib/store.test.ts stale-refusal idiom plus ticket-refuse-null plus not.toThrow idiom plus ticket-coherence back-to-back epoch idiom</read_first>
  <acceptance_criteria>selectPools returns null on stale NQ leg with nq.lastError preserved, null on empty closed NQ leg, a degraded object with thin=true on thin history instead of null, a valid array when only ES is stale, null without throwing on garbage candle input, and identical epoch on back-to-back calls</acceptance_criteria>
  <action>Per D-21 harden selectPools in src/lib/store.ts: keep the NQ-D1-only guard (nq.stale true returns null, closedOnly(nq.candles).length equals 0 returns null, reason rides nq.lastError, never silent empty array); add thin-history degrade via computeRange thinHistory on the same NQ snapshot — thin yields the ranked result wrapped with { pools, asOf, epoch, degraded: { stale: false, thin: true, leg: 'nq' } } following the selectTicket degraded precedent instead of null, giving Phase 20 its dim signal; ES-stale explicitly ignored (no es.stale read in this selector); single const epoch = sharedEpoch(get) call per invocation; full try/catch returning null with zero set calls; D1 NQ candles only, no intraday reads. Extend src/lib/store.test.ts with the POOL-06 matrix using resetDualState plus setState stale flips plus not.toThrow: stale-NQ nulls with lastError preserved, empty-NQ nulls, ES-stale does not null pools, garbage candles null without throwing, thin leg returns thin=true flag, back-to-back calls share epoch. Must NOT wire pools into trigger/invalidation/ticket and must NOT emit prose or chart code per D-17.</action>
  <verify>
    <automated>npm test -- src/lib/store.test.ts</automated>
    <fails_when>thin history returns null instead of a flagged result or ES-stale nulls pools or garbage input throws into the test runner</fails_when>
  </verify>
  <done>POOL-06 matrix green: stale/empty null, thin flagged, ES-stale independent, never-throws, single sharedEpoch proven</done>
  <reversibility rating="reversible">Selector-only hardening plus additive tests; revert restores thin envelope with pure math untouched</reversibility>
</task>

<task type="auto">
  <name>Shared-swings regression pins plus phase-exit suite proof</name>
  <files>src/lib/ict/swings.test.ts</files>
  <read_first>src/lib/ict/smt.ts predicate sections plus constant pins; src/lib/ict/swings.ts exported surface from Plan 01; src/lib/ict/purity.test.ts self-scan allowlist</read_first>
  <acceptance_criteria>swings.test.ts pins strict-fractal edges (equality means no swing), k-validation throws, and boundary indices; smt.test.ts green unmodified; purity.test.ts green with no exclusions added; full npm test suite green at 420 baseline with zero new failures</acceptance_criteria>
  <action>Create src/lib/ict/swings.test.ts minimal per D-12: import SWING_K plus SWING_LOOKBACK plus isSwingHigh plus isSwingLow from ./swings; pin locked constants (SWING_K 2, SWING_LOOKBACK 60); strict-fractal edges (equal neighbor high means no swing high, equal neighbor low means no swing low); k validation throws on zero and non-integer; boundary indices (i less than k and i+k beyond length return false); mirror of the smt.test.ts leg() builder shape for one positive swing-high and one swing-low case. Then run the phase-exit proof: quick pools plus smt plus purity file, then the full npm test suite; any failure is fixed in this task (extraction must be move-only, no drive-by fixes to SMT logic per D-24). Must NOT add purity exclusions and must NOT tune provisional constants per D-22/D-23.</action>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite shows any red test or purity self-scan flags pools.ts/swings.ts or smt.test.ts diverges from its pre-extraction verdicts</fails_when>
  </verify>
  <done>swings.test.ts pins green, smt.test.ts green unmodified, purity.test.ts green, full 420-test suite green at phase exit</done>
  <reversibility rating="reversible">Additive test file plus verification run; no production code changed in this task</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store leg -> selectPools | stale/empty/thin NQ leg state crosses here into render |
| test runner -> suite baseline | 420-test green contract crosses here at phase exit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Information Disclosure (honesty) | selectPools degrade paths | high | mitigate | Stale/empty refuse-null with reason on nq.lastError; thin surfaced as degraded flag for Phase 20 dimming; full matrix pinned in store.test.ts in this plan |
| T-19-04 | Denial of Service (render crash) | selectPools throw surface | high | mitigate | try/catch to null on every path plus never-throws garbage test; null means degraded never a throw into render |
| T-19-SC | Tampering | npm installs | high | mitigate | No new packages; no install step in this plan |
</threat_model>

<verification>
store.test.ts POOL-06 matrix green; swings.test.ts plus smt.test.ts plus purity.test.ts green; full npm test suite green at phase exit per D-24.
</verification>

<success_criteria>
selectPools degrades honestly on stale/empty/thin legs with sharedEpoch and never throws; swing extraction proven zero-change; full suite green for Phase 20 handoff.
</success_criteria>

<output>
Create `.planning/phases/19-pools-math/19-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Hardened symbols from this plan: `selectPools` degraded envelope (`{ pools, asOf, epoch, degraded }`) in `src/lib/store.ts`; POOL-06 refuse-null matrix in `src/lib/store.test.ts`; predicate regression pins in `src/lib/ict/swings.test.ts`.
