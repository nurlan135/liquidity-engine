---
phase: 03-production-deploy-verification
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - src/lib/yahoo.ts
  - src/lib/yahoo-drill.test.ts
  - .planning/ROADMAP.md
autonomous: true
requirements: [DEPLOY-01]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - No drill code or drill test remains in the tree and no client-exposed env read exists
    - Production URL verifies green end-to-end after drill removal (headers, envelope, page)
    - Baku-date rendering and the rollover banner path read sensibly on the live URL (rendering spot-checks per D-10)
    - Unit logic (ICT, DST, freshness, rollover) plus lint plus build are green at the phase gate
  artifacts:
    - src/lib/yahoo.ts
    - scripts/verify-deploy.sh
  key_links:
    - Removal restores the exact reviewed serve-stale contract with no leftover branch
    - Final script run is the same script that proved the tracer in plan 03-01
    - Live spot-checks confirm rendering only; March and November DST plus rollover-suspect logic stay covered by unit suites
  prohibitions:
    - must NOT leave the drill flag, the drill env read, or the drill test file in the tree
    - must NOT add vercel.json or next.config.ts header config as part of cleanup
    - must NOT treat September rendering spot-checks as substitutes for the DST and rollover unit suites
  flagged_assumptions:
    - DEPLOY-01 spec-less probe returned one unclassified/unresolved row with no further decomposable edge predicates; scope is exactly the three roadmap success criteria closed by this gate
---

<objective>
Close the phase per D-07 and D-10: delete the temporary drill flag and drill test, re-verify production clean with the same script, spot-check Baku-date rendering and the rollover banner path on the live URL, and finish with the full gate green plus roadmap update.

Purpose: Prove nothing drill-specific ships to prod and the live URL meets all three roadmap success criteria in one final pass.
Output: Clean tree with the flag gone, production evidence plus spot-check notes in 03-04-SUMMARY.md, roadmap plans filled in.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-deploy-verification/03-CONTEXT.md
@src/lib/yahoo.ts
@src/lib/freshness.ts
</context>

<tasks>

<task type="auto">
  <name>Remove drill flag and drill test, restore clean lib</name>
  <files>src/lib/yahoo.ts, src/lib/yahoo-drill.test.ts</files>
  <read_first>src/lib/yahoo.ts</read_first>
  <action>Delete the temporary environment-read block at the top of fetchNQDaily in src/lib/yahoo.ts in full, restoring the function to open directly on the cache lookup line const cached = payloadCache.get(CACHE_KEY) with no leftover branch or comment, and delete src/lib/yahoo-drill.test.ts from disk. Then run the full gate: npm run lint plus npm test plus npm run build, all green. Confirm zero environment reads remain in the server lib and no stray drill file exists.</action>
  <verify>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any error or warning line in output</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any error or Failed to compile line in output</fails_when>
    <automated>! grep -q 'DRILL_FORCE_STALE' src/lib/yahoo.ts && test ! -f src/lib/yahoo-drill.test.ts && ! grep -rq 'process\.env' src/lib/</automated>
    <fails_when>non-zero exit, meaning drill text, the drill test file, or any env read remains in the server lib</fails_when>
  </verify>
  <done>Drill code fully removed with lint, tests, and build green and no env reads in the server lib.</done>
  <acceptance_criteria>npm run lint exits 0; npm test exits 0; npm run build exits 0; drill-text grep plus drill-file test plus env-read grep all pass negated; git status shows only intended modifications</acceptance_criteria>
</task>

<task type="auto">
  <name>Production gate: script green plus rendering spot-checks plus roadmap</name>
  <files>scripts/verify-deploy.sh, .planning/ROADMAP.md</files>
  <read_first>scripts/verify-deploy.sh, .planning/ROADMAP.md, src/lib/freshness.ts</read_first>
  <action>Re-run bash scripts/verify-deploy.sh per D-09 against the production URL recorded in the 03-03 summary and require all five PASS lines with the envelope reading stale false and source live or cache, proving the drill flag is gone from prod. Then perform the D-10 rendering spot-checks on the live production URL: open the terminal page and confirm Baku-time dates render sensibly on the chart axis and header (yyyy-MM-dd Baku strings from the candles, no UTC-shifted day), and confirm the rollover banner path by noting banner absent-or-present state with no crash and no fabricated levels. September has no live DST transition or rollover event, so these are rendering-only confirmations while March and November DST plus rollover-suspect logic stay proven by the unit suites. Then update .planning/ROADMAP.md Phase 3 section with an Edit scoped to that section only: set Plans to 4 plans with the four plan lines and brief objectives. Commit the plans plus roadmap per the commit footer.</action>
  <verify>
    <automated>npm run test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>bash scripts/verify-deploy.sh "$PROD_URL"</automated>
    <fails_when>non-zero exit, or any FAIL line in the script output, or a stale true envelope on production</fails_when>
    <human-check>
      <test>Open the production terminal URL and confirm chart plus header dates read as sensible Baku dates and note the rollover banner absent-or-present state with no crash</test>
      <expected>Dates render as Baku yyyy-MM-dd values consistent with the envelope; no UTC-shifted day; page renders with no error toast on fresh load</expected>
      <why_human>September has no live DST transition or rollover event to assert programmatically; only a human can confirm the rendered dates read sensibly per D-10</why_human>
    </human-check>
  </verify>
  <done>Production green after drill removal with rendering spot-checks noted and roadmap updated.</done>
  <acceptance_criteria>Script against PROD_URL exits 0 with five PASS lines and stale false; 03-04-SUMMARY.md records the production evidence plus the DST-date and rollover-banner spot-check notes; .planning/ROADMAP.md Phase 3 lists 4 plans with objectives</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drill code → production tree | Temporary test-only logic crosses into the shipped lib here |
| Live rendering → human judgment | Pixel-level date correctness crosses into eyeball verification here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | Drill flag surviving into production (ASVS V5) | high | mitigate | Full block deletion plus test-file deletion plus negated triple grep gate (flag text, file absence, env reads) plus production stale-false script re-verification proving the flag is gone from prod |
| T-03-09 | Information Disclosure | Live envelope parsed during final gate | low | mitigate | Same defensive script parsing as plan 03-01; no envelope bytes are committed to the repo, only PASS lines and the stale-false observation |
| T-03-10 | Tampering | September spot-check mistaken for logic proof | low | mitigate | Spot-checks explicitly rendering-only per D-10; March and November DST plus rollover-suspect suites stay green in the same gate run |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: removal plus script plus docs edits only, so no blocking-human package checkpoint is owed |
</threat_model>

<verification>
Drill gone by triple-negated grep, full gate green, production script green with stale false, spot-checks noted, roadmap updated.
</verification>

<success_criteria>
All three roadmap success criteria hold on the live URL with a clean tree: headers verified, drill passed and removed, checklist green.
</success_criteria>

<output>
Create `.planning/phases/03-production-deploy-verification/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 04 removes: the DRILL_FORCE_STALE block from src/lib/yahoo.ts (restored to open on the cache lookup) and src/lib/yahoo-drill.test.ts (deleted). Plan 04 finalizes: production gate evidence plus D-10 rendering spot-check notes in 03-04-SUMMARY.md and the Phase 3 plan list in .planning/ROADMAP.md. No new source symbols ship; the phase net-creates scripts/verify-deploy.sh only.
