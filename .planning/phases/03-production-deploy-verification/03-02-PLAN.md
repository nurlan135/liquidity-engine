---
phase: 03-production-deploy-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPLOY-01]

estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Local default branch is main and origin/main exists and tracks it
    - No work remains on master locally; origin/master deletion is sequenced after the GitHub default retarget
    - Vercel connection in plan 03-03 can select main as production branch with no rename churn after connecting
  artifacts: []
  key_links:
    - Local main tracks origin/main so the first Vercel import builds the renamed branch
    - GitHub default-branch retarget follows the push so open clones and future GSD bases follow the new name
  prohibitions:
    - must NOT connect Vercel before the rename lands (dashboard or CLI would pin the old name)
    - must NOT delete origin/master before the GitHub default is retargeted to main
---

<objective>
Execute the one-way master to main rename per D-02 before any Vercel connection, with an explicit confirmation gate since undo means re-pointing every clone, open PR, and future branch base.

Purpose: Vercel and GitHub default to main; renaming first keeps the production branch, preview branches, and all future GSD bases on the conventional name with a single churn event.
Output: Local branch main pushed to origin with tracking set; GitHub default retarget sequenced via human check.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-deploy-verification/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the one-way default-branch rename master to main per locked D-02 before the executor runs it</decision>
  <context>D-02 is locked: rename before connecting Vercel. Undo cost is a second rename event across every clone, open PR, and future GSD branch base, so the run stops here for explicit confirmation even though the direction itself is already decided.</context>
  <options>
    <option id="proceed">
      <name>Proceed with rename now (recommended, implements D-02)</name>
      <pros>Single churn event before Vercel exists; production branch, preview branches, and GSD bases all start on main</pros>
      <cons>Every existing clone must re-point; origin/master lingers until the GitHub default is retargeted</cons>
    </option>
    <option id="abort">
      <name>Abort the phase here</name>
      <pros>No rename churn if the timing is wrong</pros>
      <cons>Vercel connection cannot proceed under D-02 sequencing; the whole phase stalls</cons>
    </option>
  </options>
  <resume-signal>Type proceed to rename, or abort to stop the phase</resume-signal>
</task>

<task type="auto">
  <name>Rename master to main and push with tracking</name>
  <reversibility rating="one-way">Renames the published default branch per D-02; every clone, open PR, and future GSD branch base follows the new name and undo is a second rename event.</reversibility>
  <files>none - git branch operation only, no repo files change</files>
  <read_first>.planning/phases/03-production-deploy-verification/03-CONTEXT.md</read_first>
  <action>Confirm a clean tree first with git status --porcelain printing nothing, then run git branch -m master main per D-02 followed by git push -u origin main so local main tracks origin/main. Do not delete origin/master in this task: it stays until the GitHub default is retargeted. gh CLI is absent, so the default retarget happens in the GitHub web UI and is covered by the human check below.</action>
  <verify>
    <automated>git branch --show-current | grep -qx main</automated>
    <fails_when>non-zero exit, meaning the current branch is not main</fails_when>
    <automated>git ls-remote --heads origin main | grep -q main</automated>
    <fails_when>non-zero exit, or empty output meaning origin/main was not pushed</fails_when>
    <automated>git status --porcelain | grep -q .; test $? -ne 0</automated>
    <fails_when>non-zero exit from the test, meaning the tree is dirty after the rename</fails_when>
    <human-check>
      <test>Open the GitHub repo Settings and change the default branch from master to main, then delete the origin/master branch</test>
      <expected>Default branch reads main; origin/master no longer listed; open PRs if any retarget to main</expected>
      <why_human>gh CLI is absent so only a human can retarget the remote default in the browser UI</why_human>
    </human-check>
  </verify>
  <done>Local main tracks origin/main on a clean tree; remote default retarget is confirmed by the human check.</done>
  <acceptance_criteria>git branch --show-current prints main; git ls-remote --heads origin main is non-empty; git status --porcelain is empty; 03-02-SUMMARY.md records the new default and the origin/master deletion sequencing</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local rename → remote default | Branch identity crosses into every clone and PR base here |
| Old master ref → new clones | Stale origin/master crosses into fresh checkouts until deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | Default-branch rename (D-02) | medium | mitigate | Rename lands before any Vercel connection per D-02; clean-tree gate first; push with tracking; origin/master deletion sequenced after the remote-default retarget so no push lands on the orphaned name |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: git operations only, so no package-legitimacy checkpoint is owed |
</threat_model>

<verification>
Current branch main, origin/main pushed with tracking, clean tree, remote-default retarget confirmed.
</verification>

<success_criteria>
The repo stands on main end to end and plan 03-03 can connect Vercel with main as the production branch.
</success_criteria>

<output>
Create `.planning/phases/03-production-deploy-verification/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 02 creates no files: it renames the git default branch master to main per D-02 (local rename plus push with tracking) behind a one-way confirmation gate. Recorded in 03-02-SUMMARY.md only.
