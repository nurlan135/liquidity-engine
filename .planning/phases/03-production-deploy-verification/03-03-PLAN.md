---
phase: 03-production-deploy-verification
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: []
autonomous: true
requirements: [DEPLOY-01]
user_setup:
  - service: vercel
    why: "Only the user can confirm the Vercel account (D-01) and click through project import plus GitHub connection plus Preview-scoped env var; the executor drives what is drivable via CLI and dashboard instructions"
    env_vars:
      - name: DRILL_FORCE_STALE
        source: "Set to 1 in the Vercel dashboard on the Preview environment only for the drill window, deleted immediately after"
    dashboard_config:
      - task: "Import the repo as a NEW Vercel project (D-01) with GitHub-connected auto-deploys (D-03), main as production branch, vercel.app subdomain (D-04)"
        location: "Vercel dashboard -> Add New -> Project -> Import Git Repository"
      - task: "Add DRILL_FORCE_STALE=1 scoped to Preview only, then redeploy the preview before the drill"
        location: "Vercel dashboard -> Project -> Settings -> Environment Variables"
      - task: "Delete DRILL_FORCE_STALE and redeploy the preview clean after the drill passes"
        location: "Vercel dashboard -> Project -> Settings -> Environment Variables"

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A new Vercel project serves the app on a vercel.app URL with GitHub-connected auto-deploys and main as production branch
    - Fresh preview proxy responses carry s-maxage plus SWR while stale and 502 branches carry no-store, verified through the CDN
    - With the upstream killed on the preview URL only, cached candles render with the stale flag and no silent fresh response
    - The drill flag is removed after the drill and the preview verifies clean again
  artifacts: []
  key_links:
    - Plan 03-01 verify script asserts the Vercel-served headers through x-vercel-cache
    - Preview-scoped env var is the only drill input; nothing drill-specific ships to prod
    - Weekday scheduling separates real STALE from weekend CLOSED (today is Saturday)
  prohibitions:
    - must NOT run the kill drill on the production deployment (preview URL only per D-06)
    - must NOT scope the drill flag to Production or leave it set after the drill window
    - must NOT use a query-param kill-switch (CDN cache-key pollution); env var only
---

<objective>
Connect the renamed repo to a new GitHub-backed Vercel project per D-01 and D-03 through D-05, verify CDN headers through the edge per D-09, then run the stale-serve drill on a preview URL per D-06 through D-08 and remove the flag per D-07.

Purpose: This is the production proof of the Phase 1 serve-stale contract and the Phase 2 status-strip states through real Vercel infrastructure.
Output: Live project URLs plus drill evidence recorded in 03-03-SUMMARY.md; no repo files change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-deploy-verification/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Vercel project connection plus edge header verification</name>
  <files>none - process-only, the script is executed not modified</files>
  <read_first>scripts/verify-deploy.sh</read_first>
  <action>Drive the dashboard path only: instruct the user to import the repo as a NEW Vercel project (D-01) with GitHub-connected auto-deploys (D-03), main as the production branch, and a vercel.app subdomain (D-04), then capture the production URL they report. Confirm the D-05 flow while there: every push to main auto-deploys and rollback is a dashboard redeploy of a prior build with no manual promotion gate. Dashboard-only config per discretion: no vercel.json and no next.config.ts headers changes, since function-set headers in app/api/yahoo/route.ts already override config for the same route. Then run bash scripts/verify-deploy.sh against the production URL from plan 03-01: require the WARMUP, HEADERS, ENVELOPE, and PAGE PASS lines, and read x-vercel-cache across the two HEADs to confirm MISS then HIT behavior. If the first request returns 502, run the script once more before scoring: a cold CDN plus cold module-level payloadCache legitimately 502s per contract, and the warm-up sequence distinguishes that from a real outage. Record the production URL, the header values observed, and the x-vercel-cache progression in the plan summary.</action>
  <verify>
    <automated>test -x scripts/verify-deploy.sh && bash -n scripts/verify-deploy.sh</automated>
    <fails_when>non-zero exit, meaning the script is missing, non-executable, or has a syntax error</fails_when>
    <human-check>
      <test>Create the NEW Vercel project via dashboard import with GitHub auto-deploys, main as production branch, and vercel.app subdomain, then report the production URL; re-run the script against it if the first attempt 502s on a cold origin</test>
      <expected>Production URL serves the terminal; the script prints WARMUP, HEADERS, ENVELOPE, and PAGE PASS lines with x-vercel-cache progressing MISS then HIT</expected>
      <why_human>Vercel account confirmation per D-01 plus dashboard project import plus GitHub connection are browser-only actions no CLI in this environment can complete unattended</why_human>
    </human-check>
  </verify>
  <done>A new GitHub-connected project serves the app on a vercel.app URL with edge headers verified through the CDN.</done>
  <acceptance_criteria>03-03-SUMMARY.md records the production URL, the cache-control value containing s-maxage=60 plus stale-while-revalidate=30, the x-vercel-cache MISS-to-HIT progression, and confirmation that no vercel.json was added</acceptance_criteria>
</task>

<task type="auto">
  <name>Preview stale-serve drill plus flag removal</name>
  <files>none - process-only, the script is executed not modified</files>
  <read_first>scripts/verify-deploy.sh, src/lib/freshness.ts</read_first>
  <action>Run the drill on a PREVIEW deployment only per D-06, never production. Sequence: open a preview deployment from any non-main branch (D-03 preview URL), warm it by running the plan 03-01 script once so source live or cache is observed, then instruct the user to add the Preview-scoped temporary flag per D-07 and redeploy the preview. With the flag active, curl the preview proxy body, parse it defensively failing closed on malformed JSON per the V5 shape-guard discipline, and require the D-08 success shape of stale strictly true with source stale plus at least one candle present, and confirm the page still returns HTTP 200 with no silent 200-fresh envelope. Schedule the drill on a Baku weekday: deriveStatus in src/lib/freshness.ts returns CLOSED on Baku Saturday or Sunday regardless of the envelope flag, so a Saturday run scores CLOSED plus cached candles instead of STALE and must not be misread as drill failure (today 2026-09-06 is Saturday). Then instruct the user to delete the Preview variable and redeploy clean, and re-run the script to confirm stale false with a live or cached source again. If the flag is ever observed on Production, stop and delete it before any further check.</action>
  <verify>
    <automated>test -x scripts/verify-deploy.sh && bash -n scripts/verify-deploy.sh</automated>
    <fails_when>non-zero exit, meaning the script is missing, non-executable, or has a syntax error</fails_when>
    <human-check>
      <test>On the preview URL only: warm the preview, set Preview-scoped DRILL_FORCE_STALE equal to 1 and redeploy, confirm the proxy envelope reads stale true with cached candles and the page stays HTTP 200, then delete the variable, redeploy clean, and re-run the script to confirm stale false</test>
      <expected>Drill window shows stale true with cached candles and no silent fresh response; after removal the preview verifies clean; Production never carries the flag</expected>
      <why_human>Setting and deleting a Preview-scoped Vercel env var plus preview redeploys are dashboard-only actions in this environment</why_human>
    </human-check>
  </verify>
  <done>Stale-serve drill passes on preview with the flag removed and the preview reverified clean.</done>
  <acceptance_criteria>03-03-SUMMARY.md records the preview URL, the stale true envelope observation with weekday note (STALE on weekday, CLOSED plus cached candles on Baku weekend), the flag deletion plus clean redeploy, and the post-removal stale false re-verification</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard env var → preview function | Drill input crosses into server-only code here |
| Preview envelope → drill scoring | Untrusted preview JSON crosses into pass-fail judgment here |
| Preview flag → production | A leaked flag crosses into the live terminal here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | Drill flag left enabled on preview or leaking to production (ASVS V5) | high | mitigate | Preview-only scope, time-boxed window, explicit delete plus clean-redeploy plus stale-false re-verification steps; stop-and-delete rule if ever observed on Production |
| T-03-06 | Information Disclosure | Preview 502 message during drill | low | mitigate | Neutral drill message reveals no internals; page stays HTTP 200 with cached candles so no stack detail surfaces |
| T-03-07 | Denial of Service | Drill misread on cold origin or Baku weekend | low | mitigate | Warm-up before scoring distinguishes cold-origin 502 from outage; weekday scheduling separates real STALE from weekend CLOSED per deriveStatus |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: dashboard setup plus curl plus node only, so no blocking-human package checkpoint is owed |
</threat_model>

<verification>
Production headers green through the edge, preview drill shows stale with cached candles, flag deleted, preview clean again.
</verification>

<success_criteria>
Roadmap success criteria 1 (headers verified) and 2 (drill green) hold on real Vercel infrastructure with nothing drill-specific left set.
</success_criteria>

<output>
Create `.planning/phases/03-production-deploy-verification/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 03 creates no repo files: it produces the live Vercel project (GitHub-connected, main as production branch, vercel.app subdomain), the production header verification evidence, and the preview drill evidence with flag removal. All recorded in 03-03-SUMMARY.md.
