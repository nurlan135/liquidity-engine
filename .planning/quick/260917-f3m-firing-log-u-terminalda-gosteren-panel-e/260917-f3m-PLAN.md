---
quick: 260917-f3m-firing-log-u-terminalda-gosteren-panel-e
plan: 260917-f3m-PLAN
type: execute
tasks: 2
autonomous: true
files_modified:
  - components/dashboard/firing-log-panel.tsx
  - components/dashboard/terminal-shell.tsx
---

<objective>
`firingLog` state-i TerminalShell-də görünən panelə çıxarılsın — operator
trigger tarixçəsini (verdict, gates, session) terminalda birbaşa oxusun.

Purpose: `firingLog` bugün yalnız store-da yaşayan gizli state-dir
(`src/lib/store.ts:506-507`, cap 50 + overflow sayğacı); kalibrləmə siqnalı
(TRIG-04: həftədə 1-4 fire) terminalda görünmür. Panel bu boşluğu bağlayır —
riyaziyyatsız, yalnız oxuyan render.
Output: `FiringLogPanel` komponenti + TerminalShell sol sütunda montaj,
skeleton/boş/dolu halları konvensiyaya uyğun, aidiyyəti suitlər yaşıl.
</objective>

<context>
- `src/lib/store.ts:506-507` — `firingLog: FiringLogEntry[]`, `firingLogOverflow: number`
- `src/lib/store.ts:1027-1061` — `appendFiringLog` semantika: yalnız ARMED-və-yaxşı,
  WAIT yazılmır, FIRE sessiya başına dedup olunur, cap-dən köhnələr düşür + sayğac
- `src/lib/ict/trigger.ts:71-80` — `FiringLogEntry` forması: `asOf` (epoch saniyə),
  `verdict`, `gates { timing, purge, displacement }`, `direction`, `reasonKey`,
  `sessionDate` (NY günü açarı)
- `components/dashboard/ticket-panel.tsx:19,30-36,62-69` — panel presedenti:
  `'use client'`, `EMPTY_COPY = 'Məlumat yoxdur'`, `lastUpdatedISO === null`
  skeleton, stabil selektor-funksiya abunəsi + render zamanı törəmə
- `components/dashboard/module-4.tsx:8-13` — yüngül panel presedenti:
  `selectTrigger`/`selectTicket` stabil abunə, `trigger === null && ticket === null`
  olduqda `EMPTY_COPY`
- `components/dashboard/terminal-shell.tsx:295-302` — sol sütun sırası:
  SentimentPanel, LiquidityMap, Module1, Module3, Module4, SmtRow; montaj nöqtəsi
  `<SmtRow />`-dən sonradır (trigger konteksti Module4-ün yanında qalır)
- `src/terminal-shell.test.ts` — uşaq sayı iddiası yoxdur (yalnız data-slot
  sorğuları + toast/chart-capture sayları), yeni panel mövcud testləri sındırmır
</context>

<tasks>

<task type="auto">
  <name>Task 1: FiringLogPanel komponenti</name>
  <files>components/dashboard/firing-log-panel.tsx</files>
  <action>Yeni `FiringLogPanel` komponenti yarat (ticket-panel.tsx + module-4.tsx
  konvensiyası): `'use client'`, kökdə `Card data-slot="firing-log"`, başlıq
  `Atəş Jurnalı` (CardTitle, ticket-panel sətr 59 presedenti — kiçik uppercase
  trackinq stili). Store-dan üç birbaşa abunə: `lastUpdatedISO`, `firingLog`,
  `firingLogOverflow` — massiv üzərində useShallow lazım deyil (düz massivdir),
  törəmə (newest-first sıra) render zamanı aparılır, heç bir set çağırışı yoxdur.
  Üç hal: (1) `lastUpdatedISO === null` → skeleton (ticket-panel sətr 62-67
  presedenti, animate-pulse divləri); (2) `firingLog.length === 0` → verbatim
  `Məlumat yoxdur` (EMPTY_COPY konstanti, heç vaxt dəyişdirilməsin); (3) dolu →
  `firingLogOverflow > 0` olduqda `data-slot="firing-log-overflow"` sətri
  (`+N köhnə qeyd` mətni ilə, N rəqəmi state-dən), ardından girişlər newest-first
  (`slice().reverse()`, store kronoloji append edir — sətr 1051). Hər sətir
  `data-slot="firing-log-entry"` daşıyır və atribut kimi `data-verdict`,
  `data-direction`, `data-session`, `data-asof` (xam epoch, formatlanmamış) verir;
  görünən mətn: verdict + direction (`FIRE_LONG · LONG` kimi, verbatim dəyərlər),
  gate üçlüyü Azərbaycan etiketləri ilə (`Zaman`, `Süpürmə`, `Displacement` —
  boolean-lər ✓/✗ işarəsi ilə, yeni söz yaradılmasın), `reasonKey` + `sessionDate`
  verbatim. Saat divar mətninə ÇEVRİLMƏSİN — `asOf`-u Bakı/NY saatına formatlamaq
  qadağandır (TZ bug sinfi; sessionDate artıq NY günü açarıdır, sıra newest-first
  ilə aydındır). Komponent tam riyaziyyatsızdır: evaluateTrigger/computeTicket
  çağırışı, fetch, set, Button — heç biri yoxdur.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Kompilasiya təmizdir; fayl `data-slot="firing-log"` kökü, skeleton, verbatim
  `Məlumat yoxdur`, newest-first `data-slot="firing-log-entry"` sətirləri və
  overflow xətti daşıyır; store-a yazı, saat formatı, riyaziyyat yoxdur.</done>
</task>

<task type="auto">
  <name>Task 2: TerminalShell montajı + reqressiya</name>
  <files>components/dashboard/terminal-shell.tsx</files>
  <action>TerminalShell-ə `FiringLogPanel` importu əlavə et və sol sütunda
  `<SmtRow />`-dən dərhal sonra render et (sətr 295-302 bloku; SƏBƏB: firing log
  trigger tarixçəsidir, Module4-ün canlı triggeri ilə eyni sütunda qalmalıdır —
  mərkəz chart sütunu və sağ ticket sütunu toxunulmur). Başqa heç bir shell məntiqi
  dəyişmir: prop drilling yoxdur (panel özü store-a abunədir), mövcud panellərin
  sırası dəyişmir, yalnız bir sətir əlavə olunur. Sonra reqressiya: terminal-shell
  suite-i + store suite-i işə sal; yeni panelin uşaq-sayı iddiası sındırmadığını
  təsdiqlə.</action>
  <verify>
    <automated>npx vitest run src/terminal-shell.test.ts src/lib/store.test.ts</automated>
  </verify>
  <done>Panel sol sütunda SmtRow-dan sonra görünür; hər iki suite yaşıl; `npx eslint
  components/dashboard/firing-log-panel.tsx components/dashboard/terminal-shell.tsx`
  təmizdir.</done>
</task>

</tasks>

<verification>
`npx vitest run src/terminal-shell.test.ts src/lib/store.test.ts` — hamısı yaşıl.
`npx tsc --noEmit` təmiz. `npx eslint` yeni/dəyişən fayllarda təmiz.
</verification>

<success_criteria>
- Terminal sol sütunda `Atəş Jurnalı` paneli görünür (SmtRow-dan sonra)
- Boş log → verbatim `Məlumat yoxdur`; poll başlamamış → skeleton
- Dolu log → newest-first sətirlər: verdict · direction, Zaman/Süpürmə/Displacement
  ✓/✗, reasonKey + sessionDate; cap aşımı → overflow sətri
- `asOf` saat mətninə çevrilmir (yalnız `data-asof` atributu); TZ məntiqi yoxdur
- Store yazılmır, trigger/ticket riyaziyyatı çağırılmır, mövcud testlər yaşıl
</success_criteria>

<output>
Dəyişikliklər işçi ağacda qalır (quick-fix, commit plan icraçısına aiddir).
</output>
