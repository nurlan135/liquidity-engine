---
phase: 16-fatal-flaw-invalidation
plan: '02'
type: execute
wave: 2
depends_on:
- 16-01
files_modified:
- src/lib/ict/invalidation.ts
- src/lib/ict/invalidation.test.ts
- src/lib/store.test.ts
autonomous: true
requirements:
- FLAW-01
- FLAW-03
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User reads a falsifiable fatal-flaw sentence selected from a fixed rule table keyed on bias direction and nearest unswept pool, test-pinned verbatim
  - User reads a challenge question from a fixed 3-entry bank selected deterministically by the dominant trap, test-pinned verbatim
  - Selector-level flaw behavior is coherent: stale legs refuse with null and the flaw output shares the trigger epoch with no second clock read
  artifacts:
  - src/lib/ict/invalidation.ts
  - src/lib/ict/invalidation.test.ts
  - src/lib/store.test.ts
  key_links:
  - Sentence table resolves purely from trigger.direction plus judas.sweepSide on the D-05 snapshot, never new row reads
  - Challenge selection priority (SMT flaw, then opposite-sweep, else standing caution) is a code-ordering fact
  - store.test.ts flaw cases copy the trigger-stale and trigger-coherence idiom line-for-line
---

<objective>
Expansion: lock the falsifiable sentence table plus challenge bank as verbatim Azerbaijani with toBe pins, and prove selector-level coherence with flaw-stale and flaw-coherence store cases.
Implements D-04, D-08, D-09, D-10, D-13, D-14 (see task actions; D-01, D-02, D-05, D-06, D-07, D-12, D-15 land in 16-01).

Purpose: Turn the tracer's minimal strings into the full §6-ready contract Phase 17 renders verbatim, with selector behavior pinned.
Output: Full prose tables in the flaw module, verbatim prose pins, store selector tests.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fatal-flaw-invalidation/16-CONTEXT.md
@.planning/phases/16-fatal-flaw-invalidation/16-RESEARCH.md
@.planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Sentence table plus challenge bank plus verbatim prose lock per D-08 D-09 D-10 D-13</name>
  <files>src/lib/ict/invalidation.ts</files>
  <read_first>src/lib/ict/invalidation.ts, src/lib/ict/trigger.ts, src/lib/ict/smt.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Extend the tracer src/lib/ict/invalidation.ts prose section into the full D-08 plus D-09 plus D-10 contract, keeping the disjunction body untouched. Draft the following verbatim Azerbaijani templates at implementation time exactly as written here, following the trigger D-09 precedent of one base sentence per key plus fixed strings only with no number or price interpolation and no template-literal dollar-brace placeholders. Lock five flaw reasons in a REASON_BY_KEY-style record: ROLLOVER_WEEK reason 'FATAL FLAW (HARD): Rollover həftəsi korrupsiyası — kotirovka strukturu etibarsızdır, quraşdırma ləğv edildi.', STALE_LEG reason 'FATAL FLAW (HARD): Ayaq bayatdır — qiymət axını köhnəlib, quraşdırma ləğv edildi.', SMT_SUPPRESSED reason 'FATAL FLAW (SOFT): SMT basdırılıb — korrelyasiya pozulub, atəş ARMED-ə endirildi.', OPPOSITE_SWEEP reason 'FATAL FLAW (SOFT): Əks istiqamətli süpürmə təsdiqləndi — atəş ARMED-ə endirildi.', NONE reason 'Qüsur yoxdur: trigger hökmü qüvvədədir.' Lock four per-flaw unblock templates per D-13 as fixed strings: ROLLOVER_WEEK unblock 'Blok açılır: növbəti müqavilə həftəsi təmiz kəsir gətirəndə.', STALE_LEG unblock 'Blok açılır: bütün ayaqlar təzə kotirovka göstərəndə.', SMT_SUPPRESSED unblock 'Blok açılır: korrelyasiya 0.70-dən yuxarı bərpa olanda.', OPPOSITE_SWEEP unblock 'Blok açılır: əks süpürmə təsdiqsiz qalıb istiqamət bərpa olunanda.', NONE unblock empty string. Lock the D-08 falsifiable sentence as a 4-cell SENTENCE_BY_KEY lookup on trigger.direction crossed with judas.sweepSide, resolved purely from the D-05 snapshot with no new row reads: LONG crossed with LOW yields 'FALSİFİKASİYA LONG: BSL reydi baş verməyibsə və qiymət premiumda qalırsa, LONG oxunuşu yanlışdır — BSL süpürməsi bu ssenarini təsdiqləyir.', SHORT crossed with HIGH yields 'FALSİFİKASİYA SHORT: SSL reydi baş verməyibsə və qiymət discountda qalırsa, SHORT oxunuşu yanlışdır — SSL süpürməsi bu ssenarini təsdiqləyir.', any off-diagonal or null-side combination yields the neutral fallback 'FALSİFİKASİYA: təsdiqlənmiş süpürmə istiqaməti qərəzlə uzlaşmır — əks istiqamətli qapanış bu ssenarini puç edir.' Lock the D-09 challenge bank as exactly three fixed entries keyed premium-chase, pre-news-engineering, smt-divergence-ignored: premium-chase 'Sual: premium zonada qovmursanmı — giriş FVG-yə qayıdışı gözlədinmi?', pre-news-engineering 'Sual: bu hərəkət xəbər-öncəsi mühəndislik deyilmi — təqvim pəncərəsini yoxladınmı?', smt-divergence-ignored 'Sual: SMT fərqliliyini görməzdən gəlmirsənmi — korrelyasiya 0.70-i bərpa etməyibsə, niyə indi?' Wire deterministic selection by dominant trap as code order: SMT SOFT flaw selects smt-divergence-ignored, else opposite-sweep SOFT flaw selects premium-chase, else HARD flaw or clean selects pre-news-engineering as the standing caution. Attach sentence plus challenge to every FatalFlawOutput including clean verdicts so Phase 17 always has §6 strings. Every non-clean verdict keeps reason plus flawClass plus unblock attached per D-04 so the banned generic label never appears; INVALIDATED stays neutral informative per D-14 with no styling code here.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/invalidation.test.ts -t "prose"</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Source assertion: src/lib/ict/invalidation.ts exports the reason record plus SENTENCE_BY_KEY 4-cell table plus 3-entry challenge bank with the selection priority coded. Behavior assertion: LONG plus LOW sweep input yields the LONG falsifiable sentence, SHORT plus HIGH yields the SHORT sentence, SMT flaw yields the smt-divergence-ignored challenge, clean yields the standing-caution challenge. Test command: npx vitest run src/lib/ict/invalidation.test.ts -t "prose" passes.</acceptance_criteria>
  <done>Full sentence table plus challenge bank ship as verbatim templates with deterministic selection and are committed.</done>
</task>

<task type="auto">
  <name>Verbatim prose pins plus bank-size plus ban assertions per D-04 D-10</name>
  <files>src/lib/ict/invalidation.test.ts</files>
  <read_first>src/lib/ict/invalidation.ts, src/lib/ict/trigger.test.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Extend src/lib/ict/invalidation.test.ts with a prose describe block copying the trigger.test.ts lines 134-160 verbatim-pin idiom. Pin every reasonKey reason plus every sentence-table cell plus every challenge-bank entry with exact toBe equality against the locked Azerbaijani strings from the module task (ROLLOVER_WEEK, STALE_LEG, SMT_SUPPRESSED, OPPOSITE_SWEEP, NONE reasons; LONG-LOW, SHORT-HIGH, fallback sentences; premium-chase, pre-news-engineering, smt-divergence-ignored challenges). Assert the bank holds exactly 3 entries and selection is deterministic: same SMT-flaw input twice yields the same challenge string, same sweep-flaw input twice yields the same challenge string. Assert the 4-cell sentence table including the null-sweepSide fallback row. Assert the ban: no non-clean reason equals the banned generic single-word label, and every non-clean output carries a non-empty unblock string. Assert fixed-threshold discipline: the SMT unblock names the fixed 0.70 threshold string and no sentence or challenge template contains a live interpolated reading.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/invalidation.test.ts -t "prose"</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: all five reasons plus three sentences plus three challenges pinned with toBe equality, bank size exactly 3, deterministic selection on repeat calls. Test command: npx vitest run src/lib/ict/invalidation.test.ts -t "prose" passes with the ban assertion green. Source assertion: prose describe block with toBe pins exists.</acceptance_criteria>
  <done>Verbatim prose is locked by tests and any wording drift fails loudly.</done>
</task>

<task type="auto">
  <name>Store flaw-stale plus flaw-coherence selector cases per FLAW-01</name>
  <files>src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/store.test.ts, src/lib/ict/invalidation.ts, .planning/phases/16-fatal-flaw-invalidation/16-PATTERNS.md</read_first>
  <action>Extend src/lib/store.test.ts with two flaw selector cases copying the trigger-block idiom at lines 1110-1174 line-for-line in structure. Add flaw-stale mirroring the trigger-stale shape: resetDualState plus stubFourLegs plus refresh, then setState marking nq15m stale true with lastError text, then assert selectFatalFlaw returns null without throwing. Add flaw-coherence mirroring the trigger-coherence shape: back-to-back selectFatalFlaw calls on the same live London confirmed-sweep state return equal outputs, the flaw asOf equals the trigger epoch from selectTrigger on the same poll proving no second clock read, and the flaw output carries the trigger verdict context via carriedArmedReason or clean NONE. Also assert flaw purity at the boundary: with trigger null from stale legs the selector returns null and never a SOFT downgrade. Run the purity guard alongside since invalidation.ts must stay clock-free and store-free via the auto-covering glob.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: stale nq15m forces selectFatalFlaw null, back-to-back calls return equal outputs sharing the trigger epoch. Test command: npx vitest run src/lib/store.test.ts passes. CLI output: npx vitest run src/lib/ict/purity.test.ts passes with the new module auto-scanned.</acceptance_criteria>
  <done>Selector-level flaw behavior is pinned: refuse-null on stale, epoch coherence with the trigger, never-throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flaw-output→§6-render | Sentence plus challenge strings cross here into the Phase 17 §6 block; fixed templates only, no interpolated prices or live readings |
| store-selector→render | Degraded null versus flaw verdict crosses here; selector never throws into render |
| prose→replay | Byte-identical strings cross here into Phase 18 replay; determinism test proves equality |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information disclosure | Sentence plus challenge plus reason strings | low | mitigate | Fixed verbatim templates with toBe pins; no number or price interpolation so replay equality holds and verdict strings can never be mistaken for live numbers |
| T-16-03 | Tampering | Challenge-bank selection | low | accept | Deterministic priority order (SMT flaw, then opposite-sweep, else standing caution) makes selection a code-ordering fact with no hidden state |
| T-16-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; package-legitimacy gate skipped with reason since only installed vitest plus date-fns-tz plus zustand are used |
</threat_model>

<verification>
Per-task vitest runs plus npx vitest run src/lib/ict/ plus npx tsc --noEmit at plan merge plus full npx vitest run gate before verify-work.
</verification>

<success_criteria>
Sentence 4-cell table plus 3-entry challenge bank verbatim-pinned, ban assertion green, flaw-stale and flaw-coherence store cases green, purity guard green, full suite green.
</success_criteria>

<output>
Create `.planning/phases/16-fatal-flaw-invalidation/16-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ict/invalidation.ts: REASON_BY_KEY record (ROLLOVER_WEEK, STALE_LEG, SMT_SUPPRESSED, OPPOSITE_SWEEP, NONE), SENTENCE_BY_KEY 4-cell direction-cross-sweepSide table plus neutral fallback, 3-entry challenge bank (premium-chase, pre-news-engineering, smt-divergence-ignored) with dominant-trap selection order, per-flaw unblock templates with fixed 0.70 threshold string
- src/lib/ict/invalidation.test.ts: prose describe block with toBe pins on all reasons plus sentences plus challenges, bank-size assertion, determinism assertion, generic-label ban assertion
- src/lib/store.test.ts: flaw-stale refuse-null case, flaw-coherence epoch-sharing case, trigger-null-never-SOFT case
