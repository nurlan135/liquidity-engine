---
phase: 17-paper-ticket-4-6-live-ui-chart-pins
plan: '04'
type: execute
wave: 3
depends_on:
- 17-03
files_modified:
- components/charts/nq-chart.tsx
autonomous: true
requirements:
- TICK-04
estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: high
must_haves:
  truths:
  - Chart shows T trigger pin arrow on the FIRE bar on EXECUTE only with J to S to T ordering
  - Chart shows entry SL TP1 TP2 TP3 accent price lines on EXECUTE only reusing EQ DOL lifecycle
  - STAND ASIDE or INVALIDATED clears pins and lines with no ghost what-if levels
  truths_ref: 17-UI-SPEC.md chart trigger pin plus cleared rows, 17-CONTEXT.md D-09 D-10
  artifacts:
  - components/charts/nq-chart.tsx
  key_links:
  - Consumes the exact ticket prop names Plan 03 passes: ticketVerdict ticketDirection ticketEntry ticketSL ticketTP1 ticketTP2 ticketTP3 fireBarDate
  - T pin appended third in buildOverlayMarkers after Judas then SMT; ticket lines follow Asia-pair remove-then-create in both mount and update effects
---

<objective>
Chart execution overlays: T trigger pin plus entry SL TP price lines, additive props-only, cleared on STAND ASIDE. Covers the chart half of TICK-04 per D-09 and D-10.

Purpose: Put the full execution picture on canvas without touching any existing overlay or inventing a new primitive.
Output: T pin in J-S-T order plus five price lines with empty-marker-set and unconditional-removal clearing.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-CONTEXT.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-RESEARCH.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-PATTERNS.md
@.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-UI-SPEC.md
@components/charts/nq-chart.tsx
@components/dashboard/terminal-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>T trigger pin third in buildOverlayMarkers</name>
  <files>components/charts/nq-chart.tsx</files>
  <read_first>components/charts/nq-chart.tsx lines 49-106 for buildOverlayMarkers Judas then SMT ordering plus empty input returns empty marker set plus tone overlayStale MUTED_GRAY accent, lines 30-33 for judas judasBarDate prop shape, lines 126-130 for propsRef sync, lines 450-472 for marker-refresh plugin setMarkers lazy createSeriesMarkers never blocking candles</read_first>
  <action>Extend buildOverlayMarkers with optional ticket inputs ticketVerdict ticketDirection fireBarDate appended AFTER the Judas block then the SMT block preserving J to S to T ordering per D-09. Push one marker time fireBarDate position LONG belowBar SHORT aboveBar shape arrowUp arrowDown color tone text T only when ticketVerdict is EXECUTE_LONG or EXECUTE_SHORT, contributing nothing on STAND ASIDE null or INVALIDATED so the existing empty-marker-set path clears it per D-10. Add matching optional props ticketVerdict ticketDirection fireBarDate mirroring judas judasBarDate shape, sync in propsRef, add to both effect dep arrays, rebuild via buildOverlayMarkers on every input change with catch leaving markers empty never blocking candles or lines. Accent tone for T with MUTED_GRAY fallback when overlayStale. Props-only addition, existing J and S blocks byte-identical.</action>
  <verify><automated>npm test</automated><fails_when>vitest reports any failing suite after the T pin lands</fails_when><human-check>Render chart on EXECUTE: single T arrow on FIRE bar after J and S markers; flip to STAND ASIDE and confirm the T marker clears with no ghost pin</human-check></verify>
  <acceptance_criteria>T pin renders on FIRE bar on EXECUTE only in J-S-T order with accent tone and MUTED_GRAY stale fallback; STAND ASIDE or INVALIDATED clears via empty-marker-set path; existing J S markers unchanged; suite green</acceptance_criteria>
  <done>T trigger pin live third in marker order with STAND ASIDE clearing proven</done>
</task>

<task type="auto">
  <name>Entry SL TP price lines with unconditional removal</name>
  <files>components/charts/nq-chart.tsx</files>
  <read_first>components/charts/nq-chart.tsx lines 205-242 for mount effect guarded create with asiaLineInputs plus overlayStale MUTED_GRAY accent plus title Asia-H, lines 341-472 for update effect unconditional remove-first then re-create plus marker refresh, lines 321-329 for unmount cleanup; src/lib/chart-mapper.ts lines 17-19 plus 43-59 for isFiniteNumber predicate plus asiaLineInputs got-String throw idiom</read_first>
  <action>Add entryLineRef slLineRef tp1LineRef tp2LineRef tp3LineRef following the Asia-pair remove-then-create cycle per D-09. Optional props ticketEntry ticketSL ticketTP1 ticketTP2 ticketTP3 consumed with finite-guard: mount effect guarded create with titles Entry SL TP1 TP2 TP3 accent tone MUTED_GRAY when overlayStale, update effect removing all five ticket refs unconditionally first then re-creating only on EXECUTE verdict with finite inputs, unmount cleanup removing all five refs. Wrap guard plus create in try-catch so a throw leaves refs null rendering no lines never blocking candles per chart boundary contract. Skipping creation alone is not enough: the update effect must remove first unconditionally so no ghost what-if levels persist after verdict flips to STAND ASIDE per D-10 and Pitfall 6. Keep synchronous lightweight-charts import for LineStyle inside effects so module top stays DOM-free, keep all existing Asia EQ DOL OTE line refs and behavior untouched.</action>
  <verify><automated>npm test</automated><fails_when>vitest reports any failing suite after the ticket lines land</fails_when><human-check>Render chart on EXECUTE: Entry SL TP1 TP2 TP3 accent lines present; flip to STAND ASIDE and INVALIDATED and confirm all five lines removed with candles and existing overlays intact</human-check></verify>
  <acceptance_criteria>Five ticket lines render on EXECUTE with accent tone and stale MUTED_GRAY fallback; update effect removes unconditionally first; STAND ASIDE and INVALIDATED leave zero ticket lines; existing overlays untouched; suite green</acceptance_criteria>
  <done>Entry SL TP lines live with ghost-free clearing on STAND ASIDE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket selector to canvas overlays | EXECUTE truth crosses here; rendering pins or lines on STAND ASIDE would mislead as live levels |
| price inputs to createPriceLine | Non-finite numbers cross here; unguarded create would throw into the chart effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-10 | Tampering | ghost pins or lines after verdict flips | high | mitigate | Empty-marker-set clearing for T plus unconditional-removal-first for all five lines in this plan per D-10; caller passes nulls on STAND ASIDE from Plan 03 |
| T-17-11 | Denial of Service | chart effect throw blocking candles | medium | mitigate | try-catch around guard plus marker plus line calls leaving null refs or empty markers, never blocking candles per chart boundary contract |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages in this phase; lightweight-charts v5 APIs only, no install step in any task of this plan |
</threat_model>

<verification>
npm test after each chart task plus UAT visual glance per 17-VALIDATION.md manual-only row: EXECUTE shows T plus five lines, STAND ASIDE clears both, stale falls back to MUTED_GRAY tone.
</verification>

<success_criteria>
T pin third in J-S-T order on FIRE bar EXECUTE only; Entry SL TP1 TP2 TP3 lines EXECUTE only with Asia-pair lifecycle; STAND ASIDE and INVALIDATED clear all ticket overlays; no existing marker or line regresses.
</success_criteria>

<output>
Create `.planning/phases/17-paper-ticket-4-6-live-ui-chart-pins/17-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- components/charts/nq-chart.tsx — T trigger pin plus entry SL TP1 TP2 TP3 price lines with STAND ASIDE clearing
