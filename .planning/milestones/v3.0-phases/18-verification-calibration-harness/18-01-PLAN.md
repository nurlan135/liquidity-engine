---
phase: 18-verification-calibration-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/replay.test.ts
autonomous: true
requirements:
  - VERF-01
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Bar-by-bar replay over the 20-session fixture never takes an illegal combined-state edge (no flicker)
    - Replay fixture contains at least one FIRING setup and at least one INVALIDATED setup
    - Replay run emits fires/week plus kill-rate plus INVALIDATED count as structured JSON against the 1-4/week band
  artifacts:
    - src/lib/ict/replay.test.ts
  key_links:
    - replay driver calls evaluateTrigger then checkFatalFlaw on the identical judas/smt object per bar
    - replay-owned alreadyFired mirrors appendFiringLog three-clause dedup (ARMED-or-better only, one FIRE per NY date, already-fired echoes never re-append)
---

<objective>
Bar-by-bar replay harness proving monotonic trigger/flaw transitions (VERF-01) per D-01, D-02, D-03, D-08, D-09.
Purpose: This is the population no-flicker proof for Phases 15-17 — the single end-to-end slice everything else hangs off.
Output: Co-located `src/lib/ict/replay.test.ts` with hand-built 20-session fixture, pinned transition table, replay-owned cooldown, determinism pin, and calibration summary emission.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ict/trigger.ts
@src/lib/ict/invalidation.ts
@src/lib/ict/judas.test.ts
@src/lib/ict/trigger.test.ts
@.planning/phases/18-verification-calibration-harness/18-CONTEXT.md
@.planning/phases/18-verification-calibration-harness/18-RESEARCH.md
@.planning/phases/18-verification-calibration-harness/18-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: end-to-end mini-replay QUIET to FIRING to INVALIDATED with summary</name>
  <files>src/lib/ict/replay.test.ts</files>
  <read_first>src/lib/ict/trigger.ts (TriggerInput/TriggerOutput, gate counting, alreadyFired downgrade), src/lib/ict/invalidation.ts (FatalFlawInput first-match-wins order, SOFT FIRING-only gating), src/lib/ict/judas.test.ts (row/block15/nyMinuteEpoch builders), src/lib/ict/aggregate.ts (NY_TZ wall-clock discipline)</read_first>
  <action>Build the thinnest production-quality replay slice per D-02 and D-03 in src/lib/ict/replay.test.ts: copy row plus nyMinuteEpoch plus block15 plus fixtureFvg builders verbatim from judas.test.ts and trigger.test.ts precedent (STEP 900, BASE 20000, epochs via fromZonedTime with NY_TZ, never hand-rolled offsets). Construct exactly 3 NY sessions on consecutive trading dates (for example 2026-08-03, 2026-08-04, 2026-08-05): session A is choppy-QUIET rows strictly inside Asia range asserting WAIT_FOR_MANIPULATION with all gates false; session B is a firing path with a 02:01 just-inside killzone bar, a confirmed LOW sweep bar piercing strictly below Asia low, and a displacement bar closing back through with TRIGGER_DISP_MULT 0.5 satisfied plus resolvable direction-polarity FVG, asserting FIRE_LONG with 3 of 3 gates; session C replays a firing bar then injects stale es true on the next bar asserting HARD STALE_LEG INVALIDATED per D-09. Per bar in chronological order call evaluateTrigger with judas/amd/smt/fvg/asOf/alreadyFired then checkFatalFlaw with the identical judas/smt object plus rollover plus stale plus asOf, never re-derived copies, never the Zustand store. Own session-scoped alreadyFired in the test (one FIRE per NY date, ARMED-or-better only, already-fired echoes never re-append) and set firedThisSession true on any FIRE_LONG or FIRE_SHORT. Pin the combined-state function QUIET equals WAIT plus clean, ARMED equals ARMED plus clean, FIRING equals FIRE plus clean, INVALIDATED equals HARD invalidated, SOFT-transient equals FIRE plus downgraded renders ARMED; assert the legal-edge lookup per bar where FIRING to ARMED is legal only when downgraded true with carriedArmedReason non-null, FIRING to QUIET is always illegal, INVALIDATED to anything within one setup is illegal, same-state hold is always legal. Emit one console.log JSON object with fires, sessions, firesPerWeek, invalidated, killRate, band 1-4/week, verdict IN-BAND or OUT-OF-BAND using NY-date week bucketing via formatInTimeZone with NY_TZ.</action>
  <acceptance_criteria>Mini-replay of 3 sessions runs green with at least one FIRING bar and at least one INVALIDATED bar; illegal FIRING-to-QUIET edge fails if injected; summary JSON line contains firesPerWeek and killRate and band fields</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/replay.test.ts</automated>
    <fails_when>non-zero exit, or illegal transition edge asserted, or missing FIRING/INVALIDATED, or summary JSON line absent</fails_when>
  </verify>
  <done>Single happy path QUIET to FIRING to INVALIDATED works end-to-end with transition table and summary emission committed</done>
  <reversibility rating="reversible">Test-only file addition, no production code touched, deletable without migration</reversibility>
</task>

<task type="auto">
  <name>Expand fixture to hand-built 20-session population with killzone-edge probes</name>
  <files>src/lib/ict/replay.test.ts</files>
  <read_first>src/lib/ict/replay.test.ts (tracer slice from prior task), src/lib/ict/trigger.test.ts (killzone-exclusive probes 02:01/04:59 fire versus 02:00/05:00 ARMED_MISSING_TIMING, QUIET range-inside asserts, alreadyFired downgrade), src/lib/ict/amd.ts (NY-session 570-960 wholesale branch)</read_first>
  <action>Expand the replay fixture to 20 consecutive NY trading sessions across 4 Mon-Fri weeks with no weekend rows per D-01 (for example 2026-08-03 through 2026-08-28 skipping Saturdays and Sundays) so firesPerWeek divides by 4 NY weeks. Keep the tracer sessions and add explicit rows exercising killzone entries at 02:01 and 04:59 just-inside probes for firing bars and 02:00 and 05:00 exact-edge negative controls asserting ARMED_MISSING_TIMING, sweep bars with wicks piercing strictly beyond Asia high or low, displacement bars closing back through with the 0.5 multiple satisfied, choppy-QUIET stretches of at least one full 12-row block15 strictly inside range asserting WAIT_FOR_MANIPULATION, and at least one NY-hours stretch with minutes 570 to 960 present but excluded from firing expectations. Pin every fixture bar's nyMinutesOf value in a trailing comment. Route every bar through the same evaluateTrigger then checkFatalFlaw same-snapshot order at 15M closes only per D-02, stepping each close in chronological order with closedOnly semantics, never forming bars. Assert the SOFT-transient FIRING to ARMED edge appears at least once via SMT-suppressed downgrade with carriedArmedReason set, and assert every INVALIDATED bar carries a winning reasonKey of ROLLOVER_WEEK or STALE_LEG or SMT_SUPPRESSED, never OPPOSITE_SWEEP.</action>
  <acceptance_criteria>Twenty sessions present with consecutive trading-date calendar and weekend gap; 02:01 firing probe and 02:00 negative control both pinned; at least one SMT-suppressed FIRING-to-ARMED downgrade edge; no OPPOSITE_SWEEP INVALIDATED bar</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/replay.test.ts</automated>
    <fails_when>non-zero exit, or killzone-edge probe misfires, or downgrade edge missing, or OPPOSITE_SWEEP used as INVALIDATED source</fails_when>
  </verify>
  <done>Twenty-session hand-built population replays green with edge probes and required FIRING plus INVALIDATED paths</done>
  <reversibility rating="reversible">Fixture-row expansion inside the test file only, revertible by truncating sessions</reversibility>
</task>

<task type="auto">
  <name>Harden transition table with cause asserts plus determinism plus V5 boundary coverage</name>
  <files>src/lib/ict/replay.test.ts</files>
  <read_first>src/lib/ict/replay.test.ts (expanded fixture), src/lib/ict/trigger.test.ts (determinism toEqual plus JSON.stringify repeat-evaluation pin, ARMED-key total function of missing gate), src/lib/ict/invalidation.ts (assertValidJudas/assertValidStale got-string throws, null-degrade honest paths)</read_first>
  <action>Strengthen the replay asserts without adding new files: on every ARMED to QUIET edge additionally assert the trigger gate count dropped to 1 or fewer (reduced gates, never a vanished FIRE); on every FIRING exit assert the triple of trigger verdict plus flaw downgraded plus flaw carriedArmedReason rather than the verdict string alone so a clean-flaw FIRING to ARMED fails as flicker per D-03. Add a determinism test that runs the full 20-session driver twice and asserts toEqual plus JSON.stringify equality of both replay logs. Add V5 input-validation coverage: one case passing a malformed judas envelope string asserting a got-string throw, and one case passing null smt plus null judas asserting honest WAIT degradation rather than a throw. Finalize the calibration summary helper as the single definition of firesPerWeek and killRate (killRate equals invalidated-setups divided by fired-setups) with band verdict IN-BAND when firesPerWeek is within 1 to 4 inclusive else OUT-OF-BAND logged informationally without throwing, and assert the summary test checks verdict correctness against the 4-week denominator per D-08. Keep the file pure with injected asOf per bar and no store imports so the existing purity guard stays green.</action>
  <acceptance_criteria>ARMED-to-QUIET cause assert and FIRING-exit triple assert present; double-run determinism test green; malformed-throw and null-degrade cases green; summary band verdict correct for the fixture denominator</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/replay.test.ts</automated>
    <fails_when>non-zero exit, or clean-flaw FIRING-to-ARMED passes, or determinism mismatch, or boundary throw/degrade missing</fails_when>
  </verify>
  <done>Transition table hardened against the flicker hole with determinism and boundary coverage green</done>
  <reversibility rating="reversible">Assertion tightening inside the test file only, no production surface change</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture→trigger→flaw | Hand-built rows cross into pure detectors; malformed envelopes must throw, nulls must degrade |
| replay→console | Summary JSON crosses into operator reading; band verdict must not invent precision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | replay transition table (replay.test.ts) | high | mitigate | Assert FIRING-exit triple plus ARMED-to-QUIET gate-cause per D-03; clean downgrade fails the build |
| T-18-02 | Information disclosure | calibration summary band | medium | mitigate | Single summary helper with pinned 4-week denominator; OUT-OF-BAND logs without throwing, no invented fires/week |
| T-18-03 | Spoofing | paper-taken-for-real identifiers | medium | mitigate | Phase 18 adds no identifiers; banned-word grep stays green (verified in Plan 03 full suite) |
| T-18-SC | Tampering | npm installs | high | mitigate | Zero new installs in this phase; vitest plus date-fns-tz already in tree, no legitimacy checkpoint needed |
</threat_model>

<verification>
Per-task quick runs plus full-suite gate: `npx vitest run src/lib/ict/replay.test.ts` after each commit; `npm test` full suite must stay green including existing trigger/invalidation/ticket/store suites before verify-work.
</verification>

<success_criteria>
Replay proves VERF-01: every bar edge legal with cause asserts, fixture holds 20 sessions with at least one FIRING and at least one INVALIDATED, summary emits firesPerWeek plus killRate plus INVALIDATED count against the 1-4/week band, determinism double-run byte-identical.
</success_criteria>

## Artifacts this phase produces

- `src/lib/ict/replay.test.ts` — 20-session hand-built 15M fixture (row/block15/nyMinuteEpoch/fixtureFvg builders), bar-by-bar driver (evaluateTrigger then checkFatalFlaw same-snapshot, replay-owned alreadyFired), pinned transition-table lookup with cause asserts, calibration summary helper emitting `{ fires, sessions, firesPerWeek, invalidated, killRate, band, verdict }`, determinism double-run test, V5 malformed-throw plus null-degrade cases.

<output>
Create `.planning/phases/18-verification-calibration-harness/18-01-SUMMARY.md` when done
</output>
