---
phase: 15-why-now-trigger-engine
plan: '02'
type: execute
wave: 2
depends_on:
- 15-01
files_modified:
- src/lib/ict/trigger.ts
- src/lib/ict/trigger.test.ts
autonomous: true
requirements:
- TRIG-01
- TRIG-02
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
  - User sees ARMED with the correct missing-gate reason when exactly 2 of 3 gates pass, and WAIT_FOR_MANIPULATION when 0 to 1 pass
  - User never sees a second FIRE in the same NY-date session; repeat 3-of-3 evaluations return ARMED with the already-fired reason and no log append
  - User never sees FIRE without a resolvable entry FVG; missing or wrong-polarity inventory downgrades to ARMED at best per D-04
  artifacts:
  - src/lib/ict/trigger.ts
  - src/lib/ict/trigger.test.ts
  key_links:
  - Displacement gate couples magnitude re-check with FVG polarity selection so ARMED appears on real near-miss fixtures
  - alreadyFired boolean is the only cooldown path; no clock or log reads inside ict
  - Boundary pins prove killzone edges exclusive and disp threshold exact with TOL_EPS tolerance
---

<objective>
Expansion: ARMED counting matrix, per-session cooldown, entry-FVG downgrade, boundary pins, malformed-input throws.
Implements D-03, D-04, D-05, D-06 (see task actions; builds on D-01, D-02, D-07, D-10 tracer from 15-01).

Purpose: Make ARMED meaningful on genuinely independent gates and prove cooldown plus FVG downgrade before store hardening.
Output: Hardened evaluateTrigger plus full gate-table test coverage in co-located trigger.test.ts.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-why-now-trigger-engine/15-CONTEXT.md
@.planning/phases/15-why-now-trigger-engine/15-RESEARCH.md
@.planning/phases/15-why-now-trigger-engine/15-PATTERNS.md
@.planning/phases/15-why-now-trigger-engine/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>ARMED counting matrix plus boundary pins per D-05 and D-03</name>
  <files>src/lib/ict/trigger.test.ts, src/lib/ict/trigger.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/trigger.test.ts, src/lib/ict/judas.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Extend src/lib/ict/trigger.test.ts with the exactly-2-of-3 ARMED matrix per D-05, fixing src/lib/ict/trigger.ts counting only if a case fails. Cover missing-timing case with asOf at 06:00 NY plus confirmed LOW sweep plus displacementMult 0.6 plus BULLISH FVG expecting ARMED with reasonKey ARMED_MISSING_TIMING, missing-purge case with asOf 03:00 NY plus judas confirmed false plus displacementMult 0.6 plus matching FVG expecting ARMED with reasonKey ARMED_MISSING_PURGE, missing-displacement case with asOf 03:00 NY plus confirmed sweep plus displacementMult 0.2 expecting ARMED with reasonKey ARMED_MISSING_DISPLACEMENT. Cover 0-to-1 gate cases expecting WAIT_FOR_MANIPULATION with reasonKey WAIT_FOR_MANIPULATION. Cover boundary pins per D-03 with disp just-below 0.5 at 0.499 expecting displacement false and ARMED, disp just-above at 0.501 expecting displacement true, killzone edge minutes exactly 120 and exactly 300 expecting timing false per strict exclusivity, minutes 121 and 299 expecting timing true. Use TOL_EPS 1e-9 float-compare precedent from judas.ts line 197 for threshold compares. Keep verbatim reason toBe pins aligned with the D-09 wording locked in Plan 03; if Plan 03 wording differs, update expected strings to match trigger.ts constants exactly.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: each exactly-2-of-3 fixture yields ARMED with its matching reasonKey, 0-to-1 yields WAIT_FOR_MANIPULATION. Test command: npx vitest run src/lib/ict/trigger.test.ts passes. Source assertion: boundary tests for 120, 300, 0.499, 0.501 exist.</acceptance_criteria>
  <done>ARMED matrix plus killzone and displacement boundaries are green with matching reasonKeys.</done>
</task>

<task type="auto">
  <name>Cooldown matrix plus no-FVG downgrade plus suppressed SMT per D-04 and D-06</name>
  <files>src/lib/ict/trigger.test.ts, src/lib/ict/trigger.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/trigger.test.ts, src/lib/ict/smt.ts, src/lib/ict/fvg.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Extend src/lib/ict/trigger.test.ts with cooldown plus FVG plus SMT cases per D-04 and D-06, fixing src/lib/ict/trigger.ts only if a case fails. Cover alreadyFired true with otherwise 3-of-3 LOW sweep expecting ARMED with reasonKey ARMED_ALREADY_FIRED and entryFvg null and verdict never FIRE, plus alreadyFired false control expecting FIRE_LONG. Cover D-04 downgrade with fvg null expecting max ARMED even at 3-of-3 otherwise, empty array expecting ARMED, wrong-polarity-only inventory with BEARISH gaps on a LONG setup expecting ARMED with reasonKey ARMED_MISSING_DISPLACEMENT, mitigated-only inventory expecting ARMED. Cover FVG selection rule with two BULLISH gaps of different originDate expecting the most recent originDate handle on FIRE and entryFvg top bottom polarity originDate preserved for Phase 17 ticket. Cover suppressed SMT with suppressed true reason CORR_DECOUPLED expecting FIRE still allowed with no agree suffix, plus concordant unsuppressed SMT HIGH-BEARISH and LOW-BULLISH expecting FIRE with space plus SMT razılaşır period suffix, discordant expecting FIRE without suffix. Assert entryFvg null on every ARMED and WAIT verdict.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: alreadyFired true plus 3-of-3 yields ARMED_ALREADY_FIRED with entryFvg null; null FVG plus 3-of-3-otherwise yields max ARMED; suppressed SMT still FIREs. Test command: npx vitest run src/lib/ict/trigger.test.ts passes.</acceptance_criteria>
  <done>Cooldown dedup plus FVG downgrade plus SMT read-only tag are green.</done>
</task>

<task type="auto">
  <name>Fail-at-boundary throws plus determinism per V5 input validation</name>
  <files>src/lib/ict/trigger.test.ts, src/lib/ict/trigger.ts</files>
  <read_first>src/lib/ict/trigger.ts, src/lib/ict/trigger.test.ts, src/lib/ict/amd.ts, .planning/phases/15-why-now-trigger-engine/15-PATTERNS.md</read_first>
  <action>Extend src/lib/ict/trigger.test.ts with malformed-input throw cases copying amd.test.ts lines 227 to 243 precedent, hardening src/lib/ict/trigger.ts boundary asserts only if a case fails. Cover null input object throwing, asOf NaN plus Infinity plus zero plus negative plus string throwing with got-string message, judas with candidate 1 plus string sweepTime plus BOTH sweepSide throwing, alreadyFired non-boolean throwing, FvgGap with non-finite top or bottom throwing via assertFiniteGap precedent from fvg.ts lines 29 to 35. Cover null detector inputs degrading honestly: judas null plus amd null plus smt null with valid asOf and fvg null yields WAIT_FOR_MANIPULATION with gates all false and never throws. Cover determinism with same TriggerInput evaluated twice yielding byte-identical verdict plus reason plus gates plus entryFvg. Keep all throw assertions with toThrow and message matching got substring, never bare failure words.</action>
  <verify>
    <automated>npx vitest run src/lib/ict/trigger.test.ts</automated>
    <fails_when>non-zero exit, or the string FAIL in output, or 0 passed in the summary line</fails_when>
  </verify>
  <acceptance_criteria>Behavior assertion: every malformed envelope throws with got-string message; null detectors yield WAIT without throw; double evaluation is byte-identical. Test command: npx vitest run src/lib/ict/trigger.test.ts passes.</acceptance_criteria>
  <done>Boundary validation plus honest WAIT degrade plus determinism are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture→evaluateTrigger | Adversarial gate combinations cross here; every 2-of-3 row must resolve ARMED deterministically |
| caller→alreadyFired | Cooldown state crosses here as a plain boolean; trigger never reads clocks or logs |
| fvg-inventory→displacement-gate | Untrusted gap bounds cross here; finiteness enforced before selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering | TriggerInput envelope plus FvgGap bounds | high | mitigate | Throw at boundary on malformed judas, non-finite or non-positive asOf, non-finite FvgGap top and bottom; null inputs degrade to WAIT, tested in this plan |
| T-15-05 | Denial of service | ARMED evaluation volume | low | accept | ARMED repeats are calibration signal per research; cap plus overflow counter in store bounds growth, no per-bar FIRE spam by D-06 dedup |
| T-15-SC | Tampering | npm installs | high | mitigate | Zero new packages; only installed date-fns-tz plus vitest plus zustand used |
</threat_model>

<verification>
npx vitest run src/lib/ict/trigger.test.ts after each task, npm test at plan merge, npx tsc --noEmit before commit.
</verification>

<success_criteria>
Full gate table green: 3-of-3 FIRE both directions, each 2-of-3 ARMED with matching key, 0-to-1 WAIT, boundaries exclusive, cooldown downgrades, no-FVG caps at ARMED, malformed throws, determinism holds.
</success_criteria>

<output>
Create `.planning/phases/15-why-now-trigger-engine/15-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/ict/trigger.test.ts: ARMED 2-of-3 matrix tests, boundary-pin tests for 120 plus 300 plus 0.499 plus 0.501, cooldown alreadyFired matrix tests, D-04 no-FVG downgrade tests, FVG most-recent-originDate selection test, suppressed-SMT tests, malformed-throw tests, determinism test
- src/lib/ict/trigger.ts: counting plus cooldown plus FVG-selection hardening fixes only if expansion tests fail, TOL_EPS threshold compare
