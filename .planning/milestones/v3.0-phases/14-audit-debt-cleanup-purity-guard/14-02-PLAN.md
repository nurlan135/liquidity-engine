---
phase: 14-audit-debt-cleanup-purity-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/thin-tier.ts, src/lib/thin-tier.test.ts]
autonomous: true
requirements: [DEBT-02]
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 12500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - thin-tier public surface is exactly ThinTier, thinTierCopy, resolveThinTier, thinBannerOrder with no external import break
    - boundary tiers 19/20/33/34 still pinned, now asserted through the public resolveThinTier entry
    - thinTier/thinHistory predicate equivalence still holds with zero drift
  artifacts:
    - src/lib/thin-tier.ts (two symbols unexported)
    - src/lib/thin-tier.test.ts (migrated to public entry)
  key_links:
    - internal thinTier helper -> resolveThinTier -> terminal-shell consumer
---

<objective>
Unexport the orphaned thinTier helper and ThinBannerEntry type per D-03, and migrate thin-tier.test.ts boundary pins to the public resolveThinTier entry per D-04 — public-API-from-test, zero behavior change.

Purpose: Shrink the public surface so new execution code inherits exactly four public names; the test file stops reaching past the public API.
Output: Two modified files, suite green, no external importer broken.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-CONTEXT.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-PATTERNS.md
@src/lib/thin-tier.ts
@components/dashboard/terminal-shell.tsx
</context>

<tasks>

<!-- planner-discipline-allow: export function thinTier -->
<!-- planner-discipline-allow: ThinBannerEntry -->

<task type="auto">
  <name>Task 1: Verify zero external importers, then unexport the two orphaned symbols</name>
  <files>src/lib/thin-tier.ts</files>
  <read_first>src/lib/thin-tier.ts (full file, 47 lines — line 8 helper, line 34 internal call, line 40 type, line 42 consumer); components/dashboard/terminal-shell.tsx line 17 (import surface); src/lib/thin-tier.test.ts lines 5-10 (import block to be migrated in Task 2)</read_first>
  <action>First run a repo-wide search for the bare helper name with a call paren and for the banner-entry type name across all ts and tsx files. Confirm the only matches are the definition plus the internal call at line 34 and the return-type annotation at line 42 inside thin-tier.ts itself, plus the test-file imports that Task 2 migrates — and that terminal-shell.tsx imports only resolveThinTier, thinBannerOrder and thinTierCopy. Only after that confirmation, drop the export keyword on the line 8 helper function and on the line 40 banner-entry type, leaving both signatures and bodies verbatim. Keep ThinTier, thinTierCopy, resolveThinTier and thinBannerOrder exported; named-only exports convention preserved. Implements D-03 including its mandatory external-import-break check.</action>
  <acceptance_criteria>Line 8 begins with the function keyword and no export keyword; line 40 begins with the type keyword and no export keyword; the internal call at line 34 and the line 42 annotation still resolve; terminal-shell.tsx import line is byte-identical; the pre-edit search showed zero external importers outside the module and its test. Note: the scoped test file goes red after this task (it still imports the now-internal helper) and returns green in Task 2 — that interim red is expected, not a gate failure.</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | head -30</automated>
    <fails_when>the output contains a thin-tier.ts error line referencing the module itself (errors naming only the test file are expected until Task 2)</fails_when>
  </verify>
  <done>Precondition logged with zero external importers; two symbols unexported, bodies verbatim.</done>
  <reversibility rating="reversible">Keyword-only deletion; git revert restores both exports.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate thin-tier.test.ts boundary pins to resolveThinTier</name>
  <files>src/lib/thin-tier.test.ts</files>
  <read_first>src/lib/thin-tier.test.ts (full file — helpers flatSeries lines 16-23 and anyRange lines 25-27, boundary pins lines 30-42, null-input tests lines 60-63); src/lib/thin-tier.ts (resolveThinTier contract lines 26-38); src/lib/ict/range.ts (computeRange signature used by the predicate loop)</read_first>
  <action>Rewrite the boundary test (lines 30-35) to assert through the public entry: for each count in 19, 20, 33, 34 call resolveThinTier with flatSeries of that count plus anyRange and expect toEqual the tier plus closedCount pair — range-thin with 19, regime-degraded with 20, regime-degraded with 33, full with 34. Rewrite the predicate-equivalence loop (lines 37-42) over counts 1, 5, 19, 20, 25, 33, 34, 40 to compare resolveThinTier built from flatSeries of n against the date-string range, dot tier, triple-equal range-thin, against range dot thinHistory from computeRange on the same series — preserving the D-02 same-predicate check with thinHistory still read from the range object. Drop the bare helper from the line 5-10 import block, keeping resolveThinTier, thinBannerOrder, thinTierCopy and the Candle, DealingRange, ANCHOR_WINDOW, computeRange, MIN_CANDLES_FULL imports. Leave the never-throw and null-input tests, the copy tests and the banner-order test verbatim. Implements D-04, honors D-02.</action>
  <acceptance_criteria>No direct call of the bare helper remains in the test file; the exact-boundary set 19/20/33/34 is asserted with matching closedCount values; the eight-count predicate loop still compares against range dot thinHistory; scoped file green; project-wide tsc clean.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/thin-tier.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in the output</fails_when>
  </verify>
  <done>Tests exercise the public API only; internal helper no longer tested directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thin-tier module → terminal-shell | public-surface shrink crosses to the single consumer; a missed importer breaks the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | thin-tier unexport | low | mitigate | pre-edit repo-wide importer search plus tsc clean plus suite green prove no break |
| T-14-04 | Repudiation | boundary-pin migration | low | accept | pins keep exact 19/20/33/34 values through the public entry; suite output records the pass |
</threat_model>

<verification>
Plan-level: npm test (full suite) green; npx tsc --noEmit clean; repo-wide search for the unexported names shows matches only inside thin-tier.ts (definition, internal call, annotation); git diff shows keyword removals plus test-entry migration only.
</verification>

<success_criteria>
Thin-tier public surface is exactly the four locked names with no external import break; boundary pins pass via resolveThinTier; DEBT-02 satisfied with zero behavior change.
</success_criteria>

## Artifacts this phase produces

Phase-wide artifact record (this plan contributes the DEBT-02 rows):
- Modified: src/lib/thin-tier.ts — export keyword removed from the closedCount helper (line 8) and from the banner-entry type (line 40); both stay as module-internal helpers; public surface is exactly ThinTier, thinTierCopy, resolveThinTier, thinBannerOrder
- Modified: src/lib/thin-tier.test.ts — boundary pins and predicate loop rewritten through resolveThinTier (exact-boundary set 19/20/33/34 preserved)
- Modified (in 14-01): src/lib/zone-bands.ts, components/charts/nq-chart.tsx, src/lib/zone-bands.test.ts — zoneBands narrowing per D-01
- Created (in 14-03): src/lib/ict/purity.test.ts — co-located Vitest grep guard with two specs (clock-read spec, store-import spec)
- Doc-only (in 14-03): Asia fallback comment extension in src/lib/store.ts selectAsia (no new symbol, no logic change)

<output>
Create `.planning/phases/14-audit-debt-cleanup-purity-guard/14-02-SUMMARY.md` when done
</output>
