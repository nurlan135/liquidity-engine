---
phase: 14-audit-debt-cleanup-purity-guard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/ict/purity.test.ts, src/lib/store.ts]
autonomous: true
requirements: [DEBT-03]
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 12500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - a co-located Vitest guard fails on clock reads and store imports inside src/lib/ict sources
    - the guard passes green on existing code with no false positives on injected-time calendar math
    - the Asia fallback rationale is documented with no behavior change
  artifacts:
    - src/lib/ict/purity.test.ts (new guard)
    - src/lib/store.ts (comment-only Asia note)
  key_links:
    - purity guard raw-source loading -> every src/lib/ict non-test source -> vitest include glob
---

<objective>
Create the co-located Vitest purity guard for src/lib/ict per D-05/D-06, and land the doc-only Asia fallback note per D-07 — no behavior change anywhere.

Purpose: Phase 15 trigger code lands next; the guard must be green before new ict modules arrive so purity violations fail loudly at test time instead of slipping into methodology math.
Output: One new test file, one comment-only edit, suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-CONTEXT.md
@.planning/phases/14-audit-debt-cleanup-purity-guard/14-PATTERNS.md
@vitest.config.ts
@src/lib/ict/asia.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the co-located ict purity grep guard</name>
  <files>src/lib/ict/purity.test.ts</files>
  <read_first>vitest.config.ts (include glob lines 6-8 plus node environment — new file must be picked up with zero config change); src/lib/ict/asia.test.ts (describe/it/expect style to copy); src/lib/ict/asia.ts lines 1-16 (module header purity contract plus killzone constants); directory listing of src/lib/ict/*.ts (full source set the guard must cover)</read_first>
  <action>Create src/lib/ict/purity.test.ts as a co-located Vitest file with a single describe block named ict purity guard and exactly two specs: one spec asserting no source contains a Date dot now call with an open paren, and one spec asserting no source contains a store import (match the zustand package path and the src/lib/store and at-store style path fragments in both quote styles). Load sources with the Vite-native raw glob over the sibling directory (query raw, default import), excluding every path ending in dot test dot ts so the guard never scans itself; if the glob proves unworkable under the node test environment (returns empty or unresolvable), fall back to node builtins readdirSync plus readFileSync scoped to src/lib/ict. Assert only the minimal pattern set — never assert on new Date, performance dot now, or fetch — so the legitimate injected-time calendar math in asia.ts line 78 and rollover.ts stays green. Use named exports only, no default export, no node:fs import unless the fallback is taken. Implements D-05, honors D-06.</action>
  <acceptance_criteria>New file is matched by the existing src glob with zero config change; both specs pass on the current tree; a temporary probe file containing a clock call makes the clock spec go red and a temporary probe importing the store package makes the store spec go red, with both probes deleted afterwards; the guard source contains no filesystem import unless the documented fallback was required.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/ict/purity.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <done>Guard fails on violations, passes green on existing code; NOT an eslint rule.</done>
  <reversibility rating="reversible">Additive test-only file; deleting it restores the prior state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Document the Asia fallback frequency rationale (doc-only)</name>
  <files>src/lib/store.ts</files>
  <read_first>src/lib/store.ts lines 706-732 (selectAsia with the candidate-dates fallback loop); src/lib/ict/asia.ts lines 6-16 (killzone window constants and purity header for exact hours language)</read_first>
  <action>Extend the existing fallback comment at store.ts lines 713-718 with two to four terse double-slash lines in the file's existing voice (not JSDoc) stating the Asia killzone 20:00 to 23:45 NY wall-clock window plus the last-completed-session fallback from commit edbc70a, and that the fallback increases overlay frequency by design because the newest date's morning window is often still empty so the most recent completed session is returned instead of null. Change comment lines only — zero code, export, or logic change. Implements D-07.</action>
  <acceptance_criteria>The comment names the 20:00-23:45 window, the last-completed-session fallback, and the increases-frequency-by-design rationale; git diff on store.ts shows comment lines only with zero changed code lines; project-wide tsc stays clean.</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in the output</fails_when>
  </verify>
  <done>Asia note landed as documentation; behavior identical.</done>
  <reversibility rating="reversible">Comment-only edit; git revert removes the lines.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard test → ict sources | test reads source text at run time; a loader misconfiguration could silently scan nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | purity guard false-green | medium | mitigate | probe-file mutation proof (red on violation, green after removal) plus exclusion of self-scan paths |
| T-14-06 | Information Disclosure | store.ts comment edit | low | accept | comment describes already-shipped fallback logic; no secrets, keys, or internals added |
</threat_model>

<verification>
Plan-level: npm test (full suite) green including the two new guard specs; probe-file mutation proof performed and probes deleted; git diff on store.ts is comment-only; npx tsc --noEmit clean.
</verification>

<success_criteria>
Guard fails on Date dot now and store imports inside src/lib/ict and passes green on existing code; Asia note explains the fallback frequency effect; DEBT-03 satisfied with zero behavior change.
</success_criteria>

## Artifacts this phase produces

Phase-wide artifact record (this plan contributes the DEBT-03 and D-07 rows):
- Created: src/lib/ict/purity.test.ts — co-located Vitest grep guard; describe block ict purity guard with exactly two specs (clock-read spec matching the Date-now-call pattern; store-import spec matching the zustand package path and store path fragments); Vite-native raw source loading with a node-builtin fallback; test files excluded from self-scan
- Modified: src/lib/store.ts — Asia fallback comment extension in selectAsia (doc-only, zero logic change)
- Modified (in 14-01): src/lib/zone-bands.ts, components/charts/nq-chart.tsx, src/lib/zone-bands.test.ts — zoneBands narrowing per D-01
- Removed from public surface (in 14-02): the export keyword on thinTier(closedCount) and on the ThinBannerEntry type — both stay as module-internal helpers
- Note: no new runtime symbol is exported anywhere in this phase; the only new file is the guard test

<output>
Create `.planning/phases/14-audit-debt-cleanup-purity-guard/14-03-SUMMARY.md` when done
</output>
