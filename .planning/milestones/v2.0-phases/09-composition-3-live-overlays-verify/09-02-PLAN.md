---
phase: 09-composition-3-live-overlays-verify
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/lib/chart-mapper.ts
  - src/lib/chart-mapper.test.ts
  - components/charts/nq-chart.tsx
  - components/dashboard/report.tsx
  - components/dashboard/terminal-shell.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
  - ICT-15
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each §3 sub-block renders live detector prose verbatim or its own reason line plus at most one tag, never a §3-level banner per D-01 and D-04"
    - "Liquidity Path shows describeDeliveryTransition prose verbatim, SMT Status shows direction plus sweeper-leg plus window refs verbatim, AMD Timing shows amdPhase reason plus SMT tag verbatim"
    - "All-degraded §3 stacks three reasons in stable sub-block order Liquidity Path, SMT Status, AMD Timing with deterministic order per D-02 and the UI-05 ordering predicate"
    - "Adjacent simultaneously-degraded sub-blocks keep separate reason lines and never merge into one banner per the UI-05 adjacency predicate"
    - "AMD Timing shows the amdPhase reason plus optional SMT tag plus dimmed NY line with exact copy NY Gözlənilir line below live content per D-03"
    - "Empty or null selector input renders Məlumat yoxdur or the detector reason, never a blank sub-block or crash per the UI-05 empty predicate"
    - "First paint with null envelope shows the three-bar animate-pulse skeleton for §3 per the loading consideration"
    - "Stale or suppressed input renders the owning sub-block refusal verbatim with the STALE badge while overlays dim-but-persist per D-08 and the error consideration"
    - "Asia high and low render as dashed accent Asia-H and Asia-L price lines via remove-then-create with null-autoscale, never a shaded box per D-05"
    - "Zero-width Asia range with equal high and low renders deterministically as coincident lines without throwing per the UI-06 adjacency predicate"
    - "No Asia data renders no Asia lines while candles render clean and the AMD sub-block shows its Asia reason per the UI-06 empty predicate"
    - "No Asia lines render until the nq1h leg resolves, with the §3 skeleton showing meanwhile, per the loading consideration"
    - "Stale nq1h legs persist Asia lines desaturated to muted gray, never removed, per D-08"
    - "Judas candidates render hollow-split circle with text J-question and confirmed render solid accent circle with text J, with HIGH sweep aboveBar and LOW sweep belowBar per D-06"
    - "SMT renders a single S pin on the confirming D1 bar with BULLISH arrowUp belowBar and BEARISH arrowDown aboveBar and no marker on NO-SIGNAL or suppressed per D-07, with sweeper-leg detail in prose only"
    - "No sweep or NO-SIGNAL input clears markers via empty marker set with clean candles and no placeholder pins per the UI-06 empty predicate"
    - "Unresolved selectors render no markers and no placeholder pins per the loading consideration"
    - "Stale legs persist markers desaturated with the STALE badge, never cleared, and the chart never falls back to clean D1 silently per D-08"
    - "Multiple markers on the same D1 bar stack with one pin per signal per bar and sweeper-leg detail in §3 prose per the UI-06 ordering predicate"
    - "ES stays off-chart with no ES series and no ES markers"
    - "Conviction line sits under the 3. LIQUIDITY SEQUENCING title showing standart in muted or yüksək inam in accent with Display 20px value word per D-09 and D-10"
    - "Sub-block reason lines wrap to at most two lines at narrow widths without truncating the tag per the long-text consideration"
  backstop:
    statement: "Conviction-line value word wraps beneath the §3 title without truncating"
    verification: backstop
  artifacts:
    - path: "src/lib/chart-mapper.ts"
      provides: "Asia line pass-through inputs with finite guard"
      exports: ["asiaLineInputs"]
      contains: "asiaLineInputs"
    - path: "src/lib/chart-mapper.test.ts"
      provides: "Asia mapper finite-guard plus zero-width cases"
      contains: "asiaLineInputs"
    - path: "components/charts/nq-chart.tsx"
      provides: "Asia line pair plus Judas and SMT markers plus stale-dim plus data slots"
      contains: "Asia-H"
    - path: "components/dashboard/report.tsx"
      provides: "Live §3 block with three sub-blocks plus conviction line plus data slots"
      contains: "s3-conviction"
    - path: "components/dashboard/terminal-shell.tsx"
      provides: "Selector subscriptions plus overlay prop pass-through into NqChart"
      contains: "selectConfluence"
  key_links:
    - from: "components/charts/nq-chart.tsx"
      to: "src/lib/chart-mapper.ts"
      via: "Asia pair creation consumes asiaLineInputs with guard-throw rendering no lines and never blocking the chart"
      pattern: "asiaLineInputs"
    - from: "components/charts/nq-chart.tsx"
      to: "src/lib/store.ts"
      via: "Overlay props carry AsiaRange plus JudasOutput plus SmtOutput plus staleness from selectAsia, selectJudas, selectSMT"
      pattern: "selectAsia"
    - from: "components/dashboard/report.tsx"
      to: "src/lib/store.ts"
      via: "Sub-blocks subscribe selectLiquidityPath, selectSMT, selectAMD, selectConfluence and render verbatim or reason per block"
      pattern: "selectAMD"
    - from: "components/dashboard/report.tsx"
      to: "src/lib/report.ts"
      via: "§3 live shell plus conviction label constant drive the header and conviction line"
      pattern: "CONVICTION_LABEL"
    - from: "components/dashboard/terminal-shell.tsx"
      to: "src/lib/store.ts"
      via: "Shell subscribes the six §3 selectors and passes Asia, Judas, SMT, and staleness into NqChart props"
      pattern: "selectConfluence"
  prohibitions:
    - "Never present degraded or suppressed session reads as actionable trading certainty"
    - "Never hide staleness or suppression from the reader to make the terminal look live"
    - "Never invent detector prose that misattributes market intent to a specific actor"
---

<objective>
Presentation expansion from the Plan 01 trunk: live report §3 with three sub-blocks plus conviction line, and Asia plus Judas plus SMT overlays on the D1 canvas, per D-01 through D-12.

Purpose: Proves the same selector trunk through the two user-visible leaves — report prose and chart canvas — with per-block honest degrade, dim-but-persist staleness, and ES off-chart. Plan 01 proved the trunk through the report contract; this plan wires the full render surface the live-URL drill (Plan 03) verifies.
Output: Extended chart mapper with Asia inputs, extended NqChart with line pair plus markers plus stale-dim, live §3 report block with conviction line, and shell selector wiring. No store shape changes, no route changes, no new packages.
API coverage: no new external surface in this plan — overlays derive from Plan 01 intraday legs over the already-allowlisted proxy; the reasoned declaration lands in 09-COVERAGE.md in Plan 03.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composition-3-live-overlays-verify/09-CONTEXT.md
@.planning/phases/09-composition-3-live-overlays-verify/09-RESEARCH.md
@.planning/phases/09-composition-3-live-overlays-verify/09-PATTERNS.md
@.planning/phases/09-composition-3-live-overlays-verify/09-UI-SPEC.md
@.planning/phases/09-composition-3-live-overlays-verify/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Asia line mapper inputs plus edge tests</name>
  <files>src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts</files>
  <read_first>src/lib/chart-mapper.ts, src/lib/chart-mapper.test.ts, src/lib/ict/asia.ts</read_first>
  <reversibility rating="reversible">Pure pass-through helper with no stored state; removal deletes one export plus its tests.</reversibility>
  <action>Extend src/lib/chart-mapper.ts with export interface AsiaLineInputs holding asiaHigh and asiaLow numbers plus export function asiaLineInputs taking high and low numbers, copying the priceLineInputs shape verbatim per D-05: guard with the module isFiniteNumber predicate, throw an Error naming asiaLineInputs on any non-finite input, return the pair unchanged on finite input including the equal high-equals-low zero-width case which passes without throwing per the UI-06 adjacency predicate. Extend src/lib/chart-mapper.test.ts with cases asserting a finite pair passes through unchanged, asserting NaN high throws naming asiaLineInputs, asserting positive-Infinity low throws naming asiaLineInputs, and asserting an equal high-low pair returns coincident values without throwing.</action>
  <verify>
    <automated>npm test -- src/lib/chart-mapper.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
  </verify>
  <done>Asia mapper pinned with finite pass-through, named throws, and deterministic zero-width equality.</done>
  <acceptance_criteria>Mapper suite passes asserting finite pair equality, throws naming asiaLineInputs on NaN and on Infinity, and coincident return on equal high-low without throwing.</acceptance_criteria>
</task>

<task type="auto">
  <name>Chart Asia lines plus Judas and SMT markers with stale-dim</name>
  <files>components/charts/nq-chart.tsx</files>
  <read_first>components/charts/nq-chart.tsx, src/lib/chart-mapper.ts, src/lib/ict/judas.ts, src/lib/ict/smt.ts</read_first>
  <reversibility rating="reversible">Presentational overlay props with no store or route changes; removal restores the prior prop list.</reversibility>
  <action>Extend components/charts/nq-chart.tsx per D-05 through D-08 without touching the EQ, DOL, quadrant, OTE, zone-fill, STALE badge, MARKET CLOSED ribbon, forming chip, or empty-state paths: extend NqChartProps with asiaHigh as number or null, asiaLow as number or null, judas as JudasOutput or null, judasBarDate as string or null, smt as SmtOutput or null, smtBarDate as string or null, and overlayStale as boolean. Add asiaHighLineRef and asiaLowLineRef unknown refs beside the existing line refs per D-05. In the mount effect after the DOL line creation, wrap asiaLineInputs of the Asia props in try-catch and on success create two price lines with lineWidth 1, LineStyle Dashed, titles Asia-H and Asia-L, color accent when overlayStale is false and chart muted gray #71717A when overlayStale is true; on throw or null props leave both refs null so no lines render and the chart never blocks. In the update effect add the Asia refs to the remove-then-create cycle (remove Asia lines with the EQ and DOL removals, null them, then re-create with the same dashed accent-or-muted rule). Integrate markers with the lightweight-charts v5 plugin form only: lazily import createSeriesMarkers beside LineStyle inside the update effect, build the marker array as Judas first then SMT with at most one pin per signal per bar per the UI-06 ordering predicate — Judas candidate maps to circle shape with hollow-contrast color and text J-question-mark, Judas confirmed maps to circle shape with solid accent color and text J, sweep side HIGH maps to aboveBar and LOW maps to belowBar with time judasBarDate, SMT unsuppressed BULLISH maps to arrowUp belowBar with text S and time smtBarDate, SMT unsuppressed BEARISH maps to arrowDown aboveBar with text S, SMT NO-SIGNAL or suppressed or null maps to no marker; empty input calls the plugin setMarkers with an empty array so candles render clean with no placeholder pins per the UI-06 empty predicate; unresolved selectors render no markers and no placeholders per the loading consideration; stale overlayStale desaturates marker colors to muted gray while persisting markers with the existing STALE badge, never clearing on stale and never falling back to clean D1 silently per D-08. Map Judas sweepTime epoch to the containing D1 date string at the caller and pass it as judasBarDate, and pass the SMT windowEnd date as smtBarDate, so marker time always equals a D1 candle date. Add data-slot asia-lines on the line-pair presence path, data-slot judas-markers and data-slot smt-marker on the marker paths. Never add an ES series and never add ES markers. Never use the v4 series dot setMarkers form anywhere in the file.</action>
  <verify>
    <automated>grep -q "createSeriesMarkers" components/charts/nq-chart.tsx</automated>
    <fails_when>grep exits non-zero or prints nothing, meaning the v5 marker plugin path is absent</fails_when>
    <automated>grep -rn "\.setMarkers" components/charts/nq-chart.tsx | grep -v "markers.setMarkers\|plugin.setMarkers\|markersPlugin.setMarkers"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a v4-style marker call entered the chart</fails_when>
    <automated>grep -q 'data-slot="asia-lines"' components/charts/nq-chart.tsx && grep -q 'data-slot="judas-markers"' components/charts/nq-chart.tsx && grep -q 'data-slot="smt-marker"' components/charts/nq-chart.tsx</automated>
    <fails_when>any data-slot probe exits non-zero, meaning an overlay hook is missing from the chart</fails_when>
  </verify>
  <done>Asia pair plus Judas and SMT pins render with null-autoscale, dim-but-persist on stale, and ES off-chart.</done>
  <acceptance_criteria>Chart source carries Asia-H and Asia-L dashed line creation with accent-or-muted color plus remove-then-create cleanup, createSeriesMarkers mapping with candidate hollow versus confirmed solid plus SMT single pin plus empty-array clear, zero-width-safe mapper consumption, stale-dim persistence with STALE badge, no ES series, no v4 marker form, and all three overlay data-slots present.</acceptance_criteria>
</task>

<task type="auto">
  <name>Live §3 report block plus shell selector wiring</name>
  <files>components/dashboard/report.tsx, components/dashboard/terminal-shell.tsx</files>
  <read_first>components/dashboard/report.tsx, components/dashboard/terminal-shell.tsx, src/lib/report.ts, src/lib/store.ts, src/lib/ict/amd.ts, node_modules/next/dist/docs/01-app/02-guides/server-and-client-boundary.md</read_first>
  <reversibility rating="reversible">Render-only composition over Plan 01 selectors; removal restores the dimmed §3 block with no store changes.</reversibility>
  <action>Implement the live §3 block in components/dashboard/report.tsx per D-01 through D-04 and D-09 through D-12, keeping sections 1, 2, 4, 5, 6 untouched: subscribe selectLiquidityPath, selectSMT, selectAMD, and selectConfluence as stable selector functions called during render following the selectLevels precedent; render the §3 section live with the unchanged title, then a conviction line div with data-slot s3-conviction showing the CONVICTION_LABEL prefix plus the tier word where the tier comes straight from selectConfluence with no numeric transform — value word yüksək inam in accent, value word standart in muted, Label 12px prefix with Display 20px value word that wraps without truncating per the backstop. Render three sub-block divs in fixed stable order Liquidity Path with data-slot s3-liquidity-path and title Likvidlik Yolu, SMT Status with data-slot s3-smt-status and title SMT Statusu, Session AMD with data-slot s3-amd-timing and title Sessiya AMD; each sub-block independently renders live prose or its own reason string verbatim per D-01 with one reason line plus at most one tag per D-04 — Liquidity Path shows selectLiquidityPath prose or its leg refusal reason, SMT Status shows direction plus sweeper-leg plus window refs or the suppression reason plus SMT Gözlənilir tag, AMD Timing shows the amdPhase reason plus optional SMT razılaşır suffix plus an inline dimmed NY line div with data-slot s3-ny-line, class opacity-45, and exact copy NY colon Gözlənilir dash v2.0-da ölçülmür beneath live Asia plus London content per D-03; adjacent degraded sub-blocks keep separate lines and never merge per the UI-05 adjacency predicate; all-degraded stacks all three reasons in the fixed order per D-02 and the UI-05 ordering predicate; loading with lastUpdatedISO null shows the existing three-bar animate-pulse skeleton; null selectors show Məlumat yoxdur per the UI-05 empty predicate; unavailable detector families keep the locked UNAVAILABLE chip only inside the owning sub-block, never as a §3-level banner. Use exact locked copy throughout: Məlumat yoxdur, Hələlik şam məlumatı əlçatan deyil, the five AMD reason sentences, SMT razılaşır and SMT Gözlənilir tags, Asia Range Təmizlənib and Təmizlənməyib forms, London Judas Swing Baş verib and Gözlənilir forms, Formlaşan şam, STALE, MARKET CLOSED; sweeper-leg NQ-or-ES detail lives in SMT prose, never on canvas per D-07; candidates render hedge prose with zero score impact per D-12; suppressed SMT caps at base with the reason visible per D-11. Apply UI-SPEC roles exactly: Body 16px regular for reason lines and fallbacks, Label 12px regular for detail lines and dual-stamp session labels, Heading 11px semibold uppercase tracking for titles plus UNAVAILABLE chip plus STALE badge plus MARKET CLOSED ribbon plus forming chip, mono tabular-nums for prices plus badges plus session timestamps, sentence case for Azerbaijani prose, 8-point spacing tokens only. Extend components/dashboard/terminal-shell.tsx minimally: subscribe the same six selectors with the useShallow array pattern for values or stable-function derivation during render, pass asiaHigh and asiaLow from selectAsia, judas plus its bar date from selectJudas, smt plus its windowEnd date from selectSMT, and overlayStale from any overlay leg stale into NqChart props; keep the dynamic ssr-false chart loader, range and dol and levels pass-through, rollover banner, chart skeleton and empty paths, and poll ownership untouched.</action>
  <verify>
    <automated>npm test -- src/lib/report.test.ts src/lib/store.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -q 'data-slot="s3-conviction"' components/dashboard/report.tsx && grep -q 'data-slot="s3-liquidity-path"' components/dashboard/report.tsx && grep -q 'data-slot="s3-smt-status"' components/dashboard/report.tsx && grep -q 'data-slot="s3-amd-timing"' components/dashboard/report.tsx && grep -q 'data-slot="s3-ny-line"' components/dashboard/report.tsx</automated>
    <fails_when>any §3 data-slot probe exits non-zero, meaning a sub-block or conviction hook is missing from the report</fails_when>
  </verify>
  <done>Live §3 renders three honest sub-blocks plus the conviction line with stable degrade order and the shell feeds the chart overlays.</done>
  <acceptance_criteria>Report source carries the conviction line with both tier words and accent-or-muted rule, three sub-blocks in fixed order with exact titles and data-slots, per-block verbatim reasons with stable all-degraded stacking and separate adjacent lines, dimmed NY line with exact copy, skeleton plus Məlumat yoxdur paths, exact locked copy throughout, UI-SPEC typography plus color plus spacing roles, and shell subscriptions passing Asia plus Judas plus SMT plus staleness into NqChart with existing chart and poll paths untouched.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/chart-mapper.ts — new asiaLineInputs export with AsiaLineInputs interface (extended in this plan; NqChart consumes it)
- src/lib/chart-mapper.test.ts — finite, named-throw, and zero-width equality cases for asiaLineInputs (extended in this plan)
- components/charts/nq-chart.tsx — new Asia-H and Asia-L refs plus remove-then-create pair, createSeriesMarkers Judas plus SMT mapping, stale-dim persistence, asia-lines plus judas-markers plus smt-marker slots (extended in this plan; Plan 03 drill probes overlay presence)
- components/dashboard/report.tsx — live §3 section with s3-conviction line plus s3-liquidity-path, s3-smt-status, s3-amd-timing, s3-ny-line slots (extended in this plan; Plan 03 drill probes all four §3 slots)
- components/dashboard/terminal-shell.tsx — six-selector subscriptions plus Asia, Judas, SMT, and overlayStale pass-through into NqChart (extended in this plan)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store selectors → report prose | Refusal reasons and suppressed envelopes render verbatim in sub-blocks |
| Store selectors → chart overlays | Asia extremes plus sweep instants plus staleness flow into canvas lines and markers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Tampering | Overlay prop mapping (nq-chart.tsx) | medium | mitigate | Asia lines consume finite-guarded asiaLineInputs with throw rendering no lines and never blocking the chart; marker times must equal D1 dates with mapping done at the caller; empty input clears via empty marker set so no stale or placeholder pins persist across sessions |
| T-09-04 | Information disclosure (integrity) | Stale overlay presentation (nq-chart.tsx, report.tsx) | medium | mitigate | Stale overlays desaturate to muted gray with the STALE badge and persist per D-08; sub-blocks render the owning-leg refusal verbatim; the chart never falls back to clean D1 silently and ES stays off-chart with no ES series or markers |
</threat_model>

<verification>
Plan gate: mapper plus report plus store suites green with overlay contract greps clean — `npm test -- src/lib/chart-mapper.test.ts`, `npm test -- src/lib/report.test.ts src/lib/store.test.ts`, marker and data-slot greps pass, then `npm test` full suite green.
</verification>

<success_criteria>
- §3 shows three live sub-blocks plus the conviction line with per-block verbatim reasons and stable all-degraded stacking
- Asia pair plus Judas and SMT pins render with correct mapping, stale-dim persistence, and ES off-chart
- Mapper edges pinned including zero-width equality with full suite green
</success_criteria>

<output>
Create `.planning/phases/09-composition-3-live-overlays-verify/09-02-SUMMARY.md` when done
</output>
