---
phase: 09-composition-3-live-overlays-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/confluence.ts
  - src/lib/confluence.test.ts
  - src/lib/store.ts
  - src/lib/store.test.ts
  - src/lib/report.ts
  - src/lib/report.test.ts
autonomous: true
requirements:
  - ICT-15
  - UI-05
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Confirmed Judas plus aligned unsuppressed SMT derives the highest tier `yüksək inam`; every other combination (candidate, misaligned, suppressed, null, NO-SIGNAL) derives the base tier `standart` per D-09"
    - "Agreement-count threshold is explicit and testable one step either side: two agreements (confirmed + aligned) yield highest, any single agreement yields base per the ICT-15 boundary predicate"
    - "Conviction output is discrete tier words only, never numeric or percentage-like, and numeric coercion of any tier is NaN per the ICT-15 precision predicate"
    - "Suppressed SMT caps confluence at the base tier with the suppression reason visible in SMT state; absence never reads as disagreement and no separate unrated state exists per D-11"
    - "Candidate Judas renders zero score impact; only confirmed (solid) Judas counts toward highest conviction per D-12"
    - "Each §3 input selector refuses with a stated reason on stale or empty legs and never throws on bad data; null means degraded with the owning leg lastError carrying the reason per D-01 and D-15"
    - "All-degraded input (every leg stale or empty) yields all selectors null plus base-tier confluence, so the worst case keeps three renderable reasons and never collapses per D-02"
    - "Empty or null detector input yields null prose input, which the render contract turns into the `Məlumat yoxdur` fallback or the detector reason, never a blank block or a crash per the UI-05 empty predicate"
    - "Confluence agreement counting covers zero, one, and many agreements deterministically: suppressed caps at base, candidates score zero, confirmed plus aligned SMT yields highest per the zero-one-many consideration"
  artifacts:
    - path: "src/lib/confluence.ts"
      provides: "Rule-based conviction tier derivation from confirmed-Judas plus unsuppressed-aligned-SMT agreement count"
      exports: ["ConvictionTier", "deriveConvictionTier"]
      contains: "yüksək inam"
    - path: "src/lib/confluence.test.ts"
      provides: "Highest-tier, suppression-cap, candidate-zero-impact, boundary one-step-either-side, and discreteness cases"
      min_lines: 120
    - path: "src/lib/store.ts"
      provides: "nq1h/nq15m intraday legs with per-leg envelopes plus six §3 selectors plus extended stagger grid"
      contains: "selectConfluence"
    - path: "src/lib/store.test.ts"
      provides: "Intraday refresher, stagger-offset, independent-failure, stale-refusal, and all-degraded selector fixtures"
    - path: "src/lib/report.ts"
      provides: "§3 index-3 live flip plus conviction-line label constant"
      contains: "CONVICTION_LABEL"
    - path: "src/lib/report.test.ts"
      provides: "§3 live contract plus conviction label pins"
  key_links:
    - from: "src/lib/store.ts"
      to: "src/lib/confluence.ts"
      via: "selectConfluence reads selectJudas plus selectSMT and returns deriveConvictionTier output, never null"
      pattern: "deriveConvictionTier"
    - from: "src/lib/store.ts"
      to: "src/lib/ict/asia.ts"
      via: "selectAsia feeds closedOnlyIntraday nq1h rows plus an NY-date sessionDate into asiaRange"
      pattern: "asiaRange"
    - from: "src/lib/store.ts"
      to: "src/lib/ict/judas.ts"
      via: "selectJudas feeds closed 15M rows plus the Asia range into judasSwing"
      pattern: "judasSwing"
    - from: "src/lib/store.ts"
      to: "src/lib/ict/smt.ts"
      via: "selectSMT feeds joined daily legs plus asOfBaku into evaluateSMT after per-leg stale refusal"
      pattern: "evaluateSMT"
    - from: "src/lib/store.ts"
      to: "src/lib/ict/amd.ts"
      via: "selectAMD fuses selectAsia plus selectJudas plus selectSMT plus an injected epoch into amdPhase"
      pattern: "amdPhase"
    - from: "src/lib/store.ts"
      to: "src/lib/ict/fvg.ts"
      via: "selectLiquidityPath derives delivery prose via detectFVGs, applyMitigation, detectTransition, describeDeliveryTransition"
      pattern: "describeDeliveryTransition"
  prohibitions:
    - "Never present a numeric confidence score or percentage-like conviction value anywhere in tiers, selectors, prose, or tests"
    - "Never hide or remove live user-visible state on stale or degraded input; refuse with a stated reason and let the block dim with its reason instead"
    - "Never invent detector prose or numeric approximations; render detector reason strings verbatim and derive session levels from intraday legs only, never daily-derived fallbacks"
---

<objective>
Tracer conviction trunk for Phase 9: intraday store legs plus rule-based SMT/Judas confluence tiers plus the §3 live report contract, end to end on the report path per D-09 through D-15.

Purpose: This is the phase tracer (TRACER_MODE true): the thinnest production-quality slice proving intraday legs → selectors → tier derivation → live §3 contract works before chart overlays (Plan 02) and the live-URL drill (Plan 03) expand from it. The chart canvas is a parallel presentation leaf on the same selector trunk, proven in Plan 02; the tracer proves the trunk through the report contract as the far end of the data stack.
Output: New selector-level module src/lib/confluence.ts with its suite, extended src/lib/store.ts (nq1h/nq15m legs plus six selectors plus stagger), flipped src/lib/report.ts (§3 live plus conviction label) with contract tests. No chart, no network route changes, no new packages.
SCHEMA_PUSH_REQUIRED=false (no Prisma, Drizzle, Supabase, or other ORM schema files in scope; filesystem fixtures plus Yahoo proxy plus Zustand only).
API coverage: no new external surface in this plan — the two intraday fetch URLs reuse the Phase 6 parameterized proxy shape with already-allowlisted symbol NQ=F and intervals 1h and 15m; the reasoned no-new-integration declaration lands in 09-COVERAGE.md in Plan 03.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composition-3-live-overlays-verify/09-CONTEXT.md
@.planning/phases/09-composition-3-live-overlays-verify/09-RESEARCH.md
@.planning/phases/09-composition-3-live-overlays-verify/09-PATTERNS.md
@.planning/phases/09-composition-3-live-overlays-verify/09-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: end-to-end conviction trunk — one path only</name>
  <files>src/lib/confluence.ts, src/lib/confluence.test.ts, src/lib/store.ts, src/lib/report.ts</files>
  <read_first>src/lib/ict/judas.ts, src/lib/ict/smt.ts, src/lib/ict/amd.ts, src/lib/store.ts, src/lib/report.ts</read_first>
  <reversibility rating="costly">Two more polled legs flow into store shape, stagger offsets, selectors, and leg-aware tests per D-13; removal means unwiring legs, timers, and six selectors together.</reversibility>
  <action>Create src/lib/confluence.ts as selector-level scoring (never inside src/lib/ict, keeping ict purity per D-09 context): import the JudasOutput type from @/src/lib/ict/judas and the SmtOutput type from @/src/lib/ict/smt; export type ConvictionTier as the literal union standart and yüksək inam (display-ready words, no mapping table, nothing numeric per D-09); export function deriveConvictionTier taking judas (JudasOutput or null) and smt (SmtOutput or null) returning ConvictionTier with the exact rule — return yüksək inam only when judas is non-null with confirmed true and smt is non-null with suppressed false and the direction aligns with the sweep side (HIGH sweep with BEARISH, LOW sweep with BULLISH, copying the amd.ts smtTag agreement read verbatim per D-09 and D-12); return standart otherwise, which bakes in D-11 (suppressed caps at base, no separate state) and D-12 (candidate confirmed false scores zero). Keep the module free of clock reads and store imports. Extend src/lib/store.ts minimally for the single path: add exported interface IntradayLegState with candles as IntradayCandle rows plus contractHint, lastUpdatedISO, stale, source, lastError (same envelope shape as LegState, epoch rows per the Phase 6 epoch contract); add nq1h and nq15m state via an emptyIntradayLeg helper per D-13; add inFlightNQ1H and inFlightNQ15M flags; add an intraday envelope guard mirroring isValidEnvelope with epoch-ascending time validation plus a fetchIntradayEnvelope helper; add refreshNQ1H fetching the NQ 1h envelope URL and refreshNQ15M fetching the NQ 15m envelope URL, each copying the refreshES independent-failure shape (own singleflight flag, own leg stale plus lastError on failure, coverage recompute untouched, never writing sibling legs) per D-13 and D-15; add selectSMT returning evaluateSMT over the daily legs after refusing null when either daily leg is stale or holds zero candles; add selectAsia returning asiaRange over closedOnlyIntraday nq1h rows with the sessionDate taken as the NY calendar date of the latest closed 1H candle, refusing null on stale or empty legs; add selectJudas returning judasSwing over closed 15M rows plus the Asia range, refusing null when Asia is null or the leg is stale or empty; add selectAMD returning amdPhase over asia plus judas plus smt plus an epoch derived from the stored lastUpdatedISO instant with wall-clock fallback at call time; add selectConfluence returning deriveConvictionTier of selectJudas and selectSMT output, never null (nulls yield standart per D-11); add selectLiquidityPath returning describeDeliveryTransition prose from detectFVGs plus applyMitigation plus detectTransition over daily candles with asOfBaku, null on empty input (keeps components math-free; sixth selector beside the five named in PATTERNS because FVG prose needs the same refuse-with-reason envelope). Wrap every detector call in try/catch returning null so selectors never throw on bad data per D-15. Extend startDualPoll minimally: keep the NQ :00 and ES :30 grid, add the nq1h leg at :15 and the nq15m leg at :45 each with the existing jitter helper, record all four offsets in lastSchedule additively (delayNQ and delayES names unchanged), fire the first intraday refreshes immediately on start, and extend clearDualTimers plus stopDualPoll for the new timers with offsets in state, never module cells, keeping the uniform 60s cadence per D-14. Flip src/lib/report.ts index 3 to live with the title unchanged and export CONVICTION_LABEL with the exact value İnam plus colon plus trailing space. Create src/lib/confluence.test.ts with small JudasOutput and SmtOutput builder helpers covering the single happy path plus its nearest neighbors: confirmed HIGH plus BEARISH yields highest, confirmed LOW plus BULLISH yields highest, candidate plus aligned yields base, confirmed plus misaligned yields base, confirmed plus suppressed yields base, null judas yields base, null smt yields base, NO-SIGNAL yields base.</action>
  <verify>
    <automated>npm test -- src/lib/confluence.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/confluence.ts src/lib/confluence.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure scoring module or its test</fails_when>
  </verify>
  <done>Single happy path works end to end and is committed: confirmed Judas plus aligned SMT derives the highest tier through the store selectors with §3 live.</done>
  <acceptance_criteria>Confluence suite passes asserting highest tier on both aligned directions and base tier on candidate, misaligned, suppressed, null-judas, null-smt, and NO-SIGNAL inputs; store holds nq1h and nq15m legs with independent refreshers and six selectors; report index 3 reads live with the conviction label constant; purity grep over the new module and test returns exit 1 with no output.</acceptance_criteria>
</task>

<task type="auto">
  <name>Intraday stagger plus selector refusal fixtures</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/store.test.ts, src/lib/ict/asia.ts, src/lib/ict/judas.ts</read_first>
  <reversibility rating="costly">Extends the D-13 store shape (stagger grid, four-leg timers); unwiring touches every leg-aware test.</reversibility>
  <action>Harden the Plan 01 tracer trunk in src/lib/store.ts per D-13, D-14, and D-15: pin the four-leg stagger grid so NQ fires at second 00, nq1h at second 15, ES at second 30, and nq15m at second 45, each plus the existing 5 to 10 second jitter, with all four offsets recorded in lastSchedule state and exactly one timer pair per leg owned by startDualPoll and cleared by stopDualPoll; keep asOfBaku refresh on intraday success via the existing Baku helper at call time; keep the per-leg envelope guard rejecting malformed intraday payloads without touching healthy legs. Extend src/lib/store.test.ts with URL-dispatched fetch mocks serving distinct contractHints for the daily NQ, ES daily, NQ 1h, and NQ 15m envelopes: assert startDualPoll with a fixed clock pins delayNQ on the :00 grid, delayNQ1H near :15, delayES with the :30 phase, and delayNQ15M near :45 within jitter bounds; assert a failed 15m refresh marks only nq15m stale with its message while nq1h stays live; assert stale nq1h forces selectAsia null, stale ES forces selectSMT null, and empty legs force nulls without throwing; assert the all-degraded state (every leg stale) yields selectSMT, selectAsia, selectJudas, selectAMD, and selectLiquidityPath all null with selectConfluence at base tier, giving the render layer three renderable reasons and no collapse per D-02.</action>
  <verify>
    <automated>npm test -- src/lib/store.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn 'Date\.now' src/lib/store.ts src/lib/store.test.ts | grep -v '//'; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a Date.now clock read entered the store or its test</fails_when>
  </verify>
  <done>Four-leg stagger and refusal behavior pinned: offsets phase-locked, failures independent, stale legs refuse with reasons, all-degraded stays renderable.</done>
  <acceptance_criteria>Store suite passes asserting four stagger offsets within jitter bounds of the :00, :15, :30, and :45 grid, single-leg 15m failure isolating to nq15m, stale-leg selector nulls with preserved lastError reasons, all-degraded nulls with base-tier confluence, and the Date.now probe grep returning exit 1 with no output.</acceptance_criteria>
</task>

<task type="auto">
  <name>Tier boundary plus discreteness plus report contract tests</name>
  <files>src/lib/confluence.test.ts, src/lib/report.ts, src/lib/report.test.ts</files>
  <read_first>src/lib/confluence.ts, src/lib/report.ts, src/lib/report.test.ts</read_first>
  <action>Extend src/lib/confluence.test.ts with the ICT-15 edge predicates per D-09: tier-boundary cases asserting the exact agreement-count threshold one step either side (two agreements confirmed-plus-aligned yield highest; each one-agreement neighbor — confirmed-plus-misaligned, confirmed-plus-suppressed, candidate-plus-aligned — yields base; zero agreements yield base), and precision cases asserting every tier value is exactly one of the two word literals, asserting numeric coercion of each tier is NaN, and asserting no tier string contains a digit or percent sign, so off-by-one tier assignment and number-like output are both testable. Keep src/lib/report.ts to the index-3 live flip plus the conviction label constant (tier display words stay sourced from confluence.ts with no duplicated mapping table). Extend src/lib/report.test.ts asserting exactly two live sections (indexes 2 and 3), the unchanged §3 title 3. LIQUIDITY SEQUENCING and CROSS-MARKET SMT (1H/15M), and the conviction label constant equal to the locked İnam prefix with trailing space.</action>
  <verify>
    <automated>npm test -- src/lib/report.test.ts src/lib/confluence.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>npm test</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
  </verify>
  <done>Tier edges and the §3 live contract pinned with the full suite green and no regressions.</done>
  <acceptance_criteria>Boundary cases pass with highest tier only at two agreements and base at every one-step neighbor; discreteness cases pass with exact word literals, NaN coercion, and no digit or percent characters; report tests pass with two live sections and the exact §3 title plus label; full npm test run reports zero failed suites.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/confluence.ts — exports ConvictionTier (standart, yüksək inam) and deriveConvictionTier(judas, smt); selector-level scoring, never ict math (new in this plan; store selectConfluence and Plan 02 conviction line consume it)
- src/lib/confluence.test.ts — builder helpers plus happy-path, boundary one-step-either-side, suppression-cap, candidate-zero-impact, and discreteness cases (new in this plan)
- src/lib/store.ts — new IntradayLegState interface, nq1h and nq15m legs, inFlightNQ1H and inFlightNQ15M flags, refreshNQ1H and refreshNQ15M refreshers, extended lastSchedule with delayNQ1H and delayNQ15M, six selectors selectSMT, selectAsia, selectJudas, selectAMD, selectConfluence, selectLiquidityPath (extended in this plan; Plan 02 report and chart consume them)
- src/lib/store.test.ts — URL-dispatched intraday fixtures plus stagger, independent-failure, stale-refusal, and all-degraded cases (extended in this plan)
- src/lib/report.ts — §3 index 3 unavailable-to-live flip plus CONVICTION_LABEL İnam prefix constant (extended in this plan; Plan 02 report component consumes both)
- src/lib/report.test.ts — two-live-sections contract plus §3 title and label pins (extended in this plan)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo intraday rows → store legs | Untrusted numeric OHLC plus epoch times cross here; malformed rows possible |
| Store selectors → §3 prose inputs | Suppressed envelopes and refusal reasons flow here and render verbatim downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | Intraday envelope guard plus selectors (store.ts) | medium | mitigate | Per-leg shape guard (finite OHLC, epoch-ascending, non-empty contractHint, parseable ISO) rejects malformed legs without touching healthy legs; selectors wrap detector calls so bad data yields null plus reason, never a throw into render |
| T-09-02 | Information disclosure (integrity) | Stale intraday legs (store.ts) | medium | mitigate | Per-leg stale plus lastError envelopes preserved independently; selectors refuse with the owning-leg reason so stale session levels never masquerade as live and no daily-derived fallback approximations are constructed |
</threat_model>

<verification>
Phase gate for this plan: narrow suites plus full suite green with purity clean — `npm test -- src/lib/confluence.test.ts`, `npm test -- src/lib/store.test.ts`, `npm test -- src/lib/report.test.ts src/lib/confluence.test.ts`, then `npm test` all pass; purity greps over the new and touched modules return exit 1.
</verification>

<success_criteria>
- Highest conviction derives only from confirmed Judas plus aligned unsuppressed SMT; every neighbor state derives base
- Intraday legs poll on the four-phase 60s grid with independent per-leg freshness and refusal
- Report contract holds §3 live with the locked title and conviction label; full suite green
</success_criteria>

<output>
Create `.planning/phases/09-composition-3-live-overlays-verify/09-01-SUMMARY.md` when done
</output>
