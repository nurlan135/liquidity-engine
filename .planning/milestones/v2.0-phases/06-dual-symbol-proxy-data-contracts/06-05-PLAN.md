---
phase: 06-dual-symbol-proxy-data-contracts
plan: 05
type: execute
wave: 1
depends_on: [06-01]
files_modified: [app/api/yahoo/route.ts, app/api/yahoo/route.test.ts]
autonomous: true
gap_closure: true
requirements: [DATA-04, DATA-05, DATA-06, DATA-07]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Route dispatches interval 1d to fetchSymbol and 1h/15m to fetchIntraday per D-08 allowlist order, preserving the fresh/stale Cache-Control shapes per D-12 and the 502 shape on both lanes"
    - "GET /api/yahoo?symbol=ES=F&interval=1h returns 200 with epoch-integer times per D-16 and ES contractHint per D-04"
    - "Intraday failure path returns honest 502/stale per D-06 and D-10, never daily-shaped data"
  artifacts:
    - app/api/yahoo/route.ts
    - app/api/yahoo/route.test.ts
  key_links:
    - route.ts GET to fetchIntraday: 1h/15m requests reach the epoch lane after the allowlist guards
    - route.ts GET to fetchSymbol: 1d requests keep the exact current daily path with identical headers
  prohibitions:
    - "MUST NOT label a served-stale intraday envelope as fresh — stale intraday responses carry stale:true with no-store, identical honesty semantics to the daily lane"
    - "MUST NOT return daily-contract date-string rows for a 1h/15m request — intraday responses carry epoch-integer times or an honest 502/stale, never a mislabeled contract"
---

<objective>
Close the single VERIFICATION.md root cause: wire the orphaned intraday lane into the parameterized proxy so 1h/15m requests stop falling into the daily date-string parser, with route tests pinning both the happy path and the honest-error path.

Purpose: Restore the SC2 serving dimension (NQ + ES 1H/15M through the proxy) without touching the proven daily path, the D1 anchor, or any executed plan.
Output: Interval dispatch in route.ts plus two new route tests, full suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-CONTEXT.md
@src/lib/yahoo.ts
@app/api/yahoo/route.ts
@app/api/yahoo/route.test.ts
</context>

## Source audit (this plan)

| SOURCE | ID | Feature | Status |
|--------|----|---------|--------|
| GOAL | — | Terminal serves dual-symbol daily + intraday candles through honest per-symbol stale envelopes | COVERED (dispatch wires intraday serving; daily lane preserved) |
| REQ | DATA-04 | ES=F daily via parameterized proxy, NQ pipe untouched | COVERED (1d lane delegates to fetchSymbol exactly as today; existing bare/ES-daily tests stay green) |
| REQ | DATA-05 | NQ + ES 1H/15M epoch contract through the proxy | COVERED (1h/15m lane delegates to fetchIntraday; new happy-path test) |
| REQ | DATA-06 | Per-leg stale honesty, no merged boolean | COVERED (intraday lane reuses the identical stale ternary + 502 shape; SMT refusal stays Phase 7 per VERIFICATION deferred) |
| REQ | DATA-07 | Timestamp inner-join, anchor untouched | COVERED (no join/anchor file touched; range.ts explicitly excluded) |
| RESEARCH | — | Epoch contract, bounded ranges, validate-then-cache, allowlist 400s | COVERED BY REFERENCE (all implemented + tested in src/lib/yahoo.ts; read-only, no changes) |
| CONTEXT | D-05 | Bare GET byte-identical NQ daily | COVERED (existing bare-GET test unchanged and green) |
| CONTEXT | D-06 | Never substitute across legs | COVERED (separate payloadCache vs intradayPayloadCache maps; dispatch never crosses lanes) |
| CONTEXT | D-08 | Strict allowlists, 400 never forwarded | COVERED (dispatch branch sits strictly after both allowlist guards) |
| CONTEXT | D-12 | Same CDN Cache-Control on all combos | COVERED (shared header ternary reused unchanged) |
| CONTEXT | D-16 | Epoch intraday, D1 strings stay | COVERED (lanes emit their own contracts; no unification) |
| CONTEXT | D-01 | Stagger lives in client loops, route stays dumb per-leg fetch | PRESERVED (no store change; dispatch adds no coupling, no Promise.all, per Phase 7 ownership of intraday polling) |
| CONTEXT | D-02 | Uniform 60s cadence, bounded intraday windows | PRESERVED (server-derived RANGE_FOR_INTERVAL read-only; no range value touched) |
| CONTEXT | D-03 | Polling always on | PRESERVED (no timer code touched) |
| CONTEXT | D-07 | D1 anchor path independent | PRESERVED (range.ts and computeRange call sites get zero edits; verified in plan verification gate) |
| CONTEXT | D-09 | Uniform 60s TTL | PRESERVED (CACHE_TTL_MS lives in yahoo.ts, untouched) |
| CONTEXT | D-10 | 30-min serve-stale ceiling | PRESERVED (MAX_STALE_MS lives in yahoo.ts, untouched) |
| CONTEXT | D-11 | Shared Map with composite keys | PRESERVED (cacheKey plus both Maps live in yahoo.ts, untouched; dispatch preserves key inputs) |
| CONTEXT | D-13 | Coverage rides in envelope | PRESERVED (join/store untouched; coverage path unaffected) |
| CONTEXT | D-14 | Join warns, never refuses | PRESERVED (join.ts untouched) |
| CONTEXT | D-15 | Forming exclusion at both layers | PRESERVED (parser flags plus join drops, both untouched; intraday lane carries the flagged contract through) |

No deferred ideas exist in CONTEXT.md. No phase split needed: one surgical slice, two tasks, two files.

## Flagged assumptions (specless probe — all 7 rows unresolved, surfaced not dropped)

- DATA-04 adjacency unresolved: daily vs intraday lanes for the same symbol must never share cache entries. Standing resolution: payloadCache and intradayPayloadCache are already separate Maps in src/lib/yahoo.ts; the dispatch must pass the identical allowlisted symbol and interval into the lane function so composite keys cannot collapse.
- DATA-04 empty unresolved: an empty intraday upstream payload must never yield a live envelope. Standing resolution: fetchIntraday throws UpstreamError on empty payloads and the route catch maps it to 502; pinned by the Task 2 failure test.
- DATA-04 ordering unresolved: intraday row order through the route must be stable and ascending. Standing resolution: parseIntradayChartJson enforces strictly ascending epoch time; the route returns the lane envelope rows verbatim without re-sorting.
- DATA-04 concurrency unresolved: parallel refreshes of the same intraday combo must join one upstream call. Standing resolution: per-key singleflight already exists in the intraday lane; the route adds no shared mutable state and no Promise.all coupling.
- DATA-05 unclassified unresolved: review manually — the intraday epoch shape arriving through the route matches the unit-level contract. Pinned explicitly by the Task 1 test asserting integer time on every row and 30 rows from the fixture.
- DATA-06 unclassified unresolved: review manually — per-leg stale honesty on the new lane. Pinned by reusing the identical stale ternary and asserting Cache-Control on both fresh and failure responses.
- DATA-07 unclassified unresolved: review manually — join and anchor behavior unchanged. Held by touching neither src/lib/ict nor src/lib/store.ts in this plan.

## API coverage declaration

No external API integration: the api-coverage detector returned detected:false over the Phase 6 plan corpus plus roadmap entry. The Yahoo Finance chart API daily and intraday lanes were decided in prior Phase 6 plans; this plan adds no new external capability and only routes to the existing fetchIntraday entry point.

<assumption_delta_decision>
Noun: route-dispatch. Decision: add-alongside (not promote). Rationale: the single fetchSymbol call generalizes into a two-lane dispatch, but the daily lane is a byte-identical frozen contract per D-05 — promoting both lanes behind a new abstraction risks the proven path. A branch alongside keeps each lane calling its existing tested entry with zero shared-code churn.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>End-to-end intraday lane — route dispatch plus 1h happy-path test</name>
  <files>app/api/yahoo/route.ts, app/api/yahoo/route.test.ts</files>
  <read_first>app/api/yahoo/route.ts, src/lib/yahoo.ts, app/api/yahoo/route.test.ts, src/lib/__fixtures__/intraday-1h.json</read_first>
  <action>Read src/lib/yahoo.ts first for the fetchIntraday symbol interval now signature and the IntradayEnvelope stale and source fields, and read the vendored Next.js route-handler guide section on request nextUrl only to confirm the existing GET request pattern needs no change. Then edit GET in app/api/yahoo/route.ts: extend the existing lib import to include fetchIntraday alongside fetchSymbol and keep every other import identical; leave the bare-default param reads, the two allowlist 400 guards, force-dynamic, maxDuration 15, the stale-aware Cache-Control ternary, and the 502 error with retryAfter 60 catch block exactly as they are. Replace the single fetchSymbol call with an interval branch placed strictly after both allowlist guards: when intervalParam equals 1d call fetchSymbol with symbolParam intervalParam and a fresh Date, otherwise call fetchIntraday with the same three arguments. Rely on the unchanged envelope stale header ternary since IntradayEnvelope carries identical stale and source fields. Do not modify src/lib/yahoo.ts, src/lib/ict/range.ts, src/lib/store.ts, or any 06-01 through 06-04 plan file. Then extend app/api/yahoo/route.test.ts with a test titled GET with symbol ES=F and interval 1h returns the intraday epoch envelope that imports the intraday-1h fixture, stubs global fetch to serve it, issues GET against http://localhost/api/yahoo?symbol=ES%3DF&interval=1h, and asserts status 200, contractHint containing ES=F, candles length 30, every candle time an integer with no date key present, stale false, and the fresh public s-maxage 60 header.</action>
  <verify>
    <automated>npx vitest run app/api/yahoo/route.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>The single 1h happy path works end-to-end through route dispatch into fetchIntraday and is committed, with the 5 pre-existing route tests still green.</done>
  <acceptance_criteria>route.ts contains the fetchIntraday import and the intervalParam equals 1d branch selecting fetchSymbol versus fetchIntraday; the new 1h test returns 200 with 30 candles each carrying integer time and contractHint containing ES=F; bare GET, ES daily, both 400, and stale-header tests still pass; vitest exits 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Intraday honest-error route test plus full regression gate</name>
  <files>app/api/yahoo/route.test.ts</files>
  <read_first>app/api/yahoo/route.ts, app/api/yahoo/route.test.ts</read_first>
  <action>Add a test titled intraday upstream failure returns honest 502 or stale never daily-shaped data to app/api/yahoo/route.test.ts: with the existing beforeEach cache reset giving an empty cache, stub global fetch to status-500 responses, drive GET against http://localhost/api/yahoo?symbol=ES%3DF&interval=1h under fake timers mirroring the existing stale-envelope test by setting system time then advancing timers to exhaust the retry backoff, and assert status 502, body error as a non-empty string, body retryAfter equal to 60, Cache-Control equal to no-store, Retry-After equal to 60, and no candles key and no date key anywhere in the body. Touch no source file in this task. Then run the full suite and the type gate.</action>
  <verify>
    <automated>npx vitest run app/api/yahoo/route.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test file</fails_when>
  </verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <done>Intraday failure is proven honest at the route boundary and the whole repository stays green with no new type errors.</done>
  <acceptance_criteria>Failure test returns 502 with retryAfter 60, no-store plus Retry-After 60 headers, and a body without candles or date keys; npm test exits 0 with zero failing files; npx tsc --noEmit exits 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→route | Untrusted symbol and interval query values cross here, now selecting between two lanes |
| route→Yahoo | Server-side egress to query1/query2 hosts over the intraday range-style path |
| lane→cache | Daily and intraday lanes share the route but must never share cache entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | app/api/yahoo/route.ts lane dispatch | high | mitigate | Both allowlist guards run before lane selection so only allowlisted symbol and interval values ever reach a lane; fetchIntraday keeps its defense-in-depth guard rejecting the 1d interval on direct calls |
| T-06-06 | Tampering | app/api/yahoo/route.ts per-combo cache separation | high | mitigate | Dispatch passes the identical allowlisted symbol and interval into the lane function; daily uses payloadCache and intraday uses intradayPayloadCache with composite symbol-interval-range keys, so no lane can read or poison the other |
| T-06-07 | Information Disclosure | app/api/yahoo/route.ts 502 path on intraday lane | medium | mitigate | The shared catch maps every lane error to error plus retryAfter 60 with no-store and Retry-After 60; raw upstream bodies never enter the response, only the UpstreamError message or the static upstream unavailable string |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan; existing stack only, verified by unchanged package.json |
</threat_model>

<verification>
Full suite npm test stays green including the 163-test baseline plus the 2 new intraday route tests. Type gate npx tsc --noEmit exits 0. src/lib/ict/range.ts and src/lib/store.ts show no diff. No unit test performs a live network call.
</verification>

<success_criteria>
GET with interval 1h or 15m serves epoch-integer intraday rows through fetchIntraday with ES contractHint; 1d serves the byte-identical daily path; failures on either lane return the honest 502 shape with no-store; the two VERIFICATION.md failed truths flip to verified on re-run.
</success_criteria>

## Artifacts this phase produces

- Interval dispatch branch in app/api/yahoo/route.ts GET: 1d selects fetchSymbol, 1h/15m selects fetchIntraday, after the allowlist guards with shared header and 502 shapes (modifies existing GET, creates no new exported symbol)
- Test GET with symbol ES=F and interval 1h returns the intraday epoch envelope in app/api/yahoo/route.test.ts: 200 plus 30 epoch-integer rows plus ES contractHint
- Test intraday upstream failure returns honest 502 or stale never daily-shaped data in app/api/yahoo/route.test.ts: 502 plus retryAfter 60 plus no-store, body free of candles and date keys
- File paths touched: app/api/yahoo/route.ts, app/api/yahoo/route.test.ts (both git-tracked; src/lib/yahoo.ts read-only reference for the fetchIntraday signature)

<output>
Create `.planning/phases/06-dual-symbol-proxy-data-contracts/06-05-SUMMARY.md` when done
</output>
