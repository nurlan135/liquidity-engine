---
phase: 06-dual-symbol-proxy-data-contracts
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: [src/lib/store.ts, components/dashboard/terminal-shell.tsx, components/dashboard/status-strip.tsx, src/lib/store.test.ts, src/terminal-shell.test.ts]
autonomous: true
requirements: [DATA-06, DATA-05, DATA-07]

estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Dual-symbol polling staggers with NQ at second 00 and ES at second 30 plus 5 to 10 second jitter on independent store timers per D-01, never a coupled fetch
    - One leg failing marks only that leg stale and preserves its last-known rows while the other leg stays live per D-06, with no merged stale boolean anywhere
    - Status strip shows per-leg ages in NQ seconds middle-dot ES seconds style per D-04 using the existing Azerbaijani copy vocabulary
    - Coverage diagnostics ride in the data with a dev debug line per D-13 while the join warns never refuses per D-14
    - Existing single-leg shell, strip, and poll-loop assertions keep passing with the NQ daily path unchanged per D-05
  artifacts:
    - src/lib/store.ts
    - components/dashboard/terminal-shell.tsx
    - components/dashboard/status-strip.tsx
    - src/lib/store.test.ts
    - src/terminal-shell.test.ts
  key_links:
    - store.ts per-leg refreshers: Plan 02 intraday envelopes flow through staggered timers into per-leg state
    - join.ts coverage: Plan 03 coverage object surfaces through the store debug line
    - status-strip.tsx per-leg deriveStatus: existing freshness vocabulary extended never reinvented
  prohibitions:
    - Never cross-substitute NQ data for a failed ES leg per D-06
    - Never merge per-leg stale into one boolean per D-03 goal
    - Never forward unknown symbols to Yahoo per D-08
    - Never let ES gaps shorten the NQ anchor window per D-07
---

<objective>
Client expansion per D-01, D-02, D-03, D-04, D-06, D-13: split the store into staggered per-leg refreshers with independent singleflight and per-leg stale envelopes, extend the strip with paired ages, and surface coverage, while every existing shell and poll assertion keeps passing.

Purpose: Make mixed freshness honest in the open so SMT can later refuse with a stated reason instead of guessing.
Output: Dual-leg store with staggered timers, per-leg strip ages, coverage debug line, full suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-CONTEXT.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-RESEARCH.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-PATTERNS.md
@.planning/phases/06-dual-symbol-proxy-data-contracts/06-VALIDATION.md
@src/lib/store.ts
@src/lib/store.test.ts
@src/lib/freshness.ts
@components/dashboard/status-strip.tsx
@components/dashboard/terminal-shell.tsx
@src/terminal-shell.test.ts
</context>

## Flagged assumptions (specless probe — this plan)

- DATA-06 unclassified unresolved: stagger plus independent failure handling must hold under real timers. Resolution: NQ at second 00 and ES at second 30 with 5 to 10 second jitter, independent in-flight flags, per-leg catch marking only that leg stale per D-01 and D-06, asserted by fake-timer and independent-failure tests in Task 1.
- DATA-05 and DATA-07 unclassified unresolved: coverage diagnostics must be visible and comparisons must use joined rows. Resolution: coverage object in store state plus dev debug line per D-13, join consumed from Plan 03, asserted in Tasks 1 and 2.

## Source audit (this plan)

| SOURCE | ID | Feature | Status |
|--------|----|---------|--------|
| REQ | DATA-06 | Staggered polling with per-symbol stale envelopes, refuse-ready honesty | COVERED |
| REQ | DATA-05 | Coverage diagnostics surfaced | COVERED |
| REQ | DATA-07 | Comparisons on joined rows via Plan 03 join | COVERED |
| RESEARCH | — | Two-timer store design with per-leg in-flight flags | COVERED |
| RESEARCH | — | deriveStatus applied per leg, paired-age render | COVERED |
| CONTEXT | D-01 | Client-side stagger NQ 00 ES 30 plus jitter, server stays dumb | COVERED |
| CONTEXT | D-02 | Uniform 60s cadence every leg | COVERED |
| CONTEXT | D-03 | Always-on polling, no visibility or weekend pause | COVERED |
| CONTEXT | D-04 | Per-leg lastUpdated ages visible in the strip | COVERED |
| CONTEXT | D-06 | Independent failure, no cross-substitution | COVERED |
| CONTEXT | D-13 | Coverage in data plus dev debug line | COVERED |
| CONTEXT | D-14 | Warn never refuse at the data layer | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Dual-leg store with staggered refreshers and per-leg envelopes</name>
  <files>src/lib/store.ts, src/lib/store.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/store.test.ts, src/lib/freshness.ts, src/lib/ict/join.ts</read_first>
  <action>Split src/lib/store.ts per D-01, D-03, D-04, D-06, D-13 while keeping every existing NQ daily selector and behavior intact: keep the candles, contractHint, lastUpdatedISO, stale, source, inFlight, and refresh fields with exact current semantics so all existing store and shell tests pass unchanged, and add nq and es leg states each holding candles, contractHint, lastUpdatedISO, stale, source, and lastError, plus inFlightNQ and inFlightES booleans, plus refreshNQ and refreshES functions fetching /api/yahoo with symbol NQ=F and ES=F at interval 1d and cache no-store, each guarded by its own in-flight flag, each validating with the existing isValidEnvelope guard, each catch path marking only that leg stale while preserving its last-known rows, never writing one leg data into the other per D-06, and never introducing a merged stale boolean. Add startDualPoll and stopDualPoll owning two always-on 60s timers per D-02 and D-03 with NQ firing at second 00 and ES at second 30 plus 5 to 10 second jitter, computing offsets from the wall clock at start, with no visibility gating and no Promise dot all coupling. Add a coverage field holding nq es joined dropped computed via the Plan 03 innerJoinOnTimestamp over a projected working copy of the legs, warning via counts never refusing per D-14, in a shape Phase 7 selectors can consume without touching selectRange or computeRange inputs per D-07. Project each leg daily Candle row to IntradayCandle-shaped join input by mapping the business-day date string to UTC epoch seconds at midnight UTC while copying OHLC verbatim, using the projection solely for the join call while stored leg rows keep the date-string contract per D-16. Extend src/lib/store.test.ts with stubbed-fetch fake-timer cases: staggered offsets fire NQ then ES 30s apart, ES failure marks only es stale with NQ live and rows preserved, concurrent same-leg refreshes coalesce while cross-leg refreshes proceed independently, and coverage counts match the joined fixture expectation.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>Store holds two independent legs with staggered always-on timers, per-leg stale envelopes, no merged stale, and coverage, with existing single-leg assertions still green.</done>
  <acceptance_criteria>refresh with /api/yahoo still writes NQ state exactly as before; refreshES failure sets es.stale true with es rows preserved while nq.stale stays false; fake timers show NQ fetch at offset 0 to 10s and ES fetch 30 to 40s later; two concurrent refreshNQ calls trigger one fetch while refreshES proceeds; coverage equals nq 20 es 17 joined 14 on the fixture; vitest exits 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Per-leg strip ages plus coverage debug line in shell</name>
  <files>components/dashboard/status-strip.tsx, components/dashboard/terminal-shell.tsx, src/terminal-shell.test.ts</files>
  <read_first>components/dashboard/status-strip.tsx, components/dashboard/terminal-shell.tsx, src/lib/freshness.ts, src/lib/store.ts, src/terminal-shell.test.ts</read_first>
  <action>Extend components/dashboard/status-strip.tsx per D-04 to derive status independently per leg with the existing deriveStatus and formatStripAge vocabulary and no new status enum: subscribe to the nq and es leg lastUpdatedISO and stale values, render the paired age line NQ 12s middle-dot ES 48s with tabular-nums inside the existing status-strip section, keep the loading branch when either leg has never landed, and keep the locked Azerbaijani copy paths including the weekend closed ribbon. Extend components/dashboard/terminal-shell.tsx per D-01 and D-03 to replace the single visibility-gated 60s loop with startDualPoll on mount and stopDualPoll on unmount, keeping the manual Yenilə button wired to the NQ refresh path and the mount-owns-first-poll semantics, and add a dev-only coverage debug line with data-slot join-coverage rendering NQ count slash ES count slash joined count via the store coverage field, collapsible in production via the existing muted text styling, always present in data per D-13. Extend src/terminal-shell.test.ts with jsdom cases reusing the existing renderShell and stub patterns: paired ages render with both legs fresh, one stale leg shows its STALE copy while the other stays LIVE with no merged boolean, and the coverage line shows the pinned counts.</action>
  <verify>
    <automated>npx vitest run src/terminal-shell.test.ts src/lib/status-strip.test.ts src/lib/store.test.ts</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test</fails_when>
  </verify>
  <done>Strip shows per-leg ages with existing copy vocabulary, shell owns the dual staggered loop, coverage line renders, and all existing shell assertions pass.</done>
  <acceptance_criteria>Fresh legs render text matching NQ plus digits plus s plus middle-dot plus ES plus digits plus s; stale-ES envelope shows STALE in the ES segment with NQ segment LIVE; join-coverage line shows NQ 20 slash ES 17 slash joined 14 on fixture legs; mount still triggers the NQ fetch with /api/yahoo and cache no-store; vitest exits 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate with full suite plus type residual check plus live smoke</name>
  <files>src/lib/store.test.ts</files>
  <read_first>src/lib/store.test.ts, src/lib/yahoo.test.ts, src/lib/ict/join.test.ts</read_first>
  <action>Run the full phase gate with no source changes except test hardening if a gap is found: execute npm test for the full suite, execute npx tsc --noEmit and confirm the only error names app/layout.tsx LayoutProps as the pre-existing residual with no new error naming any Phase 6 file, execute the purity grep over src/lib/ict/join.ts confirming no wall-clock reads, and smoke bare GET /api/yahoo plus symbol ES=F daily plus ES=F 1h through the dev server confirming 200 envelopes with contractHint values NQ=F and ES=F and epoch times on intraday rows. Record the final counts, the tsc residual confirmation, and the three smoke statuses in the plan SUMMARY.</action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or a summary line showing any failing test file</fails_when>
  </verify>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "app/layout.tsx" | grep -E "error TS" ; test $? -eq 1</automated>
    <fails_when>exit code 0, which means a type error exists outside the pre-existing layout residual</fails_when>
  </verify>
  <done>Full suite green, no new type errors beyond the noted residual, join purity holds, and all three smoke endpoints return honest envelopes.</done>
  <acceptance_criteria>npm test exits 0 with 20 plus test files passed and zero failures; tsc filtered grep exits 1 meaning no non-layout errors; SUMMARY lists bare NQ 200, ES daily 200, ES 1h 200 with epoch times confirmed; purity grep finds no wall-clock reads in join files.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store→route | Client per-leg fetch crosses here with allowlisted params only |
| route→Yahoo | Staggered legs share the egress quota, offset to avoid synchronized bursts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Denial of Service | src/lib/store.ts dual timers | medium | mitigate | Independent per-leg fetch with per-leg singleflight and NQ 00 ES 30 stagger plus jitter, uniform 60s cadence, always-on per D-01 D-02 D-03, never a coupled fetch, D-06 no-substitution on failure |
| T-06-02 | Tampering | src/lib/store.ts per-leg guard | high | mitigate | Existing isValidEnvelope checks applied per envelope plus coverage object guard with finite non-negative integers and joined bounded by the smaller leg, malformed legs rejected without touching the healthy leg |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan; existing stack only, verified by unchanged package.json |
</threat_model>

<verification>
Full suite npm test green including the 133 baseline plus all Phase 6 cases. Type gate npx tsc --noEmit shows no error outside the pre-existing app/layout.tsx LayoutProps residual. Purity grep over new join code clean. Dev smoke of bare NQ, ES daily, and ES 1h all 200 with honest per-leg stale flags.
</verification>

<success_criteria>
Dual-symbol polling staggers load with NQ at 00 and ES at 30 plus jitter, one leg failing never touches the other, per-leg ages render in the strip, coverage diagnostics ride in the data, and every existing shell assertion still passes.
</success_criteria>

## Artifacts this phase produces

- nq and es leg states plus refreshNQ, refreshES, startDualPoll, stopDualPoll in src/lib/store.ts: staggered per-leg polling with independent singleflight
- Per-leg isValidEnvelope application plus coverage guard in src/lib/store.ts: honest per-leg validation
- Paired-age render in components/dashboard/status-strip.tsx: NQ seconds middle-dot ES seconds with existing copy
- Dual poll ownership plus join-coverage debug line in components/dashboard/terminal-shell.tsx: staggered loop with visible diagnostics

<output>
Create `.planning/phases/06-dual-symbol-proxy-data-contracts/06-04-SUMMARY.md` when done
</output>
