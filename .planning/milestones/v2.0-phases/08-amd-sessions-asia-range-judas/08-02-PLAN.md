---
phase: 08-amd-sessions-asia-range-judas
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/ict/judas.ts
  - src/lib/ict/judas.test.ts
  - scripts/judas-budget.ts
autonomous: true
requirements:
  - ICT-13
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "London Judas Swing fires only on the three-gate conjunction per D-07, gate 2, and D-11: in-killzone candle AND swept Asia extreme AND close-based reversal with Asia-height displacement"
    - "Pre-killzone sweeps surface as preRun analyst flags per D-08 and are never promoted to candidates under strict 02:00-05:00 edges"
    - "Sweeps without in-window displacement persist as unconfirmed candidates per D-12, never silently expired and never promoted"
    - "The 60-day budget dev-script confirms at most 25 percent of sessions with the DISP_MULT 0.5 seed per D-09"
  artifacts:
    - path: "src/lib/ict/judas.ts"
      provides: "Three-gate London Judas detector on 15M NQ plus AsiaRange"
      exports: ["KILLZONE_START_MIN", "KILLZONE_END_MIN", "DISP_MULT", "CONFIRM_WINDOW", "JudasOutput", "judasSwing"]
      contains: "preRun"
    - path: "src/lib/ict/judas.test.ts"
      provides: "Conjunction, candidate-vs-confirmed, preRun, window-bound, forming-exclusion tests"
      min_lines: 120
    - path: "scripts/judas-budget.ts"
      provides: "Seeded deterministic 60-session confirmed-ratio budget run"
      contains: "BUDGET confirmed="
  key_links:
    - from: "src/lib/ict/judas.ts"
      to: "src/lib/ict/asia.ts"
      via: "AsiaRange high/low/height consumed as the swept extreme and displacement denominator"
      pattern: "AsiaRange"
    - from: "src/lib/ict/judas.ts"
      to: "src/lib/ict/fvg.ts"
      via: "Gate 3 copies the sweep-then-reject two-half semantics, extended to sweep plus next 3 candles"
      pattern: "close"
    - from: "src/lib/ict/judas.ts"
      to: "src/lib/ict/aggregate.ts"
      via: "NY_TZ imported from aggregate for per-candle killzone resolution"
      pattern: "NY_TZ"
    - from: "scripts/judas-budget.ts"
      to: "src/lib/ict/judas.ts"
      via: "Budget run drives judasSwing over stitched 15M killzone strips"
      pattern: "judasSwing"
  prohibitions:
    - "Never promote a pre-killzone sweep to candidate and never confirm on pierce plus clock alone"
    - "Never use an ATR or fixed-point displacement denominator; Asia height only"
    - "Never let the budget run touch the network or the Yahoo pipes; seeded synthesis only"
---

<objective>
Three-gate London Judas detector (ICT-13) with candidate/confirmed vocabulary plus the 60-day budget dev-script — per D-05 through D-12.

Purpose: The 15M-sweep side of the D-05 mixed-interval split (costly): killzone membership, Asia-extreme sweep, and close-based reversal with Asia-height displacement must all conjoin before confirmation, with near-misses held honestly as candidates or preRun flags.
Output: New pure module src/lib/ict/judas.ts, its vitest suite, and new dev-script scripts/judas-budget.ts. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-amd-sessions-asia-range-judas/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Judas three-gate detector with candidate and preRun states</name>
  <reversibility rating="costly">The 15M-sweep side of the 1H-range/15M-sweep split flows into detector signatures, fixtures, and Phase 9 selector inputs per D-05.</reversibility>
  <files>src/lib/ict/judas.ts, src/lib/ict/judas.test.ts</files>
  <read_first>src/lib/ict/asia.ts, src/lib/ict/fvg.ts, src/lib/ict/aggregate.ts, src/lib/ict/types.ts, src/lib/ict/aggregate.test.ts</read_first>
  <action>Create src/lib/ict/judas.ts following the fvg.ts house template (@-aliased type imports, caps constants at module top, forming-exclusion first line, fail-fast throws with echoed values). Import NY_TZ from the aggregate module (reuse, do not redefine), formatInTimeZone from date-fns-tz, the IntradayCandle type plus closedOnlyIntraday from the types module, and the AsiaRange type from the asia module. Export constants KILLZONE_START_MIN = 120 and KILLZONE_END_MIN = 300 with a D-07 strict-inequality comment, DISP_MULT = 0.5 with a D-09 seed comment (tuned by the budget run), CONFIRM_WINDOW = 4 with a D-10 sweep-plus-next-3 comment, and TOL_EPS = 1e-9 for displacement boundary compares. Export type SweepSide as HIGH or LOW and interface JudasOutput with boolean candidate, boolean confirmed, boolean preRun, sweepSide of SweepSide or null, sweepTime epoch seconds or null, and numeric displacementMult. Implement export judasSwing taking 15M IntradayCandle rows plus an AsiaRange plus an optional dispMult defaulting to DISP_MULT: throw with the echoed received value on non-array candle input per the fvg precedent, on non-finite Asia high/low or height at or below zero, and on non-finite or non-positive dispMult; drop forming rows via closedOnlyIntraday, then drop rows with any non-finite OHLC field and rows with non-finite or non-positive time; sort ascending by time with first-row-wins timestamp dedupe on copies so sweep-first ordering holds and caller arrays are never mutated; resolve each candle to NY minutes since midnight via formatInTimeZone at call time with minute precision per D-07; scan chronologically with the first sweep winning — HIGH sweep when candle high exceeds Asia high, LOW sweep when candle low goes below Asia low; when the sweeping candle falls outside the strict killzone (minutes at or below 120, or at or above 300) return preRun true with candidate false, confirmed false, the sweep side and sweep time set, and displacementMult zero per D-08 with preRun and candidate mutually exclusive; when the sweep is inside the killzone set candidate true per gates 1 plus 2, then evaluate gate 3 over the sweep candle plus the next 3 rows per D-10 — HIGH confirms when any in-window candle closes at or below Asia high with Asia high minus that close at or above dispMult times Asia height per D-09 and D-11, LOW mirrors with close at or above Asia low and close minus Asia low at or above dispMult times height; set confirmed true only on that conjunction with confirmed implying candidate, otherwise leave confirmed false so pierce-without-reversal persists as candidate per D-12; report displacementMult as the realized maximum in-window close distance beyond the swept extreme divided by Asia height, floored at zero. Take NQ rows only with no ES parameters per D-06, keep the module free of current-time reads and store imports, and never re-derive rollover or SMT state per the read-only rule. Create src/lib/ict/judas.test.ts copying the row builder, the fromZonedTime-based nyHourEpoch helper, and the consecutive-block builder verbatim from aggregate.test.ts lines 13 through 45 with STEP 900 plus a minute-precision epoch helper built on fromZonedTime with HH:MM input mirroring the nyHourEpoch shape, and cover: HIGH-side full conjunction asserting candidate true, confirmed true, sweepSide HIGH, and displacementMult at or above 0.5; LOW-side mirror asserting sweepSide LOW; pierce-without-reversal asserting candidate true with confirmed false; a 01:47 pre-killzone sweep asserting preRun true with candidate false and confirmed false; a reversal landing on the fifth candle after the sweep asserting confirmed false under the 4-candle bound; a 05:00-edge sweep asserting preRun under strict inequality; forming exclusion; empty input returning all-false states with null side, null time, and zero displacementMult; determinism with deep-equal consecutive outputs; and a constant pin asserting DISP_MULT equals 0.5.</action>
  <verify>
    <automated>npm test -- src/lib/ict/judas.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/judas.ts src/lib/ict/judas.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module or its test</fails_when>
  </verify>
  <done>Detector green: three-gate conjunction confirms, near-misses hold as candidate or preRun, window bound and polarity mirrors pinned.</done>
  <acceptance_criteria>Suite passes asserting confirmed true with sweepSide HIGH and displacementMult at or above 0.5 on the full conjunction, candidate true with confirmed false on pierce-without-reversal, preRun true with candidate false on the 01:47 fixture, confirmed false on the fifth-candle reversal, DISP_MULT exactly 0.5, and the purity grep gate returning exit 1 with no output.</acceptance_criteria>
</task>

<task type="auto">
  <name>60-day Judas budget dev-script with seeded synthesis</name>
  <files>scripts/judas-budget.ts</files>
  <read_first>src/lib/ict/judas.ts, src/lib/ict/asia.ts, src/lib/ict/types.ts</read_first>
  <action>Create scripts/judas-budget.ts as a dependency-free Node script using relative imports only (no @ alias so plain node executes it) that imports asiaRange, judasSwing, and the fromZonedTime helper from date-fns-tz. Seed a mulberry32 generator with the fixed integer 8 and synthesize 60 consecutive NY sessions from base date 2026-06-01: per session build five 1H Asia candles at the 20:00 NY open hour plus twelve 15M killzone candles across 02:00 to 05:00 NY, assigning archetypes by session index modulo 10 — indices 0 through 6 carry wicks strictly inside the Asia extremes (no sweep), indices 7 and 8 pierce an extreme then close back at the extreme with no follow-through (candidate only), index 9 pierces, closes back through the extreme, and continues at least six-tenths of Asia height beyond it (confirmed) — with seeded jitter capped at five percent of Asia height so jitter never crosses a gate threshold outside archetype 9. Run asiaRange over each 1H window and judasSwing with the default displacement multiple over each 15M strip, count sessions with confirmed true, print exactly one summary line of the form BUDGET confirmed=X/60 (Y%) with Y to one decimal, and exit 1 when the confirmed share exceeds 25 percent else exit 0. Keep the script free of network calls, Yahoo pipe imports, store imports, and current-time reads; all epochs derive from the fixed base date plus deterministic offsets.</action>
  <verify>
    <automated>node scripts/judas-budget.ts</automated>
    <fails_when>non-zero exit, or the BUDGET confirmed= summary line is missing, or the printed percentage is above 25 percent</fails_when>
  </verify>
  <done>Budget run green: 60 seeded sessions execute offline and the confirmed share prints at or under the 25 percent ceiling.</done>
  <acceptance_criteria>Script exits 0 printing one line matching BUDGET confirmed=X/60 (Y%) with the share at or under 25 percent; reruns print the identical line; the file contains no Yahoo, fetch, or store import.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/judas.ts — exports KILLZONE_START_MIN, KILLZONE_END_MIN, DISP_MULT, CONFIRM_WINDOW, types SweepSide and JudasOutput, function judasSwing (new in this plan; amd.ts consumes JudasOutput in the next plan)
- src/lib/ict/judas.test.ts — 15M builders plus conjunction mirrors, candidate persistence, preRun, window-bound, edge, forming-exclusion, determinism, constant-pin cases
- scripts/judas-budget.ts — seeded 60-session offline budget run printing the BUDGET confirmed= line (new in this plan; planner-named per the no-analog guidance)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure functions on caller-supplied 15M candle arrays plus an AsiaRange value; no I/O, no user input, no store — same posture as Phase 7 plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering | judasSwing boundary (judas.ts) | medium | mitigate | closedOnlyIntraday first, then finite-OHLC plus finite-positive-time drop, ascending sort, timestamp dedupe; Asia bounds asserted finite with positive height before any comparison; dispMult asserted finite positive |
| T-08-04 | Tampering | Killzone clock edge (judas.ts) | low | mitigate | Strict inequality on both 02:00 and 05:00 edges with minute-precision IANA resolution; pre-killzone sweeps return preRun and are never promoted |
</threat_model>

<verification>
Full suite stays green so Asia Range and all prior modules hold: `npm test` passes after both tasks.
</verification>

<success_criteria>
- Confirmation requires in-killzone sweep plus close-based reversal with Asia-height displacement inside sweep plus next 3 candles
- Pre-killzone sweeps flag preRun without promotion and displacement-free sweeps persist as candidates
- Seeded 60-session budget run prints a confirmed share at or under 25 percent
</success_criteria>

<output>
Create `.planning/phases/08-amd-sessions-asia-range-judas/08-02-SUMMARY.md` when done
</output>
