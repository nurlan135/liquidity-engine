---
phase: 08-amd-sessions-asia-range-judas
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/lib/ict/amd.ts
  - src/lib/ict/amd.test.ts
autonomous: true
requirements:
  - ICT-14
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AMD phase follows time plus events per D-13: Asia hours default to accumulation, killzone sweeps promote to manipulation, post-displacement continuation promotes to distribution"
    - "Phase 7 SMT fuses as a read-only regime tag per D-14: aligned confirmation appends agreement, suppression passes through, and the input object is never mutated"
    - "NY hours render the honest unavailable marker per D-15 instead of a time-only guess"
    - "Every output carries phase plus reason plus inputs per D-16 with Azerbaijani section-3-ready reason strings"
  artifacts:
    - path: "src/lib/ict/amd.ts"
      provides: "AMD phase classifier fusing AsiaRange plus JudasOutput plus read-only SMT"
      exports: ["NY_SESSION_START_MIN", "NY_SESSION_END_MIN", "AmdPhase", "AmdOutput", "amdPhase"]
      contains: "Gözlənilir"
    - path: "src/lib/ict/amd.test.ts"
      provides: "Transition, SMT tag, NY branch, envelope-shape, end-to-end tests"
      min_lines: 120
  key_links:
    - from: "src/lib/ict/amd.ts"
      to: "src/lib/ict/judas.ts"
      via: "JudasOutput candidate/confirmed plus sweepTime drive manipulation and distribution promotions"
      pattern: "JudasOutput"
    - from: "src/lib/ict/amd.ts"
      to: "src/lib/ict/smt.ts"
      via: "SmtOutput direction read only when unsuppressed; CORR_DECOUPLED and rollover-week pass through to the reason"
      pattern: "SmtOutput"
    - from: "src/lib/ict/amd.ts"
      to: "src/lib/ict/asia.ts"
      via: "AsiaRange null drives the empty-range degraded branch"
      pattern: "AsiaRange"
  prohibitions:
    - "Never mutate or re-derive SMT output inside the classifier and never guess a NY phase from the clock alone"
    - "Never emit an AMD branch without a reason string and never invent a new envelope pattern"
---

<objective>
AMD phase classifier (ICT-14) fusing range plus Judas plus SMT state — per D-12 through D-16.

Purpose: Time gives the skeleton and detector events give the promotions, with SMT as a read-only regime tag and NY honestly unavailable, so Phase 9 section 3 can render reasons verbatim. Closes the phase with the full suite green.
Output: New pure module src/lib/ict/amd.ts and its vitest suite. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-amd-sessions-asia-range-judas/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>AMD classifier core with time-plus-event transitions</name>
  <files>src/lib/ict/amd.ts, src/lib/ict/amd.test.ts</files>
  <read_first>src/lib/ict/asia.ts, src/lib/ict/judas.ts, src/lib/ict/smt.ts, src/lib/ict/fvg.ts, src/lib/ict/aggregate.ts</read_first>
  <action>Create src/lib/ict/amd.ts following the smt.ts house template (@-aliased type imports, caps constants at module top, fixed gate order, fail-fast throws with echoed values). Import NY_TZ from the aggregate module (reuse, do not redefine), formatInTimeZone from date-fns-tz, the AsiaRange type from the asia module, the JudasOutput type from the judas module, and the SmtOutput type from the smt module. Export constants NY_SESSION_START_MIN = 570 and NY_SESSION_END_MIN = 960 with a D-15 comment, export type AmdPhase as accumulation or manipulation or distribution, and export interface AmdOutput with phase, reason string, and inputs carrying asia (AsiaRange or null), judas (JudasOutput or null), and smt (SmtOutput or null) per D-16. Implement export amdPhase taking one input object with asia, judas, smt, and asOf epoch seconds (injected time, never a clock read): resolve asOf to NY minutes since midnight via formatInTimeZone at call time; run the fixed gate order clock skeleton first, Judas promotion second, SMT regime tag third, NY unavailable branch last. Apply the branch table — null Asia returns accumulation with reason Asia Range Təmizlənməyib — aralıq tapılmadı. London Judas Swing Gözlənilir.; Asia hours (minutes at or after 1200) with no Judas candidate returns accumulation with reason Asia Range Təmizlənməyib. London Judas Swing Gözlənilir.; unconfirmed Judas candidate returns manipulation with reason Asia Range Təmizlənib. London Judas Swing Gözlənilir — təsdiq gözlənilir. per D-12; confirmed Judas inside its 45-minute window (asOf at or before sweepTime plus 2700) returns manipulation with reason Asia Range Təmizlənib. London Judas Swing Baş verib.; confirmed Judas with asOf past sweepTime plus 2700 returns distribution with reason Asia Range Təmizlənib. London Judas Swing Baş verib — davam mərhələsi. per D-13; NY hours (minutes at or after 570 and strictly before 960) returns the prior phase (distribution when Judas is confirmed past its window, else manipulation when Judas is candidate or confirmed, else accumulation) with reason Gözlənilir — NY sessiyası v2.0-da ölçülmür. per D-15. Apply the SMT tag per D-14 without mutating the input: read direction only when smt is non-null and suppressed is false, append SMT razılaşır. to the reason when Judas is confirmed and the direction aligns with the sweep side (HIGH sweep with BEARISH, LOW sweep with BULLISH), append SMT Gözlənilir. when smt is non-null and suppressed is true (covering CORR_DECOUPLED and rollover-week pass-through), and leave the reason unchanged otherwise. Keep the module free of current-time reads and store imports, validate no candle rows here since amd consumes already-validated detector outputs, and model the reason builder on the describeDeliveryTransition deterministic-sentence precedent. Create src/lib/ict/amd.test.ts with small builders for AsiaRange, JudasOutput, and SmtOutput values plus epoch construction via fromZonedTime mirroring the nyHourEpoch shape, and cover: accumulation on Asia hours with null Judas asserting the exact accumulation reason; candidate-only manipulation asserting the exact təsdiq-gözlənilir reason; confirmed manipulation inside the window asserting the exact Baş-verib reason; distribution past sweepTime plus 2700 asserting the exact davam-mərhələsi reason; the NY branch asserting the exact NY unavailable reason with the prior phase preserved; the empty-Asia branch asserting the exact aralıq-tapılmadı reason; SMT agreement appending the exact agreement sentence on aligned confirmation; SMT suppression appending the exact SMT unavailable sentence; and SMT read-only where the passed SMT object stays deep-equal after the call.</action>
  <verify>
    <automated>npm test -- src/lib/ict/amd.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/amd.ts src/lib/ict/amd.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module or its test</fails_when>
  </verify>
  <done>Classifier green: time-plus-event promotions, read-only SMT tag, and NY unavailable branch all pinned with exact reasons.</done>
  <acceptance_criteria>Suite passes asserting exact reason equality on all seven branches (accumulation, candidate-only, confirmed, distribution, empty-range, NY, plus both SMT appends), distribution only past sweepTime plus 2700, NY preserving the prior phase, the SMT input deep-equal after the call, and the purity grep gate returning exit 1 with no output.</acceptance_criteria>
</task>

<task type="auto">
  <name>AMD end-to-end fusion plus full-suite green</name>
  <files>src/lib/ict/amd.ts, src/lib/ict/amd.test.ts</files>
  <read_first>src/lib/ict/amd.ts, src/lib/ict/asia.ts, src/lib/ict/judas.ts, src/lib/ict/smt.ts</read_first>
  <action>Extend src/lib/ict/amd.test.ts with an end-to-end fusion case that builds one deterministic evening: five 1H Asia candles at the 20:00 NY open hour with known wick extremes, twelve 15M killzone candles across 02:00 to 05:00 NY carrying a HIGH-side pierce plus a close at least six-tenths of Asia height back through the extreme, then runs asiaRange over the 1H window and judasSwing over the 15M strip and feeds both outputs plus an unsuppressed BEARISH SMT value into amdPhase with an asOf inside the confirmation window, asserting phase manipulation with the exact Baş-verib reason and inputs echoing the Asia high and low, the HIGH sweep side, and the BEARISH SMT direction. Add the phase purity gate over all six new files and keep the full suite green with no regressions to the 133-test baseline.</action>
  <verify>
    <automated>npm test -- src/lib/ict/amd.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>npm test</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/asia.ts src/lib/ict/asia.test.ts src/lib/ict/judas.ts src/lib/ict/judas.test.ts src/lib/ict/amd.ts src/lib/ict/amd.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered a phase module or its test</fails_when>
  </verify>
  <done>Fusion proven end to end and the whole suite stays green with phase-wide purity clean.</done>
  <acceptance_criteria>End-to-end case passes asserting manipulation with the exact Baş-verib reason and inputs carrying the Asia extremes, HIGH sweep side, and BEARISH SMT direction; full npm test run reports zero failed suites; phase-wide purity grep returns exit 1 with no output.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/amd.ts — exports NY_SESSION_START_MIN, NY_SESSION_END_MIN, types AmdPhase and AmdOutput, function amdPhase (new in this plan; Phase 9 selectors and section-3 prose consume AmdOutput)
- src/lib/ict/amd.test.ts — detector-value builders plus seven-branch reason, SMT tag, NY, read-only, and asiaRange-through-judasSwing-to-amdPhase end-to-end cases

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure classifier on already-validated detector outputs plus an injected epoch; no I/O, no user input, no store — same posture as Phase 7 plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | amdPhase input handling (amd.ts) | low | mitigate | No candle validation here by design; SMT direction read only when unsuppressed and the SMT object never mutated; every branch emits a reason string so no silent confident output exists |
</threat_model>

<verification>
Phase gate: full suite green plus DST triple-test green plus budget run at or under 25 percent before verify-work: `npm test` passes, the DST-filtered Asia run passes, and `node scripts/judas-budget.ts` exits 0.
</verification>

<success_criteria>
- Time-plus-event transitions promote accumulation to manipulation to distribution with exact Azerbaijani reasons
- SMT fuses read-only with agreement and suppression appends, and NY renders the unavailable marker with the prior phase
- Full suite green with phase-wide purity clean
</success_criteria>

<output>
Create `.planning/phases/08-amd-sessions-asia-range-judas/08-03-SUMMARY.md` when done
</output>
