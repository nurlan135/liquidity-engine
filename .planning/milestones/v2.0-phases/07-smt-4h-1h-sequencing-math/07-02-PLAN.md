---
phase: 07-smt-4h-1h-sequencing-math
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/fvg.ts
  - src/lib/ict/fvg.test.ts
autonomous: true
requirements:
  - ICT-10
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FVG map detects both bullish and bearish gaps on NQ D1 and drops a gap when a later candle closes through it"
    - "Active map stays bounded to the latest ~20 gaps even over multi-year D1 history"
    - "ERL/IRL transition fires only when one candle both pierces an FVG boundary with its wick and closes back inside on the same candle; pierce alone changes nothing"
    - "A pure delivery-sentence builder returns an IRL-aware string from transition state for the §2 Delivery Cycle upgrade, with no new report section"
    - "[Flagged assumption ICT-10-unclassified] No hidden edge contract beyond D-09..D-11 governs gap identity and mitigation; the polarity, mitigation, and bound fixtures define truth for what counts as a gap"
  artifacts:
    - path: "src/lib/ict/fvg.ts"
      provides: "FVG map plus sweep-then-reject transition plus delivery-sentence builder"
      exports: ["FVG_MAP_BOUND", "detectFVGs", "applyMitigation", "detectTransition", "describeDeliveryTransition"]
      contains: "originFvg"
    - path: "src/lib/ict/fvg.test.ts"
      provides: "Polarity, mitigation, bound, sweep-vs-reject, sentence tests"
      min_lines: 100
  key_links:
    - from: "src/lib/ict/fvg.ts"
      to: "src/lib/ict/types.ts"
      via: "closedOnly first line plus finite-OHLC boundary guard before the 3-candle scan"
      pattern: "closedOnly"
    - from: "src/lib/ict/fvg.test.ts"
      to: "src/lib/ict/range.test.ts"
      via: "wick-pierce contrast pinned: range extends extremes, FVG transition fires a state flip instead"
      pattern: "pierce"
  prohibitions:
    - "Never flip ERL/IRL state on a wick pierce whose candle closes outside; sweep alone leaves state and map untouched"
    - "Never let the active map grow without bound; the latest-20 cap applies after every update"
---

<objective>
FVG map plus ERL/IRL transition state plus §2 Delivery Cycle prose logic (ICT-10) — per D-09, D-10, D-11.

Purpose: NQ D1 fair-value-gap inventory with close-through mitigation and a sweep-then-reject regime flip that feeds the deterministic §2 delivery sentence. Parallel with Plan 01 (disjoint files, same wave).
Output: New pure module src/lib/ict/fvg.ts and its vitest suite. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>FVG map core: both polarities, close-through mitigation, latest-20 bound</name>
  <files>src/lib/ict/fvg.ts, src/lib/ict/fvg.test.ts</files>
  <read_first>src/lib/ict/range.ts, src/lib/ict/types.ts, src/lib/ict/rollover.test.ts</read_first>
  <action>Create src/lib/ict/fvg.ts following the range.ts house template (@-aliased type imports, caps constant at module top, closedOnly first line, fail-fast throw with echoed value on non-finite input). Export constant FVG_MAP_BOUND = 20 per D-09. Export types FvgPolarity as BULLISH|BEARISH and FvgGap with fields polarity, top, bottom, originDate, mitigated boolean. Implement export detectFVGs taking NQ D1 Candle array: filter through closedOnly, drop rows with any non-finite OHLC field at the boundary, scan triples and record a bullish gap at middle index i when low of candle i+1 exceeds high of candle i-1 with zone top equal to low[i+1] and bottom equal to high[i-1], record a bearish gap when high[i+1] is below low[i-1] with zone top equal to low[i-1] and bottom equal to high[i+1], skip triples with equality on the boundary (touch is not a gap), keep originDate from the middle candle date. Implement export applyMitigation taking the gap list plus the full closed D1 array: a bullish gap is removed once a strictly later candle closes below the gap bottom, a bearish gap once a strictly later candle closes above the gap top, per the D-09 fill-on-close-through rule; after mitigation keep only the trailing FVG_MAP_BOUND gaps in origin order. Feed NQ D1 daily candles only; never extend range extremes on pierce (the range.ts rolling-extremes semantic stays in its own module). Keep the module free of current-time reads with asOf passed as a plain string parameter, zero imports from any store module, and raw OHLC fields only. Create src/lib/ict/fvg.test.ts in the rollover.test.ts builder style with candle and trend helpers using Partial Candle overrides, covering bullish detection with exact top and bottom values, bearish detection with exact top and bottom values, close-through fill removing the gap while a wick touch without close-through keeps it, a 200-bar synthetic history asserting the returned map length is at most 20, and a constant pin asserting FVG_MAP_BOUND equals 20.</action>
  <verify>
    <automated>npm test -- src/lib/ict/fvg.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
  </verify>
  <done>FVG map green: both polarities detected with exact zones, mitigation and the 20-gap bound pinned.</done>
  <acceptance_criteria>Suite passes asserting a bullish gap with top and bottom equal to the constructed low[i+1] and high[i-1] values, a bearish mirror, gap removal only on close-through, map length at most 20 on the 200-bar fixture, and FVG_MAP_BOUND exactly 20.</acceptance_criteria>
</task>

<task type="auto">
  <name>Sweep-then-reject transition plus delivery-sentence builder</name>
  <files>src/lib/ict/fvg.ts, src/lib/ict/fvg.test.ts</files>
  <read_first>src/lib/ict/fvg.ts, src/lib/ict/dol.ts</read_first>
  <action>Extend src/lib/ict/fvg.ts with export type TransitionState carrying state ERL|IRL plus originFvg and triggerCandle date string per D-11 verbatim shape. Implement export detectTransition taking the active gap list plus D1 candles plus asOf string: start from state ERL, evaluate candles after each gap origin in chronological order, flip to IRL only when a single candle satisfies both halves of the D-10 rule — for a bullish gap the candle low pierces strictly below the gap bottom while the same candle closes at or above the bottom, for a bearish gap the candle high pierces strictly above the gap top while the same candle closes at or below the top — recording that gap as originFvg and the candle date as triggerCandle; a pierce whose close stays outside flips nothing and leaves the gap active. Implement export describeDeliveryTransition taking a TransitionState and returning a deterministic sentence string beside the computePrimaryDOL consumers per D-11: the ERL branch names the ERL state token, the IRL branch names the IRL state token together with the origin gap top and bottom numbers and the triggerCandle date, worded neutrally in the trap-vs-genuine house prose style with no new report section and no model calls. Extend src/lib/ict/fvg.test.ts with a sweep-then-reject case asserting state IRL with the expected originFvg bounds and triggerCandle date, a sweep-alone negative case asserting state stays ERL with the gap still active, and sentence tests asserting the ERL string contains ERL and the IRL string contains IRL plus the origin bounds and trigger date.</action>
  <verify>
    <automated>npm test -- src/lib/ict/fvg.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/fvg.ts src/lib/ict/fvg.test.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module or its test</fails_when>
  </verify>
  <done>Transition plus prose logic green: reject flips, sweep alone holds, sentence builder deterministic; purity gate clean.</done>
  <acceptance_criteria>Suite passes asserting IRL with exact origin bounds and trigger date on pierce-plus-close-back, ERL with gap active on pierce-without-close-back, sentence strings containing their state tokens with IRL also carrying bounds and date, and the grep gate returning exit 1 with no output.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/fvg.ts — exports FVG_MAP_BOUND, types FvgPolarity / FvgGap / TransitionState, functions detectFVGs, applyMitigation, detectTransition, describeDeliveryTransition
- src/lib/ict/fvg.test.ts — builder helpers candle / trend plus polarity, mitigation, bound, sweep-vs-reject, sentence cases

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NQ D1 rows → detectFVGs | Untrusted numeric OHLC crosses here; nulls possible |
| TransitionState → Phase 9 §2 prose | Sentence builder output rendered into the report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | detectFVGs / detectTransition (fvg.ts) | medium | mitigate | Drop non-finite OHLC rows at the module boundary; fail-fast throw with echoed value on bad input; sweep-alone never flips state so a single bad wick cannot whiplash §2 prose |
| T-07-03 | Denial of Service | Active FVG map growth (fvg.ts) | low | mitigate | Hard bound FVG_MAP_BOUND 20 applied after every update; 200-bar fixture asserts length cap |
</threat_model>

<verification>
Full suite stays green so the D1 NQ anchor path is untouched: `npm test` passes after both tasks.
</verification>

<success_criteria>
- FVG map finds both polarities on NQ D1, fills on close-through, stays bounded to the latest 20
- Transitions fire only on sweep-then-reject; sweep alone leaves ERL with the gap active
- describeDeliveryTransition returns the IRL-aware delivery sentence from transition state
</success_criteria>

<output>
Create `.planning/phases/07-smt-4h-1h-sequencing-math/07-02-SUMMARY.md` when done
</output>
