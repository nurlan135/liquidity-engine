---
phase: 07-smt-4h-1h-sequencing-math
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ict/aggregate.ts
  - src/lib/ict/aggregate.test.ts
autonomous: true
requirements:
  - ICT-11
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Closed 1H rows bucket into 4H blocks on the 18:00 ET grid resolved per candle through IANA America/New_York, stable across the March and November DST transitions"
    - "Only complete 4-candle blocks are emitted; the trailing partial block is excluded until its 4th hour closes and aggregate output is byte-identical between polls with no new closed candle"
    - "Emitted blocks carry the reused D1 Candle shape and feed computeRange plus computeBias unchanged"
    - "[Flagged assumption ICT-11-adjacency] A 1H row landing exactly on a 4H block boundary belongs to the block starting at that boundary under floor semantics; it is never duplicated into both blocks"
    - "[Flagged assumption ICT-11-empty] Empty, single-row, or sub-block input returns an empty block array, never a partial block and never a throw"
    - "[Flagged assumption ICT-11-ordering] Blocks emit in ascending chronological order with first-open / last-close aggregation regardless of input row order; input is sorted ascending first"
  artifacts:
    - path: "src/lib/ict/aggregate.ts"
      provides: "NY-anchored 1H to 4H synthesis emitting Candle blocks"
      exports: ["NY_TZ", "DAY_OPEN_NY_HOUR", "BLOCK_SIZE", "aggregate1Hto4H"]
      contains: "America/New_York"
    - path: "src/lib/ict/aggregate.test.ts"
      provides: "Anchor, DST pair, partial-block, determinism, Candle-reuse tests"
      min_lines: 100
  key_links:
    - from: "src/lib/ict/aggregate.ts"
      to: "src/lib/ict/types.ts"
      via: "closedOnlyIntraday at the boundary plus Candle-shaped block emission"
      pattern: "closedOnlyIntraday"
    - from: "src/lib/ict/aggregate.test.ts"
      to: "src/lib/ict/range.ts"
      via: "reuse test feeds emitted blocks into computeRange unchanged"
      pattern: "computeRange"
    - from: "src/lib/ict/aggregate.test.ts"
      to: "src/lib/ict/bias.ts"
      via: "reuse test feeds emitted blocks into computeBias unchanged"
      pattern: "computeBias"
  prohibitions:
    - "Never emit a partial trailing 4H block or synthesize filler rows for maintenance-break gaps; absence of a block is correct output"
    - "Never resolve the 18:00 ET anchor with a fixed UTC offset; per-candle IANA wall-clock resolution only"
---

<objective>
NY-anchored 1H to 4H synthesis in pure aggregate.ts (ICT-11) — per D-12, D-13, D-14.

Purpose: Yahoo has no 4H interval, so 4H structure is synthesized from complete 1H blocks on the 18:00 ET futures-day grid; blocks reuse the D1 Candle shape so existing range and bias math reads internal and external sequencing unchanged. Parallel with Plans 01 and 02 (disjoint files, same wave).
Output: New pure module src/lib/ict/aggregate.ts and its vitest suite. No store, no rendering, no network.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Aggregate core: NY-anchored bucketing with complete-blocks-only emission</name>
  <files>src/lib/ict/aggregate.ts, src/lib/ict/aggregate.test.ts</files>
  <read_first>src/lib/ict/join.ts, src/lib/ict/types.ts, src/lib/time.ts, src/lib/time.test.ts, src/lib/ict/join.test.ts</read_first>
  <action>Create src/lib/ict/aggregate.ts following the join.ts house template (@-aliased type imports, boundary re-validation first with a comment citing the forming-regress precedent, deterministic ascending output). Export constants NY_TZ = America/New_York per D-12, DAY_OPEN_NY_HOUR = 18, BLOCK_SIZE = 4 per D-13. Import getTimezoneOffset from date-fns-tz 3.2.0, the installed house time primitive. Export function aggregate1Hto4H taking IntradayCandle rows plus an asOf date string in yyyy-MM-dd form: filter through closedOnlyIntraday first so the forming 1H candle renders on the chart but never enters blocks, drop rows with any non-finite OHLC field at the boundary, sort ascending by epoch time, dedupe duplicate timestamps keeping the first row, exclude rows whose NY calendar date sorts after the asOf date so the injected time acts as the right edge with no clock reads of any kind. For each surviving row compute NY wall-clock fields per candle as the hour and calendar date of epoch-milliseconds plus getTimezoneOffset of NY_TZ at that instant, assign the anchor day as the row NY date when its NY hour is at or past 18 else the previous calendar day, assign the within-day block index as the floor of the forward distance from 18 divided by BLOCK_SIZE giving indices 0 through 5, group by anchor-day plus index key preserving chronological order, aggregate each group as open of first row, high of max high, low of min low, close of last row, label each block date as anchor-day plus block-index suffix in B1 through B6 form, and emit only groups with exactly BLOCK_SIZE constituents while dropping the trailing partial silently with no filler rows synthesized for the 16:00 to 17:00 CT maintenance-break gap. Accept only UTC epoch-second IntradayCandle input, never business-day strings; keep the module free of current-time reads, zero imports from any store module, and raw OHLC fields only. Create src/lib/ict/aggregate.test.ts in the join.test.ts style with an epoch-second row builder using STEP 3600 and Partial IntradayCandle overrides, covering an 18:00 ET boundary split asserting two blocks with exact B-suffix labels and counts, a March spring-forward pair around the 2026-03-08 transition instant asserting identical NY wall-clock bucketing both sides, a November fall-back pair around the 2026-11-01 transition instant with the same assertion, a 7-closed-row case asserting one emitted block with 3 rows silently dropped, and a forming-exclusion case asserting forming rows never enter blocks.</action>
  <verify>
    <automated>npm test -- src/lib/ict/aggregate.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
  </verify>
  <done>Aggregation core green: anchor split, both DST pairs, partial exclusion, and forming exclusion pinned.</done>
  <acceptance_criteria>Suite passes asserting exact block labels on the 18:00 ET split, same-bucket assertions both sides of the March and November transition instants, exactly 1 block from 7 closed rows, zero forming rows in output, and NY_TZ, DAY_OPEN_NY_HOUR, BLOCK_SIZE equal to America/New_York, 18, and 4.</acceptance_criteria>
  <reversibility rating="costly">Complete-blocks-only emission flows into aggregate tests, selector inputs, and Phase 8 session consumers per D-13; changing it later rewrites this suite and downstream readers.</reversibility>
</task>

<task type="auto">
  <name>Aggregate hardening: determinism, Candle reuse, boundary edges</name>
  <files>src/lib/ict/aggregate.ts, src/lib/ict/aggregate.test.ts</files>
  <read_first>src/lib/ict/aggregate.ts, src/lib/ict/range.ts, src/lib/ict/bias.ts</read_first>
  <action>Extend src/lib/ict/aggregate.test.ts without changing the emission contract: a consecutive-poll case calling aggregate1Hto4H twice on the same 8-row input asserting deep equality of both outputs, a Candle-reuse case feeding emitted blocks into computeRange with an asOf string asserting the returned high equals the max block high and low equals the min block low plus a computeBias call over the derived position asserting a finite non-empty rationale string, an empty-input case asserting an empty array, a sub-block 3-row case asserting an empty array with no throw, an exact-boundary case with a row timestamped precisely on a block start asserting floor membership in the opening block with no duplication, an out-of-order input case asserting ascending block output identical to sorted input, and a non-finite OHLC row case asserting the bad row is dropped at the boundary. No source change is expected unless a case exposes a defect; if one does, fix aggregate.ts minimally keeping the anchor-day plus index key and complete-blocks-only rule intact.</action>
  <verify>
    <automated>npm test -- src/lib/ict/aggregate.test.ts</automated>
    <fails_when>vitest reports any failed test, a failed suite, or a non-zero exit code</fails_when>
    <automated>grep -rn "Date.now\|new Date()" src/lib/ict/aggregate.ts | grep -v "://"; test $? -eq 1</automated>
    <fails_when>grep exits 0 or prints any match line, meaning a current-time read entered the pure module</fails_when>
  </verify>
  <done>Hardening green: determinism, reuse, and edge cases pinned; purity gate clean.</done>
  <acceptance_criteria>Suite passes asserting deep-equal consecutive outputs, computeRange high and low matching block extremes, empty arrays for empty and 3-row inputs, single-membership of the exact-boundary row, order-independent output, and the grep gate returning exit 1 with no output.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- src/lib/ict/aggregate.ts — exports NY_TZ, DAY_OPEN_NY_HOUR, BLOCK_SIZE, function aggregate1Hto4H
- src/lib/ict/aggregate.test.ts — epoch row builder plus anchor, DST pair, partial-block, determinism, reuse, edge cases

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 1H epoch rows → aggregate1Hto4H | Untrusted numeric OHLC plus timestamps cross here |
| 4H blocks → computeRange / computeBias | Emitted blocks feed existing math unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | aggregate1Hto4H boundary (aggregate.ts) | medium | mitigate | closedOnlyIntraday first, then finite-OHLC drop, ascending sort, timestamp dedupe keeping the first row; non-finite input never reaches aggregation |
| T-07-04 | Tampering | Block timestamp ordering (aggregate.ts) | low | mitigate | Input sorted ascending before grouping and output emitted in key order; out-of-order rows cannot misattribute first-open or last-close |
</threat_model>

<verification>
Full suite stays green so existing range, bias, regime, rollover, and join tests hold: `npm test` passes after both tasks.
</verification>

<success_criteria>
- 1H rows synthesize 4H blocks on the 18:00 ET grid with per-candle IANA resolution across both DST transitions
- Trailing partial blocks are excluded; consecutive polls without new closed rows return identical output
- Blocks feed computeRange and computeBias with no changes to those modules
</success_criteria>

<output>
Create `.planning/phases/07-smt-4h-1h-sequencing-math/07-03-SUMMARY.md` when done
</output>
