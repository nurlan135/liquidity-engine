---
phase: "03.1"
slug: "close-gap-ict-07-banner"
status: verified
threats_open: 0
asvs_level: 1
created: "2026-09-06"
---

# Phase 03.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Validated envelope to selector | Store state crosses into rollover derivation here; only validated candles plus hint plus Baku date enter | Validated candles + contractHint + asOfBaku (internal, low sensitivity) |
| Selector output to banner | Derived flag crosses into rendered HTML here; only suspect boolean plus hint plus template warning render | RolloverFlag (internal derived state) |
| Upstream Yahoo payload to parser | Untrusted fetch bytes cross into the envelope here; parser reads raw OHLC only and the banner never touches fetch text | Untrusted JSON bytes (external) |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-03.1-01 | Tampering | Adjusted-price ingestion in banner path (ASVS V5) | high | mitigate | Parser reads raw OHLC arrays only (`quote.open/high/low/close` in `src/lib/yahoo.ts:66-69`); zero `adjclose` matches under `src/`; contractHint built from meta symbol + exchangeName, never price data | closed |
| T-03.1-02 | Spoofing | Upstream echo into banner text (ASVS V5) | medium | mitigate | Banner renders only validated `contractHint` plus deterministic `proximityWarning` template built from `asOfBaku` (`components/dashboard/terminal-shell.tsx:168-176`); no raw upstream body interpolation; follows POLL_FAILURE_COPY locked-copy precedent | closed |
| T-03.1-03 | Repudiation | Silent absorb of the flag (honesty) | medium | mitigate | Render keyed directly on `selectRollover` output with suspect-or-warning visibility (`terminal-shell.tsx:169`); presence asserted on gapped fixture, absence on clean fixture, co-render with chart + report (`src/terminal-shell.test.ts:345-394`, 20/20 pass) | closed |
| T-03.1-04 | Denial of Service | Selector throw into render on bad ATR | medium | mitigate | Guards return null on empty candles and non-finite/zero-or-negative ATR before `detectRollover` (`src/lib/store.ts:189-191`); thin-history and empty cases prove no throw (`src/lib/store.test.ts:241-268`) | closed |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs: zustand + vitest + jsdom already lockfile-pinned; `package.json`/`package-lock.json` untouched by phase commits | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-03.1-01 | T-03.1-SC | No new packages added; supply chain unchanged from lockfile baseline | GSD secure-phase | 2026-09-06 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-06 | 5 | 5 | 0 | gsd-secure-phase (orchestrator, ASVS L1) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-06
