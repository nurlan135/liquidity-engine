---
phase: 03.1-close-gap-ict-07-banner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/store.ts
  - components/dashboard/terminal-shell.tsx
  - src/lib/store.test.ts
  - src/terminal-shell.test.ts
autonomous: true
requirements: [ICT-07]

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Gapped rollover-week fixture makes selectRollover return rolloverSuspect true with contractHint and proximityWarning populated
    - Thin-history and empty-candle states make selectRollover return null with the banner hidden and no throw into render
    - Rollover fixture renders data-slot rollover-banner with hint plus warning text while chart and report keep rendering (flag-and-continue)
    - Clean fixture renders no rollover-banner element
    - Full suite stays green plus lint plus typecheck plus production build pass with no adjusted-price read added
  artifacts:
    - src/lib/store.ts
    - components/dashboard/terminal-shell.tsx
    - src/lib/store.test.ts
    - src/terminal-shell.test.ts
  key_links:
    - selectRollover ATR guard feeds detectRollover so thin history degrades instead of false-flagging
    - Banner conditional reads selectRollover output directly so the flag cannot be silently absorbed
    - data-slot rollover-banner hook is queried by the integration test so presence and absence are both asserted
---

<objective>
Close the ICT-07 functional gap by wiring the existing tested detectRollover math into a selectRollover store selector plus a flag-and-continue banner in the chart card, so E2E flow F5 (rollover week leading to flagged banner) exists. Banner-only scope.

Purpose: detectRollover and isNearRolloverWeek are proven with zero callers outside tests; until a selector invokes them and a banner renders the result, ICT-07 is unsatisfied and flow F5 fails.
Output: selectRollover in the store, inline rollover-banner block in the chart card, selector unit cases plus banner render cases, all gates green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-close-gap-ict-07-banner/03.1-RESEARCH.md
@.planning/phases/03.1-close-gap-ict-07-banner/03.1-PATTERNS.md
@.planning/phases/03.1-close-gap-ict-07-banner/03.1-VALIDATION.md
@src/lib/store.ts
@src/lib/ict/rollover.ts
@src/lib/ict/types.ts
@src/lib/ict/regime.ts
@components/dashboard/terminal-shell.tsx
@components/charts/nq-chart.tsx
@src/lib/store.test.ts
@src/terminal-shell.test.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end rollover flag slice — selector plus banner plus happy-path proof</name>
  <files>src/lib/store.ts, components/dashboard/terminal-shell.tsx, src/lib/store.test.ts, src/terminal-shell.test.ts</files>
  <read_first>src/lib/store.ts, src/lib/ict/rollover.ts, src/lib/ict/types.ts, src/lib/ict/regime.ts, components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx, src/lib/store.test.ts, src/terminal-shell.test.ts, .planning/phases/03.1-close-gap-ict-07-banner/03.1-PATTERNS.md, AGENTS.md</read_first>
  <reversibility rating="reversible">Adds one selector plus one conditional banner block; removal is deleting the selector entry and the banner block with no migration.</reversibility>
  <action>Extend src/lib/store.ts following the selectRegime shape at lines 172-176. Add type import RolloverFlag from src/lib/ict/types and value import detectRollover from src/lib/ict/rollover alongside the existing ict imports, keeping import order framework then third-party then internal alias. Add DashboardState interface entry selectRollover returning RolloverFlag or null directly after the selectLevels line. Implement selectRollover to read candles plus contractHint plus asOfBaku through get, return null when candles length is zero, derive atr through computeRegime applied to full candles, return null when atr is non-finite or less than or equal to zero, otherwise return detectRollover applied to full candles plus atr plus asOfBaku plus contractHint in that argument order. Pass full candles untouched with no closedOnly pre-filter, use store asOfBaku with no inline Date construction, leave selectLastClose and deriveStatus and chartStatus untouched, leave the Yahoo parser untouched. Wire components/dashboard/terminal-shell.tsx by adding a selectRollover subscription beside the selectRange and selectDOL subscriptions, deriving rollover once per render after range and dol. Insert one conditional block inside the panel-center CardContent above the NqChart branch and before the lastUpdatedISO null ternary, rendering a div with data-slot rollover-banner and role status using terminal token classes in the mono 11px tracking style of the stale-badge precedent. Render the block only when rollover is non-null and either rolloverSuspect is true or proximityWarning is non-null. Inside show ROLLOVER SUSPECT plus contractHint plus levels-held-on-current-extremes copy when suspect is true, otherwise ROLLOVER WATCH plus contractHint, followed by a span with proximityWarning text when non-null. Render only contractHint from the validated envelope plus the deterministic proximityWarning template, never raw upstream bodies. Keep NqChart props untouched, keep the use client pragma, keep the nextjs-agent-rules block intact, add no Route Handler and no config change. Prove the slice with one gapped selector case in src/lib/store.test.ts using a 20-plus closed-candle 10pt-step trend plus one close lifted by 250 against atr near 10 with asOfBaku 2026-03-19, asserting suspect true plus hint passthrough plus warning matching rollover week, and one banner render case in src/terminal-shell.test.ts seeding the gapped envelope and asserting the rollover-banner element exists with hint and warning text while the chart stub still renders.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts src/lib/ict/rollover.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npx vitest run src/terminal-shell.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
  </verify>
  <done>The single happy path works end-to-end and is committed.</done>
  <acceptance_criteria>useDashboard getState selectRollover on the gapped fixture returns rolloverSuspect true with contractHint NQ=F middle-dot CME and proximityWarning matching rollover week; shell container query for data-slot rollover-banner is non-null with hint plus warning text and data-slot nq-chart-stub is non-null in the same render; npx vitest run src/lib/store.test.ts src/lib/ict/rollover.test.ts exits 0; npx vitest run src/terminal-shell.test.ts exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Edge-case expansion plus flag-and-continue lock</name>
  <files>src/lib/store.test.ts, src/terminal-shell.test.ts</files>
  <read_first>src/lib/store.test.ts, src/terminal-shell.test.ts, src/lib/ict/rollover.test.ts, src/lib/yahoo.ts</read_first>
  <reversibility rating="reversible">Adds test cases only; removal is deleting the added cases with no production change.</reversibility>
  <action>Extend src/lib/store.test.ts with five selector cases reusing the fixtureCandles plus mockEnvelope shape. Add a boundary case where the close-to-close gap equals exactly 3 times ATR asserting rolloverSuspect false under the strict greater-than tripwire. Add a thin-history case with about 5 closed candles asserting selectRollover returns null because the regime ATR degrades to zero. Add an empty-candles case asserting null with no throw. Add a contractHint passthrough case asserting the returned hint equals the seeded envelope hint exactly. Add a proximity pair asserting asOfBaku 2026-03-19 yields a non-null warning matching rollover week while asOfBaku 2026-01-25 yields null. Extend src/terminal-shell.test.ts with two render cases reusing the installDom plus renderShell plus beforeEach reset harness with asOfBaku reset and per-case contractHint seeding. Add a clean-trend 20-candle fixture case asserting the rollover-banner query is null. Add a flag-and-continue case on the gapped fixture asserting the rollover-banner query is non-null and the chart stub query is non-null in the same container and the report query is non-null. Change no production math and no parser behavior in this task; the existing rollover and yahoo suites must pass unchanged. Keep the Report section 2 rollover line out of scope and keep W1 selectLastClose plus W2 header copy plus stale-on-refresh-failure plus verify-deploy extensions out of scope.</action>
  <verify>
    <automated>npx vitest run src/lib/store.test.ts src/lib/ict/rollover.test.ts src/lib/yahoo.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npx vitest run src/terminal-shell.test.ts</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <human-check>Open the chart card on the gapped fixture and confirm the banner reads as a warning badge above the chart without covering candles or the DOL marker; logic is covered by automation, this check is aesthetics only.</human-check>
  </verify>
  <done>Boundary plus degrade plus absence plus co-render cases are green and the pre-existing tripwire and parser suites are unchanged green.</done>
  <acceptance_criteria>Boundary gap equal to 3 times ATR asserts suspect false; thin-history and empty fixtures assert selectRollover null; proximity far date asserts warning null and near date asserts warning matching rollover week; clean fixture asserts rollover-banner query null; gapped fixture asserts rollover-banner plus chart stub plus report queries all non-null; npx vitest run src/lib/store.test.ts src/lib/ict/rollover.test.ts src/lib/yahoo.test.ts exits 0; npx vitest run src/terminal-shell.test.ts exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Pre-verify gates green</name>
  <files>src/lib/store.ts, components/dashboard/terminal-shell.tsx</files>
  <read_first>package.json, vitest.config.ts, src/lib/store.ts</read_first>
  <action>Run the repo gate sequence with no source edits unless a gate names a regression this phase introduced. Run lint first, then the full test suite against the 111-green baseline, then the production build, then the typecheck. On any gate failure, fix only files this plan owns and re-run the failing gate plus the two vitest suite commands from the prior tasks before proceeding. Install no packages.</action>
  <verify>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any error or warning line in output</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any error or Failed to compile line in output</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Gates plus build plus typecheck are green on the final tree.</done>
  <acceptance_criteria>npm run lint exits 0; npm test exits 0 with zero failed suites and passed count at or above the 111 baseline; npm run build exits 0; npx tsc --noEmit exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated envelope to selector | Store state crosses into rollover derivation here; only validated candles plus hint plus Baku date enter |
| Selector output to banner | Derived flag crosses into rendered HTML here; only suspect boolean plus hint plus template warning render |
| Upstream Yahoo payload to parser | Untrusted fetch bytes cross into the envelope here; parser reads raw OHLC only and the banner never touches fetch text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Tampering | Adjusted-price ingestion in banner path (ASVS V5) | high | mitigate | Selector consumes raw candles already held in store; parser stays raw-OHLC only; regression suite src/lib/yahoo.test.ts runs in Task 2 to lock the never-adjusted contract |
| T-03.1-02 | Spoofing | Upstream echo into banner text (ASVS V5) | medium | mitigate | Banner renders only contractHint from the validated envelope plus the deterministic proximityWarning template built from asOfBaku; locked copy discipline follows the POLL_FAILURE_COPY precedent with no raw upstream body interpolation |
| T-03.1-03 | Repudiation | Silent absorb of the flag (honesty) | medium | mitigate | Conditional render is keyed directly on selectRollover output with suspect-or-warning visibility; Task 1 asserts banner presence on the gapped fixture and Task 2 asserts absence on the clean fixture plus co-render with chart and report |
| T-03.1-04 | Denial of Service | Selector throw into render on bad ATR | medium | mitigate | Guard returns null on empty candles and on non-finite or zero-or-negative ATR before calling detectRollover, mirroring the selectRegime early return; thin-history and empty cases in Task 2 prove no throw |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: zustand plus vitest plus jsdom are already lockfile-pinned; no new package is added so no blocking-human checkpoint is owed |
</threat_model>

<verification>
Full suite npm test green at or above the 111 baseline, npm run lint green, npm run build green, npx tsc --noEmit green, selector suites plus shell suite green, banner presence on gapped fixture plus absence on clean fixture plus chart co-render asserted, parser and tripwire regression suites unchanged green.
</verification>

<success_criteria>
Flow F5 exists: a rollover-week envelope produces selectRollover flagged plus a visible rollover-banner with hint and warning while the chart keeps rendering, and clean plus thin plus empty states hide the banner without errors.
</success_criteria>

<output>
Create `.planning/phases/03.1-close-gap-ict-07-banner/03.1-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- src/lib/store.ts: DashboardState entry selectRollover plus selector implementation selectRollover reading candles plus contractHint plus asOfBaku, guarding empty plus non-finite-or-non-positive ATR to null, delegating to detectRollover.
- components/dashboard/terminal-shell.tsx: inline conditional banner block with data-slot rollover-banner and role status inside the panel-center CardContent above the NqChart branch; no new component file; NqChart props unchanged; deriveStatus and chartStatus untouched.
- src/lib/store.test.ts: new ICT-07a selector cases named gapped-flags, boundary-clear, thin-history-null, empty-null, hint-passthrough, proximity-pair.
- src/terminal-shell.test.ts: new ICT-07b render cases named banner-renders-with-hint-and-warning, banner-absent-on-clean, flag-and-continue-co-render.
- Symbols created: selectRollover. No new files. No new dependencies.

## Source coverage audit

- GOAL (close ICT-07 gap, F5 exists, banner-only): covered by Task 1 slice plus Task 2 expansion plus Task 3 gates.
- REQ ICT-07 (raw OHLC never adjusted, suspect flag on N-times-ATR jumps, hint plus proximity): math already proven in rollover.ts; wiring covered by Task 1; edge plus regression locked by Task 2; raw-OHLC half locked by yahoo suite in Task 2 verify.
- RESEARCH (selectRollover composition with atr guard, chart-card overlay with data-slot, anti-patterns no-merge no-math-in-component no-freeze): each maps to Task 1 action constraints; pitfalls 1-4 addressed by guard plus full-candles plus asOfBaku rules.
- CONTEXT (no CONTEXT.md, no locked decisions, no deferred ideas): nothing to honor; OUT list honored by explicit exclusion in Task 2 action (W1, W2, stale-on-failure, verify-deploy extensions, Report section 2 stretch).
- Exclusions are not gaps: OUT items listed above are audit-directed follow-ups, not unplanned scope.
