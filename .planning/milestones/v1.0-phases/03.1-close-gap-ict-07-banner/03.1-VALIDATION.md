---
phase: "03.1"
slug: "close-gap-ict-07-banner"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-06"
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^5.0.0 |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run src/lib/store.test.ts src/lib/ict/rollover.test.ts` |
| **Full suite command** | `npm test` + `npm run lint` + `npm run build` (pre-verify gate) |
| **Estimated runtime** | ~30 seconds (unit suites) |

---

## Sampling Rate

- **After every task commit:** Run quick command (selector + rollover suites)
- **After every plan wave:** Run full `npm test` (baseline: 111 green across 20 files — must stay green)
- **Before `/gsd-verify-work`:** Full suite green + `npm run lint` + `npm run build`
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | ICT-07a | T-03.1-01 / T-03.1-04 | Gapped fixture → `selectRollover()` returns flagged; thin-history/empty → `null`; boundary `== 3×ATR` → clear | unit | `npx vitest run src/lib/store.test.ts` | ✅ | ✅ green |
| 03.1-01-02 | 01 | 1 | ICT-07b | T-03.1-02 / T-03.1-03 | Rollover fixture → `data-slot="rollover-banner"` renders hint + warning; clean fixture → absent; chart/report co-render (flag-and-continue) | integration (jsdom) | `npx vitest run src/terminal-shell.test.ts` | ✅ | ✅ green |
| 03.1-01-03 | 01 | 1 | ICT-07c | — | Tripwire boundary + raw-OHLC parser unchanged (regression) | unit | `npx vitest run src/lib/ict/rollover.test.ts src/lib/yahoo.test.ts` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Selector unit cases (ICT-07a): gapped → flagged, boundary, thin-history/empty → `null`, `contractHint` passthrough, proximity null-vs-near pair (`src/lib/store.test.ts:193-330`, 6 cases green)
- [x] Banner render cases (ICT-07b): renders with hint + warning; absent on clean fixture; co-renders with chart (`src/terminal-shell.test.ts:345-394`, 3 cases green)
- [x] Regression (ICT-07c): tripwire boundary + raw-OHLC parser unchanged (`src/lib/ict/rollover.test.ts`, `src/lib/yahoo.test.ts` green)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Banner styling/placement confirmation | ICT-07 | Logic fully automatable; aesthetics are not | Screenshot or `npm run dev` glance at chart card on rollover fixture |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-09-06

---

## Validation Audit 2026-09-06
| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Wave 0 seeded as MISSING (`❌ W0`) at plan time; all items now exist and run green: 39/39 across `store.test.ts` + `terminal-shell.test.ts` + `rollover.test.ts` + `yahoo.test.ts` (6.67s). Manual-only banner styling/placement confirmed via UAT pass (03.1-UAT.md test 1). No auditor spawn needed — zero gaps.

