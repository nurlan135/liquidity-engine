---
phase: 03-production-deploy-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-deploy.sh
  - src/lib/yahoo.ts
  - src/lib/yahoo-drill.test.ts
autonomous: true
requirements: [DEPLOY-01]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Pre-deploy gates (lint, unit tests, production build) pass on a clean tree
    - A checked-in script re-verifies any deployment URL for CDN headers, envelope freshness, and page render
    - The production build serves locally and passes the script end-to-end
    - Forcing the upstream failure path serves stale cached candles with the stale flag (drill mechanism proven before preview)
  artifacts:
    - scripts/verify-deploy.sh
    - src/lib/yahoo.ts
    - src/lib/yahoo-drill.test.ts
  key_links:
    - Verify script asserts the route handler branch-split headers (fresh s-maxage+SWR vs stale/502 no-store)
    - Temporary flag rides the existing serve-stale branch through a retryable structured error
    - Drill unit test isolates the env mutation so no other suite observes the flag
  prohibitions:
    - must NOT ship the drill kill-switch to production (flag block plus drill test are removed in plan 03-04 before the prod gate)
    - must NOT expose the drill flag through any client-visible channel (server-only read, neutral 502 copy)
    - must NOT add edge-header config in vercel.json or next.config.ts headers (function-set headers are the source of truth and config would be overridden)
  flagged_assumptions:
    - DEPLOY-01 spec-less probe returned one unclassified/unresolved row with no further decomposable edge predicates; scope is exactly the three roadmap success criteria covered as truths above
---

<objective>
Prove the deploy-verification loop end-to-end on the local machine and prove the drill mechanism in isolation: gates green, a re-runnable live-URL check script passing against a local production build, and a temporary server-only kill-switch that forces the existing serve-stale path under unit test. Per D-09 the checks live in the repo; per D-07 the flag is temporary and preview-scoped later.

Purpose: Everything later (rename, Vercel connection, preview drill, prod gate) depends on trusted gates and a proven script plus a proven flag. This tracer establishes both before any infrastructure is touched.
Output: scripts/verify-deploy.sh (executable, zero-dependency), temporary drill flag in src/lib/yahoo.ts, temporary src/lib/yahoo-drill.test.ts, all committed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-deploy-verification/03-CONTEXT.md
@src/lib/yahoo.ts
@app/api/yahoo/route.ts
@src/lib/freshness.ts
</context>

<tasks>

<task type="tracer">
  <name>Tracer: gates plus verify script plus local production proof</name>
  <files>scripts/verify-deploy.sh</files>
  <read_first>app/api/yahoo/route.ts, src/lib/freshness.ts, package.json</read_first>
  <action>Run the pre-deploy gate first with npm run lint followed by npm test, both green, before writing anything. Then create scripts/verify-deploy.sh per D-09 as a zero-dependency check script using only curl and node one-liners (curl 8.17.0 and node v24 verified present; no jq, no new packages), taking BASE_URL as its first argument, stripping any trailing slash, printing usage and exiting 2 when absent. Implement exactly five checks in order, each echoing a PASS line with its name or a FAIL line with detail, exiting 1 on the first failure. WARMUP: up to 4 GET attempts against BASE/api/yahoo five seconds apart until HTTP 200; a non-200 first attempt is tolerated per the cold-origin contract, persistent non-200 fails naming WARMUP. HEADERS: fetch headers with curl -sSI against the deployment URL directly (never browser DevTools, since Vercel strips s-maxage before the browser) and require the cache-control value to contain s-maxage=60 and stale-while-revalidate=30 case-insensitively, matching the fresh branch of app/api/yahoo/route.ts; then read x-vercel-cache across two sequential HEADs and require MISS-then-HIT-or-STALE progression when the header is present, printing a SKIP line and continuing when it is absent (local next start emits no such header). ENVELOPE: GET the body and parse it defensively with node JSON.parse from stdin, failing as BAD-ENVELOPE on parse error, and require candles to be an array of length at least 1, lastUpdatedISO to parse to a valid date, stale to be strictly false, and source to be live or cache; compute age seconds as now minus parsed time and fail as ENVELOPE-AGE when age reaches 120 (STALE_AFTER_SEC in src/lib/freshness.ts). PAGE: GET BASE with trailing path / and require HTTP 200 plus a non-empty body. Mark the script executable with git update-index --chmod=+x on its path. Then run npm run build, start the production server in the background on port 3100, wait for HTTP 200, run bash scripts/verify-deploy.sh against http://localhost:3100 observing all five PASS lines, stop the server, and commit the script. Port 3100 avoids clashing with any dev server on 3000.</action>
  <verify>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any error or warning line in output</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any Failed Tests section in output, or 0 passed in the summary line</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any error or Failed to compile line in output</fails_when>
    <automated>bash -n scripts/verify-deploy.sh && grep -q 's-maxage=60' scripts/verify-deploy.sh && grep -q 'x-vercel-cache' scripts/verify-deploy.sh && grep -q 'ENVELOPE-AGE' scripts/verify-deploy.sh</automated>
    <fails_when>non-zero exit, meaning the script has a syntax error or is missing a named check</fails_when>
    <automated>npm run build && (npm run start -- -p 3100 & SRV=$!; sleep 12; bash scripts/verify-deploy.sh http://localhost:3100; RC=$?; kill $SRV 2>/dev/null; exit $RC)</automated>
    <fails_when>non-zero exit, or any FAIL line in the script output, or the server never reaches HTTP 200</fails_when>
  </verify>
  <done>Verify script passes all five checks against a local production build and the gates plus build are green.</done>
  <acceptance_criteria>npm run lint exits 0; npm test exits 0; npm run build exits 0; scripts/verify-deploy.sh is executable and contains WARMUP, HEADERS, ENVELOPE, ENVELOPE-AGE, PAGE, s-maxage=60, stale-while-revalidate=30, and x-vercel-cache; the compound serve-plus-script command exits 0 with five PASS lines</acceptance_criteria>
</task>

<task type="auto">
  <name>Temporary drill kill-switch plus isolated unit proof</name>
  <files>src/lib/yahoo.ts, src/lib/yahoo-drill.test.ts</files>
  <read_first>src/lib/yahoo.ts</read_first>
  <behavior>
    - Warmed cache plus flag set returns the warm candles with stale true and source stale.
    - Cold cache plus flag set throws instead of returning fabricated candles.
    - Flag unset restores the normal fetch path with no behavior change.
  </behavior>
  <action>Insert a temporary server-only environment read per D-07 at the very top of fetchNQDaily in src/lib/yahoo.ts, before the cache lookup line const cached = payloadCache.get(CACHE_KEY): when the DRILL_FORCE_STALE variable equals the string 1, throw a new UpstreamError with a neutral drill message and the retryable true option. This exact variable name is the contract plan 03-03 sets in the Vercel dashboard, so spell it exactly. The retryable true discriminator is load-bearing: it routes into the existing warm-cache stale branch at lines 300-307, while a non-retryable 4xx would bypass it, so do not add any status field and do not place the check inside the fetchUpstream retry loop (that would multiply sleeps against the 15s maxDuration budget). Keep the read server-side only with no client-exposed prefix and keep the thrown message neutral since UpstreamError text surfaces in the route 502 body. Then create src/lib/yahoo-drill.test.ts following the src/lib/yahoo.test.ts pattern (vitest imports, injectable fetchFn, noSleep stub, __resetYahooCacheForTests in beforeEach): one case warming the cache with a mock fetchFn then setting the flag and asserting stale true plus source stale plus preserved candles, one case with cold cache plus flag asserting a throw, each case setting the variable inside the test and restoring the prior value in a finally block so no other suite observes it. This flag and this test file are both deleted in plan 03-04 per D-07; nothing here ships to prod.</action>
  <verify>
    <automated>npm run test -- drill</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
    <automated>! grep -q 'NEXT_PUBLIC_' src/lib/yahoo.ts</automated>
    <fails_when>non-zero exit, meaning a client-exposed env prefix is present in the server lib</fails_when>
  </verify>
  <done>Drill flag forces the serve-stale branch under test with suites green and no client-exposed env read.</done>
  <acceptance_criteria>npm run test -- drill exits 0; npx tsc --noEmit exits 0; src/lib/yahoo.ts contains the env read before the CACHE_KEY lookup throwing UpstreamError with retryable true; src/lib/yahoo-drill.test.ts restores the variable in a finally block; the negated prefix grep exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live JSON → check script | Untrusted envelope bytes cross into assertion logic here |
| Upstream error text → 502 body | Failure detail crosses into a client-visible response here |
| Local prod server → loopback | Tracer serves the production build on localhost:3100 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | scripts/verify-deploy.sh envelope parse (ASVS V5) | medium | mitigate | Defensive node JSON.parse from stdin with fail-closed BAD-ENVELOPE plus strict shape assertions (array, ISO date, boolean stale) so malformed live JSON fails the check instead of passing silently |
| T-03-02 | Tampering | Temporary drill flag in src/lib/yahoo.ts | high | mitigate | Server-only read with no client-exposed prefix (negated-grep gate), neutral 502 message revealing nothing, retryable-true routing through the existing tested stale branch, and scheduled deletion in plan 03-04 |
| T-03-03 | Tampering | Stale data presented as live by the script | medium | mitigate | Script asserts stale strictly false plus age under 120s on the fresh path; the drill assertions in plan 03-03 assert the mirror image on the preview path |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs in this plan: curl plus node only, vitest already lockfile-pinned; dashboard-only Vercel setup means no unverified CLI package executes, so no blocking-human checkpoint is owed |
</threat_model>

<verification>
Gates green, build green, all five script checks PASS against the local production build, drill unit cases green, typecheck clean, no client-exposed env read.
</verification>

<success_criteria>
The verification loop is proven locally and the drill mechanism is proven in isolation; later plans can trust the script and the flag.
</success_criteria>

<output>
Create `.planning/phases/03-production-deploy-verification/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 01 creates: scripts/verify-deploy.sh (executable zero-dependency live-URL checklist with WARMUP, HEADERS, ENVELOPE, ENVELOPE-AGE, PAGE checks); temporary DRILL_FORCE_STALE server-only read at the top of fetchNQDaily in src/lib/yahoo.ts throwing retryable UpstreamError (deleted in plan 03-04); temporary src/lib/yahoo-drill.test.ts with warm-stale and cold-throw cases (deleted in plan 03-04).
