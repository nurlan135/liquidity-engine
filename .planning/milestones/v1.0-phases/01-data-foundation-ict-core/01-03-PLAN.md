---
phase: 01-data-foundation-ict-core
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/lib/yahoo.ts, src/lib/yahoo.test.ts, src/lib/__fixtures__/yahoo-null.json, src/lib/time.ts, src/lib/time.test.ts, app/api/yahoo/route.ts]
autonomous: true
requirements: [DATA-01, DATA-02, STATE-02]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - DATA-01 concurrency explicit: concurrent cache misses join one in-flight upstream promise via singleflight and produce a single upstream call
    - DATA-01 adjacency explicit: two adjacent daily timestamps map to adjacent Baku YYYY-MM-DD strings with no merge or gap from bucketing
    - DATA-02 idempotency explicit: repeating a failed upstream sequence with a warm cache returns the identical stale payload with unchanged lastUpdatedISO
    - DATA-02 concurrency explicit: parallel requests during one upstream fetch share the single in-flight promise instead of fanning out to Yahoo
    - STATE-02 concurrency explicit: concurrent formatting of distinct instants returns the correct per-instant Baku date for each call with no shared mutable state
    - STATE-02 empty explicit: formatting the Unix epoch yields 1970-01-01 Baku string with no throw on the minimum input
    - STATE-02 encoding explicit: all time outputs are ASCII YYYY-MM-DD strings plus numeric minute offsets, never locale-shaped text
    - "{ statement: STATE-02 far-future and far-past instants outside the dated DST cases bucket without throw, verification: backstop }"
    - "{ statement: DATA-02 wait-time saturation exactly at the 10s Retry-After cap resolves deterministically, verification: backstop }"
    - "{ statement: STATE-02 sub-second fractional-millisecond instants bucket to the containing Baku date, verification: backstop }"
  artifacts:
    - src/lib/__fixtures__/yahoo-null.json
    - src/lib/yahoo.ts
    - src/lib/yahoo.test.ts
    - src/lib/time.ts
    - src/lib/time.test.ts
    - app/api/yahoo/route.ts
  key_links:
    - yahoo.ts time import: parser bucketing calls the single Baku helper so upstream timestamps and math share one date rule
    - route.ts yahoo.ts call: GET delegates fetch plus cache plus stale plus 502 mapping to the lib and only sets JSON plus cache headers
    - fixture to parser: recorded Yahoo JSON with mid-array null run parses to exact expected candle set proving null-row handling
  prohibitions:
    - Never cache error payloads or soft-throttle responses as valid candles
    - Never retry non-429 client errors beyond a single host failover
    - Never widen backstop markers into prose notes; keep flat-scalar statement plus verification markers
---

<objective>
Resilience plus time expansion per D-02, D-03, D-04, D-17, DATA-01, DATA-02, STATE-02: harden the tracer fetch skeleton with query1 to query2 failover, jittered backoff honoring Retry-After, singleflight dedupe, 60-second cache with serve-stale and 502 honesty, plus the null-row JSON fixture, plus the full Baku time suite with March and November DST and midnight boundary.

Purpose: Prove the proxy survives upstream failure and DST transitions where production actually bites, with no network in tests.
Output: Hardened yahoo lib plus fixture plus time suite plus final route wiring, full suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-ict-core/01-CONTEXT.md
@tsconfig.json
@package.json
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Null-row fixture plus parser edge proof</name>
  <files>src/lib/__fixtures__/yahoo-null.json, src/lib/yahoo.test.ts</files>
  <read_first>src/lib/yahoo.ts, src/lib/ict/types.ts, src/lib/time.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md</read_first>
  <acceptance_criteria>Fixture file contains chart result with timestamp array plus quote arrays with at least two consecutive mid-array null OHLC rows and one valid last row plus meta with symbol; yahoo test null suite asserts parsed candle count equals valid rows only and parsed dates strictly ascend and no parsed candle contains NaN; npx vitest run src slash lib slash yahoo dot test dot ts dash t null reports passed</acceptance_criteria>
  <action>Author src slash lib slash yahoo-null fixture as JSON under src slash lib slash double-underscore fixtures with a realistic v8 chart result of about 10 timestamps plus parallel open high low close volume arrays containing a mid-array run of nulls across all four OHLC arrays and meta carrying NQ=F symbol plus exchange name plus exchange timezone name. Extend src slash lib slash yahoo dot test dot ts with a null suite importing the fixture JSON that calls parseChartJson and asserts the exact surviving candle count, the exact surviving Baku date sequence, strict ascending order, finite numbers on every OHLC field, and forming flag absent on fully complete rows. Keep all fetch mocked with no network access.</action>
  <verify><automated>npx vitest run src/lib/yahoo.test.ts -t "null"</automated><fails_when>exit code is non-zero or stdout does not contain passed for the null filtered run</fails_when></verify>
  <done>Mid-array null runs drop cleanly with exact surviving candle set proven by fixture test.</done>
</task>

<task type="auto">
  <name>Task 2: Failover plus backoff plus singleflight plus serve-stale</name>
  <files>src/lib/yahoo.ts, src/lib/yahoo.test.ts, app/api/yahoo/route.ts</files>
  <read_first>node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md, src/lib/yahoo.ts, src/lib/time.ts, app/api/yahoo/route.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md</read_first>
  <acceptance_criteria>Yahoo lib retries initial plus 3 with delays 500 then 1000 then 2000 ms each plus minus 25 percent jitter and honors Retry-After seconds or HTTP-date capped at 10 s; attempts alternate query1 and query2 hosts; retryable signals are 429 plus 5xx plus network plus HTTP 200 with chart error code, while other 4xx fail over once then throw; 60-second Map cache serves fresh hits as cache source and upstream failure with warm cache serves identical payload with stale true and source stale and unchanged lastUpdatedISO; failure with empty cache throws UpstreamError mapped by route to 502 with no-store; concurrent misses issue exactly one upstream call; npx vitest run resilience suite reports passed</acceptance_criteria>
  <action>Harden fetchNQDaily in src slash lib slash yahoo.ts with injectable fetch and sleep: initial attempt plus 3 retries with 500 1000 2000 millisecond delays each jittered plus minus 25 percent, honoring the Retry-After response header in both delta-seconds and HTTP-date forms capped at 10 seconds, alternating HOSTS entries per attempt starting query1 then query2. Treat HTTP 429, HTTP 5xx, network throws, 8-second abort timeouts, and HTTP 200 bodies carrying chart error as retryable; treat other 4xx as fail-over-once then throw without further retry. Add a module-level Map keyed NQ=F colon D1 storing payload plus fetchedAt with 60-second TTL, a module-level in-flight Map for singleflight where concurrent misses join the same promise with finally cleanup, validate-before-store gating on symbol match plus ascending dates plus minimum 5 complete candles, and stale-serve returning cached payload with stale true and source stale on upstream failure. Keep the route thin: map warm-cache failure to stale envelope and empty-cache failure to 502 error plus retryAfter 60 with no-store header. Prove with mocked fetch sequences including 429 twice then 200, persistent 500 with warm cache, and empty-cache 502.</action>
  <verify><automated>npx vitest run src/lib/yahoo.test.ts -t "resilience"</automated><fails_when>exit code is non-zero or stdout does not contain passed for the resilience filtered run</fails_when></verify>
  <done>Proxy survives throttling and outage via failover, honest backoff, singleflight, and stale-serve with errors never cached.</done>
</task>

<task type="auto">
  <name>Task 3: Baku time suite with DST plus midnight boundary</name>
  <files>src/lib/time.ts, src/lib/time.test.ts</files>
  <read_first>src/lib/time.ts, package.json, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md</read_first>
  <acceptance_criteria>time.test.ts asserts March instants 2026-03-08T06-59Z and 2026-03-08T07-01Z both bucket to 2026-03-08 with Baku offset stable at plus 240 minutes; November window around 2026-11-01T06-00Z keeps Baku at plus 240 while Chicago flips; midnight boundary UTC 2026-06-15T19-59Z buckets to 06-15 and 20-01Z buckets to 06-16; epoch instant formats to 1970-01-01; repeated formatting of same instant returns identical string; all outputs match ASCII date pattern; npx vitest run time suite reports passed</acceptance_criteria>
  <action>Finalize src slash lib slash time.ts exports BAKU TZ, CME TZ, toBakuYMD, getAsOfBakuDate with injected now default, and bakuOffsetMinutes, using only formatInTimeZone and getTimezoneOffset from date-fns-tz with no hand offsets and no Intl in ict consumers. Write src slash lib slash time dot test dot ts with literal ISO instants for the March spring-forward pair, the November fall-back window, and the June midnight boundary pair, asserting Baku YYYY-MM-DD strings plus Baku offset 240 minutes computed via the library rather than hardcoded assumptions, plus epoch empty-input and repeat-call idempotency cases, plus a parallel Promise-all formatting batch asserting per-instant correctness. Keep identifiers English with any domain terms in comments only.</action>
  <verify><automated>npx vitest run src/lib/time.test.ts</automated><fails_when>exit code is non-zero or stdout does not contain passed for the time suite</fails_when></verify>
  <done>Baku bucketing proven across both DST transitions and the midnight boundary with injected clock and stable offset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yahoo-upstream to proxy | Untrusted JSON plus throttle signals plus Retry-After values cross here and are validated or capped before use |
| proxy-cache to envelope | Only validated payloads enter the 60-second Map; failures bypass the cache and surface as stale or 502 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | src/lib/yahoo.ts cache gate | high | mitigate | Validate-before-store on symbol plus ascending plus complete OHLC plus minimum 5 candles, errors never stored, 502 carries no-store, proven by null plus resilience suites |
| T-03-02 | Denial of Service | src/lib/yahoo.ts fetch path | high | mitigate | Browser UA plus query1 to query2 alternate-host failover, 500 to 1000 to 2000 ms jittered backoff honoring Retry-After capped at 10 s, 8 s abort timeout, singleflight dedupe, 60 s TTL; non-429 4xx fail over once then throw |
| T-03-03 | Spoofing | src/lib/time.ts Baku bucketing | medium | mitigate | Single IANA Asia Baku rule via formatInTimeZone with injected clock, no hand offsets, March plus November plus midnight-boundary suites gate DST correctness |
| T-03-SC | Tampering | npm installs | low | accept | No installs in this plan; toolchain approved in 01-01 |
</threat_model>

<verification>
Targeted null, resilience, and time suites per task plus full npx vitest run plus npx tsc noEmit plus npm run lint at plan end. Manual-only live checks after automation: curl local GET slash api slash yahoo to confirm source live with about 6 months of D1 candles, then log one live meta object to lock contractHint field picks without changing the type.
</verification>

<success_criteria>
Null fixture parses to exact candle set, failover plus backoff plus singleflight plus stale-serve plus 502 chain all green with mocked fetch, DST plus midnight suites green, full suite plus typecheck plus lint clean.
</success_criteria>

## Artifacts this phase produces

This plan creates or finalizes: src slash lib slash double-underscore fixtures slash yahoo-null.json with mid-array null run; src slash lib slash yahoo.test.ts null plus resilience suites; hardened src slash lib slash yahoo.ts cache plus singleflight plus backoff exports; src slash lib slash time.test.ts March plus November plus midnight plus epoch suites; finalized app slash api slash yahoo slash route.ts stale and 502 header mapping.

## Backstop markers and flagged assumptions

The following three probe edges could not be expressed as runnable unit assertions and are recorded as structured flat-scalar statement plus verification markers in must_haves truths above, never as prose: STATE-02 far-future and far-past instants outside the dated DST cases; DATA-02 wait-time saturation exactly at the 10-second Retry-After cap; STATE-02 sub-second fractional-millisecond instant handling. Flagged assumption: quarterly third-Friday plus-minus 10-day roll-proximity convention is a cosmetic calendar heuristic per RESEARCH.md assumption A5 and is asserted structurally in 01-02, not against the live 2026 CME calendar.

<output>
Create `.planning/phases/01-data-foundation-ict-core/01-03-SUMMARY.md` when done
</output>
