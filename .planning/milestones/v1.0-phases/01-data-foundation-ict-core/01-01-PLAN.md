---
phase: 01-data-foundation-ict-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [vitest.config.ts, package.json, src/lib/ict/types.ts, src/lib/time.ts, src/lib/yahoo.ts, src/lib/ict/range.ts, app/api/yahoo/route.ts, src/lib/tracer.test.ts]
autonomous: true
requirements: [DATA-01, ICT-01, STATE-02]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Mocked Yahoo v8 JSON with 25 ascending daily candles flows through fetch, shape-guard, envelope builder, and range math to produce correct high, low, and eq without any network call
    - DATA-01 empty explicit: empty chart result or missing timestamp array raises UpstreamError and never yields a live envelope with zero candles
    - DATA-01 ordering explicit: parsed candles are strictly ascending by Baku YYYY-MM-DD business date after null-row filtering and any duplicate or out-of-order date raises UpstreamError
    - ICT-01 boundary explicit: zero-height range where highest high equals lowest low yields position 0.5 with honest thin-history flag instead of dividing by zero
    - ICT-01 precision explicit: equilibrium equals half the sum of range high and low in IEEE double precision and unit tests assert with closeTo tolerance
    - STATE-02 encoding explicit: Baku date helper always returns four-digit-year hyphen two-digit-month hyphen two-digit-day ASCII strings computed in the IANA Asia Baku zone
    - STATE-02 idempotency explicit: formatting the same instant twice returns the identical date string with no side effects
  artifacts:
    - vitest.config.ts
    - src/lib/ict/types.ts
    - src/lib/time.ts
    - src/lib/yahoo.ts
    - src/lib/ict/range.ts
    - app/api/yahoo/route.ts
    - src/lib/tracer.test.ts
  key_links:
    - yahoo.ts Baku import: unix timestamps map to Baku business-day strings via the single time module
    - route.ts lib call: GET handler calls the lib fetch with current date and returns envelope JSON with cache headers
    - range.ts forming filter: forming rows are excluded before anchor math via the shared closedOnly helper
  prohibitions:
    - Never store validation failures or error payloads in the cache map
    - Never synthesize or interpolate missing candles
    - Never parse caller-supplied symbol or range params in the route handler
    - Never perform wall-clock reads inside ict modules
    - Never format dates with fixed hour offsets
---

<objective>
Tracer slice per D-01, D-02, D-04, D-05, D-08, D-14, D-15, D-16, D-17: establish the vitest harness plus canonical types, then prove one production-quality end-to-end path of fixed NQ=F daily fetch with shape-guard plus Baku date bucketing plus envelope plus range anchor math plus thin route passthrough, verified by a green mocked test before any expansion work.

Purpose: Prove the architecture end-to-end on fresh tokens and catch dead ends after one commit instead of after ten layers.
Output: Green tracer suite plus thin route handler plus infra that Waves 2 builds on.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-ict-core/01-CONTEXT.md
@.planning/phases/01-data-foundation-ict-core/01-RESEARCH.md
@.planning/phases/01-data-foundation-ict-core/01-PATTERNS.md
@.planning/phases/01-data-foundation-ict-core/01-VALIDATION.md
@package.json
@tsconfig.json
@next.config.ts
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish vitest harness and canonical ICT types</name>
  <files>vitest.config.ts, package.json, src/lib/ict/types.ts</files>
  <read_first>package.json, tsconfig.json, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md, .planning/phases/01-data-foundation-ict-core/01-RESEARCH.md</read_first>
  <acceptance_criteria>package.json test script equals vitest run and test colon watch script equals vitest; vitest.config.ts default-exports defineConfig with node environment and include globs for src double-star test ts and app double-star test ts plus alias mapping of at-sign to repo root; src/lib/ict/types.ts exports Candle with date open high low close and optional forming flag plus DealingRange with high low eq window asOf thinHistory plus BiasOutput with mandatory rationale string plus DOLTarget with name and price plus RegimeOutput plus RolloverFlag plus closedOnly helper; npx vitest version reports major 5; npx tsc noEmit exits zero</acceptance_criteria>
  <action>Install vitest 5.0.0 as a devDependency per D-14 with no new runtime dependencies. Create vitest.config.ts using defineConfig from vitest config with node path resolve for the at-sign alias to repository root, test environment node, and test include patterns covering src and app test files. Add test and test watch scripts to package.json. Create src/lib/ict/types.ts per D-15 and D-17 with English identifiers only, defining Candle with YYYY-MM-DD date string and numeric OHLC plus optional forming boolean, DealingRange with numeric high low eq plus window count plus asOf Baku date string plus thinHistory boolean, BiasOutput with BULLISH BEARISH COMPRESSION union plus mandatory rationale string plus position number plus regime union, DOLTarget with name string and price number, RegimeOutput with regime union plus rationale plus atr and avg numbers, RolloverFlag with rolloverSuspect boolean plus contractHint string plus nullable proximityWarning, and a closedOnly helper that returns only rows without the forming flag. Use at-sign slash src slash lib slash ict import paths consistent with tsconfig paths mapping of at-sign slash star to dot slash star.</action>
  <verify><automated>npx vitest --version</automated><fails_when>exit code is non-zero or stdout does not contain 5.</fails_when></verify>
  <done>Vitest 5 installed, config present, canonical types importable, typecheck green.</done>
</task>

<task type="tracer">
  <name>End-to-end NQ=F fetch validate envelope range — one path only</name>
  <files>src/lib/time.ts, src/lib/yahoo.ts, src/lib/ict/range.ts, src/lib/tracer.test.ts</files>
  <read_first>package.json, tsconfig.json, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md, .planning/phases/01-data-foundation-ict-core/01-RESEARCH.md, src/lib/ict/types.ts, vitest.config.ts</read_first>
  <acceptance_criteria>Tracer test file mocks fetch and asserts envelope source live with 25 candles plus range high equals max high and low equals min low over last 20 closed candles plus eq equals half sum; toBakuYMD returns 2026-03-08 for both 2026-03-08T06-59Z and 2026-03-08T07-01Z instants; parseChartJson drops mid-array rows with non-finite OHLC and keeps last partial row with forming true only when close is finite; computeRange sets thinHistory false at 20 plus candles and true below; zero-height range yields position 0.5; npx vitest run src slash lib slash tracer dot test dot ts reports all passed</acceptance_criteria>
  <action>Build the single happy path per D-02 D-04 D-05 D-08 D-16 D-17. In src slash lib slash time.ts export BAKU TZ constant Asia Baku and CME TZ constant America Chicago plus toBakuYMD accepting Date or number via formatInTimeZone with yyyy-MM-dd pattern plus getAsOfBakuDate accepting now defaulting to new Date plus bakuOffsetMinutes via getTimezoneOffset, importing only formatInTimeZone and getTimezoneOffset from date-fns-tz 3.2.0. In src slash lib slash yahoo.ts define HOSTS with query1 then query2 finance yahoo hosts and HEADERS with Mozilla 5.0 Windows NT 10.0 Win64 x64 AppleWebKit Chrome 126 Safari 537.36 plus application json Accept plus en-US Accept-Language plus finance yahoo Referer, export fetchNQDaily accepting now Date plus injectable fetch function plus injectable sleep that computes period1 as floor now seconds minus 182 times 86400 and period2 as floor now seconds with interval 1d and events history against URL-encoded NQ percent 3D F, export parseChartJson that checks chart error before chart result then zips timestamp and quote open high low close parallel arrays by index dropping rows with non-finite values except last row kept with forming true when close is finite then maps timestamps through toBakuYMD then asserts symbol matches NQ case-insensitive and dates strictly ascending, export buildEnvelope returning candles plus lastUpdatedISO instant ISO plus stale boolean plus source string, ignoring the fifth adjusted price array entirely per D-08. In src slash lib slash ict slash range.ts export ANCHOR WINDOW 20 plus computeRange accepting closed candles plus asOf Baku date plus window default 20 that operates over last window closed rows with no fetching or trimming and sets thinHistory when fewer than window rows available plus checkReanchor that retires only on strict close greater than prior high or strict close less than prior low with wick pierce ignored. Create tracer test with mocked fetch returning fixed 25-candle Yahoo JSON proving fetch through range without network.</action>
  <verify><automated>npx vitest run src/lib/tracer.test.ts</automated><fails_when>exit code is non-zero or stdout does not contain passed and does not list the tracer test file as passed</fails_when></verify>
  <done>The single happy path works end-to-end and is committed with real error handling on that path.</done>
</task>

<task type="auto">
  <name>Task 3: Thin Yahoo route passthrough with honest cache headers</name>
  <files>app/api/yahoo/route.ts</files>
  <read_first>node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md, app/page.tsx, next.config.ts, .planning/phases/01-data-foundation-ict-core/01-CONTEXT.md, .planning/phases/01-data-foundation-ict-core/01-RESEARCH.md, src/lib/yahoo.ts, src/lib/time.ts</read_first>
  <acceptance_criteria>Route file exports dynamic force-dynamic and maxDuration 15; GET takes no params and never reads URL search params; success response JSON contains candles plus lastUpdatedISO plus stale plus source with Cache-Control public s-maxage 60 stale-while-revalidate 30; cold failure maps to 502 JSON with error and retryAfter 60 plus Cache-Control no-store; file contains no use client directive; npx tsc noEmit exits zero; tracer suite still green</acceptance_criteria>
  <action>Create app slash api slash yahoo slash route.ts per D-01 D-02 D-03 as a server-only Route Handler with dynamic force-dynamic and maxDuration 15. Implement async GET with no request argument parsing that calls the lib fetch with a fresh Date and returns Response json envelope with success cache header public s-maxage 60 stale-while-revalidate 30. Map upstream failure with empty cache to status 502 JSON error plus retryAfter 60 with no-store header and never synthesize candles. Keep the handler thin with all fetch failover backoff cache logic in src lib yahoo. Do not add any client directive and do not convert the root layout to client. Using fixed NQ=F daily per user decision D-01, a costly reversibility choice because Phase 2 polling and envelope consumers assume this fixed contract.</action>
  <verify><automated>npx tsc --noEmit</automated><fails_when>exit code is non-zero or stderr contains error TS</fails_when></verify>
  <done>GET slash api slash yahoo returns the lib envelope with honest headers and fixed-symbol contract.</done>
  <reversibility rating="costly">Fixed NQ=F daily contract per D-01; parameterizing later touches proxy plus all callers.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yahoo-upstream to proxy | Untrusted upstream JSON crosses here and must pass shape-guard before cache or envelope |
| proxy to ict-math | Only validated closed candles plus explicit asOf date cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | src/lib/yahoo.ts parseChartJson | high | mitigate | Check chart error before result, validate symbol match plus ascending dates plus complete OHLC, throw UpstreamError on drift, never cache failures |
| T-01-02 | Denial of Service | src/lib/yahoo.ts fetchNQDaily plus HOSTS | high | mitigate | Send browser UA plus Accept plus Referer constants, define query1 primary plus query2 fallback host list, full jittered backoff and singleflight land in 01-03 on top of this tracer skeleton |
| T-01-03 | Tampering | app/api/yahoo/route.ts envelope | high | mitigate | Validate-before-store only, success carries honest stale false source live, cold failure returns 502 with no-store and retryAfter 60 |
| T-01-SC | Tampering | npm installs | high | mitigate | Install only vitest 5.0.0 which holds an approved legitimacy audit with High-reputation docs and no postinstall script; no other packages installed in this plan |
</threat_model>

<verification>
Full tracer suite green via npx vitest run plus typecheck via npx tsc noEmit plus lint via npm run lint before verify-work. Manual-only live check deferred to 01-03: curl local GET slash api slash yahoo and confirm source live with about 6 months of D1 candles.
</verification>

<success_criteria>
Vitest harness installed, canonical types importable, mocked 25-candle Yahoo payload flows to correct range high low eq, thin route returns envelope with honest headers, typecheck green.
</success_criteria>

## Artifacts this phase produces

This plan creates: vitest.config.ts with node env plus at-sign alias plus src and app includes; package.json test and test watch scripts plus vitest 5.0.0 devDependency; src slash lib slash ict slash types.ts exporting Candle, DealingRange, BiasOutput, DOLTarget, RegimeOutput, RolloverFlag, closedOnly; src slash lib slash time.ts exporting BAKU TZ, CME TZ, toBakuYMD, getAsOfBakuDate, bakuOffsetMinutes; src slash lib slash yahoo.ts exporting HOSTS, HEADERS, fetchNQDaily, parseChartJson, buildEnvelope, UpstreamError; src slash lib slash ict slash range.ts exporting ANCHOR WINDOW, computeRange, checkReanchor, computePosition; app slash api slash yahoo slash route.ts exporting dynamic, maxDuration, GET; src slash lib slash tracer.test.ts proving the slice.

<output>
Create `.planning/phases/01-data-foundation-ict-core/01-01-SUMMARY.md` when done
</output>
