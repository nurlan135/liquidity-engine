---
phase: 02-terminal-composition
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/fixtures/crowded-long.json
  - src/lib/fixtures/crowded-short.json
  - src/lib/fixtures/balanced.json
  - src/lib/fixture-guard.ts
  - src/lib/fixtures.test.ts
  - components/dashboard/sentiment-panel.tsx
  - components/dashboard/calendar-panel.tsx
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Three fixture snapshots load as local JSON with sentiment rows, calendar offsets, and static interpretation prose
    - Sentiment table shows Broker Long Short rows with a True AVG footer excluding Insta and FiboGroup entries
    - Crowded flag chip appears only when a side reaches 60 percent and the scenario switcher shows all shipped snapshots
    - Calendar lists high-impact events with Baku countdowns and pre-news flags plus zero-event copy when empty
    - Trap-vs-genuine interpretation prose renders statically per scenario and never claims to be computed
  artifacts:
    - src/lib/fixtures/crowded-long.json
    - src/lib/fixtures/crowded-short.json
    - src/lib/fixtures/balanced.json
    - src/lib/fixture-guard.ts
    - src/lib/fixtures.test.ts
    - components/dashboard/sentiment-panel.tsx
    - components/dashboard/calendar-panel.tsx
  key_links:
    - Panels import fixtures as static JSON and validate through the shape guard before rendering
    - SentimentPanel calls trueAvg from src/lib/sentiment and CalendarPanel calls formatCountdown plus isPreNews from src/lib/countdown
    - Scenario switching calls setScenario on the dashboard store from plan 01 with no store rewrite
  prohibitions:
    - must NOT present fixture sentiment, calendar, or interpretation prose as live market data; the scenario label stays visible
    - must NOT compute trap-vs-genuine interpretation from live inputs; prose is static per scenario
    - must NOT render bias, DOL, or confidence from insufficient history; degrade to COMPRESSION with a stated reason
---

<objective>
Fixture-driven Module 1 and calendar surface per D-05 through D-07: three rotating local-JSON snapshots with shape guards, a sentiment exposure table with True AVG and the 60 percent crowded flag, and a high-impact calendar with countdowns, pre-news flags, and static trap-vs-genuine prose. No new API routes.

Purpose: Prove the fixture panels visibly respond across crowded-long, crowded-short, and balanced scenarios.
Output: Three snapshot files plus guard plus resolver with shape tests, SentimentPanel with switcher, CalendarPanel with interpretation block.

Flagged assumptions (spec-less probe fallback, surfaced not dropped):
- A-m1 (MOCK-01): a True AVG side at exactly 60.0 counts as crowded (greater-or-equal rule); every broker row must sum longPct plus shortPct to 100 within 0.01 or the guard rejects the snapshot.
- A-m2 (MOCK-02): far-future event offsets render full day counts with no cap; an event whose offset has fully elapsed resolves to 0g 0s sonra and keeps its row rather than vanishing.
- A-m3 (MOCK-03): interpretation prose is static Azerbaijani text of 2 to 4 sentences authored per snapshot, fusing calendar plus regime plus position at authorship time, and never recomputed from live selector output.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-composition/02-CONTEXT.md
@.planning/phases/02-terminal-composition/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Fixture snapshots plus shape guard and resolver</name>
  <files>src/lib/fixtures/crowded-long.json, src/lib/fixtures/crowded-short.json, src/lib/fixtures/balanced.json, src/lib/fixture-guard.ts, src/lib/fixtures.test.ts</files>
  <read_first>src/lib/__fixtures__/yahoo-null.json, src/lib/yahoo.test.ts, src/lib/time.ts, src/lib/sentiment.ts, src/lib/countdown.ts, reference/institutional_rules.md</read_first>
  <behavior>
    - Test 1: guard accepts the crowded-long snapshot and trueAvg over its rows returns crowded LONG with buy at or above 60.
    - Test 2: guard accepts the crowded-short snapshot with crowded SHORT and the balanced snapshot with crowded null.
    - Test 3: guard rejects a broker row whose percentages do not sum to 100 and a row with a non-finite percentage.
    - Test 4: resolver turns startsAtOffsetHours into startsAt ISO strings anchored at the injected now with no Date.now inside.
    - Test 5: every snapshot carries non-empty Azerbaijani interpretation prose and only high impact calendar entries.
  </behavior>
  <action>Author three scenario files under src/lib/fixtures with exact names crowded-long.json, crowded-short.json, and balanced.json, each holding scenario id, Azerbaijani label izdihamlı-long or izdihamlı-short or balanslı, sentiment object with symbol NQ and brokers array of broker longPct shortPct rows, calendar object with events array of event impact startsAtOffsetHours rows, and interpretation string of 2 to 4 Azerbaijani sentences fusing calendar plus regime plus position per D-06. Use broker names OANDA, Forex.com, and IG as included rows plus InstaForex and FiboGroup rows so the trueAvg exclusion has entries to exclude per RESEARCH A1, with percentages that land crowded-long at buy average at or above 60, crowded-short at sell average at or above 60, and balanced with both sides below 60. Set event offsets in hours from load time (for example a near event within 24 hours to trip the pre-news flag and a farther event beyond it) so countdowns stay fresh forever. Create src/lib/fixture-guard.ts with named exports ScenarioFixture and ResolvedEvent interfaces, SCENARIO_IDS constant with the three ids, isScenarioFixture guard validating row shapes plus finite percentages within 0 to 100 plus rows summing to 100 within 0.01 plus impact literal high, and resolveScenario accepting a fixture plus now Date defaulting to current time only at the boundary and returning events with startsAt ISO computed as now plus offset hours. Cover with src/lib/fixtures.test.ts importing all three JSON files directly through resolveJsonModule, asserting the crowded outcomes above plus rejection paths plus resolver anchoring on a fixed instant. English identifiers and comments, Azerbaijani prose only inside JSON strings.</action>
  <verify>
    <automated>npx vitest run src/lib/fixtures.test.ts</automated>
    <fails_when>non-zero exit, or No test files found in output, or any Failed Tests section in output</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Three snapshots validate through the guard, resolve to ISO countdown targets from any injected now, and both verify commands pass.</done>
  <acceptance_criteria>npx vitest run src/lib/fixtures.test.ts exits 0; src/lib/fixture-guard.ts exports isScenarioFixture and resolveScenario and SCENARIO_IDS; each fixture file contains InstaForex and FiboGroup broker rows and a non-empty interpretation string; no fixture file contains a startsAt literal</acceptance_criteria>
</task>

<task type="auto">
  <name>SentimentPanel with True AVG and scenario switcher</name>
  <files>components/dashboard/sentiment-panel.tsx</files>
  <read_first>src/lib/store.ts, src/lib/sentiment.ts, src/lib/fixture-guard.ts, src/lib/fixtures/crowded-long.json, components/ui/card.tsx, components/ui/button.tsx</read_first>
  <behavior>
    - Switching scenario from crowded-long to balanced swaps the True AVG footer and clears the crowded flag chip.
    - Table shows Broker Long Short columns with the Həqiqi Ortalama footer row excluding Insta and FiboGroup rows.
    - Crowded side at or above 60 percent renders the İZDİHAMLI flag chip with scenario label visible.
  </behavior>
  <action>Build SentimentPanel as a named export starting with the client pragma, reading scenario plus setScenario from the dashboard store, statically importing the three snapshots and selecting by scenario id with the crowded-long file as fallback for unknown values. Validate the selected snapshot with isScenarioFixture and render the documented error copy plus Məlumat yoxdur heading when validation fails. Render a Card shell with data-slot sentiment-panel, an 11px uppercase section title, table rows for every broker with mono percentages, a footer row with the exact copy shape Həqiqi Ortalama (Insta/FiboGroup xaric): BUY X% / SELL Y% from trueAvg output, the crowded flag chip with İZDİHAMLI LONG or SHORT plus percentage when crowded is non-null, and a scenario switcher row of three small Buttons labeled izdihamlı-long, izdihamlı-short, balanslı with the accent indicator on the active scenario and the Ssenari label prefix per the UI-SPEC copy contract. Keep English identifiers with Azerbaijani rendered copy per D-04, styling through theme tokens plus the cn merger with data-slot attributes and no literal color values. Static JSON imports render synchronously so no skeleton path is needed. Never import Base UI primitives directly, only the components/ui wrappers.</action>
  <verify>
    <automated>npm run test -- sentiment</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Sentiment panel renders rows plus True AVG plus flag plus switcher for all three scenarios with suites green.</done>
  <acceptance_criteria>components/dashboard/sentiment-panel.tsx contains data-slot sentiment-panel and İZDİHAMLI and Ssenari; the panel imports trueAvg from @/src/lib/sentiment and isScenarioFixture from @/src/lib/fixture-guard; npm run test -- sentiment exits 0; npx tsc --noEmit exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>CalendarPanel with countdowns and interpretation prose</name>
  <files>components/dashboard/calendar-panel.tsx</files>
  <read_first>src/lib/store.ts, src/lib/countdown.ts, src/lib/fixture-guard.ts, src/lib/fixtures/crowded-long.json, components/ui/card.tsx</read_first>
  <behavior>
    - Each high-impact event renders its name plus Baku countdown plus pre-news flag only inside the 24 hour window.
    - A snapshot with zero events renders the Yüksək təsirli xəbər yoxdur heading plus the documented body copy.
    - The interpretation block shows the Tələ vs Həqiqi Çatdırılma heading with the scenario prose verbatim.
  </behavior>
  <action>Build CalendarPanel as a named export starting with the client pragma, reading scenario from the dashboard store, statically importing the three snapshots with the same selection and fallback rule as the sentiment panel, validating with isScenarioFixture, and resolving events through resolveScenario with a now state ticking on a 30 second interval isolated to this component. Render a Card shell with data-slot calendar-panel listing each event as name plus countdown from formatCountdown plus the XƏBƏR ÖNCƏSİ flag with event name and countdown when isPreNews is true, mono numerals for all countdowns, the zero-events heading Yüksək təsirli xəbər yoxdur with body Təqvimdə yaxın günlərdə yüksək təsirli xəbər planlaşdırılmayıb. when the list is empty, and below it the interpretation block with data-slot interpretation carrying the Tələ vs Həqiqi Çatdırılma heading and the snapshot interpretation prose rendered verbatim with no computation per D-06. English identifiers with Azerbaijani rendered copy per D-04, theme tokens plus the cn merger, data-slot attributes, no literal color values, no Base UI direct imports.</action>
  <verify>
    <automated>npm run test -- calendar</automated>
    <fails_when>non-zero exit, or No test files found in output, or 0 passed in the summary line</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any error TS line in output</fails_when>
  </verify>
  <done>Calendar panel renders countdowns plus pre-news flags plus static interpretation with suites green.</done>
  <acceptance_criteria>components/dashboard/calendar-panel.tsx contains data-slot calendar-panel and data-slot interpretation and Yüksək təsirli xəbər yoxdur; the panel imports formatCountdown and isPreNews from @/src/lib/countdown and resolveScenario from @/src/lib/fixture-guard; npm run test -- calendar exits 0; npx tsc --noEmit exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON import → shape guard → panel render | Committed static data crosses into rendered numbers here |
| Scenario switcher → store scenario → panel selection | UI selection crosses into data source choice here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | fixture shape guard (src/lib/fixture-guard.ts) | medium | mitigate | Hand-rolled guard rejects malformed rows before render per the Phase 1 precedent; shape tests pin all three snapshots; fixtures are committed source reviewed like code |
| T-02-06 | Spoofing | scenario label vs fixture data (panels) | low | mitigate | Active scenario label renders beside every derived number so fixture output can never pass as live market data |
| T-02-SC | Tampering | npm installs for panel deps | high | mitigate | Zero new installs; all rendering uses installed Card plus Button wrappers with the RESEARCH legitimacy audit passed; any future install needs a blocking human checkpoint |
</threat_model>

<verification>
Fixtures suite green plus sentiment and calendar suites green, typecheck clean, and switching scenarios visibly moves the True AVG footer, crowded flag, countdowns, and interpretation prose in dev.
</verification>

<success_criteria>
Three snapshots ship with guard plus resolver tested, SentimentPanel shows rows plus True AVG plus flag plus switcher, CalendarPanel shows countdowns plus pre-news flags plus static prose, and typecheck is clean.
</success_criteria>

<output>
Create .planning/phases/02-terminal-composition/02-03-SUMMARY.md when done
</output>

## Artifacts this phase produces

Plan 03 creates: scenario fixtures crowded-long.json plus crowded-short.json plus balanced.json in src/lib/fixtures/; guard plus resolver isScenarioFixture plus resolveScenario plus SCENARIO_IDS in src/lib/fixture-guard.ts; SentimentPanel named component with True AVG footer plus crowded chip plus three-button switcher in components/dashboard/sentiment-panel.tsx; CalendarPanel named component with countdown rows plus pre-news flags plus verbatim interpretation block in components/dashboard/calendar-panel.tsx. Test file: src/lib/fixtures.test.ts.
