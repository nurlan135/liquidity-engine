---
phase: 13-thin-history-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/thin-tier.ts, src/lib/thin-tier.test.ts, components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx]
autonomous: true
requirements: [HIST-01]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "User with 1-19 closed candles sees the range-thin banner with Tier-1 copy naming the closed count over 20 per D-09 D-13 D-14 D-15 (HIST-01)"
    - "User with 20-33 closed candles sees the regime-degraded banner with Tier-2 copy naming the closed count over 34 per D-09 D-13 D-14 D-15 (HIST-01)"
    - "Banner strip sits above the chart reusing the rollover-banner box and role status, never dismissible, persisting on every thin render per D-01 D-03 (HIST-01)"
    - "When thin-history and rollover-suspect are both true both strips render stacked thin-history first per D-02 (HIST-01)"
    - "Zero candles never shows the thin banner; the chart-empty state with Yenile retry owns that state per D-04 (HIST-01)"
    - "Loading skeleton never carries the thin banner per the UI-SPEC loading consideration (HIST-01)"
    - "Full tier (34+ closed) renders zero banner DOM per D-09 and the UI-SPEC populated consideration (HIST-01)"
    - "A single candle renders the Tier-1 banner at natural width with no stretching per D-07 and the zero-one-many consideration (HIST-01)"
    - "A throw in tier derivation degrades silently to no-banner plus full-strength chart and never blanks candles per the UI-SPEC error consideration and D-08 (HIST-01)"
    - "Chart container carries the live tier for assertion and styling hooks per UI-SPEC (HIST-01)"
    - statement: "Banner copy wraps within card width at narrow widths with no truncation or horizontal overflow holds on held-out visual verification per D-08"
      verification: backstop
  artifacts:
    - path: "src/lib/thin-tier.ts"
      provides: "Tier truth plus verbatim copy map plus order predicate"
      exports: ["ThinTier", "thinTier", "thinTierCopy", "resolveThinTier", "thinBannerOrder"]
    - path: "src/lib/thin-tier.test.ts"
      provides: "Boundary pins, forming-exclusion, copy-per-tier, order predicate, pinned constants"
      contains: "thinTier"
    - path: "components/dashboard/terminal-shell.tsx"
      provides: "Thin-history banner block stacked thin-first over rollover"
      contains: "thin-history-banner"
    - path: "components/charts/nq-chart.tsx"
      provides: "thinTier prop plumbing with live propsRef sync and container tier attribute"
      contains: "data-thin-tier"
  key_links:
    - from: "components/dashboard/terminal-shell.tsx"
      to: "src/lib/thin-tier.ts"
      via: "shell imports resolveThinTier plus thinTierCopy plus thinBannerOrder — tier derived during render, never a UI-local cutoff"
      pattern: "resolveThinTier"
    - from: "components/charts/nq-chart.tsx"
      to: "src/lib/thin-tier.ts"
      via: "chart imports the ThinTier type for the new optional prop"
      pattern: "ThinTier"
    - from: "components/dashboard/terminal-shell.tsx"
      to: "components/charts/nq-chart.tsx"
      via: "shell passes thinTier into the NqChart invocation alongside overlayStale"
      pattern: "thinTier="
---

<objective>
Prove tier truth end-to-end and surface the honest banner: a tested pure tier helper plus the persistent banner strip plumbed through the shell into the chart prop, per D-01 D-02 D-03 D-04 D-08 D-09 D-11 D-12 D-13 D-14 D-15 and HIST-01.

Purpose: Users with thin history see an honest persistent indicator instead of a confident-looking full chart — the phase goal's banner half.
Output: src/lib/thin-tier.ts plus its test, a thin-history banner block in terminal-shell.tsx, and a thinTier prop accepted end-to-end by nq-chart.tsx (plumbing only; no canvas dimming yet). Canvas dimming decisions D-05 D-06 D-07 D-10 are implemented in Plan 02; this plan owns the banner half.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-thin-history-honesty/13-CONTEXT.md
@.planning/phases/13-thin-history-honesty/13-RESEARCH.md
@.planning/phases/13-thin-history-honesty/13-PATTERNS.md
@.planning/phases/13-thin-history-honesty/13-UI-SPEC.md
@.planning/phases/13-thin-history-honesty/13-VALIDATION.md
@src/lib/ict/range.ts
@src/lib/ict/regime.ts
@src/lib/ict/types.ts
@src/lib/ict/regime.test.ts
@components/dashboard/terminal-shell.tsx
@components/charts/nq-chart.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end tier truth — helper plus boundary tests, one path only</name>
  <read_first>src/lib/ict/range.ts ANCHOR_WINDOW line 4 plus thinHistory predicate line 25 plus src/lib/ict/regime.ts MIN_CANDLES_FULL line 6 plus degrade rationale lines 44-50 plus src/lib/ict/types.ts closedOnly lines 47-49 plus DealingRange lines 10-17 plus src/lib/ict/regime.test.ts fixture builders lines 11-22 plus forming-exclusion lines 76-83 plus pinned-constants lines 85-89 plus .planning/phases/13-thin-history-honesty/13-UI-SPEC.md Copywriting Contract plus Thin-Tier Contract boundary pins per D-09 D-11 D-13 D-14 D-15</read_first>
  <files>src/lib/thin-tier.ts, src/lib/thin-tier.test.ts</files>
  <behavior>
    - Test: thinTier(19) returns range-thin; thinTier(20) returns regime-degraded; thinTier(33) returns regime-degraded; thinTier(34) returns full
    - Test: thinTier range-thin holds exactly when closed count is below ANCHOR_WINDOW (same predicate as computeRange thinHistory, no drift)
    - Test: resolveThinTier returns null for a null range; counts closedOnly candles (20 rows with 1 forming yields 19 closed yielding range-thin)
    - Test: thinTierCopy returns the verbatim Tier-1 string for count 5 and the verbatim Tier-2 string for count 25
    - Test: thinBannerOrder(true, true) returns thin then rollover; single-true inputs return the single entry; (false, false) returns empty
    - Test: ANCHOR_WINDOW equals 20 and MIN_CANDLES_FULL equals 34 (pinned constants)
  </behavior>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. Create src/lib/thin-tier.ts as a DOM-free pure module importing ANCHOR_WINDOW from @/src/lib/ict/range, MIN_CANDLES_FULL from @/src/lib/ict/regime, closedOnly plus the Candle and DealingRange types from @/src/lib/ict/types, defining zero local threshold literals per D-11. Export the ThinTier union of range-thin, regime-degraded, and full. Export thinTier taking a closed count and returning range-thin below ANCHOR_WINDOW, regime-degraded below MIN_CANDLES_FULL, else full, using the same less-than boundary form as computeRange per D-09 D-11. Export thinTierCopy taking a non-full tier plus the closed count and returning the verbatim UI-SPEC strings with the count substituted: Tier-1 Nazik tarixçə · {N} / 20 şam — ekstremumlar etibarsızdır, zonalar və səviyyələr ilkin göstərilir. and Tier-2 Sıxılma · {N} / 34 şam — tarixçə tam deyil, zonalar və səviyyələr ilkin göstərilir. per D-13 D-14 D-15. Export resolveThinTier taking candles plus a nullable range and returning null for a null range else the tier plus closed count, wrapped so it never throws and returns null on any unexpected input per the degrade-silent interaction rule. Export thinBannerOrder taking showThin plus showRollover booleans and returning the render order array with thin first when both are true per D-02. Create src/lib/thin-tier.test.ts in vitest 5 node env copying the flatCandle and flatSeries fixture shape from regime.test.ts with closed-count fixtures at 5, 19, 20, 25, 33, 34, and 40, covering every behavior case above. Write the test file first, run it to see it fail, then implement the helper until green.</action>
  <acceptance_criteria>src/lib/thin-tier.ts contains no numeric literals 20 or 34 outside import statements per D-11; src/lib/thin-tier.test.ts contains boundary pins for 19, 20, 33, 34 and a forming-exclusion case and the verbatim Tier-1 and Tier-2 copy assertions and the order-predicate assertions; npm test -- --run src/lib/thin-tier.test.ts exits 0 with all tests passed; npx tsc --noEmit exits 0</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/thin-tier.test.ts</automated>
    <fails_when>non-zero exit, or the output summary shows any failed test or zero passed tests</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or output contains the string error TS</fails_when>
  </verify>
  <done>Single tier-truth path tested end-to-end from math constants to tier value plus copy plus order, typecheck green per D-09 D-11 D-13 D-14 D-15</done>
</task>

<task type="auto">
  <name>Render the persistent banner and thread the tier into the chart</name>
  <read_first>components/dashboard/terminal-shell.tsx rollover block lines 245-252 plus render chain lines 253-290 plus selector lines 45-47 plus NqChart invocation lines 264-281 plus components/charts/nq-chart.tsx props lines 13-34 plus propsRef sync lines 124-129 plus zone getter lines 271-292 plus dep array line 461 plus container div lines 479-490 plus src/lib/thin-tier.ts exports from Task 1 plus .planning/phases/13-thin-history-honesty/13-UI-SPEC.md Banner-stack contract plus Interaction contract rules 1-3 and 5 plus .planning/phases/13-thin-history-honesty/13-CONTEXT.md D-01 D-02 D-03 D-04 per D-01 D-02 D-03 D-04 D-12</read_first>
  <files>components/dashboard/terminal-shell.tsx, components/charts/nq-chart.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. In terminal-shell.tsx import resolveThinTier, thinTierCopy, and thinBannerOrder from @/src/lib/thin-tier. During render derive the tier with resolveThinTier applied to candles and range, then compute showRollover from the existing rollover predicate and showThin as lastUpdatedISO non-null plus candles length at least 1 plus tier non-null plus tier not full per D-04 and the skeleton guard. Extract the existing rollover block verbatim into a rolloverBlock const and build a thinBlock const as a div carrying data-slot thin-history-banner, role status, className rounded px-3 py-2 font-mono text-[11px] tracking-widest text-muted-foreground byte-identical to the rollover box, plus data-tier set to the tier and data-closed-count set to the closed count, rendering thinTierCopy output as plain JSX text per D-01 D-03 D-13. Render the thinBannerOrder array with Array map using React keys thin and rollover, wrapping in a div with className flex flex-col gap-2 only when the array length is 2 with thin first, otherwise rendering the bare blocks with no wrapper per D-02, placed inside CardContent above the chart render branch. Pass thinTier set to the derived tier defaulting to full into the existing NqChart invocation per the D-discretion prop-shape call. Make zero edits to report.tsx or any report prose per D-12. In nq-chart.tsx add the optional thinTier prop typed from the ThinTier import defaulting to full in the signature, add thinTier to the propsRef object literal plus the sync effect plus the update-effect dep array per Pitfall 4, add data-thin-tier set to thinTier on the div carrying data-slot nq-chart, and replace the dead `thinHistory: false` literal in the zone getter with the live expression `thinHistory: latest.thinTier === 'range-thin'` (the locked equivalence that range-thin holds exactly when thinHistory is true per D-09) so no literal false sits beside real thin logic per Pitfall 2. Add no viewport or time-scale calls, no marker or Asia branch edits, and no dimming branches in this task — dimming belongs to Plan 02.</action>
  <acceptance_criteria>components/dashboard/terminal-shell.tsx contains data-slot="thin-history-banner" with role="status" and the rollover box class string and data-tier and data-closed-count per D-01; contains the flex flex-col gap-2 wrapper used only for the dual-banner case per D-02; banner condition requires candles length at least 1 and non-null lastUpdatedISO per D-04; NqChart invocation contains thinTier= ; components/charts/nq-chart.tsx contains thinTier in the props interface, the propsRef literal, the sync effect, the dep array, and data-thin-tier on the nq-chart container div; the zone getter no longer contains a hardcoded false for thinHistory; git diff --name-only lists exactly terminal-shell.tsx and nq-chart.tsx plus the Task 1 files; report.tsx untouched per D-12; npx tsc --noEmit exits 0; npm test -- --run src/lib/thin-tier.test.ts still exits 0</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or output contains the string error TS</fails_when>
    <automated>npm test -- --run src/lib/thin-tier.test.ts</automated>
    <fails_when>non-zero exit, or the output summary shows any failed test or zero passed tests</fails_when>
  </verify>
  <done>Honest banner renders per tier with thin-first stacking and the live tier reaches the chart prop plus container attribute, typecheck and tier tests green per D-01 D-02 D-03 D-04 D-12</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ICT math→thin-tier.ts | Threshold constants cross from the math layer into UI truth; values are compile-time imports, never redefined or fetched |
| store/shell→banner DOM | Closed-candle counts cross from zustand selectors into banner text and data attributes; derived integers only, never user input or upstream strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | terminal-shell.tsx thin banner DOM | low | mitigate | Render banner copy as plain JSX text interpolation and the count as a numeric data attribute; add no innerHTML or dangerouslySetInnerHTML in the banner block or its wrapper |
| T-13-02 | Denial of service | terminal-shell.tsx banner layout | low | accept | Closed counts are bounded integers from the candles array length and the banner inherits the wrapping rollover box with no fixed width, so oversized counts wrap rather than break layout |
| T-13-SC | Tampering | npm installs | low | accept | No package installs in this plan; lightweight-charts 5.2.1 already installed, so no legitimacy checkpoint required |
</threat_model>

<verification>
In order from the repo root: run npm test -- --run src/lib/thin-tier.test.ts for exit zero with all tests passed, run npx tsc --noEmit for exit zero, confirm git diff --name-only lists exactly src/lib/thin-tier.ts, src/lib/thin-tier.test.ts, components/dashboard/terminal-shell.tsx, and components/charts/nq-chart.tsx, and confirm report.tsx shows no diff.
</verification>

<success_criteria>
User with 1-19 closed candles sees the Tier-1 banner and user with 20-33 sees the Tier-2 banner, both persistent and stacked thin-first over rollover; zero-candle, skeleton, and full-tier states show zero banner DOM; the live tier reaches the chart prop and container attribute.
</success_criteria>

## Artifacts this phase produces

New pure module src/lib/thin-tier.ts exporting the ThinTier union type, the thinTier closed-count classifier, the thinTierCopy verbatim copy map, the resolveThinTier null-safe shell derivation, and the thinBannerOrder stacking predicate; new test file src/lib/thin-tier.test.ts; new banner block with data-slot thin-history-banner plus data-tier plus data-closed-count in components/dashboard/terminal-shell.tsx; new optional thinTier prop with default full plus data-thin-tier container attribute in components/charts/nq-chart.tsx.

<output>
Create `.planning/phases/13-thin-history-honesty/13-01-SUMMARY.md` when done
</output>
