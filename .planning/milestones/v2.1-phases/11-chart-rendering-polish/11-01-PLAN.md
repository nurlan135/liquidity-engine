---
phase: 11-chart-rendering-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/charts/nq-chart.tsx]
autonomous: true
requirements: [CHRT-01, CHRT-02]
estimate:
  tokens: 16000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Populated chart renders crisp lines and text at devicePixelRatio 2 with no blur — human visual check per D-05 (CHRT-01)"
    - "Chart follows new size live during active drag and refills its container with no stretch, clipping, or frozen canvas per D-01 D-02 (CHRT-02)"
    - "Visible time range preserved through resize, never reset to re-fit per D-03"
    - "Asia lines, Judas/SMT markers, and zone fills persist through resize with no flicker or dropped primitives per D-04"
    - "Surrounding chrome (STALE badge, forming row, MARKET CLOSED ribbon) stays pixel-stable; resize affects canvas area only per D-08"
    - "Empty chart-empty overlay stays as-is with documented copy per D-07"
    - "Console stays clean during resize and DPR/zoom switch per D-06"
    - statement: "Human HiDPI crispness plus clean console during resize and DPR/zoom switch holds on held-out visual verification per D-05 D-06"
      verification: backstop
    - statement: "No flicker or dropped overlays across rapid successive resize frames per D-04"
      verification: backstop
  artifacts:
    - path: "components/charts/nq-chart.tsx"
      provides: "Chart mount effect with library-native autosize plus view-lock and fallback gate"
      contains: "autoSize"
  key_links:
    - from: "components/charts/nq-chart.tsx"
      to: "lightweight-charts createChart"
      via: "createChart options carry autoSize plus timeScale lockVisibleTimeRangeOnResize"
      pattern: "autoSize.*lockVisibleTimeRangeOnResize"
    - from: "components/charts/nq-chart.tsx"
      to: "components/charts/zone-primitive.ts"
      via: "existing updateBands repaint nudge retained so zone geometry recomputes from live scale coordinates"
      pattern: "zoneRef\\.current\\.updateBands"
---

<objective>
Wire library-native container autosize with view-lock into the populated NqChart mount effect and prove crisp HiDPI plus clean resize per D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 and CHRT-01 CHRT-02.

Purpose: Users see a crisp chart on any display that redraws cleanly on resize — the phase goal.
Output: components/charts/nq-chart.tsx creating its chart with autosize plus visible-range lock and a ResizeObserver-absent fallback gate, with overlays, chrome, and empty state untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-chart-rendering-polish/11-CONTEXT.md
@.planning/phases/11-chart-rendering-polish/11-RESEARCH.md
@.planning/phases/11-chart-rendering-polish/11-PATTERNS.md
@.planning/phases/11-chart-rendering-polish/11-UI-SPEC.md
@.planning/phases/11-chart-rendering-polish/11-VALIDATION.md
@components/charts/nq-chart.tsx
@components/charts/zone-primitive.ts
</context>

<tasks>

<task type="tracer">
  <name>Wire library-native autosize with view-lock end-to-end — one path only</name>
  <read_first>components/charts/nq-chart.tsx mount effect lines 133-155 plus cleanup lines 286-310 plus .planning/phases/11-chart-rendering-polish/11-CONTEXT.md decisions D-01 D-02 D-03 plus .planning/phases/11-chart-rendering-polish/11-RESEARCH.md Pattern 1 plus Pattern 2 plus .planning/phases/11-chart-rendering-polish/11-PATTERNS.md insertion point 1 per D-01 D-02 D-03</read_first>
  <files>components/charts/nq-chart.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. Inside the existing createChart call at nq-chart.tsx lines 144-146, add the autosize flag set to true and a timeScale object carrying lockVisibleTimeRangeOnResize set to true, preserving the existing layout background transparent and textColor #71717A entries byte-identical per D-01 D-02 D-03. Directly after chart creation and before addSeries, add a fallback gate that queries chart.autoSizeActive and only when it returns false reads el.clientWidth and el.clientHeight and calls chart.resize with those two values guarded by widths and heights greater than zero. Keep every chart-library access inside the existing dynamic-import mount effect, keep the module top free of browser globals, do not add any host-side pixel-ratio handling, do not add a window resize listener, do not re-create the chart and do not call fitContent or setData on resize, and leave the existing cleanup with chart.remove plus the disposed flag exactly as-is per D-01 D-02 D-03.</action>
  <acceptance_criteria>components/charts/nq-chart.tsx contains the string autoSize: true inside the createChart options per D-01; contains the string lockVisibleTimeRangeOnResize: true inside a timeScale object per D-03; contains the string autoSizeActive guarding a chart.resize call reading clientWidth and clientHeight per D-01; still contains layout background transparent and textColor #71717A; git diff --name-only -- components/ src/ app/ lists only components/charts/nq-chart.tsx; npx tsc --noEmit exits 0</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or output contains the string error TS</fails_when>
  </verify>
  <done>Single autosize plus view-lock path wired end-to-end in the mount effect with typecheck green per D-01 D-02 D-03</done>
</task>

<task type="auto">
  <name>Confirm overlay persistence, chrome stability, and automated plus human proof</name>
  <read_first>components/charts/nq-chart.tsx data effect lines 444-450 plus render block lines 463-505 plus components/charts/zone-primitive.ts updateBands lines 126-128 plus .planning/phases/11-chart-rendering-polish/11-UI-SPEC.md Interaction contract plus .planning/phases/11-chart-rendering-polish/11-VALIDATION.md Manual-Only Verifications per D-04 D-05 D-06 D-07 D-08</read_first>
  <files>components/charts/nq-chart.tsx</files>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine. Retain the existing zone-refresh block at nq-chart.tsx lines 446-449 calling zoneRef.current.updateBands after the opacityScale assignment exactly as-is so zone geometry recomputes from live scale coordinates through every resize frame per D-04. Make zero edits to zone-primitive attach, updateBands, detach, and buildZoneOverlayFallback, zero edits to the stale-badge, closed-ribbon, forming-row, and chart-empty render blocks, zero edits to chart-mapper, zone-bands, and ict math modules, and zero edits to package.json per D-04 D-07 D-08. Then run the automated gate from the repo root, then start the dev server with npm run dev and walk the populated-chart protocol with candles present: drag-resize live, toggle sidebar or panel, pan then zoom then resize, switch browser zoom, watching the asia-lines, judas-markers, smt-marker hooks and the console per D-02 D-03 D-04 D-05 D-06.</action>
  <acceptance_criteria>zoneRef.current.updateBands still present in components/charts/nq-chart.tsx per D-04; git diff --name-only -- components/ src/ app/ lists only components/charts/nq-chart.tsx per D-07 D-08; chart-empty block still contains the verbatim strings Məlumat yoxdur and Hələlik şam məlumatı əlçatan deyil per D-07; stale-badge, closed-ribbon, forming-row class strings top-2 right-2, inset-x-0 top-2, px-1 py-2 unchanged per D-08; npm test exits 0; npm run build exits 0; human protocol confirms crisp edges at zoom 150 percent or higher, live refill with no stretch clip or freeze, preserved visible range, persisting overlays, and zero console errors or warnings per D-02 D-03 D-04 D-05 D-06</acceptance_criteria>
  <verify>
    <automated>npm test && npm run build</automated>
    <fails_when>non-zero exit or output contains any of the strings Failed, error TS, Type error</fails_when>
    <human-check>With npm run dev and the populated chart open: at browser zoom 150 percent or on a high-DPI display confirm candle edges, EQ and Asia-H and Asia-L titles, axis text, and J and S markers are sharp per D-05 CHRT-01; drag-resize the window live and toggle the sidebar or panel and confirm the canvas refills div data-slot nq-chart every frame with no stretch clipping or freeze per D-01 D-02 CHRT-02; pan and zoom first then resize and confirm the visible range is preserved never re-fit per D-03; confirm Asia lines plus Judas and SMT markers plus zone fills persist with no flicker per D-04; keep DevTools console open through resize and zoom switch and confirm zero errors or warnings per D-06; confirm STALE badge plus forming row plus MARKET CLOSED ribbon positions unchanged per D-08 and the empty state untouched per D-07</human-check>
  </verify>
  <done>Overlays persist, chrome and empty state untouched, suite plus build green and human visual protocol signed off per D-04 D-05 D-06 D-07 D-08</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser layout engine→nq-chart.tsx | Container geometry arrives from the browser layout engine via the library-internal observer; dimensions are measurements, never parsed input |
| editor→components/charts/nq-chart.tsx | Trusted developer edit confined to chart creation options plus a size fallback gate; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Denial of service | nq-chart.tsx fallback chart.resize gate | low | mitigate | Guard the manual fallback with widths and heights greater than zero before calling resize; the autosize path needs no guard since the library owns its observer |
| T-11-02 | Tampering | nq-chart.tsx chart containers | low | mitigate | No innerHTML on chart containers; keep the existing React ref plus data-slot structure untouched; resize work is a size notification only, never a data operation |
| T-11-SC | Tampering | package installs | low | accept | No package installs in this phase; lightweight-charts 5.2.1 already installed; package.json untouched so no legitimacy checkpoint required |
</threat_model>

<verification>
In order from the repo root: run npx tsc --noEmit for exit zero, run npm test for exit zero, run npm run build for exit zero, confirm git diff --name-only -- components/ src/ app/ lists only components/charts/nq-chart.tsx, then walk the human visual protocol on the populated chart covering crispness at zoom 150 percent or higher, live drag resize plus sidebar or panel toggle, pan-zoom-then-resize view preservation, overlay persistence via the asia-lines judas-markers smt-marker hooks, chrome stability, and a clean console through resize and zoom switch.
</verification>

<success_criteria>
User sees crisp chart lines and text on a high-DPI display with no blur; user can resize the browser window and the chart refills its container with no stretch, clipping, or frozen canvas; no console errors or warnings appear during resize or DPI switch.
</success_criteria>

## Artifacts this phase produces

No new files, no new exports, no new functions, no new flags. The single deliverable is modified chart-creation options inside the existing file components/charts/nq-chart.tsx: the autosize flag, the visible-range lock inside the timeScale options, and the observer-absent fallback gate calling resize with the container client dimensions. Zone primitive, overlays, chrome, empty state, ICT math, and dependencies are unchanged.

<output>
Create `.planning/phases/11-chart-rendering-polish/11-01-SUMMARY.md` when done
</output>
