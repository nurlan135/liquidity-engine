---
phase: 12-deployment-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DEPL-01, DEPL-02, DEPL-03]
estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Worktree code/config carries no token, flag, or branch-remnant strings — strict gate prints nothing per D-06 D-09 (DEPL-01, DEPL-03)"
    - "Git history holds no token values — token pickaxe scans return empty per D-06 (DEPL-01)"
    - "Remote exposes only refs/heads/main via authoritative ls-remote with no drill refs locally per D-10 (DEPL-03)"
    - "Production page and API answer 200 unauthenticated as the pre-change baseline per D-01 (DEPL-02 baseline)"
  artifacts:
    - path: ".planning/phases/12-deployment-hygiene/12-01-SUMMARY.md"
      provides: "Pasted gate outputs for every check above"
  key_links:
    - from: "strict worktree grep"
      to: ".planning/ history docs"
      via: "exclude-dir .planning so expected drill history never trips the CLEAN gate"
      pattern: "exclude-dir=.planning"
    - from: "git branch -r"
      to: "git ls-remote --heads origin"
      via: "ls-remote is authoritative; branch -r alone can show stale refs"
      pattern: "ls-remote --heads origin"
  prohibitions:
    - statement: "MUST NOT document the protection or token state as applied before the user confirms it — no document-now-apply-later drift per D-04"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or modify scripts/verify-deploy.sh or scripts/verify-phase9-drill.sh — they are kept tooling per D-08, not remnants"
      status: resolved
      verification: judgment
    - statement: "MUST NOT create a branch or preview deployment just to probe preview protection — a 404 means nothing to probe, never a gate failure"
      status: resolved
      verification: judgment
---

<objective>
Prove the repo side of deployment hygiene end-to-end in one agent-run pass: worktree clean, history clean, branch state clean, prod publicly reachable — per D-06 D-09 D-10 and D-01.

Purpose: Every agent-verifiable claim in DEPL-01/DEPL-03 plus the DEPL-02 public-prod baseline is evidenced before any dashboard work, so user checkpoints in 12-02 start from proven ground.
Output: Gate command outputs pasted into 12-01-SUMMARY.md; zero files modified.

UI-SPEC lift: the UI-SPEC ## UI Considerations section resolves zero applicable considerations (no app surfaces in this phase) — nothing lifted into must_haves; recorded here so the non-lift is visible, not silent.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deployment-hygiene/12-CONTEXT.md
@.planning/phases/12-deployment-hygiene/12-RESEARCH.md
@.planning/phases/12-deployment-hygiene/12-PATTERNS.md
@.planning/phases/12-deployment-hygiene/12-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end repo-clean proof — worktree grep, branch state, prod baseline in one pass</name>
  <read_first>.planning/phases/12-deployment-hygiene/12-PATTERNS.md Shared Patterns (prove-removal-by-grep plus branch-state verification) plus .planning/phases/12-deployment-hygiene/12-RESEARCH.md Code Examples (strict-gate command, branch commands, behavioral probes) plus .planning/phases/12-deployment-hygiene/12-CONTEXT.md decisions D-01 D-06 D-09 D-10</read_first>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine in Git Bash. First run the strict tier-(a) gate per D-06 D-09 with the exact literal set DRILL_FORCE_STALE plus drill-preview plus VERCEL_TOKEN plus vercel token with underscore-or-dash plus vercel_api, case-insensitive extended regex, excluding node_modules, .next, .git, .planning, and .playwright-mcp, over exactly these plan-time-verified existing paths: src, app, components, scripts, package.json, package-lock.json, .npmrc, .gitignore, next.config.ts, tsconfig.json — expecting exit 1 with no output (CLEAN). The .planning exclusion is required because archived summaries legitimately mention the drill (expected history, not dirt). Second run git fetch --prune origin, then git branch -r expecting only origin/HEAD plus origin/main, then git ls-remote --heads origin as the authoritative gate expecting only refs/heads/main, then git show-ref piped to case-insensitive drill match with a NO-DRILL-REFS fallback echo per D-10. Third run the public-prod baseline per D-01: curl the production page https://liquidity-engine-nine.vercel.app/ and the API path https://liquidity-engine-nine.vercel.app/api/yahoo with silent output writing only the HTTP code, expecting 200 on both with no auth. Modify no files; record every command plus its full output for the SUMMARY.</action>
  <acceptance_criteria>Strict-gate output shows no match lines and the exit marker reads 1 (CLEAN) per D-06 D-09; git ls-remote output lists only refs/heads/main and the show-ref check prints NO-DRILL-REFS per D-10; both prod curls print 200 per D-01; git status shows no modified tracked files after the pass</acceptance_criteria>
  <verify>
    <automated>grep -rniE 'DRILL_FORCE_STALE|drill-preview|VERCEL_TOKEN|vercel[_-]?token|vercel_api' --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git --exclude-dir=.planning --exclude-dir=.playwright-mcp src app components scripts package.json package-lock.json .npmrc .gitignore next.config.ts tsconfig.json 2>/dev/null; echo "strict-gate-exit=$?"</automated>
    <fails_when>strict-gate-exit=0 in output, or any match line appears above the exit line</fails_when>
    <automated>git ls-remote --heads origin</automated>
    <fails_when>output contains any line not ending in refs/heads/main, or non-zero exit</fails_when>
    <automated>curl -s -o /dev/null -w 'prod-page=%{http_code}' https://liquidity-engine-nine.vercel.app/ && curl -s -o /dev/null -w ' prod-api=%{http_code}' https://liquidity-engine-nine.vercel.app/api/yahoo</automated>
    <fails_when>output is anything other than prod-page=200 prod-api=200, or non-zero exit</fails_when>
  </verify>
  <done>Single repo-clean path proven end-to-end: worktree CLEAN, only-main remote, prod 200 baseline, all outputs recorded per D-01 D-06 D-09 D-10</done>
</task>

<task type="auto">
  <name>Git-history token trace plus shell env evidence and conditional preview probe</name>
  <read_first>.planning/phases/12-deployment-hygiene/12-PATTERNS.md Shared Patterns (git-history token trace) plus .planning/phases/12-deployment-hygiene/12-RESEARCH.md Pitfall 3 (no-preview-to-probe) plus Pitfall 5 (history-file reach) plus .planning/phases/12-deployment-hygiene/12-CONTEXT.md decisions D-06 D-10</read_first>
  <action>Run from the repo root C:/Users/HP/Projects/liquidity-engine in Git Bash. Run the pickaxe token scans per D-06: git log --all --oneline -S VERCEL_TOKEN plus case-insensitive vercel_token variant, each piped to head -20, expecting empty output (no commit ever carried a token value). Run git log --all --oneline -S DRILL_FORCE_STALE expecting only the known removal commits as hits — flag history is EXPECTED proof of removal, not dirt; record the hashes observed. Check the current shell environment with env piped to case-insensitive vercel match plus a NO-ENV-HITS fallback echo, expecting NO-ENV-HITS; record the result as agent-side env evidence only. Per-machine shell history files are outside agent reach, so do NOT claim history-clean from the agent side — the user-side history check lives in plan 12-02 per D-06. For previews per D-10: probe a preview URL ONLY if one was supplied; none is expected since drill-preview is deleted, so record the disposition line no preview deployment to probe — dashboard readback stands as proof, and never create a branch or preview to manufacture a probe target. Modify no files.</action>
  <acceptance_criteria>Token pickaxe scans print nothing per D-06; DRILL_FORCE_STALE log hits are recorded as removal commits, not dirt; env check prints NO-ENV-HITS and the summary marks shell-history as no observation pending the 12-02 user check per D-06; preview disposition line recorded with no branch created per D-10</acceptance_criteria>
  <verify>
    <automated>git log --all --oneline -S 'VERCEL_TOKEN' | head -20; echo "pickaxe-git-exit=${PIPESTATUS[0]}"</automated>
    <fails_when>any commit line appears above the exit marker (expect empty output), or pickaxe-git-exit is non-zero</fails_when>
    <automated>env | grep -i 'vercel' || echo NO-ENV-HITS</automated>
    <fails_when>any line other than NO-ENV-HITS appears, or non-zero exit</fails_when>
  </verify>
  <done>History token trace empty, env evidence recorded, preview-probe disposition recorded with nothing created per D-06 D-10</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-shell→repo worktree | Trusted local read-only scan; grep output is evidence, never executed |
| git-history→token strings | Committed history is untrusted input for secret traces; pickaxe reads it without checking anything out |
| agent→production URL | Unauthenticated curl baseline; response codes only, no credentials cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Drill-automation Vercel token remnant in worktree or history | high | mitigate | Strict tier-(a) worktree gate plus token pickaxe scans in this plan; dashboard deletion itself is plan 12-02 per D-05 |
| T-12-04 | Tampering | Deleted drill-preview branch resurrected or stale ref trusted | medium | mitigate | Gate on authoritative ls-remote (only refs/heads/main) plus show-ref drill check, never branch -r alone, per D-10 |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this phase; verification uses preinstalled git, grep, curl only, so no legitimacy checkpoint required |
</threat_model>

## Probe fallback assumptions (spec-less fallback, EDGE_ABSENT=1 PROHIB_ABSENT=1)

- DEPL-01 unresolved probe item → flagged assumption: the drill-automation token is recognizable to the user on the dashboard Tokens page (name or creation date); if several opaque tokens exist, user judgment selects the drill one and records which.
- DEPL-03 unresolved probe item → flagged assumption: no preview deployment survives independently of the deleted branch per D-10, so no dashboard preview sweep runs; if a stale preview URL surfaces, it is probed conditionally and never manufactured.

<verification>
In order from the repo root: strict worktree grep exits 1 with no matches; git ls-remote lists only refs/heads/main with NO-DRILL-REFS locally; token pickaxe scans print nothing; env check prints NO-ENV-HITS; prod page plus API curl 200 unauthenticated; git status shows no modified tracked files.
</verification>

<success_criteria>
No stale Vercel token string remains in env, config, or committed files per the agent-verifiable scans; no drill-preview branch, flag, or reference remains in repo or config; prod answers 200 unauthenticated as baseline.
</success_criteria>

## Artifacts this phase produces

No new files, no new exports, no new functions, no new flags, no new file paths. This plan runs read-only gate commands only; its sole output is pasted command evidence in `.planning/phases/12-deployment-hygiene/12-01-SUMMARY.md`.

<output>
Create `.planning/phases/12-deployment-hygiene/12-01-SUMMARY.md` when done
</output>
