---
phase: 12-deployment-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPL-01, DEPL-02]
user_setup:
  - service: vercel
    why: "Drill-token deletion and the Deployment Protection switch require the account holder in the browser; the agent has no Vercel CLI or API access per D-03 D-04 D-05"
    dashboard_config:
      - task: "Delete the drill-automation token"
        location: "vercel.com → Account Settings → Tokens"
      - task: "Set Standard Protection with Vercel Authentication"
        location: "Vercel dashboard → project liquidity-engine-nine → Settings → Deployment Protection"
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Drill-automation token deleted in the dashboard with a user-confirmed date recorded per D-05 D-07 (DEPL-01)"
    - "Protection scope reads Standard Protection with Vercel Authentication via pasted readback, production stays public per D-01 D-02 D-03 D-04 (DEPL-02)"
    - "Shell-history evidence recorded: user ran the paste-ready check or stated no token was ever exported per D-06"
  artifacts:
    - path: ".planning/phases/12-deployment-hygiene/12-02-SUMMARY.md"
      provides: "Token confirmation string, readback strings, shell-history evidence"
  key_links:
    - from: "dashboard readback strings"
      to: ".planning/phases/12-deployment-hygiene/12-02-SUMMARY.md"
      via: "verbatim transcription feeding the 12-03 PROJECT.md row"
      pattern: "Standard Protection.*Vercel Authentication"
  prohibitions:
    - statement: "MUST NOT document the protection or token state as applied before the user confirms it — no document-now-apply-later drift per D-04"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or modify scripts/verify-deploy.sh or scripts/verify-phase9-drill.sh — they are kept tooling per D-08, not remnants"
      status: resolved
      verification: judgment
    - statement: "MUST NOT create a branch or preview deployment just to probe preview protection — a 404 means nothing to probe, never a gate failure"
      status: resolved
      verification: judgment
---

<objective>
Collect the two dashboard-side proofs this phase cannot automate: drill-token deletion confirmation (DEPL-01) and Deployment Protection switch plus readback (DEPL-02) — per D-01 D-02 D-03 D-04 D-05 D-06 D-07.

Purpose: The agent has no Vercel CLI, API token, or dashboard access, so these two facts arrive only through the account holder; everything the agent CAN prove already runs in 12-01 in parallel.
Output: 12-02-SUMMARY.md carrying the dated token confirmation, the verbatim protection readback, and the shell-history evidence for 12-03 to transcribe.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deployment-hygiene/12-CONTEXT.md
@.planning/phases/12-deployment-hygiene/12-RESEARCH.md
@.planning/phases/12-deployment-hygiene/12-PATTERNS.md
@.planning/phases/12-deployment-hygiene/12-UI-SPEC.md
</context>

<tasks>

<!-- No checkpoint:decision precedes the one-way token deletion: D-07 already locked deletion and this task is itself a blocking-human gate (gating it again would re-ask a decided question). -->
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Delete the drill-automation Vercel token and report shell-history evidence</name>
  <reversibility rating="one-way">A revoked dashboard token cannot be restored; if automation ever needs one again it must be re-created per D-07</reversibility>
  <read_first>.planning/phases/12-deployment-hygiene/12-UI-SPEC.md Dashboard Click-Path Copy Token Deletion plus Shell-History Check Copy plus .planning/phases/12-deployment-hygiene/12-RESEARCH.md Pitfall 5 plus .planning/phases/12-deployment-hygiene/12-CONTEXT.md decisions D-05 D-06 D-07</read_first>
  <action>The user — and only the user, the agent has no dashboard access — completes the token errand in the browser per D-05.</action>
  <instructions>
    1. Open vercel.com → account avatar → Account Settings → Tokens.
    2. Find the drill-automation token created for the v1.0 preview drill.
    3. Delete (revoke) it and confirm the dashboard dialog. If it is already gone, do nothing further on this errand.
    4. If the token turns out to still be live, it is deleted now — keeping it as intentional is rejected per D-07.
    5. Shell-history evidence per D-06 (history files live on the user's machines, outside agent reach): run ONE of these and keep the output — Git Bash or bash: grep -i vercel ~/.bash_history || echo NO-HISTORY-HITS — PowerShell: Select-String -Pattern 'vercel' (Get-PSReadlineOption).HistorySavePath — or state explicitly that no token was ever exported in shell. Absence of a history file is no observation, never clean.
  </instructions>
  <acceptance_criteria>12-02-SUMMARY.md records exactly one of the strings token deleted YYYY-MM-DD or token already gone YYYY-MM-DD with a real calendar date per D-05, plus the shell-history command output or the explicit no-export statement per D-06</acceptance_criteria>
  <verification>The agent trusts the deletion confirmation and documents it with the date; no API readback exists per D-05</verification>
  <resume-signal>Reply with the confirmation string (token deleted YYYY-MM-DD or token already gone YYYY-MM-DD) plus the shell-history output or no-export statement</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Set Deployment Protection to Standard plus Vercel Authentication and paste back the readback</name>
  <read_first>.planning/phases/12-deployment-hygiene/12-UI-SPEC.md Dashboard Click-Path Copy Protection Switch plus Readback Request Copy plus Dashboard Readback Contract plus .planning/phases/12-deployment-hygiene/12-CONTEXT.md decisions D-01 D-02 D-03 D-04</read_first>
  <action>The user flips the protection switch in the browser per D-04; the agent verifies afterward from the pasted readback per D-03.</action>
  <instructions>
    1. Open the Vercel dashboard → project liquidity-engine-nine → Settings → Deployment Protection.
    2. Set the scope to Standard Protection — NOT All Deployments (production must stay public; on Hobby protecting prod is unavailable, so Standard is the correct scope).
    3. Set the preview method to Vercel Authentication (Require Vercel login) — NOT a bypass secret and NOT open URLs per D-02.
    4. If the dashboard already shows Standard Protection plus Vercel Authentication, change nothing — verification-only path per D-04. No document-now-apply-later drift.
    5. Paste back the exact scope and method strings as shown in Settings (or attach a screenshot). Trusted word alone is not enough per D-03. Expected record: Standard Protection plus Vercel Authentication.
  </instructions>
  <acceptance_criteria>12-02-SUMMARY.md records the pasted scope and method strings verbatim (or notes the attached screenshot filename) per D-03, and records whether a switch was flipped or the state already matched per D-04</acceptance_criteria>
  <verification>Agent-side behavioral proof follows in plan 12-03 (prod 200 unauthenticated via the kept scripts); a preview 302-to-sso-api curl runs ONLY against a user-supplied existing preview URL, never a manufactured one</verification>
  <resume-signal>Reply with the pasted scope and method strings (or screenshot) plus flipped or already-matching</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-browser→Vercel dashboard | Account-holder-only surface; agent never touches it, proof arrives as readback plus trusted deletion confirmation |
| user-shell-history→agent record | Per-machine history outside agent reach; user output transcribed, never asserted clean by the agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Drill-automation Vercel token still live | high | mitigate | User deletes or revokes the token during this plan per D-05 D-07; agent-side history plus env plus repo scans run in 12-01 per D-06 |
| T-12-02 | Information Disclosure | Deployment Protection left disabled, previews openly reachable | high | mitigate | User sets Standard Protection with Vercel Authentication per D-01 D-02 D-04; readback proof required per D-03 and live re-proof follows in 12-03 |
| T-12-03 | Elevation of privilege | Bypass secret created instead of Vercel login | medium | mitigate | D-02 rejects the bypass-secret option at the primary preview-auth boundary; checkpoint instructions name Vercel Authentication explicitly and forbid the secret |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this phase; no CLI or API tooling is added, so no legitimacy checkpoint required |
</threat_model>

## Probe fallback assumptions (spec-less fallback, EDGE_ABSENT=1 PROHIB_ABSENT=1)

- DEPL-01 unresolved probe item → flagged assumption: the drill-automation token is recognizable to the user on the Tokens page (name or creation date); if several opaque tokens exist, user judgment selects the drill one and records which string was deleted.
- DEPL-02 unresolved probe item → flagged assumption: the pasted readback is taken as the protection proof because no API or CLI surface exists to cross-check it; if readback shows a non-Standard scope, the PROJECT.md row records the actual strings and the phase does not claim the default.

<verification>
Both blocking-human checkpoints resolve with evidence transcribed into 12-02-SUMMARY.md: a dated token-deletion confirmation plus shell-history output per D-05 D-06, and verbatim protection scope plus method strings with flipped or already-matching noted per D-03 D-04.
</verification>

<success_criteria>
No live drill token survives the phase; preview protection is Standard Protection with Vercel Authentication by user action with agent-read proof; production stays public.
</success_criteria>

## Artifacts this phase produces

No new files, no new exports, no new functions, no new flags, no new file paths. This plan collects user-side evidence only; its sole output is the transcribed evidence in `.planning/phases/12-deployment-hygiene/12-02-SUMMARY.md` for plan 12-03 to document.
